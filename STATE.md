# STATE.md — Monitrax "You Are Here"

> **This is the first file EVERY session reads, on EVERY surface (chat · Cowork · Code), BEFORE anything else.**
> It is not a substitute for the canonical docs — it is the pointer to them plus the current cursor.
> **Prime directive: read live, never recall.** No claim about Monitrax is made from memory; it is read
> from the repo at the pinned HEAD, or it is flagged unverified. Memory and any project-knowledge cache are
> NEVER ground truth — only live `resadegh/monitrax` HEAD is.
> **No session is notified of anything.** Merge-awareness and "what changed" are a session-start PULL, never a subscription.

**Last verified against HEAD:** merge of #1518 (`72f5f1ef`) · **on:** 2026-07-28 · **by:** Code session (VR-037 findings) — **VR-037'S SEVEN FINDINGS BUILT AS TWO PRs: PR-A #1519 (draft, `fix/mon-104-110-capture-ssot`) = MON-104/105/107/108/109/110, changes NO number; PR-B (`fix/mon-106-fy2026-27-config`, stacked on A) = MON-106 alone, Reza's ≈$268 click.** PR-A: MON-104 ONE canonical FY resolver (`resolveRequestedTaxYear`/`isTaxYearConfigured` in taxYearConfig.ts) used by write+read of all three capture routes (psi/div152/smsf-return), unconfigured FY 400-rejected never persisted (the VR-037 orphan class dead); MON-105 Div 152 gets its OWN eligibility grammar (`lib/tax-engine/eligibility.ts`, entity-type-agnostic per s152-10, SMSF-only exclusion — INDIVIDUAL/PERSONAL_NAME now reach the card); MON-107 PSI card renders the ENGINE's citations (`components/tax/citationLine.ts`, s86-15 literal deleted); MON-108 clean concession labels at the producer (both twins); MON-109 engine exports ONE_CLIENT_THRESHOLD/MNAV/TURNOVER/RETIREMENT-CAP/FIFTEEN-YEAR — cards read them for comparisons AND copy + NEW NeoAudit threshold-trace detector (NEOAUDIT §7); MON-110 `gainBeforeConcessions` stated by both twins, card reconstruction deleted + the no-surface-arithmetic audit promoted to CI. Registry: MON-104…111 raised (`issues:raise`), 6 → FIXING on #1519; MON-111 = the tracked s86-15 household-attribution deferral (41e.6/41e.7). Ratchets: 6 suites/46 tests; suites 1,520 green; tsc + lints + neomatrix green (3 anchors re-pinned; eligibility.ts hand-moved into the structural graph — graphify offline, 2846163 precedent). **Next: CI green on #1519 (safe to merge on green) → PR-B build (FY2026-27 config + loud stale-FY surfacing + clock-derived CI guard + Ring-0 bracket walk on $145,426 → ≈$268 drop) → Reza reviews PR-B's before/after and clicks merge → Matrix Ring-3 on A (citations/reachability/round-trip) + separate Ring-3 on B (the $268 movement vs the Ring-0 walk).**
> _Prior pin: merge of #1516 (`0227f65e`) · **on:** 2026-07-27 · **by:** Code session (MON-103 / capture Stage 3) — **STAGE 3 BUILT (draft PR pending, branch `claude/mon-103-div152-capture-yhm8ug`): the Div 152 concession facts CAPTURED — the THIRD and FINAL Neo-G4 overlay made live-capable; the CAPTURE FEATURE IS COMPLETE.** Reza standing GO 2026-07-27 ("go with stage 3" + "after that give me a consolidated handout to the matrix"); the §18.8 9.2/10 Stitch design (screen 8ff4092f…, v1 8.3 → v3 indigo identity; the non-persisting Stitch DOM ops hand-applied in the committed artefact, disclosed) presented BEFORE any React. Built: `Div152Assessment` (4 numerics + 2 eligibility + 3 elections + lifetime-used, all nullable; migration 20260727120000 + AuditAction) → GET/PUT `/api/tax/entity/[id]/div152-assessment` (null-preserving, §13.3 audit) → `buildDiv152Input` (THE producer, assembler :264 — SIX-fact all-or-nothing gate: 4 numerics + isActiveAsset + isRetirementOrIncapacity, since for the eligibility booleans NEITHER default is safe; elections → engine-contract defaults when unanswered; elected exemption REQUIRES lifetime-used, $0 never assumed) → entity route GET feeds `div152ByEntity` (third Promise.all element) → MON-099's overlay fires with the step ladder + citations; `Div152AssessmentCard` renders below the PSI card for the same PSI entity types, reading the LIVE `crossCutting.div152ByEntity` (never re-derived). Ratchet: mon103Div152Capture (8, incl. §19.2 $120,000 → AAR −$60k → retirement exemption −$60k (cap remaining $100k) → assessable $0 end-to-end both twins, verified against the READ engine source + the 15-year direction + gate-closed byte-parity). Neo-sync: buildDiv152Input modelled + feeds edge; all three assembler anchors re-pinned +1 (div7a 82, buildFteIeeInput 120, buildPsiInput 185); route Layer-0-allowlisted; neomatrix:check green; registry MON-103 DIAGNOSED (→ FIXING on PR), 103 valid. ALSO this window: #1516 (MON-102 Stage 2) MERGED (conflict from interleaved VR-035/036 + the 55-issue promotion pass resolved in a merge commit: main's MON-101 CLOSED kept, MON-102 FIXING preserved) + prod deploy READY (§17.2 — the psi_assessments migration applied to prod; runtime logs empty = no traffic yet, a recognized §17.6 non-error). **Next: Reza merges the MON-103 PR (his §12.12 migration click) → the CONSOLIDATED Stage-2+3 Matrix handout (delivered in-session, Reza holds it until ready to paste) → Matrix Ring-3 verifies both capture stages in one run (PSI: $150k/90%/1-client → $150,000 attributed s86-15; Div 152: the $120k→$0 ladder s152-205/305; inertness: Reza byte-identical — no saved assessments exist) → registry flips on the verdicts. The capture arc is then DONE; the v2 orchestrator fork (folding overlay results into entity totals) remains Reza's parked decision.**
> _Prior pin: merge of #1515 (`117957c1`) · **on:** 2026-07-27 · **by:** Code session (MON-102 / capture Stage 2) — **STAGE 2 BUILT (draft PR pending, branch `claude/mon-102-psi-capture-yhm8ug`): the PSI questionnaire CAPTURED — the second Neo-G4 overlay made live-capable.** Reza GO'd the three presented items (Stitch design 73a1f751… §18.8 9.1/10; the `PsiAssessment` schema — 3 nullable numerics + 5 nullable test Booleans, unique (entityId, financialYear), migration 20260727000000 + `AuditAction.PSI_ASSESSMENT_SAVED`; and BOTH doctrines: numerics ALL-OR-NOTHING — the overlay computes only when totalPsiIncome + incomeFromLargestClient + unrelatedClientCount are all set — and unanswered tests = NOT-ESTABLISHED — a null test boolean maps to an absent key, the engine's own default and the ATO posture, asymmetric-safe: can only move toward MORE attribution, stated in-UI by the amber note). Flow: PsiAssessmentCard on the entity tax page for PSI types (COMPANY/SOLE_TRADER/PARTNERSHIP/DISCRETIONARY_TRUST/UNIT_TRUST — replacing the "isn't an SMSF" dead-end) → PUT `/api/tax/entity/[id]/psi-assessment` (null-preserving; §13.3-sanitized audit) → `buildPsiInput` (THE producer, assembler :184) → entity route GET feeds `psiByEntity` (Promise.all with the FTE feed) → the Stage-0 orchestrator path fires MON-097's overlay; the card renders the LIVE `crossCutting.psiByEntity` result (never re-derived client-side). Ratchet: mon102PsiCapture (8, incl. §19.2 $150,000 attribution — $135k/90% one-client → 80% test FAILS → s86-15 — end-to-end both twins + PSB-determination direction + gate-closed byte-parity). Neo-sync: buildPsiInput modelled + feeds edge; assembler anchors re-pinned (div7a 80→81, buildFteIeeInput 118→119); psi-assessment route Layer-0-allowlisted (graphify offline) + stale intake-duplicates entry pruned. Suite 1,465 green (96 files); all gates green. ALSO this window: #1509 (MON-101 Stage 1) MERGED + prod READY (§17.2 — the FTE migration applied to prod); VR-034 flip applied; then merged in from main: VR-035 (MON-078) + VR-036 (intake-dedup guardrails) run records + the 55-issue VERIFIED→CLOSED promotion pass (#1514 — MON-101 now CLOSED). **Next: Reza merges the MON-102 PR (his §12.12 migration click rides the preview→prod deploy) → Stage-2 Matrix Ring-3: seed a PSI entity (saved assessment, $150k/90%/1-client, no tests) → "$150,000 attributed to the individual" with s86-15 cited on the entity tax view; Reza + every user without a saved assessment byte-identical → then Stage 3 (Div 152 CGT-event flow — completes the capture feature) on the next GO/brief.**
> _Prior pin: merge of #1512 (`5839b2c8`) · **on:** 2026-07-27 · **by:** Code session (registry promotion pass) — **VERIFIED→CLOSED BACKLOG PROMOTED (draft PR pending, branch `claude/promote-verified-closed-g8kra5`; registry-only, parallel-safe with the Stage-2 PSI session): 55 issues flipped CLOSED in one pass — all 54 VERIFIED plus MON-101 (VERIFIED first per VR-034 #1510, then closed).** Every close cites its promotion evidence in the entry (its VR run + the CI ratchet/holistic test) and was individually reopener-checked — none held back. The flagged judgment calls resolved as briefed: MON-097/098/099/100 closed as verified-INERT overlay wirings (their defect was unwired/unreachable, now resolved; capture is the separate MON-101+ arc, Stage 2 PSI in flight); MON-088's named follow-ups (estimate-caller cover; FY-config) are NEW issues, not reopeners; MON-093's declared-basis correction and MON-095's QBE/Mate merge are Reza data actions, not defect reopeners; MON-045's Stage 3 stays FW-2-gated future work. Register: 3 → **58 CLOSED** of 101 (18 FIXING · 21 OPEN · 3 DIAGNOSED · 1 RETRACTED). Neo-sync (gate 8): registry-only housekeeping — no Neomatrix/Neobrain/NeoAudit change; `issues:check` 101 valid + registry vitest 8/8 green; ISSUES.md regenerated. **Next: Reza merges (bulk status flip — quick eyeball, or auto-merge once CI green) → the Matrix regenerates the issue register (CLOSED count 3 → 58) + pinned artifact. No Ring-3 — status housekeeping on already-verified issues, changesNumbers: no.**
> _Prior pin: merge of #1506 (`4284ee77`) · **on:** 2026-07-26 · **by:** Code session (MON-101 / capture Stage 1) — **STAGE 1 BUILT (draft PR pending, branch `claude/mon-101-fteiee-capture-yhm8ug`): FTE/IEE beneficiary facts CAPTURED — the first Neo-G4 overlay made live-capable.** Reza GO'd the three presented items (Stitch design f395b67e… §18.8 9.2/10; three nullable DistributionAllocation columns relationship/hasQuotedTfn/coveredByIee + FamilyGroupRelationship enum, migration 20260726000000; the ALL-OR-NOTHING safe default — a null hasQuotedTfn can neither fabricate nor suppress a 47% classification, so the overlay computes only when the election is ON and every beneficiary's facts are complete). Flow: Record-split dialog (election pill + per-beneficiary relationship select / TFN tri-state / conditional IEE tri-state + amber FTDT consequence) → POST distribution-resolutions → buildFteIeeInput (THE producer, assembler :118, same operative resolution + same s95 base as the Div 6 feed) → entity route GET feeds fteIeeByEntity → the Stage-0 orchestrator path fires MON-098's overlay. Ratchet: mon101FteIeeCapture (6, incl. §19.2 FTDT $18,800 = 0.47 × 40% × $100k end-to-end both twins + gate-closed byte-parity). Neo-sync: buildFteIeeInput modelled + feeds edge; div7a anchor re-pinned 79→80. Suite 1,457 green; all gates green. ALSO this window: #1506 (MON-100 Stage 0) MERGED + prod READY (§17.2); VR-032 flips applied (MON-098/099 VERIFIED — Neo-G4 formally complete); handout delivered (MON-100 one-screen verification pending). **Next: Reza merges the MON-101 PR (his §12.12 migration click rides the preview→prod deploy) → Stage-1 Matrix Ring-3: seed an FTE trust (election ON, complete facts, outside-family beneficiary) → the 47% FTDT line appears with s271-15 cited on the entity tax view, converged; Reza + every non-triggering user byte-identical → then Stage 2 (PSI questionnaire) on the next GO/brief.**
> _Prior pin: merge of #1503 (`6b2ded9c`) · **on:** 2026-07-25 · **by:** Code session (MON-100 / capture Stage 0) — **NEO-G4 COMPLETE + THE REACHABILITY UNLOCK BUILT (draft PR pending, first per-issue branch `claude/mon-100-entity-tax-reachability-yhm8ug`): `/api/tax/entity/[entityId]` (GET+POST) now routes THROUGH `buildMasterTaxPositionDecimal` — the wired PSI/FTE-IEE/Div152 overlays sit on the live path.** Parity by construction (the orchestrator maps the IDENTICAL calculateEntityTaxPositionDecimal → entities[0] byte-identical, locked by deep-equality tests across 3 facts shapes) + additive crossCutting passthrough (absent until capture) + the class lock (no app/ file may import entityTaxRouter directly; wealthGraphService = the ONE reviewed status-only exception). changesNumbers: yes-CONDITIONAL — zero movement today; overlays fire when capture Stages 1-3 feed inputs. ALSO this window: #1499 (MON-098) + #1503 (MON-099) MERGED + prod READY ×2 (§17.2); A6 island allowlist EMPTY; VR-029/030/031 verdict batch applied (MON-002/003/009-014/019/021/026/091/097 VERIFIED, register ~64%); Matrix handout delivered (combined MON-098+099 A-C run pending). **Next: Reza merges the MON-100 PR → Stage 1 (FTE/IEE capture: DistributionAllocation schema fields relationship/hasQuotedTfn/coveredByIee → Reza's migration click → assembler builds FteIeeInput → Stitch-first per-beneficiary UI presented to Reza BEFORE build) → per-stage Matrix Ring-3 with a seeded triggering entity; then Stage 2 PSI questionnaire, Stage 3 Div 152 CGT-event flow.**
> _Prior pin: merge of #1497 (P2 #1499 open at build time) · **on:** 2026-07-24 · **by:** Code session (MON-099 / Neo-G4 P3) — **MON-099 BUILT (commit held until #1499 merges — Part 24 one-issue-one-PR): the Div 152 overlay is WIRED into the master tax orchestrator, and the A6 ISLAND LIST IS NOW EMPTY — Neo-G4 COMPLETE, every built tax engine reaches the live position.** `applyDiv152` (ITAA 1997 Div 152: 15-yr exemption s152-105 / 50% AAR s152-205 / retirement exemption s152-305 capped $500k s152-310 / rollover s152-410) was the last documented-but-unwired step-3 overlay. Fix mirrors MON-097/098 with one surfaced upgrade: the Decimal twin uses the engine's TRUE `applyDiv152Decimal`/`Div152ResultDecimal` sibling (loss-rule precedent). Citations + UNCOMPUTED (aggregation/retirement-cap/rollover) aggregated, surfaced never defaulted; thresholds engine-owned; 7-test ratchet (RED pre-fix) incl. hand-computed 15-yr $0 / AAR $100k / cap-bound retirement $50k; absent-input byte-parity + Float===Decimal locked. changesNumbers: yes-CONDITIONAL — Div 152 inputs captured NOWHERE + reachability rides the documented MON-098 deferral: all three overlays stay test/tool-only until the capture feature + reachability land (both queued). Neo-sync: allowlist 1→0 EMPTY (completion recorded in graphlib.mjs), feeds edge added, anchors re-pinned 224→245, PSI/FTE refs refreshed. Registry MON-099 DIAGNOSED (→ FIXING on PR). **Next: #1499 (MON-098) merges → push this commit → open the MON-099 PR (the Neo-G4 finale) → Reza merges → Matrix confirms allowlist EMPTY + inertness vs the POST-DEPRECIATION VR-029 baseline ($172,325 / $145,426 / $26,926 / $38,054 / $2,909 / $317,751) → then the capture feature (schema + assembler + UI, Stitch-first this session) is what makes all three overlays live.**
> _Prior pin: merge of #1497 · **on:** 2026-07-24 · **by:** Code session (MON-098 / Neo-G4 P2) — **MON-098 BUILT (draft PR pending): the FTE/IEE classifier is WIRED into the master tax orchestrator (both twins) — the exact mirror of MON-097.** `classifyFteIeeDistributions` (ITAA 1936 Sch 2F: FTDT 47% on outside-family distributions s271-15; Pt VA TFN withholding 47%) was documented as a step-3 overlay but never called — the second Neo-G4 A6 island. Fix: `input.fteIeeByEntity` → `crossCutting.fteIeeByEntity` via the ONE classifier in both twins (step 3.7, after PSI); citations + UNCOMPUTED (UC-FTDT-OUTSIDE-FAMILY, UC-FTE-CONTROL-TEST) aggregated, surfaced never defaulted; 47% rate engine-owned; Float===Decimal + absent-input byte-parity locked by test (6-test ratchet, RED pre-fix). changesNumbers: yes-CONDITIONAL — census: capture PARTIAL (hasFamilyTrustElection + gross distributions exist via DistributionResolution→assembler; Sch 2F relationship/hasQuotedTfn/coveredByIee UNCAPTURED) + the live entity route bypasses the orchestrator; REACHABILITY DECISION: deferred + documented in the PR (routing a live money path = its own census-heavy change, Part 24). Neo-sync: allowlist 2→1 (only applyDiv152 remains — P3), feeds edge added, orchestrator anchors re-pinned 204→224, PSI refs refreshed :310/:625. Registry MON-098 DIAGNOSED → FIXING on PR. ALSO this window: #1497 MERGED + prod READY (§17.2 verified, no runtime errors); MON-077 + MON-088 VERIFIED at VR-028 applied; MON-097 stays FIXING awaiting the Matrix's structural A–C run. **Next: Reza merges the MON-098 PR → Matrix confirms allowlist 2→1 + inertness against the POST-DEPRECIATION VR-029 baseline (Deductions $172,325 · Taxable $145,426 · Tax Owing $26,926 · Net Tax $38,054 · Medicare $2,909 · Total Income $317,751 — NOT the stale VR-028 numbers) → P3 (Div 152, allowlist 1→0) on the next brief; PSI/FTE/IEE capture feature queued (Stitch-first, this session).**
> _Prior pin: merge of #1494 · **on:** 2026-07-24 · **by:** Code session (MON-097 / Neo-G4 P1) — **MON-097 BUILT (draft PR pending): the PSI classifier is WIRED into the master tax orchestrator (both twins).** `buildMasterTaxPosition` documented `classifyPsi` (ITAA 1997 Part 2-42) as a step-3 overlay (:27-34) but never called it — a proven engine sat as a Neomatrix A6 island since 2026-06-26. Fix mirrors the loss-rule overlays: `input.psiByEntity` → `crossCutting.psiByEntity` via the ONE classifier, citations + the s86-60 `UC-PSI-DEDUCTION-RESTRICTIONS` flag aggregated (surfaced, never defaulted), Float===Decimal locked, absent input = byte-identical output (locked by test). changesNumbers: yes-CONDITIONAL — the STEP-0 census proved ZERO live movement today: (1) PSI inputs are 100% UNCAPTURED product-wide (no schema/EntityTaxFacts/assembler fields — capture feature is the named follow-up); (2) `/api/tax/entity/[entityId]` bypasses the orchestrator entirely (calls `calculateEntityTaxPositionDecimal` directly; only non-test consumers of the orchestrator are calc-audit adapters) — a G4-class reachability gap queued for P2/P3; (3) the s86-60 net calc is the engine's own flagged future sub-PR. Neo-sync: `engine.tax.psi.classifyPsi` removed from `A6_ISLAND_ALLOWLIST` (3→2; div152/fteIee remain), feeds edge added, orchestrator anchors re-pinned 186→204. Ratchet: tests/tax/mon097PsiOverlay.test.ts (5, RED pre-fix). Registry MON-097 DIAGNOSED → FIXING on PR. **Next: Reza merges the MON-097 PR → Matrix confirms the graph gap closed (no live number re-check possible — census gaps 1+2) → P2 (FTE/IEE) and P3 (Div 152) briefs follow the same pattern. Also queued: #1495 (VR-028 verdicts — flips MON-077 + MON-088 to VERIFIED on merge) + #1496 (G4 plan docs).**
> _Prior pin: merge of #1493 · **on:** 2026-07-24 · **by:** Code session (MON-077) — **MON-077 BUILT (draft PR pending): the stale 'Potential Missed Deductions' loan-interest nudge removed from My Guide.** Root: identifyMissedDeductions checked raw INTEREST expense rows (pre-MON-045 world); post-#1425 property loan interest is auto-claimed into taxPosition.deductions.property with no row → false 'Loan interest for X' for every loan-bearing investment property, contradicting the deductions above it. Fixed by removing the branch (auto-claimed = never missable) + reconcile-against-canonical JSDoc rule; depreciation + work-related genuine gaps preserved; 3-test ratchet (RED pre-fix). changesNumbers: no. ALSO: MON-088 MERGED #1491 + prod READY (migration applied); Reza entered cover answers; Ring-3 handout delivered (expect Medicare $3,509 unchanged — combined taxable $175,432 < $194k family base; the earlier 'will rise' prediction corrected: gross-vs-taxable). **Next: Reza merges the MON-077 PR → Matrix re-checks My Guide live (no loan-interest lines; depreciation nudges only where no schedule; tax numbers unchanged $317,751/$175,432/$38,628/$142,319) → MON-077 VERIFIED. MON-088 Ring-3 also queued on the delivered handout.**
> _Prior pin: merge of #1489 · **on:** 2026-07-23 · **by:** Code session (MON-088) — **MON-088 BUILT (draft PR pending; Reza GO after census + corrected ATO rule matrix): family Medicare legs + private-hospital-cover capture, for ALL users.** Census: the surcharge was computed-and-zero (PHI defaulted TRUE in both twins; caller passed taxableIncome only; tiers singles-only). Built: HouseholdMember.hasPrivateHospitalCover Boolean? (+migration) + tri-state Yes/No/Not-sure member-dialog control (Stitch 16e80455, §18.8 9.2/10) + medicareContext threaded getUserTaxPosition→calculateTaxPosition (Float+Decimal)→calculateMedicareLevy; family MLS on COMBINED income vs config-driven family tiers (×2 +$1,500/child after first), surcharge on OWN income, ALL-OR-NOTHING family cover (the ATO correction to the brief's (c) — Reza-confirmed), low-earner exception, levy family-reduction on FAMILY income; perMember two-pass through the ONE engine. Estimate callers deliberately unchanged (named follow-up). Golden re-pinned 30,724→32,284 (conservative no-cover default made visible). **Next: Reza merges the MON-088 PR → sets cover per adult in My Household → Matrix Ring-3: Medicare reflects family threshold + dependants + cover identically on tax page ↔ /cashflow ↔ CFO; per-member correct; income tax + deductions untouched.**
> _Prior pin: merge of #1481 · **on:** 2026-07-23 · **by:** Code session (MON-096) — **#1481 MERGED (`dea8eeee`, deploy READY §17.2) + MON-096 BUILT (draft PR pending): the MON-093 cadence chip no longer false-fires on MANAGED rentals.** Reza's live find: Broadbeach (correctly declared $680/wk gross, MANAGED, agent disburses ~$2,515/mo net) showed the amber "declared weekly, but payments look monthly — check the row's frequency" chip — a false positive: for a managed stream the lease cadence legitimately differs from the disbursement cadence. ROOT (verified): `rentCadenceSuspect` in `lib/calculations/propertyCashflow.ts` compared declared vs detected frequency only, never consulting `rentalMode` (already on the input rows and used for the B3 gross-up). FIX (display-only, changesNumbers: false, ONE producer §12.2.1): the flag gains `managed: rentalRows.some(rentalMode==='MANAGED')`; the property page renders NEUTRAL copy for managed ("declared weekly · paid monthly by your agent (normal for managed rentals)") and keeps the amber warning ONLY for DIRECT (the MON-093 Broadbeach mis-declaration class stays guarded). Ratchet: 3 tests in propertyCashflow.test.ts (MANAGED mismatch → managed:true + money unchanged $2,947 gross; MANAGED agreeing → null; DIRECT mismatch → managed:false warning). Registry MON-096; anchors re-pinned (:195/:219); all gates green. **MON-094 VERIFIED at VR-022** (Total Income $327,801→$317,751, −$10,050 exact; taxable+tax fell in step; old admin surface 404) and **MON-095 VERIFIED at VR-023** (AIA group gone; QBE/Mate preserved — held for Fix B; badge 3→2; no money moved) — both run records merged via #1482, registry moved in this branch. **MON-096 VERIFIED at VR-024** (#1484 merged; Broadbeach neutral note live, Guildford DIRECT warning intact, money unchanged). **Fix B design APPROVED (#1486 merged) and the BUILD is done in this session (draft PR pending): survivorEdits {amount?, frequency?, isRecurring?} on POST /api/intake/duplicates (validated whole-or-rejected, applied to the survivor only with {id,userId} WHERE, same transaction, post-GROUP_STALE; executeMerge pure); duplicates page rebuilt to the two-zone design (KEEPING editable / REMOVING struck-through / recap chip / typed-MERGE); tab renamed 'Duplicate records'. Next: Reza merges the build PR → merges QBE (→ One-off) + Mate (→ Recurring Monthly) via the new edit fields → Matrix final duplicates re-check → MON-095 promotion toward CLOSED.**
> _Prior pin: merge of #1479 · **on:** 2026-07-23 · **by:** Code session (MON-095 Fix A) — **DUPLICATE-MATCHER GUARDRAIL BUILT (draft PR #1481): the AIA false-positive class killed in the ONE decision layer.** Reza's live review of Housekeeping → Duplicate income found two DIFFERENT AIA policies (68718123 $131 / 68718100 $158) offered as a merge — root: `normalizeMerchant` strips policy numbers + the exact path had no amount check; systemic (the same path is the #1458 intake guardrail). FIX (brief (a)+(c), expense-scoped): `expenseDuplicateCompatible` in classifyIntake — ≤10% amount band (THE one tolerance, now exported) + disjoint-identifier fail-safe (ABN/ACN excluded) on exact+near of both policies; INCOME exempt by pinned Mechanism-A doctrine (locked green). AIA no longer groups; QBE/Mate still do. Registry MON-095 FIXING (#1481). **Fix B (merge-with-edit + explicit kept/removed hierarchy + "Duplicate records" rename) = Stitch design-first → Reza >9/10 → Opus build — NOT started here.** DATA: never merge AIA; hold QBE/Mate for Fix B (cadence corrected in the same step). Also open: MON-094 awaits Reza's two RECLASSIFY confirms + Matrix VR-022; MON-091 write checks; Broadbeach declared basis; Housekeeping dark/mobile Stitch backfill. **Next: Reza merges #1481 → re-opens Duplicate records (AIA gone) → Fix B design pass → after Fix B, merge QBE/Mate with corrected cadence → Matrix live re-check.**
> _Prior pin: merge of #1477 · **on:** 2026-07-22 · **by:** Code session (Housekeeping relocation) — **THE VR-021 CLASS FIX BUILT (draft PR #1479): personal-data tools relocated OFF the staff portal INTO the app.** Per Reza's standing ruling + the Matrix build brief + the approved Stitch design (#1477, screen 124c6e36…, §18.8 9.2/10): new "Housekeeping" trailNav section (sidebar + mobile More) with sub-tabs; `/dashboard/housekeeping/tax` + `/dashboard/housekeeping/duplicates` on the USER session (useAuth token — the VR-021 principal fix); both `app/admin/*` pages DELETED; GET non-assessable-review returns server-computed annualEffect/total (canonical toAnnual + MON-053 semantics — no surface money math); AFSL guard test (the Stitch mock's fabricated "AFSL 123456" can never ship); topology Ratchet locks the class (APIs unreachable from app/admin/**); FIX_PROTOCOL §7 retro (Stage-2 gate: name the session principal for every new surface). Engine/detector/merge logic untouched. Deviations surfaced: global sidebar badge deferred (§12.10); Stitch dark+mobile artefacts = desktop-session backfill (§18.2.1). Registry MON-094 fixPRs += #1479, stays FIXING. Suite 4,187 green; all gates green. **Next: Reza merges #1479 → opens Housekeeping → Tax classification (his own session — the two ATO rows now visible) → two typed-RECLASSIFY confirms → Matrix VR-022 (principal check first; Other Income $10,300 → ~$250; Total Income −~$10,050; guards clean) → MON-094 → VERIFIED.**
> _Prior pin: merge of #1475 · **on:** 2026-07-21 · **by:** Code session (MON-094 / VR-021 FAIL) — **#1475 MERGED + deploy READY (§17.2), but VR-021 FAILED on surface placement: MON-094 stays FIXING.** The engine fix is correct yet UNREACHABLE — the Tax review page was built on the STAFF admin portal, which authenticates as `admin@monitrax.com.au`, not the consumer account owning the income; its user-scoped query returned empty ("Nothing to review") and the two ATO rows stayed fully taxable (Taxable Income $185,481 unchanged). **Reza ruling (2026-07-21, standing): personal-data review/write tools live in the MONITRAX APP, never the staff admin portal.** Audit: exactly TWO tools to relocate — `intake-duplicates` + `tax-review` → NEW app section **"Housekeeping"** (`/dashboard/housekeeping/{duplicates,tax}`). Flow: Stitch design-first (Matrix brief issued; generation runs in the DESKTOP session on the dark app design system; Reza approves >9/10 §18.2.1) → Opus 4.8 build relocates both pages (user session, income.read/write — drops the admin token interceptor), deletes both `app/admin/*` pages, engine/detector wiring (#1475) unchanged, FIX_PROTOCOL §7 ledger retro in the same PR → VR-021 re-run from the NEW user surface → MON-094 → VERIFIED. MON-091 write checks 13/14 still await Reza's authorisation; Broadbeach declared-basis correction still pending (amber chip). **Next: Stitch design → Reza's approval → the relocation build → VR-021 re-run.**
> _Prior pin: merge of #1474 · **on:** 2026-07-21 · **by:** Code session (MON-094) — **MON-093 VERIFIED AT VR-020 (#1473 merged `c7c7c80`, §17.2 deploy READY; #1474 = the run record).** The Broadbeach ×4.5 rental inflation is DEAD on every surface: property detail $2,947/mo declared / $35,360/yr / 5.89% yield ≡ list tile ≡ tax (rental total $121,881 < the old Broadbeach-alone $135,941; per-property result $14,921/yr exact); amber cadence chip LIVE flagging the uncorrected declared-Weekly/paid-monthly row; guards clean (loan $1,191, liquid $301,808/11.6mo, one-off labels, Lot-1/Lot-2 distinct); baseline diff fully explained. Reza's data correction pending (declared basis $680/wk OR $2,947/mo → clears the chip). **MON-094 BUILT (draft PR #1475)** (tax "Other Income" $10,300 counts ~$10,050 of non-assessable ATO refunds — VR-020 §D pre-fix baseline) per the Matrix code brief + **Reza's confirmed rule: assessable-only, exclude non-assessable automatically** (ATO refunds / internal transfers / loan drawdowns / reconciliation artifacts; genuine one-offs count once; exact descriptor list = confirm-at-merge; NO silent rewrite of existing rows — per-row confirm §12.11). Fix shape (census-verified, NO schema change): carry `taxCategory` into the engine's IncomeItem + row-level non-assessable override (Float+Decimal), intake tagging in classifyIntake, review-and-confirm surface for the existing rows. MON-091 stays FIXING (write checks 13/14 await Reza's authorisation). **Next: MON-094 PR → Reza merges + confirms the exclusion list + per-row reclassification → Matrix VR-021 (Other Income → assessable subset; TOTAL INCOME −~$10,050 from $327,801; guards: Broadbeach $2,947/$35,360/5.89%, loan $1,191, liquid $301,808, salary $195,620, one-off labels).**
> _Prior pin: merge of #1471 · **on:** 2026-07-20 · **by:** Code session (MON-093) — **THE BROADBEACH ×4 RENTAL KILLED AT THE ROOT (draft PR #1473) — VR-019 Fix 1 (Sev-1).** The Matrix's VR-019 run (item 16 FAIL) reported property rent $11,328/mo vs income page $2,515 (~4.5×), corrupting tax (~$115,502/yr rental). §19.2-exact root: `resolveMonthly`'s PRIVATE day-span copy (the SIXTH, after #1465 killed five) dropped the trailing payment of the 2-txn advance rental → null → single-txn fallback annualised UNSORTED txs[0] ($2,614.25) by the mis-declared WEEKLY frequency: ×52/12 = $11,328.40 reproduced to the cent. FIX: the day-span producer MOVED to `lib/calculations/actualsMonthlyAverage.ts` (DAYS_PER_MONTH = 365.25/12 THE one constant); resolveMonthly DELEGATES (private math deleted); 1-txn branch takes the LATEST payment; NEW `rentCadenceSuspect` → amber chip on the property rental row when declared cadence disagrees with payment dates. Neo-sync incl. structural-graph hand-move (graphify offline, 1ff5803d precedent). Registry: MON-093 → FIXING (#1473); VR-019 verdicts applied — MON-089/090/092 → VERIFIED, MON-091 stays FIXING (write checks 13/14 await Reza's authorisation). Suite 4,170 green; all gates green. **Next: Reza merges #1473 → per-fix check (Broadbeach ≈ $2,515/mo on property ≡ income; tax rental ~$30,180/yr; amber chip visible; he corrects the declared row basis $680/wk or $2,947/mo) → then Fix 2 (tax "Other Income" ATO-refund exclusion) starts, gated on his assessable-income rule confirmation.**
> _Prior pin: merge of #1468 · **on:** 2026-07-20 · **by:** Code session (MON-092) — **ONE-OFF DISPLAY RULE APPLIED CLASS-WIDE + SAME-DAY PHANTOM AVERAGE KILLED (draft PR #1469).** Reza's third-recurrence report diagnosed honestly: the DATA was right (recurring-unticked → isRecurring=false held; Net $0 proved it) but sibling views printed the raw 'Monthly' placeholder (MON-048 rule was list-view-only) and the MON-090 Actual cells dressed one-offs in stream language; PLUS a real formula edge — calculateMonthlyAverage clamped zero-day spans to 1 day, extrapolating two same-day gifts ($800+$700) into $22,830/mo (reproduced exactly; correct = $1,500 once). FIX: same-day guard in the ONE producer; one-off branch in every Actual cell; activityFrequencyLabel gate on EVERY label render (5 sites across both pages — the new enumerating topology lock caught a third ungated site during the build). Registry MON-092 → FIXING (#1469). Follow-ups noted for Reza: unlink orphan rows; bank-descriptor name fragmentation (NeoBrain). ALSO: #1468 (income frequency at link boundary) MERGED + §17.2 READY/clean; Ingeus now Fortnightly with correct amber quiet-chip. **Next: Reza merges #1469 → gift rows read 'One-off · $X once' → his Thornland fortnightly confirmation → Matrix runs RING3_DATE_AWARE_NUMBERS.md.**
> _Prior pin: merge of #1467 · **on:** 2026-07-20 · **by:** Code session (MON-091) — **INCOME FREQUENCY AT THE LINK BOUNDARY FIXED (draft PR #1468): the Thornland fortnightly class.** Reza confirmed the cadence world: 4 Cienna payments are FORTNIGHTLY but the link dialog had no frequency control for income (invisible MONTHLY default) AND both reuse paths (MON-009 scope-singleton :538, Mechanism-A signature update) never revisited cadence — the Lot-1 row stayed Monthly $1,195 → +202% variance vs the correct $3,608 avg (reproduced exactly). FIX: detected-cadence Frequency selector in the income section (suggest-and-confirm); frequency sent ONLY when evidence-prefilled/user-touched (freqExplicit — naked defaults OMITTED, clobber trap closed in self-review); server updates the reused row's cadence ONLY on explicit confirmation (amount never touched on rental reuse). +2 golden scenarios on the REAL route. Same PR: MON-090 grouped-view declared-first parity (Hipcamp $0 rows). Registry MON-091 → FIXING (#1468). ALSO this session: #1467 (MON-090 date-aware actuals) MERGED + §17.2 READY/clean — Reza verified income looks right; his rental re-check surfaced this issue. **Next: Reza merges #1468 → re-links one Cienna payment confirming Fortnightly (or Edits the row) → Lot-1 plan reads ~$2,590/mo and the variance collapses to the real gap → then the Matrix runs docs/verification/briefs/RING3_DATE_AWARE_NUMBERS.md.**
> _Prior pin: merge of #1466 · **on:** 2026-07-20 · **by:** Code session (MON-090) — **DATE-AWARE ACTUALS DISPLAY BUILT, DECLARED-FIRST (draft PR #1467) + THE HOLISTIC SWEEP VERDICT.** Reza's mandate ("data should be date aware… average AND actual, based on the dates… all transactions date and frequency aware") + his amendment (declared-first: declared = plan + baseline; cadence average stands in only with no declared amount). SWEEP: the transactions→monthly class had FIVE producers — ALL collapsed to `calculateMonthlyAverage` in #1465 (income-ARREARS ×N/(N−1) + link-banner were broken); the calendar-month family (actualCashflow → canonicalCashflow/master/insights/intelligence; tie/analytics; moneyStoryTrend) is CLEAN; analyzeTransactionPattern frequency-aware; forecasting = dense-stream heuristics (noted). BUILT: `detectStaleStream` (ONE producer, 1.5× cadence threshold, 5 unit tests incl. the honest Transport boundary — 38d quiet NOT yet stale for monthly, chip ~27 Jul); `lastTransactionAt`+`staleStream` on income/expenses/loans routes; income page (avg /mo marked + "N payments · last <date>" + "this month · $X" + amber quiet chip + group "received this month"; declared-first variance); expenses page NEW Actual column (was declared-only). Stitch 711e25c5… v1 6.5→v2 9.2 (§18.8), artefacts merged #1466. Registry MON-090 → FIXING (#1467, changesNumbers:false). **Matrix handout committed: `docs/verification/briefs/RING3_DATE_AWARE_NUMBERS.md` (14 numbered checks, flux-aware — Reza's data is mid-edit). Next: Reza merges #1467 → his eyeball + hands the brief to the Matrix → MON-089/090 toward VERIFIED; duplicate-group cleanup still pending on the Intake Duplicates page (Ingeus fortnightly ×3 should now group).**
> _Prior pin: merge of #1464 · **on:** 2026-07-20 · **by:** Code session (MON-089) — **ACTUAL-MONTHLY ×N/(N−1) INFLATION FIXED (draft PR #1465, branch restarted from main):** Reza's live report (Transport salary: 2×$11,074 in different months displayed as $23,247/mo, +110%) root-caused §19.2-exact — the income route's ARREARS day-span average divided N payments by N−1 intervals (22,148/29×30.44 = 23,247 reproduced; correct 22,148/58×30.44 = 11,624). Census found FIVE copies of the formula (income ARREARS buggy · income ADVANCE · expenses · loans · link-route banner buggy). Remove-the-culprit: all four route copies deleted → every surface imports THE modelled canonical `calculateMonthlyAverage` (propertyActuals — was already correct). Ratchet: Ring-0 worked examples + Ring-1 topology lock (no re-inlined ×30.44 in migrated files). Registry MON-089 → FIXING (#1465, full censuses + trio). Recorded unification: single payment reads as one month's actual (was blank). ALSO this session: #1463 (intake-duplicates auth) + #1464 (same-property rental coverage + KEEP-directive: merge tool stays permanently, removal cancelled) both merged + §17.2 READY/clean; guardrail live-proven (Reza's re-categorisation attached txns to the existing row — no new duplicate). **Next: Reza merges #1465 → his per-fix re-check (Transport ≈ $11,624; Ingeus fortnightly sensible; link-dialog banner sane) → MON-089 toward VERIFIED; then his duplicate-group cleanup + Matrix consolidated Ring-3.**
> _Prior pin: merge of #1463 · **on:** 2026-07-20 · **by:** Code session (intake-duplicates follow-up) — **INTAKE DUPLICATES: AUTH FIX MERGED (#1463, §17.2 READY+clean) + KEEP-DIRECTIVE RECORDED + SAME-PROPERTY RENTAL COVERAGE (this PR).** Reza's page load showed "No duplicate groups"; root-caused honestly: (a) his manual salary re-categorisation likely resolved Ingeus ×3, (b) a real blind spot — `findDuplicateGroups` excluded ALL rental income, hiding the VR-007 Cienna Lot-1 ×3 (same-property rent duplicates, likely pre-MON-009 data). Fix: rental rows enter grouping ONLY when property-scoped → the compatibility rule permits strictly SAME-property groups; scopeless + cross-property rent never group (Lot-1 vs Lot-2 distinct-sources correction intact); rental groups carry an explicit warning. **STRATEGIC DECISION (Reza 2026-07-20, supersedes the removal directive): the merge functionality is KEPT permanently** — standing incident tool + sentinel (expected empty; non-empty = guardrail regression alarm); the remove-the-merge-action plan is CANCELLED (MON-074/076 notes). **Next: Reza merges this PR → re-checks Admin → Intake duplicates (Cienna Lot-1 group should appear if the rows remain) → his per-group cleanup → Matrix consolidated Ring-3.**
> _Prior pin: merge of #1461 · **on:** 2026-07-20 · **by:** Code session (Part A) — **HOUSEHOLD INCOME ATTRIBUTION + PER-PERSON TAX BUILT + MERGED (#1461):** "Who earns this?" in both income forms (required for SALARY, >1 earner); listIncomeEarnerEntities (member → INDIVIDUAL entity via householdMemberId — reuses ownerEntityId, no schema change); getUserTaxPosition emits perMember (same rows partitioned by owner, SAME engine; household roll-up byte-identical); golden ring2.perMemberTax. Boundaries recorded: per-person DISPLAY = §18.2.1 Stitch pass (next); super owner-split follow-up; server default stays primary. **MON-088 raised** (family Medicare legs unwired: calculateMedicareLevy supports FAMILY/spouseIncome/dependants but taxPositionCalculator:295 always passes SINGLE — awaiting Reza queueing; no joint lodgment exists in AU, marginal rates are individual; family drives MLS $202,000 combined threshold + levy reduction + PHI tiers). Mechanism A fully live (#1458 guardrail + #1459 merge tool, both §17.2-verified). **Next: Reza attributes Newsha's income (or merges the Ingeus group with her as owner) → Part-C2 approvals in Admin → Intake duplicates → Matrix consolidated Ring-3. Part B (reconcile-dialog frequency selector) routed to Opus 4.8, unclaimed.**
> _Prior pin: `663009c9` (merge of #1458 — Mechanism-A Part 1 GUARDRAIL, prod verified §17.2 READY+clean) · **on:** 2026-07-19 · **by:** Code session (Mechanism A) — **PART 2 BUILT on top (draft PR #1459): the Reza-gated duplicate-merge preview-and-confirm.** `lib/intake/duplicateMerge.ts` (findDuplicateGroups clusters under THE Part-1 signature policy — preview ⊆ guardrail, over-merge structurally impossible; survivor = scoped-first-then-oldest; effect via canonical annualRunRate) + `GET/POST /api/intake/duplicates` (POST requires `confirm:"MERGE"` + server-side group re-derivation, stale → 409, NO merge-all) + `/admin/intake-duplicates` (per-group typed-MERGE confirmation; admin design system). executeMerge repoints ALL 7 FK references (UnifiedTransaction/Transaction/SuperContribution/TransactionSplit/AssetServiceRecord/derivedFromIncomeId/AgentDisbursementRule w/ conflict surfacing) inside a transaction, then deletes; the survivor's fields never change. 7-test suite; Neomatrix `engine.intake.findDuplicateGroups` + 3 edges; MON-074 → FIXING (#1459). The GET preview IS the live row-level census (genuine same-source groups vs distinct same-payer sources). **Next: Reza merges #1459 → opens Admin → Intake duplicates → approves each group (his §12.11 click) → Matrix Ring-3 (declared gross + tax + source counts move consistently across income ↔ /cashflow ↔ tax; guards until any merge: tax $89,287/$142,319, net worth $3,401,782, liquid $301,808).**
> _Prior pin (same day): Mechanism-A Part 1 (#1458, merged) — **CALC-SSOT WALL MECHANISM A KEYSTONE BUILT (Part 1 guardrail, draft PR #1458 on `claude/phase-59-managed-rental-yhm8ug` restarted from main): reconcile/import now UPDATES the canonical row instead of minting a duplicate, at every intake door.** `classifyIntake` gained the `source-signature` policy (identity = kind+type+normalised name+ownerEntityId over USER-WIDE candidates) with the scope-compatibility rule INSIDE the classifier (same scope or one side scopeless; two differently-scoped rows — QBE on A vs B, Lot-1 vs Lot-2 rent — NEVER converge; the Reza distinct-sources correction enforced structurally). Routed: link-route income create (non-rental → the `:831` update template — kills Ingeus ×3), link-route expense (cross-scope tier — kills battery ×3), `reconcileSuggestedAction` (income converges across amount drift; expense stays amount-bounded by pinned doctrine), `POST /api/income` (409 `DUPLICATE_INCOME_SOURCE` on exact manual dup; rental scope-singleton converges — the wizard SSOT boundary), onboarding complete (idempotent skip). Ratchet: `ring2.mechanismA.intakeDedup` golden (REAL link route, fails pre-fix) + 6 classifier unit tests; contract pins re-pointed. Neo-sync: `engine.intake.classifyIntake` + `law.intake.oneRowPerSource` (neobrain domain); CALC_SSOT_WALL Mechanism A → BUILT; GUARDRAIL §5 MON-076 brick; PHASE_54 §12.5. Registry: MON-084/085/076 → FIXING (#1458), MON-074 → DIAGNOSED. Recorded deviations for Reza: scope-compat (not blanket unscoped) + 409-not-silent-update on manual. Full suite 4,137 green; build green. **PART 2 (separate PR after #1458 merges): the Reza-gated preview-and-confirm merge of the EXISTING duplicates (Ingeus ×3, battery ×3) — its preview IS the live row-level census; NO merge without Reza's per-group click (§12.11). Next: Reza reviews/merges #1458 → Matrix Ring-3 (link a new Ingeus deposit → source count stable; income ↔ /cashflow ↔ tax counts consistent; guards: tax $89,287/$142,319, net worth $3,401,782, liquid $301,808 unmoved).**
> _Prior pin: `6b409532` (merge of #1454 — VR-017 handoff) · 2026-07-19 · Code session (MON-031/064 re-fix) — **VR-017 RE-FIX BUILT (draft PR #1455 on `claude/phase-59-managed-rental-yhm8ug` restarted from main): the emergency-fund "Liquid savings" gross leak is closed at the producer.** §19.2 EXECUTED evidence CORRECTED the VR-017 hypothesis: no sibling producer exists — #1452's netting input `netWorth.liabilities.creditCards` is LOANS-only (netWorthCalculator.ts:219) and the live Qantas card is a CREDIT_CARD-typed ACCOUNT at −$2,496 (VR-007 capture:483), so the subtraction was silently 0 and `quickMetrics.liquidCash` stayed gross ($304,304 → months 11.7); Balances' $301,808 was an accident of the buckets' `min(gross, assets.accounts)` (also explains VR-010's inert caption); the golden passed because its fixture used a LOAN card — F2 at the FIXTURE level. FIX: ONE producer `lib/calculations/liquidCash.ts` `computeLiquidCash` nets BOTH card representations (Float+Decimal); master + CFO emergency-buffer routed through it, inline netting deleted. Ratchets: `ring2.liquidCashParity.accountCard.test.ts` (LIVE topology + topology-signature assertion + I9 months identity through the REAL master + safety-net route — fails on pre-fix code) + months identity added to the loan-card golden + `liquidCash.test.ts` worked examples + 2 calc-audit card fixtures. Neo-sync: `engine.liquidCash.computeLiquidCash` (semanticKey liquidCash) + `engine.emergencyFund.buildEmergencyFundMetrics` modelled as CONSUMER, 4 edges, anchors re-pinned, all gates green. FIX_PROTOCOL §7 ledger: census rule tightened — multi-representation entities (card = loan OR account) require the fixture to mirror + assert the LIVE representation's signature. Registry: MON-031/064 stay FIXING (fixPRs += #1455). Full suite 4,125 green; build green. **Next: Reza merges #1455 → deploy READY → Matrix Ring-3 re-run: $301,808 / 11.6 on Safety Net + Home Health + Balances + /cashflow (guards: net worth $3,401,782 + bucket tie, per-property cashflow, tax $89,287/$142,319) → MON-031 + MON-064 → VERIFIED.**
> _Prior pin: `68be3680` (merge of #1447 — VR-015) · 2026-07-18 · Code session (MON-020/060) — **TAX-PRODUCER COLLAPSE BUILT (branch restarted from main, draft PR):** ONE engine had FOUR assemblers (F2 same-engine-different-inputs): canonical `getUserTaxPosition` (/cashflow+CFO) · `/api/tax/position`'s verbatim-clone assembly · master `buildTaxSummary` (NO depreciation/loan-interest/super/franking → the MON-060 activity-Sankey divergence, proven from source) · dead `/api/tax` (zero consumers, own inline brackets). Collapse: the bundle now exposes `engineInputs` (+ optional FY override); the Tax route consumes it (Float + Decimal twin from the SAME inputs — zero number change); master adapts the bundle via `buildTaxSummaryFromPosition` (numbers converge BY DESIGN); `/api/tax` deleted. Ratchet `tests/golden/ring2.taxParity.test.ts` (route ≡ bundle ≡ master; §19.2 pin taxable 124,800 / net tax 30,724 exact); golden harness +loanTransaction/groupBy (fail-loud caught the new query); 3 topology string-locks re-pointed. RECORDED FINDING for Reza (not silently changed): master's old tax base also applied rental-dedup + isTaxable — folding those into the canonical assembler = the remaining semantic unification (MON-020 notes). Registry: MON-060 → FIXING #1448 (full censuses + plain trio); MON-020 notes + test. All gates green; full suite 4,104. **Next: Reza merges → deploy READY → Matrix cross-surface Ring-3 (Tax page ≡ /cashflow ≡ activity flow ≡ CFO) → MON-020 + MON-060 → VERIFIED.**
> _Prior pin: `7163caba` (merge of #1440 + #1428) · 2026-07-17/18 · Code sessions (VR-013 loan-cost actuals-first fix #1442 → MON-081 VERIFIED at VR-014; MON-087 Select-crash class fix #1446 → MON-087/083 VERIFIED at VR-015; full-sanity sweep VR-015 PASSED — the whole VR-013/014/015 cluster MON-079/080/081/082/083/086/087 closed-loop VERIFIED)._
> _Prior pin: `94742ea` (merge of #1439 — the Calc-SSOT Wall docs) · 2026-07-17 · Code session (Calc-SSOT Wall Part 1) — **WALL PART 1 BUILT (A1+A2+A3+B1+B2+B3, one draft PR on `claude/phase-59-managed-rental-yhm8ug` restarted from main, per-part commits):** A1 source-lock lint (`npm run lint:source-lock` in `vercel-build`; raw-row reads in `app/**/page.tsx`+routes fail CI; debt seeded 84 matches/35 pairs, line-free `{file,pattern,count}`, RATCHET-DOWN-ONLY — over OR under the count fails). A2 `MATRIX_FIX_DISCIPLINE.md` wired load-bearing: CLAUDE.md §0.4 four clauses on every fix, §12.2.1 detection kit, §20.6 fix-PR 5-item checklist required for Logic 10/10; FIX_PROTOCOL Stage-1 step-0 holistic SSOT audit; `.github/pull_request_template.md` created. A3 MON-081…086 raised DIAGNOSED (loan-$0 · expenses-isRecurring · one-off-cadence · SALARY-reuse-guard · cross-scope-dedup · managed-cashflow-double-count) + MON-079/080 re-scoped ("VR-011 verified TAX only; cross-surface Ring-3 not met"). B1 `resolveLoanMonthlyCost()` (declared cadence-normalised → actuals-first → interest floor, NEVER $0) extracted + 7 surfaces migrated. B2 `monthlyRunRate()`/`annualRunRate()` (one-off → 0) + Mechanism-C form (frequency hidden for one-offs). B3 (critical): `computePropertyCashflow` grosses managed rent back up when actuals resolve NET (recurring derived fees, per stream) — fee counted EXACTLY once, cf.rent ≡ tax gross, cashflow = net received; DIRECT/declared untouched; threaded through masterFinancialService + portfolio snapshot. Ratchet `tests/golden/ring2.calcSsotWall.test.ts` (Broadbeach 2,946.67 gross / 431.67 once / 2,515 net; taxable ≡ cashflow×12). Neomatrix: 2 new nodes + 6 edges, 2 anchors re-pinned, 270 nodes green. **Next: Reza merges the draft PR → deploy READY → the Matrix runs the CROSS-SURFACE Ring-3 on live data (Broadbeach loan · HOME one-offs · Broadbeach managed cashflow) → MON-081/082/083/086 VERIFIED; Mechanism A (MON-084/085) + the 84-site ratchet queue are the next Wall parts.**
> _Prior pin: `78711bf` (merge of #1436) · 2026-07-17 · Code session (MON-080) — **MON-080 BUILT (D0+D1+D2, one draft PR on `claude/phase-59-managed-rental-yhm8ug` restarted from main): Phase 59's managed-rental deduction now works on real data.** §19.2 EXECUTED-verified root cause corrected the handoff's suspect (N≥2 normalisation always worked; the real D0 was the N=1 fallback comparing weekly gross to a monthly payout → gap −$1,835). D0: deposit-size cadence inference in the ONE engine (Broadbeach $680/wk + one $2,515 deposit → MONTHLY, gap $431.67 material, Float=Decimal). D1: retroactive reconcile on the MANAGED transition (income PUT returns the card; GET /api/rental-reconciliation powers the now click-to-claim chip). D2: 422 GROSS_REQUIRED gate (never persist gross=net, ITAA s6-5), pre-filled confirm-or-correct prompt, RENTAL streams get the Edit amount field. Ratchets: Ring-0 Broadbeach fixtures + Ring-2 order-independence golden (manage→link == link→manage) + R1 wiring lock. **Stitch (Reza directive 2026-07-17: ALL UI/UX strictly through Stitch, self-review >9/10 → his final review):** reconcile-card light (9.3/10 after one iteration) + dark (9.3/10) committed under `.stitch/designs/mon-080/`; mobile + D2 form-state screens awaiting Reza's direction. Registry: MON-080 FIXING; VR-010 verdicts applied (11 → VERIFIED, MON-053 → CLOSED, MON-031 note). Full suite 4,072 green; all static gates green. **Next: Reza reviews/merges the draft PR → deploy READY → Matrix per-fix Ring-3 on Broadbeach (~$432/mo captured without unlink/relink; gross unchanged $2,947/mo; net-only stream blocked) → MON-080 VERIFIED → unblocks MON-079 → VERIFIED → CLOSED → VR re-baseline.**
> _Prior pin: `d50ff6a` (merge of #1432) · 2026-07-16 · Code session (Phase 59) —_ **PHASE 59 BUILT (Parts 0–5, one draft PR on `claude/phase-59-managed-rental-yhm8ug`): Managed Rental Income & Agent-Cost Reconciliation** per spec PR #1433 §9. Issue **MON-079** raised + modelled (`number.rental.agentCostDeduction`/`grossDeclared`). Schema (rentalMode/managingAgentName, derived Expense fields, AgentDisbursementRule, additive migration — **awaits Reza's §12.11/§12.12 approval**); ONE engine `reconcileManagedRental()` (Float+Decimal, cadence via the canonical detector) + Neomatrix Model step same PR; suggest-and-confirm card (spec §8 tokens) + learn-once rule + anomaly re-confirm; grounded Neobrain statement parser (+ RENTAL_STATEMENT doc type); D4 rent-gap detector + R0/R1/R2 ratchets (golden $650/wk-vs-$1,100/ft locked on both engines). **Next: Reza reviews/merges the draft PR → deploy READY → hand back to the Matrix for the Ring-3 (Part 6) → VERIFIED → re-baseline → close MON-079.**
> _Prior pin: `4cd44afe` (post-#1424) · 2026-07-15 · Code session (cluster ①, continued) —_ **MON-053 VERIFIED (Ring-3 PASS — Reza reclassified the ATO rows; Total Income $412,768 / est. tax $141,548, arithmetic exact). MON-045 STAGE 2 SHIPS IN PR #1425 (draft):** deductible loan interest auto-derived in the ONE engine (actuals-first, HOME-gated, expense de-dup); all four rogue neg-gearing producers deleted (taxIntegration P1+P2, portfolioEngine P3, orphaned property-roi route P4); CFO benefit derived from the canonical position (≤ deductions by construction); Ratchet `tests/tax/mon045PropertyLoanInterest.test.ts`; Neomatrix moved with the fix. Awaits Reza merge approval + per-fix Ring-3 vs the healed base.
> _Prior pin: `f19cb31a` (merge of #1420) · 2026-07-15 · Code session (cluster ①) — MON-053 FIX SHIPPED:_ `Income.isRecurring` (schema + additive migration), one-off guard on BOTH tax-engine annualisation paths (Float + Decimal), producers P1–P3 + link-dialog gate + income-form control threaded, run-rate excludes one-offs, Ring-1 source-lock (all 4 annualisation sites guarded, count locked). Awaits Ring-3 (the two ATO rows → reclassify via Edit → declared gross falls ~$120.6K from $524,831). **Sequencing: MON-053 lands BEFORE MON-045 stage 2** (else stage 2's Ring-3 verifies against an inflated base). Re-baseline (VR-008) after this deploys.
> _Prior pin: `818a2db` (merge of #1418) · 2026-07-15 · Cowork "The Matrix" — VR-007 CONSOLIDATION: the VR-007 intake was renumbered **+2 → MON-053…MON-074** (its original MON-051…MON-072 collided with the Matrix-ingestion MON-051/052 registered in #1418). Any reference to VR-007's internal numbering maps +2._
> _Prior pin: `38abeee` (merge of #1416) · 2026-07-15 · Cowork session — The Matrix HQ (full-repo ingestion + cursor truth-restore; prior cursor was ~250 PRs stale at `b03975d`). That truth-restore stands; VR-007 only ADDS to it._

> **OPEN THREADS (Code session 2026-06-16/17) — ⚠️ status UNVERIFIED-current as of 2026-07-15; re-verify each in live logs/env before acting:** (1) **Per-item Documents upload — THIS PR**: `DocumentsSection`
> on Properties/Investments/Assets (Super deferred — needs `SUPER` LinkedEntityType + migration). (2) **Mobile scan
> recognition BROKEN** — `/api/documents/analyze-for-form` 500s at the Vision OCR step; Vision API is *enabled* but
> shows ZERO traffic → auth failing before the call. Vision authenticates via `GCS_SERVICE_ACCOUNT_KEY`
> (`visionService.ts:69-91`) with NO keyless fallback → **do NOT delete that key** (breaks OCR) until Vision is made
> keyless (WIF as `vercel-monitrax-db`). Next: make Vision keyless + get the exact `analyze-for-form` error.
> (3) GCS keyless cutover: bucket + `vercel-monitrax-db` `objectUser` granted; factory gate fixed (#1127 merged);
> key NOT yet deleted (and must not be until Vision is keyless).
**Freshness gate:** on session start, compare this HEAD to live `git rev-parse HEAD`. If they differ,
the repo moved — re-verify the cursor below against the live plan BEFORE acting. Do not trust a stale cursor.

---

## A. WHAT MONITRAX IS  (north-star — for detail, see `docs/blueprint/MASTER_BLUEPRINT.md`)

- **Product:** Monitrax (monitrax.com.au) — an Australian Wealth Operating System. Brings property, loans,
  super, investments, cashflow, tax position and entity structures into one picture so users can model the next move.
- **Built by:** Reza, under ReNew Holding Company Pty Ltd (ACN 675 267 311).
- **Regulatory boundary (HARD):** a financial *information* service, NOT a licensed adviser. Surfaces maths and
  mechanisms; never gives personal financial advice, recommends products, or implies licensing not held.
  Respects the AFSL/Credit/Tax boundary + CDR. *(Confirm current ICP/positioning live — see cursor SEC C.)*

## B. THE MAP  (authority order — full registry in `docs/00_INDEX.md`)

0. `SYSTEM_MAP.md` (repo root) — **orientation pointer-map.** What Monitrax is, every authoritative doc +
   what it owns, architecture overview, calc-engine inventory, tool stack. Start here after this STATE.md.
1. `CLAUDE.md` (repo root) — **law.** Governance, four-lens mindset, SSOT + single-calc-engine rule, warm-words,
   session protocol (Parts 1/7/10). When anything conflicts with CLAUDE.md, CLAUDE.md wins.
2. `docs/IMPLEMENTATION_PLAN.md` (hub) + `docs/implementation/*` (spokes) — **status SSOT.** Shipped / active /
   queued / blocked / reversed. Split from one 884 KB file into a thin hub + spokes (F-8, 2026-06-15) so each
   stays connector-writable: `01_ACTIVE_WORKSTREAMS` / `02_UP_NEXT` / `03_OPEN_QUESTIONS_AND_BACKLOG` /
   `04_RECENTLY_COMPLETED`. Start at the hub; read the relevant spoke. STATE.md holds the *cursor*; the spokes hold the *detail*.
3. `docs/00_INDEX.md` — **the map** of every doc. Start here to locate anything.
4. Topic authorities: architecture -> `docs/architecture/`; phases -> `docs/blueprint/MASTER_BLUEPRINT.md`;
   compliance -> `docs/compliance/`; GTM -> `docs/marketing/` (+ `docs/marketing/gtm/`); design -> Stitch system
   (`docs/design/`); calc engines -> `lib/calculations/*` + `lib/services/masterFinancialService.ts`.

## C. RESUME CURSOR  (regenerated at every session END — the live "where we are")

> Re-pinned 2026-07-15 (Cowork "The Matrix" HQ session) at live HEAD `38abeee` = merge of #1416 (was stale at `b03975d` = #1164, 2026-06-21 — ~250 PRs of drift closed this session).
> **Landed since `b03975d` (#1165–#1416, condensed):** SSOT-dedup + MON-issue fix arc (dual tax engines reconciled, depreciation 100× bug, equity floor overstatement, cashflow-tile producer unification — see `docs/issues/ISSUES.md`); CSV-import hardening (#1326–1328); AI = Gemini-only (#1323, Anthropic disabled); **NeoAudit core build COMPLETE 2026-07-14** (CLAUDE.md Part 23: Ring-2 Golden Household + parity matrix + §20.6 pre-PR gate + `issues:raise` + Release Scorecard; standing/LIVE system); verification runs **VR-001→VR-006**, accepted baseline = **VR-006** (`docs/verification/baselines/BASELINE.md`); **Issue registry live:** 50 MON issues (32 FIXING · 9 VERIFIED · 4 DIAGNOSED · 2 OPEN · 2 CLOSED · 1 RETRACTED at this pin — re-read `docs/issues/ISSUES.json` before acting, statuses move fast); **RECTIFICATION_PLAN_2026-07-14** ready with DECISION 1 (one-offs excluded from run-rate) + DECISION 2 (trailing-12-month single producer) both ✅ DECIDED by Reza 2026-07-14.
> **UPDATED 2026-07-15 by the VR-007 landing (this PR):** verification runs now **VR-001→VR-007** (`docs/verification/runs/VR-007.md` + the raw capture `VR-007-capture.md`). **Registry is now 74 MON issues** (29 FIXING · 24 OPEN · 12 VERIFIED · 6 DIAGNOSED · 2 CLOSED · 1 RETRACTED) — MON-053…MON-074 raised from VR-007, and MON-039/042/018 → VERIFIED, MON-049 → DIAGNOSED. **Accepted baseline is STILL VR-006** — VR-007 *proposes* a replacement in this PR but §3.4 says expected deltas are "Reza confirms", so it is **awaiting Reza's decision**, not settled. VR-007's Part F diff vs VR-006: **48/49 leaf values unchanged, ONE expected delta** (`Guildford.cashflowYrList null → −7387` = MON-039c via #1412), **zero unexplained, zero regressions**.

- **Current focus: 0·RECTIFY — Reza gave the "rectify" GO on 2026-07-15 (Cowork session).** Work the clusters per `docs/issues/FIX_PROTOCOL.md` (Part 24), ONE issue at a time, own PR each, per-fix Ring-3 Chrome verification before VERIFIED. Cluster order per RECTIFICATION_PLAN §4 (① MON-037 one-off hub first) — but NOTE: VR-004/VR-006 already moved some cluster issues (MON-035/036 VERIFIED); **re-read the registry for per-issue status before starting any issue.**
  - **⚠️ CLUSTER ① NOW HAS A TWIN (VR-007, 2026-07-15): `MON-053` is `MON-037`'s INCOME-SIDE twin** — the same one-off bug, the same engine file (`lib/tax-engine/position/taxPositionCalculator.ts`), the same fix shape. MON-037 guarded EXPENSES only (`:194-197` + Decimal twin `:694-697`); income is still annualised unguarded (`:128` and Decimal `:636`), so two single ATO deposits become **~$120.6K of phantom income inside the declared gross of $524,831/yr — and it is taxed** (`critical`). **Work ① as ONE cluster (MON-037 + MON-053), not as unrelated issues** — the Ring-1 source-lock (ANY IncomeItem/ExpenseItem annualisation must be one-off-guarded) covers BOTH sides and is the ratchet that kills the pattern rather than one instance.
  - **⚠️ SEQUENCING: MON-053 lands BEFORE MON-045 stage 2.** MON-045's Ring-3 verifies against the tax base; if it goes first it verifies against a base inflated by ~$120.6K, and MON-053 then moves the number again — invalidating that Ring-3 and burning a full real-data cycle of Reza's time. Order: **MON-053 → re-baseline → MON-045 stage 2.**
- **Launch decision (Reza, 2026-07-15, Cowork):** 31 July = **friendlies beta + broker outbound** (NOT consumer bank-feeds launch — Basiq stays MRR-gated/parked). Gate plan: `docs/operational/LAUNCH_PROGRAM_2026-07.md` (added this PR).
- **Immediate next action (re-pinned 2026-07-15 by the VR-007 landing):** (1) cluster ① — **MON-037 + MON-053 together** per FIX_PROTOCOL stages 1–6 (MON-053 carries a §19.2-verified root cause with 18 `file:line` anchors + the corrected producer census — the registry entry is self-contained; a cold session can build it from `docs/issues/ISSUES.json` alone); (2) **then** MON-045 stage 2 — never before MON-053; (3) then remaining DIAGNOSED/OPEN (MON-001, 006, 047, 049, 050, and the VR-007 intake MON-054…MON-074) in plan order. The 2026-07-15 VR-007 findings **are now filed** (MON-053…MON-074, `tracker: neoaudit-run:VR-007`) — no re-raise needed.
- **Open decisions / blockers (awaiting Reza):** Q-GTM-7 friendlies cohort (blocks invite send) · Q-GTM-5 fintech-lawyer engagement (blocks Reviews to strangers) · Stripe live-mode (parked) · Q-BASIQ-1/Basiq (parked, MRR-gated) · Q-GTM-4 VA timing · mobile D2/D3/D4. Dependabot major-bump PRs (#811–#816: Prisma 5→7, Next 15→16, ESLint 8→10…) — recommendation: PARK pre-launch, do not merge.
- **New findings (Cowork ingestion 2026-07-15):** (a) **= MON-051** and (b) **= MON-052**, registered via #1418; (c)–(f) remain open notes. (a) `lib/cfo/intelligenceEngine.ts:274-275` hardcodes `savingsOpportunities: 3` / `pendingActions: 5` — user-facing placeholder metrics (MON-018 class); (b) `lib/tax-engine` PAYG `paygCalculator.ts:197` TODO — HECS-HELP withholding component not implemented; (c) VR-005 run file absent though referenced; (d) `lib/calc-audit/runDifferential.ts` header cites non-existent `npm run audit:fixtures`; (e) `docs/architecture/09` top table stale (Render-era; its own §5.3 says so); (f) GCS prod provisioning state CONFLICTS between arch doc ("pending, uploads → Postgres bytea") and the pre-refresh STATE.md ("live via key") — **re-verify in Vercel env + logs before any doc claims either.**
- **HQ note:** Monitrax program orchestration now runs from the Cowork project "Monitrax HQ — The Matrix" (Mission Control artifact + daily ops brief + this cursor discipline). Repo stays the only truth; the Matrix duplicates nothing (§12.2.1).

- **0·MOB (Mobile companion app) — design phase.** Live tracker: docs/implementation/05_MOBILE_WORKSTREAM.md.
  LOCKED light+dark set — 7 screens on the canonical Stitch system; next = commit Stitch artefacts + realign design docs; RN build gated on Basiq-live + §15.1 P0. Open for Reza: D2/D3/D4 (unchanged since 2026-06-21).

## D. THE SESSION RITUAL  (all surfaces; Code ALSO follows CLAUDE.md Parts 1/7/10)

**START (before any work):**
0. RESUME CHECK. List open + recently-merged PRs on `resadegh/monitrax` (GitHub connector). If a tracked PR
   (continuity / plan / workstream) merged since this cursor's HEAD, pull the new HEAD, read what changed, and
   continue from the updated next action. There is NO notification — this pull is how a session learns a PR merged.
1. Pull live HEAD. Run freshness gate (above).
2. Read this STATE.md -> then CLAUDE.md -> then the relevant `IMPLEMENTATION_PLAN.md` section for the active task.
3. Print a <=5-line orientation: what Monitrax is (1 line) - current task (1) - next action (1) - blockers (1) - HEAD (1).
4. Open a session ledger (verified-vs-unverified, pinned to HEAD).

**DURING (every response):**
- **Cite or stop.** No state claim without a this-session source (`file:line` / tool result / HEAD). Can't cite -> flag unverified + re-pull.
- **Re-pull, don't recall.** Uncertain = read it again. Recollection is the silently-wrong option.
- **One unit at a time.** Close + write the finding before opening the next. Never hold "the whole app" in context.
- **Standing compliance check.** Anything touching user-facing money language -> AFSL/CDR boundary check before it ships.

**END (before closing):**
1. Update the RESUME CURSOR (Section C) — new HEAD, what changed, exact stop-point, next action, blockers.
2. Update `IMPLEMENTATION_PLAN.md` + changelog in the SAME PR (CLAUDE.md Sections 15/16.5).
3. Leave the next action explicit enough that a cold session resumes in <1 min.

## E. HOW THIS STAYS TRUE  (integration + enforcement)

- **Owns:** current position (cursor) + the universal session ritual. **Defers to:** CLAUDE.md (law),
  IMPLEMENTATION_PLAN (detail), 00_INDEX (map), SYSTEM_MAP (what-owns-what). No content is duplicated from
  those here — only pointers + position.
- **Enforced by:** (a) `.claude/hooks/session-start.sh` prints this cursor + HEAD at the start of every Code session
  (skip-on-failure, never blocks the session); (b) `.github/workflows/continuity-gate.yml` fails a PR that changes
  workstream files without updating STATE.md + IMPLEMENTATION_PLAN in the same PR (soft-launch first, then required;
  workflow scope GRANTED 2026-06-15 — arming is a repo-admin step); (c) chat/Cowork: read-STATE-first is the hard first instruction (project instructions Section 0).
- **Update cadence:** cursor every session end; Section A/Section B only on a real change, via PR, never ad hoc.
