# VR-033 — MON-100: capture Stage 0 (entity-route reachability), verified inert

> **Run by** The Matrix (Cowork · Opus 4.8, Claude-in-Chrome, Browser 1 macOS) · **Date** 2026-07-26 (AEST) · live `www.monitrax.com.au` + source @ `4284ee77` (#1506 merged, prod READY).
> **Under test:** MON-100 — `/api/tax/entity/[entityId]` (GET + POST) now routes THROUGH `buildMasterTaxPositionDecimal`, putting the wired PSI/FTE-IEE/Div 152 overlays on the live entity path for the first time. Pure plumbing, parity-by-construction — no number can move.
> **Headline: PASS — MON-100 → VERIFIED.** Household + entity cluster byte-identical; no `crossCutting` key in the response; the entity path is now the single orchestrator producer.

## A — Live inertness — PASS
Live tax page (FY2026-27, calc 26/07/2026) — byte-identical to the post-depreciation baseline:
Income **$317,751** · Deductions **$172,325** · Taxable **$145,426** · Tax Owing **$26,926** · Net Tax **$38,054** · Medicare **$2,909**. Only the existing negative-gearing warning; **no overlay / FTDT / PSI / Div 152 line**. No data entered since VR-032, so no explainable delta expected — and none present.

## B — Structure (source @ `4284ee77`) — PASS
- `app/api/tax/entity/[entityId]/route.ts` imports **`buildMasterTaxPositionDecimal`** (:10) + `assembleEntityTaxFacts` (:16) — routes through the orchestrator, NOT `entityTaxRouter`.
- **Direct-import ban holds:** no `app/` file imports `entityTaxRouter` (topology grep clean). `lib/services/wealthGraphService.ts` remains a reviewed status-only exception (renders `uncomputed.length` as a Money-Flow badge, never a tax dollar) — documented + locked per the Code census.
- **`crossCutting` additive-only:** response spreads `crossCutting` ONLY when `Object.keys(master.crossCutting).length > 0` (route.ts :117 / :532) — no overlay keys until capture ships, so the JSON contract is byte-unchanged (confirms A's "no crossCutting key").
- `tests/tax/mon100EntityRouteParity.test.ts` present (deep-equality parity across personal / trust-distribution / CGT-event facts shapes).

## Verdict
**MON-100 → VERIFIED.** The overlays are now wired (Neo-G4) AND reachable (Stage 0) on the live entity path; parity-locked, zero number movement, no new response key. Read-only verification — no data changed. Registry: 65/100 (65%). **Stage 1 (FTE/IEE capture) is unblocked** — schema plan (3 nullable `DistributionAllocation` fields) + Stitch per-beneficiary design incoming from the Code session for Reza's approval.

---
*Prepared by The Matrix. Live + source, 2026-07-26, `4284ee77`, Browser 1 (Reza's Mac). Reachability plumbing verified inert; the wired overlays now sit on the live path, ready for capture Stages 1-3 to make them fire.*
