# VR-023 — MON-095 Fix A live re-check (duplicate-detection accuracy), Housekeeping · Duplicate income

> **Run by** The Matrix (Cowork · Opus 4.8, Claude-in-Chrome) · **Date** 2026-07-23 (AEST) · live `www.monitrax.com.au`.
> **Under test:** MON-095 Fix A (PR #1481, MERGED — main `dea8eee`, deploy READY). The systemic matcher guardrail: expense duplicates now require an amount band AND a shared-identifier check, so distinct policies/accounts are no longer grouped. This run IS the runtime evidence (logs empty pre-run).
> **Headline: PASS. AIA false-positive gone; genuine duplicates preserved; no money number moved → MON-095 → VERIFIED.**

## 0 — Boot (code anchors verified on main)
`expenseDuplicateCompatible` gates BOTH the exact and near paths (`classifyIntake.ts:281/285`); `DUPLICATE_AMOUNT_TOLERANCE = 0.1` (`reconciliation.ts:540`, one band); `extractIdentifierTokens`/`hasDisjointIdentifiers` (`merchantNormalize.ts:74/90`, ABN/ACN excluded); ratchet `tests/intake/mon095DuplicateAccuracy.test.ts` (9 tests) in CI. Income doctrine untouched (expense-scoped guard).

## A — Symptom checks (`/dashboard/housekeeping/duplicates`, consumer session)
| Check | Expected | Live result | Verdict |
|---|---|---|---|
| **AIA pair** ($131 · …68718123 vs $158 · …68718100) | ABSENT | **No AIA group anywhere** | **PASS** — different policy numbers + 20% amount gap → both vetoes fire; the distinct policies are no longer offered as a merge |
| **QBE** $216 / $216 | PRESENT | Group present ("QBE Insurance (Australia) Limited" MONTHLY kept · "Qbe Insurance Sydney" one-off removed) | **PASS** — genuine duplicate; ABN not treated as an identifier |
| **Mate Communicate** $150 / $150 | PRESENT | Group present ("907808-7371789 Mate…" kept · "- Mate…" removed) | **PASS** — genuine duplicate, identical name+amount |
| Tab badge | reflects list WITHOUT AIA | **Duplicate income = 2** (was 3 with AIA) | **PASS** — same producer feeds the badge |

**No merge performed** (standing directive — QBE/Mate held for Fix B; AIA must never be merged).

## B — Regression guards (Fix A is grouping-only — no money may move)
- **Tax page unchanged:** Total Income **$317,751**, Total Deductions **$142,319**, Taxable **$175,432**, Tax Owing **$38,628**, **139 expenses** — byte-identical to the post-MON-094 state. PASS.
- **Housekeeping → Tax classification** still loads (shows "Nothing to review" — the correct post-VR-022 state; MON-094 suggestions already actioned). PASS.
- **No row deleted:** the fix removed a GROUPING in a read-only preview, never a row — both AIA policies still exist as separate expenses (deletion only occurs on an explicit merge, none performed). PASS.

## Verdict
**MON-095 → VERIFIED.** The AIA false-positive class is dead (distinct policies no longer grouped), the two genuine duplicates are preserved, the badge tracks the corrected list, and no money number moved. The fix lives in the one shared decision layer (`expenseDuplicateCompatible`) read by the Housekeeping preview AND the #1458 intake guardrail — so it protects all users at import, not just this data. Ratchet in CI + Neomatrix anchors re-pinned (#1481) support promotion toward CLOSED.

**Still queued:** Fix B (merge-with-edit + clearer view, Stitch-first → Opus) then Reza merges QBE + Mate with corrected cadence; Broadbeach declared-basis correction; MON-091 write checks 13/14.

---
*Prepared by The Matrix. Live, real rendered numbers, 2026-07-23. Fix A = systemic guardrail; grouping-only, no money moved; AIA gone, genuine duplicates intact.*
