# VR-047 — T2 loan-cost migration, Ring 3 (account-first half)

**Handout:** `docs/verification/briefs/RING3_T2_LOAN_COST.md` · **Kind:** `ring3`
**Ran:** 2026-08-03T09:49:31Z by the Matrix (Claude-in-Chrome) on Reza's live account
**Validated:** `npm run matrix:check` → exit 0 (`sha=null` accepted under Rule A, account-first)
**Verdict: PASS — SCOPED.** Every check the run could execute passed. **It does not complete the
handout**, and MON-130 / MON-143 stay `FIXING`.

---

## The deciding check landed

| Surface | Was | Read |
|---|---|---|
| Home · `THIS MONTH'S BUDGET · Loans` | $8,817 | **$12,779** |
| `/dashboard/expenses` · Loans | $12,779 | **$12,779** |

**Two screens, one number, for the first time.** Identity was asserted from four independent figures
first (net worth $3,401,782 · 6 properties · 5 loans · entity *Reza* $2,651,782).

The downstream figures reconciled to their **derivations**, not to remembered values — the VR-045
lesson applied: `Saved` $11,085; daily budget $358 (`11,085.07 / 31 = 357.58`); savings-rate
derivation 43.734%; debt-service 50.4183%. The `mustNotMove` cluster is byte-identical on every named
item **including `healthScore` 53**, which the migration PR predicted would hold because both
savings-rate scores clamp at 100. All five per-loan rows and their basis labels are unmoved — T2 moved
master *onto* them, as designed.

`§5`'s pre-declared non-finding is confirmed: the entity flow is byte-identical and still short
**$3,792.92/mo**. It now visibly disagrees with Home. That is T2-B, and it is why T2-B is being built
rather than queued.

## What did NOT run — and why the PASS is not sufficient

**`§2` (the 15 declared producer paths) and `§2b` (`byType`) were not executed**, nor was the `§4b`
build precondition the handout marks STOP-IF-FAIL. All three need the admin relay, and VR-044 §7 rule
4 forbids signing into `/admin/login` in the same browser profile as the account under test — doing so
is what voided VR-044's first attempt and VR-046's first attempt.

The distinction matters and the Matrix drew it correctly: *"a rendered $12,779 does not establish that
all four internal expressions agree to the cent."* `§2`'s identity check exists for exactly the case
where the tile is right and one of the four assembly paths is not.

**Consequence:** MON-130 and MON-143 stay `FIXING`. The remedy is a second Chrome profile — see
`docs/verification/briefs/MATRIX_T2_ADMIN_RELAY.md`, which completes `§2`/`§2b` and captures T2-B in
one sitting.

## Findings registered

| # | Registered | Severity |
|---|---|---|
| Laguna renders two recurring income rows, counts one (~$6,948/yr uncounted) | **MON-149** | medium |
| Thornland Lot 1 and Lot 2 both claim depreciation of exactly $12,799 from identically-sized files | **MON-150** | medium |
| Per-property tax interest ignores the offset — the D21 breach in a second producer | **MON-151** | low |
| Hunter Premium renders $797 on one card and $812 on three others | **MON-152** | low |
| Three health scores render — 53 / 56 / 25 — with D13's multiplicity unlabelled | **MON-153** | medium |

MON-149 and MON-151 are pre-existing and byte-identical across the migration: surfaced by the sweep,
**not** caused by T2. MON-150 cannot be settled by inspection — it needs Reza to open the two PDFs.

## Corrections this run makes to the record

1. **VR-046 B4 was wrong.** It reported that no surface renders a total superannuation balance;
   `/dashboard/investments/super` does (`TOTAL VALUE $0 · 1 fund`). The earlier run checked
   `/dashboard/super` (404) and the tax page's Super tab and generalised from two misses. **T7's
   blocking fact is therefore readable from a screen: total super balance = $0.**
2. **The handout's `§3` savings-rate row was mis-specified** and could not land: no Home surface
   renders `cashflow.savingsRate`. Home's SAVING RATE tile reads 1.9% on the trailing-12-month
   ACTUALS basis and read 1.9% before the migration too — it was never 59.4%. The Matrix reported
   this as a mis-specified prediction rather than forcing it to PASS or FAIL, which is the right
   handling. **Corrected in the handout in the same PR as this record.**

## Coverage, stated precisely

**Verified:** the rendered surfaces — Home, My Accounts, My Budget, My Safety Net, My Wealth
(including all six property detail pages), My Guide, Reports, Household Profile; the invariants
(Σ property equity = portfolio equity exact; investments sum exact; cross-surface property cashflow
matching within the Home tile's dollar rounding; no sentinel leaks); MON-030 parity (Home 53/C ===
CFO 53/C).

**Not verified:** `§2`, `§2b` and the build precondition — so the four-expression identity is
**untested**. The declared after-values' own correctness (that is the sweep at `915704f0`). Any
loan-cost producer other than `masterFinancialService` — 30 remain. MON-142. The deployed commit was
not asserted. Screens only: no producer was read in source. Sections not opened: Transactions,
Recurring, Vault/Documents, Settings root, the gated Budget setup page; the what-if sliders were not
moved (a state change this read-only run excludes).
