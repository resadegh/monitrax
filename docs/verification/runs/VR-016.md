# VR-016 — MON-020/060 one canonical tax producer, cross-surface Ring-3 PASS

> **Run by** The Matrix (Cowork · Opus 4.8) · **Date** 2026-07-18 (AEST)
> **Live prod pin** merge of #1449 (`dpl_DXBmhYs5xp5ScTywNpV5UHUsXRTQ`, READY). Live Claude-in-Chrome on `www.monitrax.com.au`.
> **Result:** **PASS all three parts → MON-020 + MON-060 → VERIFIED.**

## Part 1 — the ONE tax number, every surface — PASS
Single canonical tax result reads identically on all four:

| Surface | Tax owing | Deductions | Effective |
|---|---|---|---|
| `/dashboard/tax` (Overview) | **$89,287** (Tax $84,046 + Medicare $5,240) | **$142,319** (Property $142,209 + Other $110) | 34.1% |
| `/cashflow` FY estimate | **$89,287** (incl. $5,240 Medicare) | **$142,319** | 34.1% |
| `/dashboard/activity` YTD flow **Tax node** | **$89K** — converged onto canonical, off the old ~$175K basis (the MON-060 fix) | — | — |
| CFO / My Guide Tax Position | **−$89,287** (Owing) | **$142,319** (Property $142,209, Medicare $5,240 incl.) | 34.1% |

Taxable income $262,019, total income $404,338 (21 sources) consistent. The Activity node movement IS the fix, not a regression.

## Part 2 — equivalence-locked anchors UNCHANGED — PASS
Tax page + /cashflow: Total Deductions **$142,319**, Tax Owing **$89,287**, effective **34.1%** — identical to VR-015. No movement on the equivalence-locked assembly path (as designed).

## Part 3 — regression guards — PASS (live spot-check)
- Broadbeach IO loan **$1,191/mo** "from linked repayments"; loan aggregate **$12,779/mo** (Thornland L2 $2,518 · L1 $6,197 · Guildford $2,790). ✓
- Managed rental: agent fee **$432/mo once** ($5,186/yr). ✓
- Expenses total **$14,327/mo** recurring-only; one-offs $51,140 counted once. ✓
- Cash **$301,808** (cashflow balance); CFO month-end $301,735 (projection). ✓
- Medicare **$5,240** itemised on both tax cards (MON-039a). ✓ `/dashboard/tax` loads normally (shared assembler path). ✓

## Verdict & registry
- **MON-020 → VERIFIED** (fixPRs #1349, #1449). **MON-060 → VERIFIED** (fixPRs #1449). One tax engine (`calculateTaxPosition`), the four assemblers collapsed to the canonical bundle; dead `/api/tax` deleted; parity locked by `tests/golden/ring2.taxParity.test.ts`.
- **Baseline:** the Activity YTD-flow **Tax node** now reads the canonical **$89K** ($89,287) — record in `baselines/BASELINE.md`.

## Parked (not a finding — recorded for Reza's decision)
The canonical assembler reads raw Income rows; master's old path additionally applied the MON-009 rental dedup + `isTaxable` filter. On Reza's healed data the bases agree (rental $35,360 correct on the raw basis). Folding dedup + `isTaxable` into the canonical assembler is a **semantic unification awaiting Reza's call**, not a defect.

## Out of scope (censused, not re-filed)
Reports per-entity tax (`entityTaxRouter`, entity scope); `/api/tax/super/optimize` + rental-reconciliation marginal/delta bracket uses (scenario tools, not position producers); F3 two-sources guard + MON-084/085 (queued); the 80-site source-lock ratchet queue.

---
*Prepared by The Matrix. Live `www.monitrax.com.au`, prod pin merge of #1449. Cross-surface gate per MATRIX_FIX_DISCIPLINE gate 4. Self-reviewed to 10/10 before presentation — clean PASS, no caveat.*
