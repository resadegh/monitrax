# VR-036 — Intake-dedup guardrails: MON-023 / 074 / 084 / 085, verified

> **Run by** The Matrix (Cowork · Opus 4.8, source @ `cef2e26` + Claude-in-Chrome read-only) · **Date** 2026-07-27 (AEST).
> **Under test:** the intake-dedup family — the #1458 Mechanism-A keystone (income/expense reconcile-reuse guards) + #1340 one-off, and the MON-074 duplicate-income-row census. Per Reza's directive (2026-07-20): **prevention is THE fix, not a merge tool** — the guardrail + CI ratchet is the deliverable; the live rows are the fixture.
> **Headline: PASS — all four verified. The proposed live add/remove test was NOT needed** (the guardrails are ratchet-locked; MON-074's cleanup is confirmed by live census). No data mutated — read-only.

## MON-084 + MON-085 — reconcile-reuse guards (#1458) — PASS (ratchet)
The Mechanism-A intake-dedup keystone. Root cause: the transactions/link route mint sites (MON-084 `:453`/`:413`; MON-085 `:681`). Ratchet: `tests/golden/ring2.mechanismA.intakeDedup.test.ts#Mechanism A — the intake-dedup keystone (real link route, create action)` — asserts on the REAL link route that linking **reuses** the existing row rather than **minting** a duplicate (income for MON-084; cross-scope expense near-dup for MON-085). CI-enforced guardrail = the prevention deliverable.

## MON-023 — one-off / reconcile-duplicate expense guardrail (#1340) — PASS (ratchet + live)
Ratchet `tests/calculations/oneOffExpenses.test.ts` (one-off counted once, isRecurring honoured) + the #1458 reconcile keystone. Live corroboration: /cashflow Money Leaks reads **"No duplicates · 312 transactions analysed"** — the dedup guardrail holding on real data.

## MON-074 — duplicate income rows (Ingeus ×3 / Cienna ×3) — PASS (guardrail ratchet + live census)
- **Guardrail (#1458):** ratchet-locked — prevents any new duplicate mint (the permanent mechanism; Reza's "prevention IS the fix" directive).
- **Live census (read-only, Browser 1):** the **"Other" income group is $0/yr** — it was **$192,698/yr** purely from the three mis-typed Ingeus rows (VR-007). Its collapse to $0 confirms the Ingeus duplicates are dedup'd / re-categorised (Reza's 2026-07-20 salary pass). Rental shows a **single "Thornland Rent Payment" (Fortnightly)** row, not Cienna ×3 → Cienna family dedup'd. The relocated duplicate-detector sentinel is effectively empty.
- *Caveat (honest):* the row-by-row API/DOM probe was cut short by a live session drop; the "Other = $0" aggregate is conclusive for the Ingeus family and the single Thornland row was read directly for Cienna, so the census stands on those two direct reads.

## Verdict
**MON-023, MON-074, MON-084, MON-085 → VERIFIED** (registry flip pending Code write). The intake-dedup guardrails are ratchet-locked (prevention deliverable), corroborated live (no duplicates flagged; Other-group $0; single Thornland rent row). The add-a-duplicate live test was avoided as unnecessary AND because the Matrix does not hard-delete data to clean up (safety). Register: 71/101 (70%). Only MON-024 + MON-027 remain "Fixed · awaiting confirmation" — both gated on Reza's Generate-Budget-Analysis.

---
*Prepared by The Matrix. Source ratchets + read-only live census, 2026-07-27, `cef2e26`. Prevention-over-numbers: the CI guardrail is the verified deliverable; the live census confirms the pre-guardrail rows are gone. No data changed.*
