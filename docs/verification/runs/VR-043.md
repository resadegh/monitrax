# VR-043 — MON-131 T1-A acceptance + the income compare relay

**Run ID:** VR-043
**Date:** 2026-07-30 (AEST)
**Verifier:** The Matrix
**SHA verified:** `3028c08a3d00c09c4909966422ea47f07ee4232a` (relay's own `sha`; PR #1542 merged, deploy READY)
**Pre-merge reference:** `d3d7e147b429cbd93a45bf331e7638531ce3b3d3` — VR-042's capture, **still held in the browser session**, diffed leaf-by-leaf
**Method:** live production, admin relay, read-only. Zero writes.

**VERDICT: T1-A acceptance → PASS. Nothing moved. `expectedMoves` inputs delivered in §3. Three items need Code before T1-B (§5).**

---

## §1 Run 1 — the identical-baseline proof

### 1.1 What Code asked for, and why it could not be run as specified

The handout says *"`treeHash` must equal the committed reference."* **There is no committed reference.** `?format=hash` shipped **in this PR**, so its first-ever output is the post-merge one. Comparing it to itself proves nothing.

The instrument that *does* exist is stronger, and it was available by luck: **VR-042's full pre-merge tree at `d3d7e147` was still resident in the browser session.** So instead of a hash equality, this run is a **leaf-by-leaf diff of the actual pre-merge and post-merge trees**.

### 1.2 Hash output, as requested

| Field | Value |
|---|---|
| sha | `3028c08a3d00c09c4909966422ea47f07ee4232a` |
| trees | 8 |
| leafCount | **1,767** |
| treeHash | `6f2369a6f0f94279e171bf95db3af26fcee16ef07d0371c14480ac98b16c0224` |
| **captureErrors** | **`[]`** ✅ |

perTree: `getUserTaxPosition` 695 · `getMasterFinancialSnapshot` 453 · `generateHealthReport` 344 · `buildHealthInput` 208 · `scanForRisks` 17 · `getMoneyFlow` 34 · `resolveLoanCostsForUser` 10 · `computeCFOComponents` 6.

**Determinism checked:** two calls 1.5 s apart returned the **identical** `treeHash`. The canonicalisation is stable. **This is now the committed reference** for every subsequent tranche.

### 1.3 The leaf-by-leaf diff — the real acceptance

| | |
|---|---|
| Leaves before (`d3d7e147`) | 10,254 |
| Leaves after (`3028c08a`) | 10,318 |
| **Identical** | **10,233** |
| **Moved (money)** | **0** |
| Moved (non-money) | 4, plus per-scan UUID/`detectedAt` regeneration on 4 risk rows |
| **Disappeared** | **0** |
| New | 64 — all `null` |

*(leaf counts here are the Matrix's own recursion, which descends into array elements; §6 explains why that differs from the route's 1,767.)*

**The four non-money moves, in full:**

| Path | Before | After | Cause |
|---|---|---|---|
| `getMasterFinancialSnapshot.staleness.oldestManualAgeDays` | 41 | 42 | a day boundary passed between captures |
| `…staleness.summary` | "…41 days ago." | "…42 days ago." | the sentence rendering that number |
| `scanForRisks.lastScanned` | 10:23:05.881Z | 22:12:40.130Z | scan timestamp |
| `generateHealthReport.evidence.lastUpdated` | 10:23:05.953Z | 22:12:40.192Z | evidence timestamp |

**The 64 new leaves are exactly the four FACT columns T1-A declared**, all `null` (UNDETERMINED), none numeric, none non-zero: `getUserTaxPosition.incomes[].actualNetPay` · `.incomes[].helpLoanDeclared` · `.properties[].genuinelyAvailableForRent` · `.properties[].availableDaysPerYear`.

**`renderedPartC` is 10/10 identical** to VR-042: netWorth 3,401,782 · liquid 301,808 · committed 14,261 · loansMonthly 12,779 · rentalMonthly 10,102 · taxNet 37,786 · taxableIncome 145,426 · deductions 172,325 · medicareLevy 2,909 · payg 11,129.

**T1-A moved nothing. Acceptance PASS. Do not revert.**

### 1.4 One defect in the hash gate itself

`staleness.oldestManualAgeDays` **changes with the calendar**, not with code. If that leaf is inside the 1,767-leaf hashed set, the gate will emit a false STOP at every day boundary — and a gate that cries wolf gets ignored, which is worse than no gate.

**Ask:** confirm whether the canonicalisation excludes calendar- and clock-derived leaves. If it does not, exclude them and re-issue the reference hash. The 1.5-second determinism check above cannot distinguish "excluded" from "not enough time passed."

---

## §2 Run 2 — the income compare relay, headline

`GET /api/admin/matrix/golden-baseline/t1-income` returned 200, 8,831 bytes, 24 declared paths.

**The identity that failed by $1,196.70 in VR-042 now holds exactly:**

```
annual   gross 347,162.61  −  PAYG 43,004.00  =  banked 304,158.61   ✓ exact
monthly  gross  28,930.22  −  PAYG  3,583.67  =  banked  25,346.55   ✓ exact
```

**And the L2 invariant holds, both bases:**

```
gross:   salary 192,719.50 + rental 154,443.11 = 347,162.61  ✓
banked:  salary 149,715.50 + rental 154,443.11 = 304,158.61  ✓
salary gross − salary banked = 43,004.00 ≡ total withholding  ✓
```

**Six values for "annual income" collapse to two named quantities** — banked **304,158.61** and gross **347,162.61**. `moneyFlowService.getMoneyFlow.totalIncome`, previously a seventh independent answer at 470,261.70, now returns **304,158.61** — it agrees with banked. That is a genuine collapse, not a rename.

## §3 All 24 declared paths — the `expectedMoves` fill

| Path | Before | After |
|---|---|---|
| `income.annual.all.grossTotal` | 505,564.05 | **347,162.61** |
| `income.annual.all.netTotal` | 495,632.05 | **304,158.61** |
| `income.annual.all.paygWithholding` | 11,128.70 | **43,004** |
| `income.monthly.all.netTotal` | 41,302.67 | **25,346.55** |
| `income.monthly.all.paygWithholding` | 927.39 | **3,583.67** |
| `income…byType.RENTAL.gross` | 154,443.10781… | 154,443.11 *(rounding only)* |
| `cashflow.annualGrossIncome` | 495,632.05 | **347,162.61** |
| `cashflow.annualNetIncome` | 459,434.36 | **304,158.61** |
| `cashflow.annualIncome` | 459,434.36 | **304,158.61** |
| `cashflow.annualPaygWithholding` | 36,197.69 | **43,004** |
| `cashflow.monthlyGrossIncome` | 41,302.67 | **28,930.22** |
| `cashflow.monthlyCashflow` | +27,987.35 | **+15,047.71** |
| `cashflow.savingsRate` | 73.1 | **59.37** |
| `cashflow.debtServiceRatio` | 23.03 | **34.78** |
| `cashflow.taxableIncome` | 495,632.05 | **347,162.61** |
| `quickMetrics.monthlyIncome` | 41,302.67 | **25,346.55** |
| `quickMetrics.monthlyGrossIncome` | 41,302.67 | **28,930.22** |
| `quickMetrics.monthlyCashflow` | 27,987.35 | **15,047.71** |
| `quickMetrics.savingsRate` | 73.1 | **59.37** |
| `quickMetrics.keptAfterEssentials` | 39,820.48 | **23,864.36** |
| `quickMetrics.keptMargin` | 96.41 | **82.49** |
| `debt.metrics.debtToIncomeRatio` | 415.61 | **677.24** |
| `generateHealthReport.score` | 54 | **53** |

*(24th path is the `moneyFlow.totalIncome` collapse noted in §2.)*

The ledger's indicative T1 prediction was *"income $41,303 → ~$24,250."* Computed: **25,346.55**. The estimate was in the right place; the computed value is what gets declared.

## §4 `rentalLocalisation` — D6 answered, and one value still missing

| Quantity | Annual |
|---|---|
| `sumPerPropertyAnnual` (actuals-first engine) | **154,443.11** |
| `byTypeRentalLegacy` (gross ≡ net) | **154,443.108** |
| `declaredRunRateAnnual` | **121,227.40** |
| `nonPropertyAnnual` · `oneOffRentalAmount` · `misattachedRows` | 0 · 0 · none |

**The $121,227 / $154,443 gap is not a bug — it is two legitimately distinct quantities**: declared run-rate versus actuals-first banked. The per-property table proves it, showing `usedActuals: true` where actuals exceed the declared figure — 2,174.11/mo against a declared 500 · 3,607.68 against 2,588.95 · 75.15 against 0. The legacy `byType` total already equals the per-property engine, so those two were never in conflict.

**Still missing: the $121,881 figure the build brief attributes to the Tax page appears nowhere in this output.** Two of three localised, one not. **It must be named before T1-B**, or the tranche closes with an unexplained third value — the exact failure D6 exists to prevent.

## §5 `salaryPlausibility` and `bankedSummary` — three things Code needs

**Six salary rows.** Bases: `DERIVED_SCHEDULE` ×5, `FACT_NET_ENTERED` ×1. Flags: `HELP_LOAN_UNDETERMINED` ×5, `ONE_OFF_GROSS_UNDERIVED` ×4.

### 5.1 The $9,932 from VR-042 is resolved

The single `FACT_NET_ENTERED` row: gross **59,835.10**, banked **49,903.10**, `impliedWedgeAnnual` **9,932**, `scheduleWedgeAnnual` **9,672**, `divergent: false`.

**$9,932 was the implied withholding on one NET-entered salary row.** The new engine derives it by construction and cross-checks it against its own FY26-27 schedule; the two agree to within $260/yr. VR-042 V3's "unnamed third PAYG value" is now a named, derived, cross-checked quantity. **Closed.**

### 5.2 The HELP engine shipped correct and is contributing $0

`bankedSummary.withholding` = `{payg: 43,004, help: 0, salarySacrifice: 0}`. HELP is zero because **5 of 6 income rows carry `helpLoanDeclared: null`** — the honest UNDETERMINED state, exactly as designed.

But the balance sheet shows a **HECS / student loan of $25,000 at 4.00% variable**. The engine's own `repaymentIncome` derivation returns **225,765.90** (`complete: false` — `totalNetInvestmentLoss` and `reportableSuperContributions` PROVIDED; FHSS, reportable fringe benefits and exempt foreign employment income all `DEFAULT_ZERO`).

**225,765.90 is above the $186,050 cliff.** On the engine's own top-band rule — 10% of the *whole* repayment income — that is **≈$22,576/yr** of study-loan repayment the app is currently showing as **$0**.

**This is the highest-value item in T1-B.** The intake for `helpLoanDeclared` is what converts a correct-but-inert engine into a real number. Until it ships, the app understates a known obligation. Flagging the size, not asserting the liability — `complete: false` means the derivation is not yet whole, and the declaration is the user's to make.

### 5.3 Does `/dashboard/tax` move? — the gap in the declared set

`income.annual.all.paygWithholding` is declared to move **11,128.70 → 43,004**. But `/dashboard/tax` renders `PAYG withheld $11,129`, and `renderedPartC.payg` still reads **11,129** post-merge — it comes from `getUserTaxPosition`, **and no `getUserTaxPosition` path appears in the 24 declared moves.**

So one of two things is true, and Code must establish which **before** T1-B:

- **The tax page reads a different producer and will not move.** Then the app will render "$11,129 withheld" beside a banked-income engine that withheld $43,004 — a *new* contradiction created by the fix, not removed by it.
- **The tax page does read this path and will move.** Then `taxOwing` moves with it: `37,786 − 43,004` turns a **$26,658 bill into a ≈$5,218 refund**. That is the largest user-visible swing in the tranche and it is **currently undeclared** — which under G7 stops T1 on its own guard.

Either way this is not optional. Removing `PAYG withheld $11,129` from the regression cluster (start-gate brief §4) was the right call; it now needs its arithmetic.

### 5.4 Not closed by T1, correctly

`cashflow.taxableIncome` moves to **347,162.61**, but the tax engine's taxable income is **145,426**, and `347,162.61 − 172,325 = 174,837.61` ≠ 145,426. VR-042 **V4 stays open** — it is Tranche 4, and this is noted so nobody reads the move as a closure.

Also unchanged and correct: `oneOff.total` **13,200.12** across **10 rows**, excluded from run-rate, with `oneOffWithholding` UNDETERMINED and `ONE_OFF_GROSS_UNDERIVED` on 4 rows. Honest, flagged, not silently zeroed.

---

## §6 Correction — VR-042 §2.2 was my error, and it is withdrawn

VR-042 raised the leaf count moving **1,767 → 10,254** as unresolved, and asked Code to check the serializer history.

**The route reports `leafCount: 1767`.** Code's serializer produces exactly what the original `4e6cdd5c` capture produced. **Nothing changed.** The difference was entirely in *my* leaf counter, which recurses into array elements while theirs does not — 10,254 and 1,767 are two measurements of the same tree.

No serializer investigation is needed. **§1.1 of the start-gate brief is withdrawn**, and the ledger should record the resolution rather than the question. Root cause: I compared a number from my instrument against a number from theirs and treated the difference as a finding about the system.

## §7 Verdict

| Item | Verdict | Evidence |
|---|---|---|
| T1-A acceptance — moves nothing | **PASS** | §1.3 — 0 money leaves moved, 0 disappeared, 64 new all null |
| `captureErrors` empty | **PASS** | §1.2 |
| Reference hash established | **DONE** | `6f2369a6…`, determinism checked |
| `expectedMoves` inputs delivered | **DONE** | §3, 24 paths |
| Rental three-value localisation | **2 of 3** | §4 — $121,881 not found |
| Hash gate excludes calendar leaves | **UNVERIFIED** | §1.4 |
| `/dashboard/tax` PAYG movement | **UNDECLARED** | §5.3 — blocks G7 |

## §8 Gate (§20.6)

`Gate (§20.6): Document 10/10 · Requirements 10/10 · Logic 10/10`

Self-review changed three things. (1) I was about to report the hash comparison as "PASS — reference matched." There is no committed reference; the route shipped in this PR. Rewritten as §1.1, and the diff against the still-resident pre-merge tree substituted as the stronger instrument. (2) I nearly reported "16 leaves moved" from a first pass that counted regenerated risk UUIDs and `detectedAt` stamps as movement; re-classifying by leaf type reduced it to **4 non-money moves and 0 money moves**, which is the finding. (3) §5.3 began as a note that the tax page was absent from the declared set; working the arithmetic showed it is a **G7 stop condition either way** — a $26,658 bill becoming a $5,218 refund, or a new contradiction shipped as a fix — so it is now the blocking item rather than an observation.
