# VR-006 — Real-data Ring-3 verification (Claude-in-Chrome)

- **Date:** 2026-07-14 (production, fully-merged deploy incl. #1405/#1406)
- **Account:** reza.sadegh@ymail (Reza), env=production
- **Coverage:** `everyEntityOpened: true`; skipped: only the `/dashboard/debt-planner` per-loan refinance detail (gated behind "Complete Your Budget First") — refinance/LVR framing captured from the CFO Loan Opportunities card instead.
- **Purpose:** the VR-006 re-check the MON-035/036 fix was pending, PLUS resolve the 2026-07-14 open display forks from the new brief captures (MON-039/042/043 income-basis + counts), PLUS a full holistic sweep.

## Headline result — MON-035/036 VERIFIED ✅

The bug Reza chased across VR-004/VR-005 is **fixed and confirmed live**. HOME property cashflow now converges on every surface:

| Surface | HOME cashflow/yr | HOME yield |
|---|---|---|
| Property detail | −$8,668 | 0.12% |
| Properties list tile | −$8,668 | 0.12% |
| Home dashboard tile (×12) | −$8,664 (±$4 rounding) | 0.12% |

`crossSurface.HOME.cashflowYr.match === true`, `yield.match === true`. Every one of the 6 properties' `cashflowYr.match` and `yield.match` is `true` (Guildford list-tile omission is a separate render issue — MON-039c, below). The §12.2.1 duplicate-producer removal (portfolio route → `enrichPropertiesWithActuals`, #1405) is proven on live data. **MON-035 + MON-036 → VERIFIED.** Ratchet = the Ring-1 source-lock in `tests/golden/ring2.homePropertyParity.test.ts` (in CI).

## Also VERIFIED live (Ratchets already in CI)

- **MON-041** — 300Z (+200.0%) & Landcruiser (+66.7%) read **APPRECIATED** (green up-arrow), VW Golf **DEPRECIATED** (−4.3%). No "−200% depreciation". `edgeCases`: clean.
- **MON-048** — expense one-offs badged **ONE-OFF** (HOME battery −$11,385, Thornland Lot 1 legal/insurance, Guildford painting), not "Monthly". `edgeCases.oneOffShownAsMonthly === false`.
- **MON-040** (prior) — tax recommendations realistic (salary sacrifice $9,000, high rate), no 3685% / $6.27M sentinels.
- **MON-030** (prior) — Home Health 50/C === CFO 50/C; CFO bars are the SEVEN warm categories. `mon030.scoreMatch && gradeMatch === true`.
- **MON-044/046** (prior) — Loan Opportunities → `/dashboard/debt-planner` (no 404); investments route resolves.

## Open display issues — CONFIRMED real, to FIX (MON-039, one registry issue, three parts)

All `changesNumbers=false` (labels/render only):

- **039a — Medicare absent on the two tax cards.** `/cashflow` "TAX·FY ESTIMATE $194,218 / 40.0%" and CFO "Tax Position −$194,218" show **no Medicare line**; Medicare $9,706 is itemised only on `/dashboard/tax`. Both cards read the canonical `getUserTaxPosition` where `taxPosition.tax.medicareLevy` exists → adding a line is SSOT-clean.
- **039b — "MONEY IN $0" beside "1 income source fed this month".** The copy asserts income "fed this month" while actuals=$0 (`moneyIn:0, declaredSourceCount:1`); the page does not distinguish actuals from the declared source count. A labelling defect — the copy must not claim receipt when $0 landed.
- **039c — Guildford list tile omits the Cashflow/yr line.** Guildford (PRIMARY RESIDENCE that collects $26,089 rent + carries a loan) shows −$7,387 on detail and −$7,392 on the Home tile, but the **list tile drops the line entirely** — a cross-surface render inconsistency (`crossSurface.Guildford.cashflowYr.list === null`; the number exists on the other two surfaces).

## Open forks — SURFACED to Reza (not guessed, §20.5)

- **MON-042 — vehicles 4 vs 5.** `vehicleCountHousehold:4` (declared onboarding dropdown for expense calibration) vs `vehicleCountAssets:5` (actual ledger; the 5th is a Hitachi Excavator = plant, not a car). Legitimate scope difference, not a count bug. Fork: relabel / derive-from-actuals / leave.
- **MON-043 — income $239K (Home) vs $524,831 (Tax).** `incomeBasis` resolved it: Home = "Last 12 months" ACTUALS; Tax = declared gross. The gap is "Other" income $192,698 shown as **DECLARED-ONLY** (rows read Actual $0 / −100% variance — no matching transactions: ATO $9,098/$952, Ingeus, Service NSW). Both numbers are **correct for their basis** (§19.1). NOT a calc bug → re-set `changesNumbers=false`. Fork: (a) surface the basis label so Home < Tax is explained; (b) add a data-completeness nudge ("$192,698 declared income has no matching transactions").
- **MON-045 — Neg. Gearing Benefit $157,746 ~4× total deductions $39,554 (CFO Tax Position).** RE-CONFIRMED live, **HIGH** (Chrome-flagged). Mathematically impossible (benefit = loss×marginalRate ≤ property deductions < total deductions). Reza-approved **Option 1** (auto-derive property loan interest in the canonical tax engine) ready to build on his go — full §19.2/§19.4/Neomatrix/Ring-3 financial pipeline.

## New low findings — raised

- **MON-049** — Document count/storage disagree: Settings "24 documents · 12MB" vs Vault "6 all-time · 14.0 MB". Non-financial, low.
- **MON-050** — Month-end balance: CFO $301,712 vs Cashflow 30-day forecast $301,639 ($73). Low.

## Notes / non-issues

- **Balances $1:** Liquid $301,808 + Accessible $67,871 + Locked $3,032,102 = $3,401,781 vs Net worth $3,401,782 — off by **$1** from independent rounding of the three components. Sub-dollar display artifact; not raised as a defect (acceptable rounding).
- **HOME shows a yield (0.12%) — NOT a bug.** The property *named* "HOME" (903 Boree Valley Rd) is **tagged INVESTMENT PROPERTY**; the actual primary residence is Guildford. An investment property correctly shows a yield. `edgeCases.homeShowsYield:true` is a false-positive given the tag — the only real subtlety is the confusing *name*.
- **Budget not set up** → `essential_plus_discretionary_eq_recurring` invariant null (expected; Debt Freedom/Budget shows "Complete Your Budget First").

## Baseline

VR-006 becomes the new accepted baseline (`baselines/BASELINE.md`) — it captures the post-MON-035/037-fix reality (HOME −$8,668, Thornland Lot 1 −$31,068, estimatedAnnualTax $194,218). The VR-003 baseline was pre-fix (HOME −$321,280) and is superseded.
