# VR-035 — MON-078: intake-integrity keystone, verified (structural)

> **Run by** The Matrix (Cowork · Opus 4.8, source read) · **Date** 2026-07-27 (AEST) · source @ `cef2e26`. **changesNumbers: no** — the verdict for a build-gate keystone is structural (the source-lock exists + is green + no number moved), not a live number check.
> **Under test:** MON-078 (#1429) — one canonical intake classifier + a build-gate source-lock so every income/expense create path routes through it, killing the scattered silent-default root cause behind the weekly-rent / one-off-×12 / duplicate-row bug families.
> **Headline: PASS — MON-078 → VERIFIED.**

## What was verified (source @ `cef2e26`)
1. **The ONE canonical classifier** — `lib/intake/classifyIntake.ts` (`classifyIntake`, :158) with a single named `LEGACY_FALLBACK_FREQUENCY = 'MONTHLY'` (:142) as the only documented fallback (C1/MON-001 target). Frequency = declared(normalised — WEEKLY/FORTNIGHTLY preserved, ANNUALLY→ANNUAL) → detected evidence → the one named legacy default; recurrence via source-default table; streamMatch scope-singleton/merchant.
2. **The build-gate source-lock** — `tests/intake/intakeSourceLock.test.ts`: a CI census (`CREATE_RE = /prisma\.(income|expense)\.create\(/`) that (a) requires every production file creating an Income/Expense row to import AND call `classifyIntake` (LOCKED producer set + reviewed allowlist), and (b) bans any silent `'MONTHLY'` literal or `|| 'MONTHLY'` / `?? 'MONTHLY'` fallback outside the classifier. A new create site outside the wall FAILS the build.
3. **All intake paths routed** through the classifier: `income`, `expenses`, `expenses/bulk`, `documents/analyze/confirm`, `onboarding/complete`, `recurring-payments/[id]/link`, `rental-reconciliation`, `tax/non-assessable-review`, `transactions/[id]/link` (9 routes — the "8 spots" collapsed to one door, plus more).
4. **Behaviour-preserving** — Ring-0 `tests/intake/classifyIntake.test.ts` (26) pins the defaults; PUT/update routes deliberately NOT routed (an edit is not intake). Consistent with the whole-session live numbers holding the baseline (Income $317,751 / Deductions $172,325 / …) — no number moved.

## Verdict
**MON-078 → VERIFIED.** The intake-integrity keystone is in place: one classifier, a CI build-gate that fails on bypass or silent-MONTHLY, all create paths routed, behaviour-preserving. This is the structural root-cause guardrail (prevention-over-numbers) the intake bug families depend on — its tightening PRs (C1/MON-001, D1/MON-075, C3/MON-076) walk through this door. Registry: 67/101 (66%).

---
*Prepared by The Matrix. Source-level structural verification, 2026-07-27, `cef2e26`. A changesNumbers:no keystone — the source-lock's existence + green CI + zero number movement IS the Ring-3; no live screen needed.*
