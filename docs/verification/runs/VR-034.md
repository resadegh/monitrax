# VR-034 — MON-101: capture Stage 1 (FTE/IEE facts), verified

> **Run by** The Matrix (Cowork · Opus 4.8, Claude-in-Chrome + source) · **Date** 2026-07-27 (AEST) · live `www.monitrax.com.au` + source @ `9901d827` (#1509 merged, migration `20260726000000` applied to prod, deploy READY).
> **Under test:** MON-101 — capture the FTE/IEE facts (family-trust election + per-beneficiary `relationship` / `hasQuotedTfn` / `coveredByIee`) so the wired+reachable MON-098 overlay can fire on real data.
> **Headline: PASS — MON-101 → VERIFIED.** Fire path + the all-or-nothing capture gate proven at source/CI; live INERT on Reza's data (no FTE-electing trust). **Honest scope: the live-fire on a seeded trust was NOT run — Reza has no such trust and the Matrix does not fabricate entities in his prod account; the fire path is the CI golden.**

## A — Fire path + capture gate (source + CI @ `9901d827`) — PASS
- **Schema:** `FamilyGroupRelationship` enum + `DistributionAllocation.relationship? / hasQuotedTfn? / coveredByIee?` — additive nullable (migration `20260726000000`, no row touched).
- **The ONE producer + all-or-nothing GATE** (`entityTaxFactsAssembler.ts` `buildFteIeeInput`): returns `null` (overlay inert) unless `hasFamilyTrustElection` is ON **and** `allocations.every(a => a.relationship !== null && a.hasQuotedTfn !== null)`. Source comment is explicit: a null `hasQuotedTfn` is NEVER defaulted — `null→false` would fabricate a 47% Pt VA withholding, `null→true` would suppress a real one, so a null can neither invent nor hide a classification. `distributionAmount = presentlyEntitledShare × trustNetIncome` (same s95 base as the Div 6 feed); reads the CONFIRMED operative resolution.
- **Ratchet** `tests/tax/mon101FteIeeCapture.test.ts` (6): §19.2 worked example FTDT = 0.47 × (40% × $100,000) = **$18,800** end-to-end both twins; gate closed in all three failure directions; gate-closed byte-parity.
- **Capture UI:** the Reza-approved Stitch dialog (`f395b67e…`, §18.8 9.2/10; artefacts `.stitch/designs/capture-stage1/`) → `components/wealth-explorer/TrustDistributionsSection.tsx`. Flow: dialog → POST distribution-resolutions → assembler → entity route (MON-100) → `fteIeeByEntity` → MON-098 overlay fires with citations + `UC-FTDT-OUTSIDE-FAMILY`.

## B — Live inertness (Reza) — PASS
Live tax page (FY2026-27, calc 27/07/2026) byte-identical to baseline: Income **$317,751** · Deductions **$172,325** · Taxable **$145,426** · Owing **$26,926** · Net **$38,054** · Medicare **$2,909**. Only the negative-gearing warning; **no FTDT / TFN-withholding line, no `crossCutting` key** — Reza has no FTE-electing trust, so the gate holds the overlay inert.

## Not run (honest scope)
The Stage-1 fixture (#1508) is a CI/spec golden, not live data. No FTE trust exists in Reza's account, and the Matrix does not seed fabricated entities into his prod data (prevention-over-numbers / governance rule). So the **live-fire** (FTDT line appearing on a real trust) is proven by the CI golden ($18,800) + source, not a live screen. If Reza wants a live fire demo, seeding a test trust is his decision.

## Verdict
**MON-101 → VERIFIED.** FTE/IEE capture is live end-to-end with the migration applied to prod; the all-or-nothing gate guarantees no fabricated/suppressed 47% classification; Reza stays byte-identical by construction. Registry: 66/101 (65%). **Stage 2 (PSI questionnaire — zero-capture, broadest trigger) next**, then Stage 3 (Div 152 CGT-event flow) completes the capture feature.

---
*Prepared by The Matrix. Source + live, 2026-07-27, `9901d827`. The gate is the control (a null never fabricates or suppresses a classification); Reza's numbers unmoved; fire path is the CI golden — no fabricated trust in his account.*
