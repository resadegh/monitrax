# Monitrax Implementation Plan

> **This is the live, single source of truth for "what is being worked on, what is queued, what is blocked, what changed recently."**
>
> Every session starts here. Every PR that materially changes a workstream updates the relevant spoke below. If a workstream isn't in this plan, it isn't real.
>
> See CLAUDE.md §1 (Session Startup Protocol) and §15 (Implementation Plan Protocol) for the rules that govern this document.

**Last updated:** 2026-06-27 (**Neobrain on manual reconciliation — auto-apply learned category to similar rows**: categorising a transaction now auto-applies the user-confirmed category to other uncategorised same-merchant rows (guardrails: exact merchant, same direction, uncategorised-only, user-scoped; visible + reversible Undo) + a learned-suggestion pill on uncategorised rows; reuses the same merchantMapping store (§12.2.1). Prior: **Fix: batch categorisation applies to transfer + investment actions** — the "categorize selected together" path was wired only for expense/income/link, so batch-reconciling transfers (NAB/loan repayments) or investment contributions marked only the primary row; now symmetric across all four actions, FE + server, user-scoped + §12.11-safe, transfers stay excluded from spend (§19.1). Prior: **Phase 55 Activity reconciliation simplification — ONE derived status per row**: the Activity page showed up to five overlapping/contradictory status signals per row; new SSOT `lib/bookkeeping/transactionStatus.ts` (`deriveRowStatus` + `summariseRowStates`) collapses them to done / suggested / needs-category with at most one action, label = strongest signal (transfer/link wins over raw category) — fixes salary-shows-OTHER, the three-competing-chips, and transfer-shows-Uncategorised (display fixed without waiting on the data backfill); honest sort banner; idempotent stale-transfer backfill migration `20260626000000`; Stitch-first desktop light 9.2 / dark 9.3 (§18.8). Header confidence-band-card → action-state-count reframe queued as 55.2. New workstream `0·ACTIVITY-SIMPLIFY`. Prior: **Neo Inventory NI-0✅ NI-1✅ NI-2✅ (Reza directive 2026-06-26)**: **NI-1 restored Graphify as Layer 0** — `npm run neomatrix:graphify` extracts the whole codebase (**1,064 files · 8,587 nodes**, code-only/offline) into a committed structural graph + a pure-node disk-vs-graph completeness gate in `neomatrix:check` (caught 3 graphify drops → allowlisted with verified reasons); **NI-2 binds** the semantic nodes to L0 + prints coverage (L0 8,587 / proven calc-audit 84 / modelled 64 / binding 75/75) — completeness is now a build output, not a claim. PR #1258 (NI-0) **merged**; #1260 (NI-1→main, corrective after a base-branch mis-merge) + NI-2 PR open; **#1250–#1257 closed** (salvage → NI-3). NI-3 = exact proven↔modelled reconciliation + backfill toward 100% + re-home the closed-PR properties as calc-audit fixtures. See `NEO_INVENTORY.md` §8/§9, `NEO_INVENTORY_BASELINE.md`, `PHASE_53_*.md` §17, `0·NEO-INVENTORY` workstream. Prior NI-0: research (calc-audit inspection) found the recurring "you covered everything → next audit finds gaps" root cause = FOUR overlapping, unreconciled inventories of "what calculations exist" — `calc-audit` Phase 41i (`calcEngineRegistry` + `surfaces/registry` + **92 CI-gated fixtures** covering the whole tax engine + divisions + 8 states + CFO + scenarios + aggregators + primitives), the **Neomatrix** (103 hand-built nodes, a *subset*), the **Trust Engine** (2026-06-25 nodes), the **Phase 4 rail/A1/linter**. Model: `calcEngineRegistry` = the SINGLE inventory (most complete + already gate-enforced "no engine without a fixture"); the Neomatrix = a GENERATED view over it; calc-audit fixtures = the proof spine — NO fifth platform. NI-0 ships the design doc `docs/blueprint/NEO_INVENTORY.md` + CLAUDE.md **Part 22** + the `0·NEO-INVENTORY` workstream + the #1250–#1257 comparison (the 8 overnight Trust-Engine PRs verified in a parallel silo what calc-audit already fixtured → **HELD**, to be reconciled via NI-1→NI-4, not merged as-is). Docs only; no code, no financial logic. Prior: **Phase 54 Neobrain — AI perception & learning layer brought under one umbrella + modelled into the Neomatrix**: named the constellation of AI engines (transaction categorisation cascade, per-user + k-anon shared-KB learning loop, transfer/loan-repayment detection, document/receipt intelligence) **Neobrain**; added a `neobrain` domain to `financial-graph.json` — 39 verified nodes / 47 verified edges, every anchor + edge read in source (§19.2), bridging to core via UnifiedTransaction/Expense/Loan; extended the domain enum in `graphlib.mjs` + schema + explorer (`#EC4899`); `neomatrix:check` green (150 nodes/194 edges, all verified). Consolidated nine source phase docs (13/18/25/26/29/42/50/51/52) into one design SSOT `docs/blueprint/PHASE_54_NEOBRAIN.md`, resolving the six scattered-threshold/duplicate contradictions. Pure consolidation + modelling, **no behaviour change**. New `0·NEOBRAIN` workstream. Prior: **Financial Trust Engine + SSOT-DEDUP roadmap registered** — two new active workstreams in `01_ACTIVE_WORKSTREAMS.md`: **`0·TRUST-ENGINE`** (Reza directive 2026-06-25: *"you need a system to check and be 100% sure the results are valid, correct and trustworthy"* — an audit-grade verification layer over the Neomatrix; Reza chose to **build the safety-critical core first** = Layers **L0 authority-anchored golden cases** + **L1 independent recomputation** + **L2 invariants/property tests** + **L3 reconciliation tie-outs** + **L5 build gate**, with L6 show-the-working UI + L7 FY re-anchoring deferred; EXTENDS the Phase 4 test-rail L2/L3 + the Neomatrix A1 audit, doesn't rebuild — §12.2.1) and **`1·SSOT-DEDUP`** (the 2026-06-25 duplicate-source audit's W0–W7 waves: **W0 stale-tax-brackets DONE** #1238, **W1 layer-aware linter DONE** #1240; **W2–W7 PARKED behind the Trust Engine** so each dedup is provably safe). SSOT lessons doc merged (#1239). Suspected stale super-cap `27500` + magic `0.34` at `cashflow/intelligence/route.ts:481` flagged for a follow-up financial PR (§21.2, not silently fixed). Prior: 2026-06-24 (**Phase 53 Neomatrix — DEPTH cont.: income aggregator modelled + A1-audited** (PR — `aggregateIncome` :143, the §6.2 income SSOT: gross/net/PAYG, salary GROSS-vs-NET asymmetry + already-annual grossAmount/netAmount/PAYG contract, taxable/non-taxable split never-dropped per §19.1; 17 engines A1-audited, graph v0.16.0, 70/70 tests, no suspected-issue, 10/10 §20.4). Prior: **Phase 53 Neomatrix — foundation + tax modelled & audited (PRs #1204–#1207 merged; #1208 in flight)**: typed financial-logic knowledge graph (`docs/financial-logic/graph/financial-graph.json`) + pure-Node generator + CI/build-gate audit. N0 Graphify (code-only/offline, adopted); N1 schema + 4-engine core slice + generator (`GENERATED_CORE.md`); N3-seed `neomatrix:check` in `vercel-build` + vitest (CLAUDE.md **Part 21**); N4 tax backfill (income/PAYG/CGT-reform/super/land-stamp-GST — all `verified` file:line + 14 ATO law nodes); **A1 executable law-referenced audit** (the model *refers* the code — law-derived expected values, real engines run, mismatch → `suspected-issue` raised never auto-fixed; core + tax audited, no issue found; locks the income-tax bracket-boundary P0 + SG-cap P1 fixes). Governance: CLAUDE.md **Part 20** (3×/10-10 sign-off) + **§20.4** (financial builds 10/10) + **Part 21** (every build runs through CLAUDE.md + Neomatrix). See `PHASE_53_MONITRAX_NEOMATRIX.md` §0. Prior: **Cashflow SSOT convergence Phase 2** — root cause: SSOT was documented but never *enforced*, so two cashflow tiles on one page showed different numbers. New canonical accessor `lib/calculations/canonicalCashflow.ts` (the ONE actuals-vs-declared rule, §19.1); `/cashflow` hero + cashflow health-score converged off declared records — the +$10,505 surplus / 51.9% saving-rate fiction — to match the page's own actual waterfall; emergency-fund tile now displays the engine's own denominator (was declared, contradicting `monthsCovered`); trailing-avg divisor fixed (fixed `/3` → data-driven, kills the false ~$938 emergency for recently-connected users); structural enforcement-gate test so SSOT can't silently drift again. Home "Monthly Cash Flow/Annual Outgoings/Saving Rate" tiles DEFERRED to Phase 2b (converging the headline breaks the declared drill-down tie-out — needs a Stitch §18.2.1 drill-down change, not a backend overwrite). Prior: **Cashflow correctness Phase 1 — actual-transaction headlines**: new pure engine `lib/calculations/actualCashflow.ts` + additive actual fields on master `quickMetrics` (current-month + trailing-3mo-avg outflow/inflow, per-category, `hasActualData`) computed from ALL non-transfer `UnifiedTransaction`; 5 headline surfaces repointed off declared records (cashflow waterfall + budget-vs-actual, Money-Story kept/margin, safety-net survivability, Gemini narrative) so uncategorised spend stops being silently dropped. Declared fields kept (back-compat / plan side). New workstream `0·CASHFLOW-ACTUALS` in `01_ACTIVE_WORKSTREAMS.md`. Prior: **Phase 52 Shared Categorisation KB — engine shipped, enabled, seeded + full doc-sync** (PRs #1171–#1181): de-identified cross-user merchant→category KB the AI consults (lookup-first → Gemini-on-miss; k=5 graduation; PII-scrubber; housekeeping; ~275 AU merchant seed; fuzzy brand-prefix matching), `KB_WRITE/READ_ENABLED` ON in prod. Docs updated into #1181: MASTER_BLUEPRINT (Phase 51 + 52 rows), `00_INDEX`, BAU `06_CDR_COMPLIANCE_OPERATIONS` + operational runbook 14 (KB ops), `04_RECENTLY_COMPLETED` (Phase 51.1 + 52), compliance matrix + de-id procedure. Remaining: 52.5b embeddings, 52.5c review-exceptions UI. Prior: 2026-06-22 (**Phase 52 build started — 52.1 KB schema + PII-scrubber**: shared de-identified `TransactionSignature` (unique `(region,pattern,matchType)` dedup key + indexes; no userId) + private `SignatureContribution` ledger (`@@unique(signatureId,userId)`) + `scrubToSignature()` de-identification gate (rejects transfers/PII, strips numbers/dates/method-noise) + migration; 15 tests; SignatureContribution classified in accountReset. Write-back stays flag-gated until the de-identification procedure is signed off. Prior: **Phase 51.1 loan-tracking SHIPPED (PRs #1166–#1170, all merged + prod-verified)** — import → match → confirm → tax-correct interest/principal, live behind the loan's Repayments tab. **Phase 52 — Shared Categorisation KB** registered (designed + decided: RAG knowledge base enriched by all users' confirmations, **Hybrid private + k-anon shared**; powers Phase 51.2). Prior: **Phase 51 `0·LOAN` registered — Loan Ledger, Repayment Matching & Low-Effort Categorisation (design phase)**: scoped from a deep-research pass (5 cited streams — categorisation UX, transfer detection, loan modelling, AU tax, CDR/Basiq — + a codebase audit) answering Reza's two pains: multiple loans paid from one offset (same payee) are untrackable, and categorisation is overwhelming. Recommendation (Phase doc `PHASE_51_LOAN_LEDGER_AND_CATEGORISATION.md`): model each loan as a liability account with its own **quarantined** transaction ledger (interest-charged + repayment-received), bridge to spending via an **actual-vs-actual** transfer-matching engine with a confirm-queue (robust to variable IO / offset / rate changes — never predicted amounts), take deductible **interest from the loan's own interest line** (offset breaks `rate × balance`; offset ≠ redraw per TR 2000/2), design a **CDR-forward store** (QIF now → Basiq later, same store/engine), and overhaul categorisation to **review-exceptions-only** + "apply to past+future" + ATO-label mapping. Docs-only PR; Phase 51.1 Stitch designs next. Prior: **Mobile workstream `0·MOB` registered + STATE cursor refreshed**: stood up the `docs/implementation/05_MOBILE_WORKSTREAM.md` spoke (mobile companion design phase — main-repo half: design docs + `/api/v1/mobile/*` + `@monitrax/core`; the RN app gets its own `monitrax-mobile` repo + cursor), registered it in `01_ACTIVE_WORKSTREAMS.md`, added the continuity-gate watch + STATE.md sub-cursor, and re-pinned the stale STATE web cursor (`654bc55` → live HEAD). Prior: **Document Vault — fix "This content is blocked" preview**: GCS-with-key handed the browser a cross-origin native `storage.googleapis.com` signed URL that the preview `<iframe>` blocks (and an `<img>` of an octet-stream object renders broken); now every server-backed read (MONITRAX + GCS, key OR keyless) streams through the same-origin `/api/documents/download` route — correct content-type + inline disposition + the better CDR posture. Prior: **Tax-pack — Unified Accountant Pack**: added `format=pack` to the existing FY tax-pack export (`/api/bookkeeping/tax-pack/export`) — ONE ZIP combining the PDF + XLSX summary AND every FY receipt/invoice/tax doc + a README, so the user forwards a single file to their accountant. New `accountantPackBuilder.ts` is orchestration-only (§12.1/§12.2): reuses `buildTaxPackSummary` + the existing PDF/XLSX exporters + a `addReceiptDocuments` helper extracted from `zipBundleBuilder`. `TaxPackExportButton` gains a "Complete pack" option (default). 6 tests. Also refreshed `MASTER_BLUEPRINT.md` (Phase 50 status: Phase D complete). **0·DOC is now feature-complete.** Prior: **DME D.5a — renewal → reminder (Stitch-first)**: the scan flow's `analyze-for-form` now extracts an optional `expiryDate` (policy/rego/warranty only); when found, after the expense save the new `RenewalReminderCard` (calm sky→teal "safety-net" glass; date editable; explicit Set/Not-now) offers a reminder, and on confirm **creates a custom `Reminder`** via the existing `POST /api/reminders/custom` — a CREATE, not an update (§12.11-safe) — which `computeCustomReminders()` lights up in the bell + RenewalsCard. No schema change. **Phase 50 Phase D engine-intelligence layer (D.1–D.6 + D.1.1) is now complete.** 4 Stitch variants + 6 tests. Prior: **Wealth Universe WX.6.1 — tap to zoom first, details second**: clicking an entity on L1 was zooming in AND opening the detail file at once, greying out the universe; now the FIRST tap zooms in only and the SECOND tap on the centred bubble opens its file — a cluster (no file) zooms back out instead. Both canvases; interaction-only fix, no Stitch. Prior: **Wealth Universe WX.6 — entity → class → asset zoom (Level-2 bundles)**: focusing a multi-asset entity now groups its holdings into per-class bundle bubbles (Properties / Loans / Vehicles / Shares / Cash) that zoom into their individual assets at L3; `expandedEntityIds` became a navigation stack + tappable breadcrumb (`Universe ‹ Reza ‹ Properties`); desktop + mobile, Stitch-first (desktop+mobile dark artefacts), 6 new tests (42 total). Fixes Reza's "the first layer is messy" prod report. Prior: **DME D.1.1 — scan path funnelled through the DME dedup chokepoint**: `/api/documents/analyze-for-form` (the global "Scan a receipt" + form-autofill upload) called the legacy `documentService.uploadDocument()` which has no content-hash dedup; rewired to the canonical `DocumentManagementEngine.processUpload()` so a byte-identical re-scan returns the existing document instead of duplicating, and it inherits the per-user storage quota. No schema change (D.1's `contentHash` migration already in). Prior: **DME D.6 — bulk-approve Smart Inbox (Stitch-first)**: new `components/documents/SmartInbox.tsx` replaces the inline inbox — multi-select with the D.3 AUTO band **pre-selected**, per-row **inline edit** of the AI's findings, and **"Approve N selected"** looping the SSOT confirm path once per item (D.2 reconcile + receipt-match run per item; **no batch shortcut, no silent writes**). Track glass design; lives in My Vault. The sanctioned autonomy convenience — AI suggests, the user always confirms + edits. 4 Stitch variants (desktop/mobile × light/dark) + 6 tests; removed the dead inline JSX + 3 page helpers (§12.1). Prior: **DME D.5b — retention clock (advisory)**: pure `retentionClock.ts` ATO 5-year rule → `RETAIN | ARCHIVE_SAFE | NO_CLOCK`, quiet "Safe to archive" pill, never auto-archives. 10 tests. Prior: **DME D.4 — learned routing (suggest-only)** + the **autonomy model** decision. New `VendorEntityHint` table (migration `20260618090000`) + canonical `lib/documents/intelligence/learnedRouting.ts`: records vendor→entity routings at the link/confirm chokepoints and **pre-selects** the usual entity in the scan flow's "What is this for?" selector (with a "Suggested for you" cue) — but **never auto-applies**. Honours the load-bearing **autonomy decision (Reza 2026-06-18): AI always suggests, the user ALWAYS confirms + can edit; bulk-approve is the only convenience; no silent auto-execution** — which also flips D.3's auto-EXECUTION from "deferred" to "rejected" (the AUTO band is a display cue only). `GET /api/documents/vendor-hint`; 14 tests. Prior: **DME D.3 (policy SSOT) — `confidencePolicy.ts`** unifies the scattered 0.9/0.7 confidence thresholds into one source + autonomy bands (AUTO/CONFIRM/ASK) + earned-autonomy TRAIL downgrade; 6 tests. Prior: **DME D.2 — record reconciliation.** Before the document-confirm flow creates an expense/income/loan it checks for an existing matching record (`reconcileSuggestedAction()`); on a match it links the doc to the existing record (`reconciled:true`) instead of duplicating. Generalises D.1's `/api/expenses` dedup. 7 tests. Prior: **Fix: 'Add as expense' now refreshes the asset Expenses tab** — the expense was created (201) + linked but the tab showed stale '0 expenses'; `DocumentsSection.onExpenseAdded` → `loadAssetDetail`. Prior: **Fix: document "Asset" link deep-links to the specific asset** (`/dashboard/assets?view=<id>` opens its detail dialog; assets have no per-id route). Prior: **Fix: document entity-link 404 + "$ Add as expense" hidden on non-receipt categories.** Clicking a doc's entity badge built `/dashboard/asset/<id>` (404 — wrong plural, no per-id route); added `entityHref()` mapping each type to a real route. The "$ Add as expense" only showed for RECEIPT/INVOICE but the QBE doc was classified INSURANCE → now shows for any doc on the asset/property Documents tab. Prior: **Documents: persistent "Add as expense" + real thumbnails + full edit dialog + tag de-clutter.** Per-document "$ Add as expense" (reads the saved analysis → survives navigation, dedup'd + doc-linked); `DocumentList` hides auto-tags that duplicate the category/entity badges or upload source. Issue #2 preview + richer metadata-edit deferred pending Reza clarification. Prior: **DME D.1: document-level dedup + single-engine clarification.** Corrected the misleading "DME 2.0/two engines" wording — ONE DME (filing) + ONE DIE (reading), separate concerns (§12.3/§12.2); Phase D extends them, not a new engine. Built D.1: `processUpload` SHA-256-hashes bytes + looks up `(userId, contentHash)` before storing → byte-identical re-upload returns the existing doc flagged `duplicate` (no second copy), stopping "same receipt twice" at the source. `Document.contentHash` + index + additive migration 20260617150000 (§12.12). Prior: **AI-on-upload propagated to Properties + Investments; dedup generalised; DME 2.0 design.** Prior2: `analyzeOnUpload` now on Properties + Investment-account Documents sections (was Assets only); `/api/expenses` dedup flag generalised to `dedupeReceipt` (matches any entity link, not just asset); new **Phase D — AI-powered DME 2.0** design in `PHASE_50_AI_DOCUMENT_ROUTER.md` (document-level hash dedup D.1 recommended next, then reconciliation/confidence-gating/learned-routing/lifecycle) — awaiting Reza go-ahead on order. Prior: **Receipt→expense dedup + doc↔expense link + per-expense delete (SSOT fix).** "Add as expense" made duplicate expenses (multiple photos of one receipt) with no doc link; deleting the files orphaned them. Fixes: `/api/expenses` opt-in `dedupeOnAsset` returns the existing expense instead of duplicating; the doc is linked to the expense; the asset Expenses tab gains a per-expense delete (manual, no auto-cascade — Reza decision); receipts default to ANNUAL not MONTHLY. Prior: **Auto-rename documents from AI recognition.** On a successful asset-Documents recognition the file is auto-renamed to `"{vendor} - {date}.{ext}"` (e.g. "QBE Insurance - 2026-06-18.jpg") via the existing rename action — organises the Vault by what the doc IS, not "IMG_6615.JPG". Prior: **Fix: Documents AI-recognise crash (React #31) + truncated filename.** "Upload document" crashed the page because `pickField` returned the engine's `{value,confidence}` ExtractedField object and the recognised banner rendered it (React #31); now unwraps `.value` + never returns objects. Filename "I…" fixed by giving it its own full-width line in `DocumentList` with compact icon buttons below. Prior: **Fix: asset detail/edit dialogs overflow mobile width.** The shadcn `DialogContent` is `display:grid` and its grid-item children (KPI grid, Tabs) had `min-width:auto`, pushing the dialog past the viewport (right column + tabs ran off-screen). Fix: explicit `w-[calc(100vw-1rem)]` + `overflow-x-hidden` + `[&>*]:min-w-0` on both dialogs; edit-form vehicle row `grid-cols-3`→`grid-cols-1 sm:grid-cols-3`. No service worker in app, so it was a real layout bug, not cache. Prior: **Asset detail mobile redesign (Stitch-first, approved).** The asset detail dialog clipped on mobile; redesigned through Stitch (screen `770abf40f2584174b52db63a4310a34a`, Reza-approved) → React: `overflow-x-hidden` dialog, gradient header, polished `KpiTile` glass tiles (2×2 mobile/4-across sm+, min-w-0/truncate), scrollable tabs. Diagnosis: the "AI not picking uploads" was a stale cached client bundle (upload sent `analyze:false` on the #1137 deploy) / expense-row paperclip — server fix is live; relaunch + use Documents-tab Upload. Prior: **Asset Documents: AI recognition on upload + mobile framing.** The per-item `DocumentsSection` upload now opt-in runs Vision OCR (`analyzeOnUpload`) and surfaces recognised vendor/amount/date with a one-tap "Add as expense for <asset>" (linked); enabled on the asset detail. Detail dialog mobile framing fixed (KPI tiles `grid-cols-2 sm:grid-cols-4`; TabsList scrollable). Prior: **Scan-a-receipt → attach to an asset/property + Vault Assets folder.** The scan result screen only made standalone GENERAL expenses; added a "What is this for?" selector (assets+properties) that links the expense (assetId/propertyId) AND the document (POST /api/documents/[id]/link) to the chosen item, plus the missing Assets branch in the Vault FolderTree (issue #3). Stitch §18.2.1 backfill screen `20c36c03c45c41ceaa6e3d070c40ba60`. No schema change. Prior: **Vision OCR: force REST transport (`fallback: true`) — THE root cause of "AI not working".** Live prod logs proved Vision auth was fine (key used) but the OCR call died in the gRPC/HTTP-2 transport on Vercel serverless (`Error: undefined undefined: undefined`, gax "not classified as transient") before reaching Google — which is why the Vision API showed zero traffic regardless of key vs keyless. Fix: `ImageAnnotatorClient({ …, fallback: true })` on all 3 auth branches. Telemetry sinks are gRPC too → same REST fix is the proper telemetry follow-up. Prior: **Vision back to KEY-FIRST + restore the GCS key.** Live prod diagnosis proved the keyless cutover was premature — keyless GCS fails with `Anonymous caller` 401 (authClient not honoured by `@google-cloud/storage`). Storage + telemetry are key-first so restoring `GCS_SERVICE_ACCOUNT_KEY` fixes them with no code change; `visionService.ts` flipped back to key-first so the restored key also fixes Vision. Keyless stays as the no-key fallback, to be re-enabled only once verified end-to-end. Operator: restore the key (Production); merging the PR deploys key + fix together. Prior: **CI: serialize vitest (`--no-file-parallelism`) to kill the Prisma-engine native abort — root-cause fix.** The exit-134 "Failed to deserialize constructor options" flake is triggered by concurrent PrismaClient/N-API engine construction across vitest's parallel fork pool; the whole-suite retry proved insufficient (hit both parallel runs on 2026-06-17). `npm test` now runs files sequentially (full suite 152 files/2897 tests green in 45s); the retry stays as residual insurance. Prior: **Fix: GCP sinks no-op under keyless** — after the key removal, getGCPCredentials() fell through to a credential-less ADC object and the Cloud Logging/Monitoring sinks threw uncaughtExceptions across prod routes (the #1121 bug, now in prod); now returns null under keyless so they no-op (audit DB primary unaffected). Prior: **Keyless Vision OCR** — fixes the broken AI scan: `visionService.ts` now prefers keyless WIF (`vercel-monitrax-db`) over the proven-bad `monitrax-backend` key; `isAvailable()` accepts keyless. **Operator action:** grant `vercel-monitrax-db` `roles/serviceusage.serviceUsageConsumer`; then OCR works keyless + the GCS key can be removed (storage cutover). Prior: **Edit asset expenses + Half-yearly option**: asset-expense frequency dropdown gains `HALF_YEARLY`; each expense row gets a pencil → edit dialog pre-filled, `handleExpenseSubmit` PUTs `/api/expenses/[id]` when editing. Storage-independent. Still open from Reza's testing: docs-under-expense, preview, vault asset-folder, AI Vision (the GCS/storage-auth cluster). Prior: **Document rename + per-expense attach + camera capture**: `renameDocument` service + `rename` PATCH action + Pencil in `DocumentList`; reusable `DocumentAttachButton` on each asset-expense row (links receipt to expense+asset); new `CameraCaptureDialog` (getUserMedia) so "Take photo" works on desktop webcam + mobile (was touch-only). §12.11 rename guarded; 3 rename tests, 22/22 docs tests. Prior: **Per-item Documents — upload/scan ON the wealth items**: new reusable `DocumentsSection` (upload + mobile camera + list/view/delete) mounted on Properties detail, Investment-account detail, and the Assets dialog; new `asset_path` storage rule files asset docs under `assets/<id>/...`; uploads link to the item and bypass the AI path so they work even while Vision is down. Super deferred (needs SUPER link type). 3 tests, 19/19 docs tests green. Prior: **Fix — storage factory GCS-detection accepts keyless**: `computeGcsConfigured()` now treats GCS as configured when project+bucket set AND (key OR keyless WIF env), so deleting `GCS_SERVICE_ACCOUNT_KEY` engages keyless instead of silently falling back to the DB. Caught during live GCS provisioning; documented with a full resolution-order + rollback runbook in PHASE_50 (Reza directive). Cutover: bucket + both SAs ready; after merge delete the key + redeploy → keyless. Prior: **AI Document Router · Phase B** (storage track) — **keyless GCS auth + GCS-aware download**: new `lib/gcp/wifAuthClient.ts` (keyless WIF, no static key — reuses the DB's identity per §13.6), provider-aware `/api/documents/download` streaming (DB bytea or GCS), read-URL policy SSOT `readUrl.ts`; 4 tests. Operator provisioning to go live now 3 steps (bucket + IAM grant + 2 non-secret env vars). Prior same day: per-user storage quota: new canonical `lib/documents/storage/storageQuota.ts` (drift-free, computed from `SUM(Document.size)`, default 2 GiB, backend-independent), enforced at the DME `processUpload` chokepoint + the legacy scan path; `/api/documents/upload` → 413 on breach; 7 tests. New `0·DOC` workstream + `PHASE_50_AI_DOCUMENT_ROUTER.md` (A✅/B~/C plan + GCS provisioning checklist). Reza decisions logged: storage=GCS, household=shared-finances-incl-docs. GCS cut-over blocked on operator provisioning. Prior: **Phase 26.7** — mobile camera capture + global "Scan a receipt": `FormDocumentUpload` gains a native `capture="environment"` "Take photo" button on touch devices; new `GlobalScanReceipt` FAB + glass bottom sheet rides the canonical upload→analyze→confirm pipeline. See `04_RECENTLY_COMPLETED.md`. Prior: **Phase 4 COMPLETE** — all four test-rail layers merged to main: L1 #1115 `vitest` CI (now a required check on the `main` ruleset) · L2 #1116 golden-master (engine-sourced snapshots) · L3 #1117 invariants/property checks · L4 #1118 Playwright UAT scaffold (wired into `tests.yml`, skipped via `vars.E2E_ENABLED` until E2E auth is provisioned). No correctness bug surfaced across L2/L3. Follow-ups (optional): enable the UAT gate (E2E_ENABLED + E2E_STORAGE_STATE_JSON), and a git-capable session pastes the four staged `04_RECENTLY_COMPLETED` Phase-4 entries (verbatim in the PR bodies; spokes exceed the connector rewrite ceiling). Prior: Phase 3 audit + fix PRs #1113/#1114; #1111 FY-review; Phase 47 D6; F-8 plan split.) _Keep this date current: it must never be older than the newest entry in [`04_RECENTLY_COMPLETED.md`](implementation/04_RECENTLY_COMPLETED.md) (CI-checked — finding F-1)._
---

## How this plan is structured (read me first)

The plan is a **hub (this file) + spokes** (under [`docs/implementation/`](implementation/)). The hub holds only navigation, the status legend, and the update rules. The detail lives in the spokes so each file stays small enough to (a) write via the GitHub connector in one call and (b) scan without scrolling forever. **`docs/IMPLEMENTATION_PLAN.md` remains the canonical entry point** — links elsewhere in the repo that point at "the plan" land here and route onward.

| Spoke | What it holds | Open it |
|---|---|---|
| 🟡 **Active Workstreams** | Work in flight right now — phase checklists, owners, risk, blockers. Sorted by priority. | [`implementation/01_ACTIVE_WORKSTREAMS.md`](implementation/01_ACTIVE_WORKSTREAMS.md) |
| 📋 **Up Next & Demo-Complete** | Agreed + queued, not started; plus the frozen Lighthouse demo-complete band. | [`implementation/02_UP_NEXT.md`](implementation/02_UP_NEXT.md) |
| ❓🚧🗑️↩️ **Questions, Blocked & Backlog** | Open Questions (strategic, undecided), Blocked items, Dead Code / Tech-Debt, Reversed Decisions. | [`implementation/03_OPEN_QUESTIONS_AND_BACKLOG.md`](implementation/03_OPEN_QUESTIONS_AND_BACKLOG.md) |
| ✅ **Recently Completed** | Rolling 30-day completion log (older items roll into the changelog). | [`implementation/04_RECENTLY_COMPLETED.md`](implementation/04_RECENTLY_COMPLETED.md) |
| 📱 **Mobile Workstream** | The `0·MOB` mobile companion app live tracker (design + main-repo backend). | [`implementation/05_MOBILE_WORKSTREAM.md`](implementation/05_MOBILE_WORKSTREAM.md) |
| 📚 **Older history** | Pre-30-day session log + the relocated preamble narrative. | [`docs/changelog/IMPLEMENTATION_CHANGELOG.md`](changelog/IMPLEMENTATION_CHANGELOG.md) |

**Freshness gate:** a spoke is the SSOT for its slice. When a claim in another doc (STATE.md, SYSTEM_MAP.md, a Phase doc) disagrees with the relevant spoke, the spoke wins — re-pull and fix the pointer.

---

## Status legend

| Symbol | Meaning |
|---|---|
| 🟢 | Active and healthy |
| 🟡 | Active, in progress, on track |
| 🔴 | Active, stuck or risk flagged — see notes |
| 🚧 | Blocked, awaiting decision or external dependency |
| 📋 | Queued — agreed, not started |
| ❓ | Idea / open question — not committed |
| 🗑️ | Dead code / tech-debt — pending cleanup |
| ↩️ | Reversed decision — preserved here so we don't re-do it |
| ✅ | Recently completed (rolling 30 days) |

---

## How to update this document (rules)

> **Mandatory.** See CLAUDE.md §15 (Implementation Plan Protocol) for full rule text. Edit the relevant **spoke**, not this hub (except the **Last updated** date above, which every plan-touching PR bumps).

1. **Every PR that starts a workstream** adds it to [`01_ACTIVE_WORKSTREAMS.md`](implementation/01_ACTIVE_WORKSTREAMS.md) with all the fields filled in (§15.2).
2. **Every PR that advances a workstream** ticks off the relevant `[ ]` checkbox(es) in that workstream's phase list and updates `Last touched`.
3. **Every PR that completes a workstream** moves it from `01_ACTIVE_WORKSTREAMS.md` to [`04_RECENTLY_COMPLETED.md`](implementation/04_RECENTLY_COMPLETED.md) with the date and PR number.
4. **Every PR that surfaces a tech-debt item** (e.g. removes a duplicate, identifies dead code) adds it to the Dead Code / Tech-Debt backlog in [`03_OPEN_QUESTIONS_AND_BACKLOG.md`](implementation/03_OPEN_QUESTIONS_AND_BACKLOG.md).
5. **Every PR that reverts a previous attempt** adds an entry to Reversed Decisions in [`03_OPEN_QUESTIONS_AND_BACKLOG.md`](implementation/03_OPEN_QUESTIONS_AND_BACKLOG.md) so the same dead-end isn't re-attempted.
6. **Every PR that introduces an open question** the user hasn't decided adds it to Open Questions in [`03_OPEN_QUESTIONS_AND_BACKLOG.md`](implementation/03_OPEN_QUESTIONS_AND_BACKLOG.md).
7. **Bump the `Last updated` date** in this hub on every plan-touching PR. CI fails if it falls behind the newest `04_RECENTLY_COMPLETED.md` entry (finding F-1, `scripts/check-plan-freshness.sh`).
8. **Reviewers reject PRs** that materially change a workstream without updating the plan. Same hygiene rule as the changelog.

### Per-spoke size budget (finding F-8)

- **Target: each spoke ≤ ~600 lines / ≤ ~150 KB** so it stays connector-writable in one call and scannable. _Practical caveat surfaced 2026-06-15 (Phase 3): even a 63 KB spoke is unreliable to rewrite verbatim from a Cowork/connector session — the safe ceiling for an in-place connector rewrite is well under that. Prefer git-capable edits, or split further._
- When a spoke exceeds budget, **retire settled content**: completed workstreams → `04_RECENTLY_COMPLETED.md`; recently-completed items older than 30 days → [`IMPLEMENTATION_CHANGELOG.md`](changelog/IMPLEMENTATION_CHANGELOG.md).
- **Known over-budget (carried, not yet pruned):** `01_ACTIVE_WORKSTREAMS.md` (~289 KB) and `04_RECENTLY_COMPLETED.md` (~296 KB). This PR did the structural split only; the **next hygiene pass** retires the completed §0x workstreams (e.g. 0·WI, 0·DG, 0·StD, 0·WX) and rolls >30-day completions into the changelog to bring both under budget. Tracked as a Dead Code / Tech-Debt item in `03_OPEN_QUESTIONS_AND_BACKLOG.md`.
