# Monitrax Implementation Plan

> **This is the live, single source of truth for "what is being worked on, what is queued, what is blocked, what changed recently."**
>
> Every session starts here. Every PR that materially changes a workstream updates the relevant spoke below. If a workstream isn't in this plan, it isn't real.
>
> See CLAUDE.md §1 (Session Startup Protocol) and §15 (Implementation Plan Protocol) for the rules that govern this document.

**Last updated:** 2026-08-02b (**NEW: the reference-numbers SCOREBOARD** — `docs/architecture/REFERENCE_NUMBERS_SCOREBOARD.md`, generated (`npm run refnums:scoreboard`, stale-check enforced in vitest), answering Reza's "do you have a table of the reference numbers and calc engines with before/after on the duplicate reconciliation?". One view over four previously-unjoined sources: **40 quantities · 1,251 producing sites · seed vs now · Δ · Neomatrix join · contract join**, plus every dated census snapshot. It refuses three things by design: a Δ never reads as deleted duplication (the relay-instrument MEASUREMENT CORRECTION dropped seven quantities without deleting one duplicate — stated at the top, causes live in the ledger), a canonical producer is never guessed (EXACT key match only, §19.2), and no count is a correctness claim. **The join surfaced a real finding: only 5/40 census quantities have a Neomatrix `semanticKey` and 13/40 have a contract** — two vocabularies for one architecture, which is the NI-1→NI-4 reconciliation Part 22 exists to close. **`loanCost` is still 31** — T2 is what takes it to one. Prior this day: **MON-131 T2 — G3 CLEARED.** The migration is unblocked except for D49 (Reza's call).** The third relay capture returned VALID at `915704f0` (`loanCount === 5`; the Matrix withdrew its own earlier capture for failing the build precondition — it predated the MON-143 fix). `.audit/expected-moves-t2.json` committed: **13 measured paths**, every after-value produced by the derivation sweep running the REAL engines on live inputs — and **all five paths that three rounds of hand-written lists had missed appear without being named**, which is the sweep proving itself. Raw payload committed as provenance (`.audit/captures/t2-loan-cost-915704f0.json`). **MON-143 confirmed fixed from the other side**: Guildford's floor 1,964.67 → 384.45 with NO per-loan cost moving (it resolves via ACTUALS and never floors). Two findings the capture forced, both G7-fatal if missed: (1) the **unrounded-feed constraint** — rounded per-loan costs sum to 12,779.28 vs the engine's 12,779.29, so the migration must feed unrounded values; (2) the **rounding-convention correction** — `cashflowOrchestrator.ts:265` computes annual leaves as `round(unrounded monthly × 12)`, NOT from annual components, so `annualCashflow` is the measured **133,020.78**; T1's written rule would have declared `.79` and failed the tranche (T1 file gains a non-value-changing clarification). **NEXT: D49 (awaiting Reza) → then the migration — `loanCost` 31 producer sites → ONE `resolveLoanCostsForUser`.** MON-141 + MON-142 wiring also await Reza. Detail: MON-131_TRANCHE_LEDGER §3 T2/§6.**) Prior: 2026-07-31e (**MON-131 T2 — both blockers CLEARED; ONE gate left.** MON-143 fixed + merged (#1562 `f7b685de` — the canonical interest floor now nets the offset; stays `FIXING`, not VERIFIED, because no rendered number moves on live data and its Ring-3 evidence is the migration run itself). The re-capture blocker is gone a level deeper than asked: three rounds missed five paths, so the hand-written list was deleted and replaced by a **derivation sweep** (#1561 `8bed66b6`) that re-runs the real engines with the canonical per-loan cost substituted and diffs every numeric leaf — declaration complete by construction. Ledger backfilled (#1563 `915704f0`, prod deploy READY 21:44:41). **The migration is now blocked on G3 alone — the THIRD relay capture**, whose `paths[]` become `.audit/expected-moves-t2.json`; the in-repo brief is amended for it (build precondition `915704f0`+; the five previously-missed paths must appear from the sweep rather than a list; a SIXTH is the sweep working, not a regression). **D49 OPEN, awaiting Reza** — every T2 migration target is Layer-1 anchored, so each file needs a manual two-artifact Layer-0 patch unless `check-binding-coverage` resolves its symbol anchor against **source** instead of the frozen Layer 0 (recommended: the anchored-drift property is held by a different gate, so narrowing this one loses nothing). MON-141 (income-page vs Home rental basis — label both, or converge) and MON-142 wiring also await Reza. Detail: MON-131_TRANCHE_LEDGER §3/§4 D49/§6.**) Prior: 2026-07-31d (**MON-131: T1 DONE, T2 IN BUILD.** T2 compare relay built + first capture measured loan cost **8,816.65 -> 12,779.29** (+$47,551.71/yr). BLOCKED before the migration on: a RE-CAPTURE (the relay was missing cashflow.annualCashflow/.annualSurplus — a $47,551.71 undeclared move G7 stops the tranche on; repaired #1559) and **MON-143** (resolveLoanMonthlyCost:199 does NOT net the offset — the only one of four derivations that breaches D21; Guildford 1,964.67 vs 384.45, 5.1x; latent today but the migration would propagate it to all 11 feeds). Also merged today: MON-142 effective-loan-rate engine (#1556, dormant) + the T2 relay handout (#1558). Detail: MON-131_TRANCHE_LEDGER §3/§6 + 01_ACTIVE_WORKSTREAMS 0·REF.**) Prior: 2026-07-31c (**MON-131 TRANCHE 1 CLOSED — VR-045 PASS.** 22/22 declared paths land; Home contradiction gone (moneyFlow 304,158.61 === master 304,158.61); regression cluster byte-identical. MON-128/137/138/140 -> VERIFIED. Tranche closes WHOLE — VR-045 §2b found the MON-138 Ring-3 surface the handout pre-declared absent. Two artefact defects fixed: expectedMoves annualCashflow 180,572.52 -> 180,572.50 (the DECLARATION was wrong, not the engine) and the handout's four income-derived "must return" expectations (would have produced four false failures). MON-141 raised (income-page vs Home rental basis — Reza's call). **NEW: ledger §6 change record + §7 `npm run mon131:check` gate** per Reza's "document all changes for MON-131 and keep it updated". Detail: MON-131_TRANCHE_LEDGER §3/§6/§7.**) Prior: 2026-07-31b (**MON-128 Tranche 1: VR-044 FAILED the T1-B flip (22 declared paths missed; two Income figures 3.1x apart live on Home) -> MON-140 root-caused + fixed (#1548, the banked engine was fed a select-narrowed row missing isRecurring; ONE engine, TWO feeds - the MON-028 class) -> **VR-045 Ring-3 handout shipped (#1550)** as the instrument for the re-run. MON-128/137/138/140 all stay FIXING until VR-045 records; MON-138 is the tranche's fourth member and may have NO reachable Ring-3 surface on Reza's account (handout §2b decides). Ledger corrected: VR-044's rendered half is NOT void. Detail: MON-131_TRANCHE_LEDGER §3 Tranche 1.**) Prior: 2026-07-31 (**MON-128 Tranche 1: T1-A DONE (VR-043 Ring-3 ACCEPTED, 0 leaves moved) · T1-B flip BUILT — PR #1545 draft (changesNumbers: YES; every income surface on the ONE banked producer; legacy producers deleted; withheld credit 11,129→43,004; intake for actualNetPay + study-loan tri-state; census landed below pre-T1). Detail: 01_ACTIVE_WORKSTREAMS 0·REF + MON-131_TRANCHE_LEDGER §3.**) Prior: 2026-07-30d (**MON-128 Tranche 1 IN BUILD — T1-A scaffold PR shipped (moves NOTHING on screen; golden baseline must hash IDENTICAL post-merge).** Two-PR execution recorded in the tranche ledger §3: T1-A = per-FY PAYG Schedule 1 config home (D35/X1/P1 — FY26-27 coefficients ATO-verified 2026-07-30, legacy default byte-identical) + greenfield HELP/STSL engine (D42 C2 cliff = 10% of the WHOLE, boundary-tested both directions) + repayment-income assembler (C3 add-backs, DEFAULT_ZERO honesty) + the banked L1/L2 stack `lib/income/banked/*` (D17/D20; salary FACT hierarchy · rental via the ONE computePropertyCashflow with the three rental quantities NAMED per D6 · received-cash · pure-sum aggregator; Float+Decimal; invariants as permanent tests + calc-audit inventory engines) + FACT-field migration (Income.actualNetPay/helpLoanDeclared, Property availability X7) + the compare relay `/api/admin/matrix/golden-baseline/t1-income` (computes every PENDING_RELAY after-value through the SAME assembly the T1-B flip wires — the MON-035 parity lesson). MON-128/137 → DIAGNOSED with full Stage-1 censuses (SEVEN producers found — wider than the brief's four); **MON-138 raised** (Schedule-1 fractional band-gap → $0 withholding; legacy preserved in T1-A, fixed + declared at T1-B). Next: merge T1-A → Matrix runs the compare on prod → expectedMoves completed per-path → T1-B flip. Prior: 2026-07-30c (**T1 START GATE cleared (start-gate brief) — Tranche 1 is READY.** §1.1 ruled: goldenBaseline serializer byte-identical between 4e6cdd5c and d3d7e147 → the 1,767→10,254 leaf jump is a PARTIAL FIRST CAPTURE (silent __captureError stubs at 0 numeric leaves; drift D8) not new data reach; `?format=hash` committable baseline summary + captureErrors tripwire shipped on the relay route (canonical hash defined once in lib); `.audit/expected-moves-t1.json` committed (14 paths anchored to VR-042; PAYG-11,129 removed from the regression cluster — sole declared removal); build brief amended with the real MON-128 mechanism + the identity invariants; MON-137 raised (cashflowOrchestrator's second withholding producer, three PAYG values, identity broken \$1,196.70/yr); MON-135 → VERIFIED (VR-042 Ring-3 PASS, 48 figures zero movement). Next: the T1 build brief runs. Prior: 2026-07-30b (**MON-131 handover PR — Code owns the repo record; the Matrix verifies only, no longer pushes.** VR-040.md recovered from the unmerged matrix/vr-038 ref; tranche-ledger corrections: baseline-captured cell → BLOCKED (no committed artefact — drift D5; recapture+commit at current main REQUIRED before any code-touching merge), contract count 49→48 (drift D6), §4b explains the deliberate MON-112…124 register gap; STATE.md re-cursored at c07669e5 + MON-131 doc-set pointers added (build spec · ledger · decisions · D42); 0·REF spec-of-record now leads with MON-131_BUILD_SPECIFICATION.md + MON-131_TRANCHE_LEDGER.md. Still blocked on hand-off: .audit/rendered-baseline-8700b1d7.json (on NO ref) + the golden-baseline capture (needs relay/DATABASE_URL — Matrix captures, Code commits). Prior: 2026-07-30 (**MON-135 T3-precondition built (PR-1 of the tranche sequence, moves NOTHING on screen)** — the AI categoriser's unconditional `isRecurring:false` stamp is now the honest tri-state (null = no determination; false only ever a REAL one-off finding); evidence-based recurrence from the previously-unused learned recurring patterns; two `aiIsRecurring` columns nullable (constraint-loosening migration `20260730000000`); Wall-B2 Float+Decimal tri-state fixtures + never-emits-false guards on the real prediction paths; declared-row exposure verified structurally EMPTY (classifyIntake wall + evidence-based link dialog); **NEW `docs/implementation/MON-131_TRANCHE_LEDGER.md`** (per-tranche gate-evidence record, §3.0 filed; relay CLEAN verdict = the Matrix's post-merge step). Next: MON-128 Tranche 1 income after this merges + verifies. Prior: 2026-07-29c (**MON-131 PHASE A COMPLETE — the Number Inventory shipped (READ-ONLY, one draft PR)**: `docs/architecture/NUMBER_INVENTORY.md` (68 named quantities, one five-word verdict each — 5 WRONG · 13 UNNAMED · 26 MULTIPLE across MON-131; **28 decisions for Reza in §5.1, 12 preconditions, 14 wrong-inputs**) + 49 Quantity Contracts under `docs/architecture/contracts/`, every one re-verified by an independent adversarial agent (~200 anchors, all worked examples recomputed; 2 verdicts changed — property-cashflow CLEAN→MULTIPLE, loan-monthly-interest → MULTIPLE+WRONG with a LIVE 100× interest bug on Home at `EntityCashflowSummary:693`). Phase A0 census extended per D16 (40 quantities · 1,307 sites · 424-function sweep); MON-135 (AI-categoriser one-off poisoning — LIVE precondition) + MON-136 (everything-else wave) raised. **Phase B writes nothing until Reza's §5.1 calls.** Same day, earlier — MON-134 stack MERGED + deploy-verified (#1529/#1530/#1532; A3 CLEAN acceptance still with the Matrix). Prior same day: 2026-07-29b (**MON-131 Tranche −1b Matrix Relay built** — capture/diff extracted to `lib/matrix/goldenBaseline.ts` (one implementation, CLI now a thin wrapper) + three admin routes (`/api/admin/matrix/golden-baseline[,/diff]`, `/api/admin/matrix/census`) behind `verifyAdminGCPAuth`+`audit:read`, HR-3 carried; the Matrix captures/diffs over an authenticated browser session, removing Reza's terminal from every remaining tranche's capture→migrate→diff→verify loop; parity ratchet `tests/matrix/goldenBaselineRelay.test.ts`. Prior same day: **MON-131 "Reference Numbers" Tranches −1 + 0 built — THE FIRST-PRIORITY PROGRAMME opened** (workstream 0·REF in `01_ACTIVE_WORKSTREAMS.md`): golden-baseline capture+diff script (`scripts/matrix/golden-baseline.mjs`, capture pending Matrix side — needs DATABASE_URL), producer-multiplicity census ratchet (`census:producers[:check]`, 13 quantities / 564 sites seeded, ratchet-down-only, in `vercel-build` + vitest), source-lock extended to `lib/` (the "engines out of scope" blind spot closed; debt reseeded app 68 + lib 155 = 223), `REFERENCE_NUMBERS_DESIGN.md` (living decisions register, reconstructed in-repo — original + VR-040 run file were never committed, §21.2.2) + `REFERENCE_NUMBERS.md` (the 23-quantity FACT/DERIVED register) published. Moves NO numbers. Registry FROZEN mid-session per Matrix directive — vr038-039 scripts dry-validated then unwound; the Matrix's reconciliation PR owns MON-104…110 advancement + MON-115…124 + MON-127…132 registration. Prior: 2026-07-20 (**Part A merged — #1461**: household income attribution ("Who earns this?" in both income forms) + per-person tax positions (perMember on the ONE producer; roll-up unchanged); MON-088 raised (family Medicare legs unwired — SINGLE defaults at taxPositionCalculator:295; MLS family threshold $202,000 not applied — awaiting Reza queueing); per-person DISPLAY = next Stitch-gated PR. Prior: 2026-07-19c (**Mechanism A Part 2 built — PR #1459**: the Reza-gated duplicate-merge preview-and-confirm (`lib/intake/duplicateMerge.ts` + `GET/POST /api/intake/duplicates` + `/admin/intake-duplicates`). Grouping reuses THE Part-1 signature policy (preview ⊆ guardrail — QBE A-vs-B / Lot-1-vs-Lot-2 / rentals never group); executeMerge repoints all 7 FK references then deletes, transactionally; POST requires `confirm:"MERGE"` + server-side re-derivation (stale → 409); NO merge-all — every merge is Reza's per-group click (§12.11). MON-074 → FIXING (#1459). **#1458 (Part 1 guardrail) MERGED same day** — prod verified §17.2; Matrix Ring-3 pending. Prior: 2026-07-19b (**Calc-SSOT Wall Mechanism A keystone built — PR #1458** — the last unbuilt wall keystone: `classifyIntake` `source-signature` policy (signature = kind+type+normalised name+owner, user-wide candidates, scope-compatibility rule: same scope or one side scopeless — distinct per-property streams protected structurally) routed through ALL six Income/Expense mint sites (link income/expense, doc-import, manual POST → 409 on exact dup, onboarding idempotent); new reuse takes the update template (amount ← txn, prior → budgetedAmount, lastReconciled); ratchet `ring2.mechanismA.intakeDedup` on the REAL link route; Neomatrix `engine.intake.classifyIntake` + `law.intake.oneRowPerSource`; MON-084/085/076 → FIXING, MON-074 → DIAGNOSED. Part 2 (Reza-gated preview-and-confirm merge of the existing Ingeus ×3 / battery ×3 — §12.11, per-group approval) ships as a separate PR after #1458 merges. Also this session: **MON-031/064 → VERIFIED at VR-018** (the #1455 re-fix PASSED cross-surface — $301,808 / 11.6 everywhere). Prior: 2026-07-19 (**MON-031/064 RE-FIX built (VR-017 FAIL → PR #1455)** — Ring-3 VR-017 proved #1452's netting silently INERT on live data: the subtracted `netWorth.liabilities.creditCards` is LOANS-only, and the live Qantas card is a CREDIT_CARD-typed ACCOUNT at −$2,496, so Safety Net "Liquid savings" + emergency months stayed gross ($304,304 / 11.7) while Balances' net figure was an accident of the buckets' `min(gross, accounts)`. Re-fix: ONE producer `lib/calculations/liquidCash.ts` `computeLiquidCash` nets BOTH card representations (loans + negative-balance card accounts); master + CFO emergency-buffer routed through it; NEW golden `ring2.liquidCashParity.accountCard.test.ts` locks the LIVE topology (signature assertion) + the I9 months identity; FIX_PROTOCOL §7 ledger tightened (multi-representation entities: fixtures must mirror + assert the live representation). MON-031/064 stay FIXING; draft PR #1455 pending Reza's merge → Matrix Ring-3 re-run ($301,808 / 11.6 everywhere). Prior: 2026-07-18c (**MON-031/064 liquid-cash canonicalised** — quickMetrics.liquidCash is now the ONE deployable figure (spendable cash NET of credit cards) at the master producer; buckets reconstruct gross internally so liquidToday ≡ quickMetrics by construction; Safety Net/emergency-fund/insights/CFO converge to $301,808; ratchet `ring2.liquidCashParity.test.ts`; draft PR pending Reza's definition-confirm + merge → Matrix Ring-3. Prior: 2026-07-18b (**MON-020/060 tax-producer collapse built** — ONE canonical tax assembler (`getUserTaxPosition` + `engineInputs`) now feeds the Tax page route (verbatim-equal, zero number change), the master snapshot tax summary (adapter — the activity Sankey's divergent Tax figure converges), /cashflow + CFO (already on it); the dead `/api/tax` fourth assembler deleted; golden `ring2.taxParity` locks route ≡ bundle ≡ master (pin 124,800/30,724); recorded finding for Reza: folding rental-dedup + isTaxable into the canonical assembler is the remaining semantic unification. Draft PR pending Reza's merge → Matrix cross-surface Ring-3. Prior: 2026-07-18 (**MON-087 Sev-1 form crash fixed** — property-context Add Expense crashed on Radix's empty-value SelectItem invariant (VR-014); the whole 15-site class removed (plain div rows / ALL·NONE sentinels), MON-083 recurring/one-off control added to the canonical ExpenseDialog, static-scan ratchet `tests/ui/selectItemEmptyValue.test.ts`; draft PR pending Reza's merge → Matrix re-runs VR-014 (MON-081 numbers + MON-083/087 form checks). Prior: 2026-07-17c (**VR-013 loan-cost actuals-first fix built** — the Matrix's cross-surface Ring-3 on #1440 PASSED B2/B3 (MON-079/080/082/086 → VERIFIED) but FAILED B1 (MON-081: expenses/overview showed the $1,271 floor vs $1,191 actuals — surfaces fed the canonical resolver NO transactions); fixed via `lib/services/loanCosts.ts` (ONE trailing-12-month linked-repayment feed → canonical resolver, fed into /api/loans + 5 routes; Overview card actuals-first + "contractual estimate" relabel); source-lock debt 84→80; draft PR pending Reza's merge → Matrix Ring-3 #2. See 0·WALL. Prior: 2026-07-17b (**Calc-SSOT Wall Part 1 built** — A1 source-lock lint (ratchet-down, 84-match debt seeded, in `vercel-build`) · A2 MATRIX_FIX_DISCIPLINE wired into CLAUDE.md §0.4/§12.2.1/§20.6 + FIX_PROTOCOL Stage-1 step-0 + the new `.github/pull_request_template.md` · A3 MON-081…086 raised + MON-079/080 re-scoped ("VR-011 verified TAX only") · B1 `resolveLoanMonthlyCost()` (interest floor, 7 surfaces migrated) · B2 `monthlyRunRate()` one-off gate + Mechanism-C form · B3 managed-rental fee double-count fixed at `computePropertyCashflow` (gross-up on actuals; cf ≡ tax by construction). Draft PR pending Reza's merge → Matrix cross-surface Ring-3. See 0·WALL in `01_ACTIVE_WORKSTREAMS.md`. Prior: 2026-07-17 (**MON-080 built** — Phase 59 managed-rental activation fixed (D0 deposit-size cadence inference · D1 retroactive reconcile on the MANAGED transition · D2 gross-integrity gate), draft PR pending Reza's merge; VR-010 verdicts applied to the registry; Stitch artefacts light+dark ≥9/10; see 0·P59 in `01_ACTIVE_WORKSTREAMS.md`. Prior: 2026-07-16b (**Phase 59 — Managed Rental Income queued + build started**: spec PR #1433, issue MON-079 raised (`changesNumbers: true`), workstream added to `02_UP_NEXT.md` with the §9 sequence; build on `claude/phase-59-managed-rental-yhm8ug`. Prior same day: **Intake-Integrity Wall built** — keystone #1429 + C1/D2 #1430 + D1 #1431 merged, R3 fixtures #1432 open; Part 4 MON-076 gated on Reza; see `01_ACTIVE_WORKSTREAMS.md`. Prior: 2026-07-15b (**MON-045 stage 2 shipped — PR #1425 draft**: deductible loan interest auto-derived in the ONE tax engine, four rogue neg-gearing producers deleted, MON-045 → FIXING; MON-053 Ring-3 PASS → VERIFIED. Earlier same day: **VR-007 consolidated into the issue registry (Matrix, renumbered +2 → MON-053…MON-074 to avoid the #1418 MON-051/052 collision)** — Ring-3 real-data run VR-007 recorded (`docs/verification/runs/VR-007.md` + raw capture); registry now **74 MON issues** (MON-053…MON-074 raised, `tracker: neoaudit-run:VR-007`); MON-039/042/018 → VERIFIED, MON-049 → DIAGNOSED; six `#PENDING` fixPR placeholders resolved. **MON-053 (critical)** — one-off income annualised ×12, ~$120.6K phantom income in the tax base — is **MON-037's income-side twin** and **must land before MON-045 stage 2**. Baseline replacement is **PROPOSED, awaiting Reza** (§3.4 "Reza confirms"). _Verification Machine workstream detail NOT updated in `01_ACTIVE_WORKSTREAMS.md` — that spoke is **390 KB**, far above the ~63 KB ceiling this hub records as unreliable for a verbatim connector rewrite; it needs a git-capable session (see § Per-spoke size budget)._)
> _Prior: 2026-07-15 (**Cursor truth-restore + The Matrix HQ (Cowork)** — STATE.md re-pinned to `38abeee`/#1416; `docs/operational/LAUNCH_PROGRAM_2026-07.md` added; Q-GTM-3 reconciled; Reza decisions 2026-07-15: rectify GO + 31-July scope = friendlies beta + broker outbound.) — that truth-restore stands; VR-007 only ADDS to it._
---

## How this plan is structured (read me first)

The plan is a **hub (this file) + spokes** (under [`docs/implementation/`](implementation/)). The hub holds only navigation, the status legend, and the update rules. The detail lives in the spokes so each file stays small enough to (a) write via the GitHub connector in one call and (b) scan without scrolling forever. **`docs/IMPLEMENTATION_PLAN.md` remains the canonical entry point** — links elsewhere in the repo that point at "the plan" land here and route onward.

| Spoke | What it holds | Open it |
|---|---|---|
| 🟡 **Active Workstreams** | Work in flight right now — phase checklists, owners, risk, blockers. Sorted by priority. | [`implementation/01_ACTIVE_WORKSTREAMS.md`](implementation/01_ACTIVE_WORKSTREAMS.md) |
| 📋 **Up Next & Demo-Complete** | Agreed + queued, not started; plus the frozen Lighthouse demo-complete band. | [`implementation/02_UP_NEXT.md`](implementation/02_UP_NEXT.md) |
| ❓🚧🗑️↩️ **Questions, Blocked & Backlog** | Open Questions (strategic, undecided), Blocked items, Dead Code / Tech-Debt, Reversed Decisions. | [`implementation/03_OPEN_QUESTIONS_AND_BACKLOG.md`](implementation/03_OPEN_QUESTIONS_AND_BACKLOG.md) |
| ✅ **Recently Completed** | Rolling 30-day completion log (older items roll into the changelog). | [`implementation/04_RECENTLY_COMPLETED.md`](implementation/04_RECENTLY_COMPLETED.md) |
| 📱 **Mobile Workstream** | The `0·MOB` mobile companion app live tracker (design + main-repo backend). | [`implementation/05_MOBILE_WORKSTREAM.md`](implementation/05_MOBILE_WORKSTREAM.md) |
| 📚 **Older history** | Pre-30-day session log + the relocated preamble narrative. | [`docs/changelog/IMPLEMENTATION_CHANGELOG.md`](changelog/IMPLEMENTATION_CHANGELOG.md) |

**Freshness gate:** a spoke is the SSOT for its slice. When a claim in another doc (STATE.md, SYSTEM_MAP.md, a Phase doc) disagrees with the relevant spoke, the spoke wins — re-pull and fix the pointer.

---

## Status legend

| Symbol | Meaning |
|---|---|
| 🟢 | Active and healthy |
| 🟡 | Active, in progress, on track |
| 🔴 | Active, stuck or risk flagged — see notes |
| 🚧 | Blocked, awaiting decision or external dependency |
| 📋 | Queued — agreed, not started |
| ❓ | Idea / open question — not committed |
| 🗑️ | Dead code / tech-debt — pending cleanup |
| ↩️ | Reversed decision — preserved here so we don't re-do it |
| ✅ | Recently completed (rolling 30 days) |

---

## How to update this document (rules)

> **Mandatory.** See CLAUDE.md §15 (Implementation Plan Protocol) for full rule text. Edit the relevant **spoke**, not this hub (except the **Last updated** date above, which every plan-touching PR bumps).

1. **Every PR that starts a workstream** adds it to [`01_ACTIVE_WORKSTREAMS.md`](implementation/01_ACTIVE_WORKSTREAMS.md) with all the fields filled in (§15.2).
2. **Every PR that advances a workstream** ticks off the relevant `[ ]` checkbox(es) in that workstream's phase list and updates `Last touched`.
3. **Every PR that completes a workstream** moves it from `01_ACTIVE_WORKSTREAMS.md` to [`04_RECENTLY_COMPLETED.md`](implementation/04_RECENTLY_COMPLETED.md) with the date and PR number.
4. **Every PR that surfaces a tech-debt item** (e.g. removes a duplicate, identifies dead code) adds it to the Dead Code / Tech-Debt backlog in [`03_OPEN_QUESTIONS_AND_BACKLOG.md`](implementation/03_OPEN_QUESTIONS_AND_BACKLOG.md).
5. **Every PR that reverts a previous attempt** adds an entry to Reversed Decisions in [`03_OPEN_QUESTIONS_AND_BACKLOG.md`](implementation/03_OPEN_QUESTIONS_AND_BACKLOG.md) so the same dead-end isn't re-attempted.
6. **Every PR that introduces an open question** the user hasn't decided adds it to Open Questions in [`03_OPEN_QUESTIONS_AND_BACKLOG.md`](implementation/03_OPEN_QUESTIONS_AND_BACKLOG.md).
7. **Bump the `Last updated` date** in this hub on every plan-touching PR. CI fails if it falls behind the newest `04_RECENTLY_COMPLETED.md` entry (finding F-1, `scripts/check-plan-freshness.sh`).
8. **Reviewers reject PRs** that materially change a workstream without updating the plan. Same hygiene rule as the changelog.

### Per-spoke size budget (finding F-8)

- **Target: each spoke ≤ ~600 lines / ≤ ~150 KB** so it stays connector-writable in one call and scannable. _Practical caveat surfaced 2026-06-15 (Phase 3): even a 63 KB spoke is unreliable to rewrite verbatim from a Cowork/connector session — the safe ceiling for an in-place connector rewrite is well under that. Prefer git-capable edits, or split further._
- When a spoke exceeds budget, **retire settled content**: completed workstreams → `04_RECENTLY_COMPLETED.md`; recently-completed items older than 30 days → [`IMPLEMENTATION_CHANGELOG.md`](changelog/IMPLEMENTATION_CHANGELOG.md).
- **Known over-budget (carried, not yet pruned):** `01_ACTIVE_WORKSTREAMS.md` (~289 KB) and `04_RECENTLY_COMPLETED.md` (~296 KB). This PR did the structural split only; the **next hygiene pass** retires the completed §0x workstreams (e.g. 0·WI, 0·DG, 0·StD, 0·WX) and rolls >30-day completions into the changelog to bring both under budget. Tracked as a Dead Code / Tech-Debt item in `03_OPEN_QUESTIONS_AND_BACKLOG.md`.
