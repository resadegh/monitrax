# MONITRAX V1 MASTER PLAN — the ONE tracking document

**Status:** 🟢 LIVE TRACKER · **Raised:** 2026-08-19 · **By:** Matrix HQ (Cowork, Fable 5) on Reza's merge-the-docs directive · **At HEAD:** `380a526a`
**This document is THE single entry point and tracker for the Monitrax v1 programme — the shared channel between Reza, Matrix HQ (Cowork), Code sessions, and the Chrome relay.** One doc read, updated and tracked by all four; registries keep the detail (§8 doc map).

**Supersession:** this plan ABSORBS the live-tracking role of `PROD_SIMPLIFICATION_PLAN.md` (now the decision-record + design archive for the module gate — its §0 rulings and §2–§4 designs remain binding and are NOT restated in full here) and the roadmap role of `PRODUCT_SCOPE_V1_RECOMMENDATION.md` (Q-SCOPE-1, archived DECIDED). It does NOT absorb: `docs/issues/ISSUES.md` (the issue REGISTRY of record — this plan carries only the launch-blocking subset, §5), the MON-131 tranche ledger (owns tranche mechanics), or `STATE.md` (stays the cross-programme cursor; carries a pointer here). SSOT: one fact, one home, pointers everywhere else.

---

## 0. BOOT PROTOCOL — every session, every surface (no drift, no guesswork)

**Read order, before ANY work:**
1. **`CLAUDE.md`** — the repo laws (SSOT · never fix a number in passing · PRs only, never merge to main · hidden ≠ deleted · §13.6 data rules · §20.6/§16.5 PR blocks). Non-negotiable on every surface.
2. **`STATE.md`** — the cross-programme "you are here" cursor.
3. **THIS FILE** — cursor block → current milestone → the tasks carrying YOUR role's icon (§4 owners legend).
4. Your session's brief (Code: the `BRIEF_*.md` named in the current milestone; Matrix: the project boot ritual; Chrome relay runs only under Matrix direction with the relay runbook).

**Standing rules for all four actors:**
- **Read live, never recall.** No claim about Monitrax state from memory or summaries — read it from HEAD or flag it unverified (cite-or-flag).
- **Do only what the current milestone briefs.** Found work → registry issue or a proposal in this doc, never a side quest.
- **D-18 build law: NO new capability without Reza's explicit GO.** Perfect and enhance what is already built first; a session that thinks something new is needed writes a proposal in the gate review and STOPS — it never builds on its own initiative.
- **D-20 depth law: finish what is visible before touching what is hidden.** A kept-surface defect or gap outranks every hidden-module improvement.
- **The write is part of the deliverable, not a courtesy after it (amended 2026-08-22 after a Matrix drift Reza caught):** 🟦 Code — boxes, cursor and §9 line in the SAME PR as the build (unchanged; structural). 🟩 **Matrix — a verdict, ruling relay, flip record, or acceptance is INCOMPLETE until the plan PR carrying it is open in the same working turn.** A Ring-3 verdict posted to a PR comment without its plan update is an unfinished verdict — the comment is evidence, the plan is the state. "The session will end later" is never a reason to defer: Matrix sessions are continuous, so the trigger is the EVENT, not the session boundary. 🧑 Reza's actions (merges, switches, rulings) are recorded by whichever session observes them, in that turn.
- **Every milestone gate closes with a GATE REVIEW** (§4 ritual): progress vs plan, fresh market/regulatory check, and upgrade proposals — the plan is a living document that improves as Monitrax does.
- **If this doc and any other doc disagree:** for programme state, THIS doc wins; for laws, CLAUDE.md wins; for issue detail, the registry wins; for tranche mechanics, the ledger wins. Fix the disagreement in the same session, don't work around it.

| Actor | Reads | Writes here |
|---|---|---|
| 🧑 Reza | cursor + current milestone + gates awaiting him | rulings (via chat → Matrix records), merge/switch confirmations |
| 🟩 Matrix (Cowork) | everything; owns this doc's integrity | cursor, briefs, verdicts, research, gate reviews, session log |
| 🟦 Code | §0 → cursor → its brief → its milestone tasks | box ticks, cursor, session log (same PR as the build) |
| 🌐 Chrome relay | the specific runbook step Matrix hands it | nothing directly — Matrix records its results |

---

## CURSOR — update every session that advances the plan

| Field | Value |
|---|---|
| **Current milestone** | **M3 running — the pack is FIXED and VERIFIED; the scoreboard is LIVE and ACCEPTED.** Remaining in M3: ATO default mappings (M3.1) · nudges (M3.2) · CSV export (M3.3) · scoreboard punch list · depth backlog (M3.6) · accountant pilot (M3.5). M2 residue: M2.2 T2 Ring-3 · M2.5 close · M2.7 gate review |
| **Last session** | 2026-08-25 (later) · Matrix HQ · **P-9 LIVE SWEEP RUN** (handout `RING3_M2_6_LIVE_SWEEP.md` committed BEFORE the run) — 6 of 7 outstanding M2.6 live-checks executed on Reza's account, verdict **PARTIAL**, 10 NEW findings F1–F10 (envelope on the PR). **Headline: the Smart Inbox intake pipeline is display-only** — select/approve dead on BOTH viewports, edits don't persist (F1–F3); upload 413s at 6.2MB behind a 10MB promise with a raw JSON-parse error surfaced (F7/F8); vault folder-tree count is exactly 2× the true count (F4); OCR is live and honest on amount/date but picked a mis-transcribed doc-type word as vendor (F5); scoreboard renders $0-as-truth during REAL transient 503s (F6). **#1612's K6 OVERTURNED:** the activity money-flow widget renders on both viewports from a non-money-flow producer. S3 pack export PASS (2–5s warm). MON-172 + catalogue #40 CONFIRMED live. S7 deferred by Reza until the intake fix. **P-10 brief CUT same turn** (`BRIEF_M2_KEPT_DEPTH.md`): PR-1 intake pipeline (Reza's active blocker — his real Bunnings $203.78 expense is stranded pending) · PR-2 MON-151 + offset thread · PR-3 display + MON-171…178 + MON-087. Prior: 🧑 **Reza: GO on P-6…P-11 (all as recommended)** → **P-6 EXECUTED SAME DAY**: the re-scoped kept-path loan-cost Ring-3 (#1612, handout committed first) — 8/8 checks PASS, verdict PARTIAL. **B1 CLEARED for cashflow** (actuals bind) but **confirmed LATENT** (`offsetBalance` FIELD_ABSENT in the property payload). **MON-151 CONFIRMED LIVE AND QUANTIFIED: the property Tax position card overstates interest by $18,962.73/yr on Guildford** by ignoring a $303,889.96 offset; no-offset control holds exactly. Prior: **M2.7 GATE REVIEW RUN → ⛔ M2 DOES NOT CLOSE** (`docs/strategy/M2_7_GATE_REVIEW.md`): B1 NEW offset-threading defect on the kept property pages · B2 M2.2 re-scope · B3 7 live-checks unrun · B4 21 kept crit/high open. Six proposals P-6…P-11 await rulings. Market re-check: TaxTank Property Tank still $15/mo (+$36/property/yr beyond 5 — $216/yr at Reza's 6) · ATO reform page now live but silent on per-property-vs-pooled quarantining and on deemed disposal. Prior: **🧑 Reza ruled 4 open questions + issued D-22 (refinement-only, depth-first) after catching a drift risk in the Stitch step** — scope fence added to M3.4; **Stitch tooling BLOCKED** (2 generations, both 60s-timeout, no retrievable screen; in-app project enumerates zero screens) — no design invented as a workaround. Matrix → M2.7 gate review. Prior: **Ring-3 on `RING3_M3_PUNCH_FIXES.md`: all 17 checks PASS** (envelope PARTIAL only for the authenticated `/api/money-flow` 503, not observable account-first — Ring-1-verified; unauth GET = 401, no data). Labelling live: labelled 23/$13,864.90 · noAtoMapping 12 explained to the cent (MODIFICATIONS ×8 + OTHER ×1 + 3 income rows) · LVR 41.3→40.8 converged both surfaces · EOFY/intake/strip/reports verified (#1606 comments) → **MON-180…184/186 VERIFIED**. |
| **Next action** | 🟦 **Code: `BRIEF_M2_KEPT_DEPTH.md` PR-1 FIRST** (intake pipeline — Reza is blocked on it; register F1–F10 with MON ids in the same PR) → PR-2 (MON-151 + offset thread, predictions before code) → PR-3 (display + sweep highs) → 🟩 Matrix: Ring-3 each PR on its predictions; plus the P-8 §7 correction (standing) → 🧑 Reza: **S7 fresh-account onboarding after PR-1 lands** (he types credentials, Matrix observes) · MON-185 runbook · approve the stranded Bunnings item once approve works → 🟦 Code (independent): registry flips MON-180…184/186 → VERIFIED citing #1606 | Prior rulings stand: `OTHER` unmapped, money-flow key MODULE_ENTITIES |
| **Blockers** | **Stitch tooling (M3.4 backfill only):** `generate_screen_from_text` times out at the 60s MCP boundary and no screen is retrievable afterwards; `list_screens` returns EMPTY for the in-app project `5991501424852019479`. Nothing else is blocked — the gate review, the registry flips and the MON-185 cleanup all proceed. |
| **Baseline of record** | `.audit/golden-baseline-12954ff.json` (VR-048, 1,756 leaves, treeHash `0d6753ef…`) |
| **PROD state** | **v1 + first two returns live:** 11 MODULE_* keys hidden · **MODULE_HOUSEKEEPING ON + MODULE_HOME ON (Reza, 2026-08-22)** — the scoreboard is the live `/dashboard`; the redirect is retired. Zero overrides. MON-163 fix deployed. Pack backfill applied (46 rows; idempotent re-run = 0). |
| **Preview state** | full app, all 13 flags ON, full PROD data copy (2026-08-11), `connection_limit=1` applied — `monitrax-git-preview-dev-full-app-…vercel.app` |

---

## 1. Identity — what the first Monitrax IS (ruled 2026-08-19)

> **The per-property record system for Australian property investors: AI reads your statements, every dollar lands in the right ATO category with its evidence attached, and your accountant gets a clean pack in one click.**

Tracking is the mechanism. **The accountant pack is the product.** AI intake is the ease. The document vault is the defensibility. This replaces both the old "wealth OS" story and the generic "tracking tool" framing — the market clears at A$10–20/mo ONLY for tax-outcome products; generic tracking fights free apps (research 2026-08-19, §7).

**The automation design law (Reza, 2026-08-19):** users hate manual work and expect AI to do most of it. Every v1 flow is designed and measured as **"you confirm, Monitrax does"** — the user's job is confirming, never keying, matching, filing or classifying. Taps-to-done is a tracked metric (M4.7); any remaining manual step is a named automation-backlog candidate, not an accepted cost. The one thing never automated away is the CONFIRM itself (D-11 — the confirm event is the ATO defensibility).

**The build law (D-18, Reza 2026-08-19):** perfect what exists before building anything new. The kept surface must work flawlessly — every existing function enhanced to its outcome — before any net-new capability starts, and nothing net-new starts without Reza's explicit GO.

**The depth law (D-20, Reza 2026-08-19):** depth and quality over surface. A deeper, more correct, more complete version of something that already ships beats anything new or anything hidden. Shallow breadth is the failure mode this programme exists to prevent.

**Why now (all verified Aug 2026, sources §7):** ATO's operative stat remains 9-in-10 rental returns wrong, with data-matching now covering PM software, investment-loan (RIPL) and landlord-insurance data · loan interest alone = 42% of the $1.2B rental tax gap · a clean per-property summary demonstrably cuts an accountant bill from ~$900 to ~$275 · the NG/CGT reform is LAW (Royal Assent 26 Jun 2026, effective 1 Jul 2027: deemed disposal, quarantined residential losses, cost-base indexation) and no incumbent has shipped for it · the invariant core of every successful analogue globally (Stessa, Landlord Studio, Hammock) is exactly Monitrax's kept surface.

**The two whitespace claims nobody owns (post-launch differentiators, not v1):** the variance loop (purchase assumptions vs actuals) and clean multi-entity ownership.

---

## 2. Decision record

### Inherited — module gate programme (Reza 2026-08-04/09/11, full text `PROD_SIMPLIFICATION_PLAN.md` §0; FINAL, do not re-open)
D-1 HIDE household cashflow · D-2 HIDE tax module (pack ships) · D-3 HIDE CFO · D-4 HIDE Home+Housekeeping, Reports→one pack, land on /dashboard/properties · D-5 HIDE Investments/Super · D-6 entity SAFE default · D-7 full-instance PROD→dev copy (attested non-real; prohibited the day real customer/CDR data exists; Sydney residency) · D-8 WIP ONE hidden module · D-9 admin PROD-only. Plus: automation-first v1 · no work lost, nothing deleted · "100% working" = producers converged + Ring-3 PASS live · held doctrine (exposure-not-defect control; CLEAN self-diff acceptance).

### New — v1 focus (Reza 2026-08-19, this session's research)
| # | Decision | Ruling |
|---|---|---|
| D-10 | v1 identity & positioning | **Per-property record system + accountant pack** (§1 line). "Full income/expense tracking" is scoped to the INVESTMENT surface (properties, their loans, simple assets) — household/everyday-spend tracking stays hidden until Basiq (it is the free-app commodity zone and dishonest without feeds). |
| D-11 | AI in v1 | **Intake only, propose→confirm, never silent write.** AI reads PM statements / bank statements / receipts → proposes categorized, property-linked, deduction-flagged rows → one-tap confirm (the confirm event = ATO defensibility). **NO AI advice/chat surface in v1** (AFSL boundary + trust). Governed by the §1 automation design law: AI does the work, the user confirms. |
| D-12 | Accountant connection v1 | **The pack, not a portal:** per-property per-FY pack (PDF + CSV) in ATO rental-schedule headings + export/share. Read-only accountant access returns later (elements exist in hidden modules). This is the conversion trigger — every paid competitor has a version. |
| D-13 | Notifications v1 | **EOFY completeness nudges only** ("3 expenses missing receipts", "no rates entered for X this FY") — tied to the pack, not a notification platform. |
| D-14 | "Best-outcome tax planning" | **Reframed to completeness + correctness + substantiation** (guided repair-vs-capital classification at entry, per-loan interest, nothing missed in July). Planning/optimisation surfaces return at R2 (property-tax slice) on verified numbers — never through the v1 side door. |
| D-15 | One tracking doc | **This file.** Registries stay SSOT for their detail (§8); every other strategy doc is archive or pointer. Boot protocol §0 binds all four actors. |
| D-16 | Dashboard in v1 | **The dashboard RETURNS, rebuilt as the v1 scoreboard — it does not return as the old wealth-OS Home.** The old Home reads gated feeds (`wealth-graph` → MODULE_ENTITIES, `money-flow` → MODULE_HOME) and tells the pre-simplification story; flicking MODULE_HOME on as-is would render broken/off-story widgets (P1.2 audit, confirmed empirically by the M1.4 inventory). Sequence: M1 inventories exactly what breaks with MODULE_HOME on; M3 lands the scoreboard version (per-property portfolio summary + EOFY-readiness from kept engines only) and THEN the flag flips ON. Amends D-4's "Home returns R4": the v1 scoreboard comes forward to M3; the full wealth-OS Home stays R4. |
| D-17 | Living plan | **Every milestone gate closes with a Matrix GATE REVIEW** (§4 ritual): progress vs plan · fresh market/competitor/regulatory re-check · upgrade/enhancement proposals for Reza's ruling. The plan is upgraded as Monitrax moves — no stage completes silently. |
| D-18 | Build law — perfect what exists FIRST | **No new capability is built without Reza's explicit confirmation** until the existing functions work perfectly. M1–M3 are enhance-only by construction (they perfect what is built: the gate, the pack, the exports, the dashboard from kept engines). **Every M4 task is a separate GO decision Reza takes at the M3 gate review** — M4 is planned here so it is briefed and sequenced, not pre-authorised. A session that believes something new is required writes the proposal into the gate review and stops (see §0 standing rule). Default answer to "build X new?" is NO. |
| D-19 | Dashboard tiles are **stage-aware, module-derived** | **Reza 2026-08-19: the dashboard shows the tiles relevant to the current go-live stage.** Mechanism (ruled after Matrix's design review — NOT a second flag vocabulary): a `DASHBOARD_TILE_REGISTRY` where **every tile declares the MODULE key it depends on** and the stage it belongs to. Visibility = `requires === null (v1 core) OR isModuleEnabled(requires)`, **minus** an optional admin suppression toggle. **The iron rule: a tile toggle can only SUPPRESS, never force-show a tile whose module is off.** So when R2 flips `MODULE_TAX` on, the tax tile appears by itself; nothing can resurrect the broken-widget failure the M1.4 inventory found. Spec in M3.4; built there, not before. |
| D-20 | **Depth before surface** | **Reza 2026-08-19: "work on the depth and quality of Monitrax rather than expanding a shallow surface."** Standing priority law: work that makes an EXISTING kept function deeper, more correct or more complete **outranks** work that adds surface — including any work on hidden modules. **No hidden-module work proceeds while a kept-surface defect or gap is open** (this hardens the P0.2 freeze from "don't touch hidden" to "finish visible first"). A milestone is measured not by how much is visible, but by how little of what is visible is wrong, shallow or dead-ended. Operationalised as M2.0 (scoping law) and M2.6 (depth sweep). |
| D-21 | Number-moving fixes authorised for M2/M3 | **Reza 2026-08-19 (recorded in `BRIEF_M2_CORRECTNESS.md`; row added at the 2026-08-22 catch-up).** The correctness slices MAY change displayed numbers because some are wrong today. Four mandatory conditions: (1) expected movement written BEFORE the fix code; (2) golden self-diff shows ONLY predicted leaves or the phase stops; (3) Ring-3 on live data closes each fix; (4) Reza merges every PR. What stays unauthorised: new features, deletions, scope expansion, hidden-module work. The end state is today's Monitrax, every module unhidden — with corrected numbers and closed dead ends. |
| D-22 | **Refinement-only, depth-first sequencing** | **Reza 2026-08-25, re-stating and hardening D-18+D-20 after catching a drift risk in the Stitch backfill:** *"we are not supposed to build a new app, we are only refining the visible parts of Monitrax and make it 100% depth wise before we move to the next function. Never build new functions or reinvent what already exists."* Three binding clauses: **(1) Refinement only** — Monitrax already exists; every M1–M3 task makes an EXISTING visible function correct/deep/complete. Nothing net-new without Reza's recorded GO (D-18 unchanged). **(2) Never reinvent** — where a function, engine, component or artefact already exists, it is repaired or deepened in place; a parallel implementation is a defect, not a fix (this is why SSOT/one-producer is enforced). **(3) 100% before next** — a function is not "done" at first-fix; it is done when its numbers are Ring-3-verified, its surface has no dead ends or dishonest empty states, and its depth items are closed. Only then does the next function start. **Applied to design work:** a Stitch backfill RECORDS the composition that already ships so the §18.2.1 paper trail exists — it is NOT a redesign brief. Any delta between the Stitch render and the shipped screen is a FINDING for Reza's ruling, never work Code starts on its own. |

**Rulings on the M1.7 gate-review proposals (Reza 2026-08-19: "go with your recommendations"):** **P-1 ADOPTED** (M1.3 folded into the M2 PR) · **P-2 ADOPTED** (MON-161 is an M2 item) · **P-3 ADOPTED** (MON-162 registered, fix deferred) · **P-4 ADOPTED AS RECOMMENDED — the M3.4 scoreboard does NOT reuse `/api/dashboard/charts`' NetWorth series** until M2's census proves the producers behind it; revisit at the M3 gate · **P-5 ADOPTED AS RECOMMENDED — cookie-based server sessions are PARKED** (auth-architecture change, outside the v1 boundary; MON-162 stays deferred with it).

**M2.7 gate-review rulings (Reza 2026-08-25: "1611 is now merged and go"):** **P-6…P-11 ALL ADOPTED AS RECOMMENDED.** P-6 M2.2 re-scoped to the kept loan-cost path — **RUN SAME DAY, see the M2.2 box** · P-7 depth-sweep #19: the legacy pack generator stays DARK as the R2 seed (hidden ≠ deleted) · P-8 §7's reform row to be corrected to separate ATO-confirmed from secondary-source claims · P-9 the 7 outstanding LIVE-CHECK items run as ONE Matrix+Chrome sweep · P-10 the kept-surface depth debt grouped into ONE Code brief, sequenced AFTER P-6 · P-11 Matrix satellite docs adopted (`M2_7_GATE_REVIEW.md` is the first; this plan carries verdict + pointer).

**Rulings 2026-08-25 (Reza, all four as recommended):** **(a) Stitch pass ownership** — 🟩 Matrix generates the scoreboard screens and shows Reza preview PNGs; only after his nod does 🟦 Code commit the artefacts + JSDoc screen IDs (§18.4 step 2 run properly, with the human in it). **(b) The pack's unmapped `OTHER` expense row ($130)** — stays honestly UNMAPPED and keeps counting in the EOFY not-ready number; the pack never guesses a deduction category. **(c) `/api/money-flow` key** — **MODULE_ENTITIES STANDS** (Code's shipped call): the route's own contract is the per-entity money-flow shape and no household surface ever consumed it; the brief's MODULE_HOUSEHOLD recommendation is withdrawn. **(d) Matrix's next work** — the **M2.7 gate review**, ahead of any further Ring-3.

**Scope traps (research-confirmed; do NOT add):** tenant ops (rent collection/leases/maintenance) · multi-currency · lifestyle budgeting breadth · forecasting before records are right · anything that locks data in (full CSV export is a TRUST requirement — M3 verifies it exists).

---

## 3. The v1 scope (existing functions only — no net-new modules)

**KEEP (visible today, PROD):** Properties + detail + depreciation · per-property cashflow engine (SSOT) · Loans as property attribute · simple Assets · Documents/Vault (OCR + auto-linking) · intake (CSV/QIF, manual, cash quick-add, receipt OCR, reconcile→link, managed-rental reconciliation, accounts/balances/recurring) · Reports narrowed to THE pack (P2.3, in M1) · settings/auth/admin.
**RETURNING IN v1 (D-16):** the Dashboard, rebuilt as the property scoreboard (M3) — until then `/dashboard` keeps redirecting to `/dashboard/properties`.
**HIDDEN (13 MODULE_* keys, return by R-stage §4):** household/budget/plan · debt planner · safety net · entities · investments/super · tax module · CFO/What-If · strategy tabs · home dashboard (until its M3 rebuild) · housekeeping · social/marketplace · labs · org portal.
**BUILT BUT DARK:** Basiq feeds (GTM-gated — the retention fast-follow M6, NOT a launch blocker; manual+OCR survives in this niche: ~10–30 tx/property/yr).

Full route/API/key inventory: `PROD_SIMPLIFICATION_PLAN.md` §2 (binding).

### 3.5 Pain point → solution map (research 2026-08-19 + Reza additions; the v1 promise, falsifiably)

| # | User pain (ranked by evidence frequency) | What exists today | The v1 answer | Milestone |
|---|---|---|---|---|
| 1 | **PM statements aren't tax-ready** — every July investors reconstruct owner-paid capex, rates, insurance, DIY repairs the statement misses; now framed as an audit trigger | Managed-rental reconciliation, CSV/QIF import, manual + receipt OCR | AI statement agent: upload the PM/bank statement → proposed, property-linked, ATO-categorized rows → one-tap confirm; owner-paid items captured year-round, not reconstructed | M4 (intake exists now; AI on top) |
| 2 | **Messy records = higher accountant fees** ($275 organised vs $900 messy — PropertyChat, with an accountant confirming "well summarised… it's not") | Per-property ledger + the reports pack | The accountant pack: per property per FY, ATO rental-schedule line headings, PDF+CSV, evidence-linked — the accountant needs nothing else (pilot-verified) | M3 |
| 3 | **Loan interest is the #1 ATO error class** (42% of the $1.2B rental gap; redraw contamination; ATO data-matches loan records via RIPL) | Loans per property, one loan-cost resolver (T2) | Interest itemised per loan in the pack, produced by the ONE converged resolver Ring-3-verified on live data; full redraw-split/deductible-portion ledger returns with the R2 tax slice | M2 (correctness) · R2 (split ledger) |
| 4 | **Repairs vs capital vs depreciation misclassified**; stale QS schedules, renovations never claimed | Categories + `isTaxDeductible` flags + depreciation schedules | Guided classification at entry (repair / <$300 / capital works 2.5% / Div 40) with AI proposing the class and the user confirming; QS schedule upload → OCR → lines | M3 (guided) · M4 (AI + QS OCR) |
| 5 | **CGT cost-base decay** — "$200k of 2002 construction costs unprovable 20 years later"; the 1-Jul-2027 deemed disposal multiplies this | Vault keeps documents forever, linked to properties | Evidence-first records outlive any accountant's archive today; the full cost-base register + 1-Jul-2027 valuation slot + quarantined-loss ledger is the R2 headline (legislated wedge, no incumbent shipped) | v1 partial · R2 full |
| 6 | **EOFY scramble / not knowing what's missing** | — | Completeness nudges ("3 receipts missing", "no rates this FY") on the scoreboard dashboard + before pack export | M3 (with D-16 dashboard) |
| 7 | **Fear of app lock-in** — experienced investors stay on Excel because "software ties you in"; "tax agents die and you're screwed using them as your archive" | Unknown — verify | Full-data CSV export, always available, first-class — stated in marketing | M3.3 |
| 8 | **Distrust of app numbers** ("numbers can't be validated" — the accountant critique that started this programme) | Golden baseline + Ring-3 machinery | Every published number traces to ONE producer, Ring-3-verified on live data before launch; no number ships unverified | M2 (the launch gate) |
| 9 | **Manual transaction reconciliation** (Reza 2026-08-19) — matching bank/PM rows to the right property, loan or recurring item by hand | reconcile→link flow + managed-rental reconciliation exist but are user-driven | AI auto-matches every imported/uploaded row via the LinkingRules cascade and PROPOSES the reconciliation (property link, loan match, recurring-item match, duplicate detection) — user one-tap confirms or bulk-accepts; unmatched rows queue with a best-guess, never silently dropped | M4 |
| 10 | **Categorisation drudgery + wrong categories** (Reza 2026-08-19) — picking categories row by row, deductibility guessed | Manual category pick; per-category `isTaxDeductible` defaults (M4.6) | AI proposes ATO category + deductibility per row with a confidence signal, learns from the user's corrections, bulk accept-all-correct; low-confidence rows surfaced first | M4 |
| 11 | **Document filing & chasing** (Reza 2026-08-19) — receipts scattered across email, phone photos, drawers; filing them is its own job | Vault + Vision OCR + auto-linking already built | Drop ANYTHING (photo, PDF, statement, invoice) → OCR → auto-filed to the right property AND linked to the matching transaction — filing becomes a byproduct of upload; the missing-doc nudge (M3.2) closes the loop from the other side | v1 today — sharpened M3, auto-link-to-transaction M4 |
| 12 | **META — users hate manual work; they expect AI to do most of it** (Reza 2026-08-19) | — | The §1 automation design law governs every flow: "you confirm, Monitrax does." Measured: taps-to-done + time-to-first-correct-number (<5 min, M4.7); every remaining manual step is a named automation-backlog candidate | ALL — design law, not a feature |

---

## 4. ROADMAP — milestones, owners, gates

**Owners legend:** 🧑 Reza (rulings, merges, switches, credentials, pilot) · 🟩 Matrix = Cowork HQ (briefs, research, Ring-3 verdicts, doc-keeping) · 🟦 Code (build PRs) · 🌐 Chrome = browser relay driven by Matrix (console ops, golden-baseline runs, PROD checks; Reza types all credentials).
**Rituals:** boot per §0. Every build PR ticks its boxes HERE in the same PR. Matrix cuts one consolidated brief per Code session. Golden self-diff CLEAN required on every phase-gate. Never fix a number in passing — registry issue instead. PRs only, Reza merges. **D-18: M1–M3 enhance existing functions only; anything net-new needs Reza's recorded GO. D-20: visible-and-shallow outranks hidden-and-broken, always. D-22: refinement only, never reinvent, 100% depth before the next function.**

**GATE REVIEW — standing ritual at EVERY milestone gate (D-17, Reza 2026-08-19).** Before a milestone is declared done, Matrix runs and records (as a `§4.x gate review` entry under the milestone + a session-log line):
1. **Progress vs plan** — every box audited against evidence (PRs, verdicts, transcripts), not memory; slipped/de-scoped items named, never silently dropped.
2. **Fresh market + regulatory re-check** — competitors moved? (TaxTank/TPA/Propva watchlist) · pricing shifted? · ATO/reform guidance published? (esp. the open per-property-vs-pooled quarantine question and the "9-in-10" stat) · new entrants?
3. **Upgrade proposals** — what the last milestone taught us; concrete enhancement/re-ordering suggestions for the plan and the product, each needing Reza's ruling before it changes scope (D-8 WIP + D-18 build law apply).
4. **Cursor + §9 updated**; any ruling Reza makes lands in §2 as the next D-number.
No stage completes without its gate review. The plan is a living document — improved at every gate, never re-litigated between gates.

### M0 — Simplification executed ✅ DONE (2026-08-04 → 08-11)
Plan+rulings (#1584) · P0 freeze (#1586) · P1 module gate (#1587) · flag-phase acceptance CLOSED (P1.10 static-equivalent, P2.2 CLEAN, P2.2b CLEAN 0/0/0, P2.1 pass — #1587 comments) · PROD live in v1 shape · P2.5 full data copy + tested runbook + standing preview branch (#1589) · preview verified with full data · `connection_limit=1` durable fix. *(Gate review for M0 = the 2026-08-19 research + refocus session that produced this plan.)*

### M1 — Mechanics close-out ✅ **CLOSED 2026-08-19** — enhance-only (D-18 ✓)
- [x] **M1.1** 🟦 #1588 brief executed in full — **#1590** (P2.3 Reports→pack · P2.4 D-6 safe default · P2.6 row 66 · P2 close-out ticks · §13.6/§7 full-copy amendment · `set-module-flags.mjs` with PROD-refusal proof) + **#1591** (R0 overrides: user-scoped reader, user-aware API enforcement across 35 handlers, admin Overrides UI, 58 featureFlags tests). Both merged 2026-08-19. Deviation surfaced honestly on #1591: server layouts have no user identity (Bearer-token auth, zero `cookies()` repo-wide) → override windows enforce client-gate + API-503; the standing no-override state remains a hard server 404.
- [x] **M1.2** 🟦 Static-prerender defect → **MON-160** registered + fixed on #1590 at the ONE shared guard (request-time decision, order-locked by test). **VERIFIED live 2026-08-19** (see M1.5).
- [x] **M1.3** 🟦 ✅ done in the M2 PR-1 (2026-08-19, Code) — tracker pointers (STATE.md cursor line · 01_ACTIVE_WORKSTREAMS row · banner + frozen cursor on `PROD_SIMPLIFICATION_PLAN.md` · hub bump · old-plan §1 story line → §1 here · **CLAUDE.md boot-section pointer to this plan + §0**). Not doable from Cowork: STATE.md (78 KB) and the trackers exceed the GitHub connector's safe carry size (P0.1 precedent). Carry texts: `BRIEF_M2_CORRECTNESS.md` §A.
- [x] **M1.4** 🟦→🟩 **D-16 dashboard dependency inventory — DONE by Matrix 2026-08-19** (analysis, no build; run at `bea895f1`). Result recorded under **M3.4** below. Headline: the old Home is **not** flip-on-able — 7 outbound links point into hidden modules and both of its data APIs return the household/CFO story. D-16's sequencing is confirmed empirically, not assumed.
- [x] **M1.5** 🟩🌐🧑 **R0 ACCEPTANCE — PASS on PROD** (2026-08-19, #1591 comments): `MODULE_TAX` HIDDEN → override for `reza.sadegh@ymail.com` → **Tax Position renders for the holder** (FY2026-27, taxable $145,426, refund $5,218) · **404 for the other account and signed-out** · override removed → **404 again for the holder** (cache-busted). **MON-160 live check:** `MODULE_HOUSEKEEPING` ON → page + content + **sidebar nav entry** appeared within the cache window **with no redeploy** → OFF → 404 again. Caveat found → MON-161 (below).
- [x] **M1.6** 🧑 #1590 → #1591 → #1592 merged in order; PROD redeployed; standing state restored (13 hidden, 0 overrides).
- [x] **M1.7** 🟩 **GATE REVIEW — recorded below.**

#### M1.7 GATE REVIEW (D-17 ritual) — Matrix HQ, 2026-08-19

**1. Progress vs plan (evidence-audited, not recalled).** M1.1/M1.2/M1.5/M1.6 complete with PR + live-run evidence. **M1.3 slipped and is named, not dropped** → carried into M2's brief §A with its carry texts. M1.4 completed by Matrix instead of Code (analysis only, no build — cheaper than a Code round-trip). Two findings surfaced during the live run and are registered (§5: MON-161, MON-162) rather than fixed in passing (§23.2.1). Net: **the R-stage machinery is operational** — the last mechanical precondition to the correctness programme is done.

**2. Market + regulatory re-check (delta since 2026-08-19 research, same day).** No material change in-window; the watchlist stands unchanged (TaxTank $15/mo · The Property Accountant $3.99/property/mo · Propva on the statement-AI wedge · Moorr free/no pack). The two open verification items remain open and are **not** M2 blockers: (a) per-property vs pooled loss quarantining under the 1-Jul-2027 regime — read the Act before any R2 loss-ledger logic; (b) the ATO "9-in-10 wrong" figure — re-verify against an ATO-primary source before it appears in marketing copy (M5.1). Next substantive re-check due at the **M2 gate**.

**3. Upgrade proposals (need Reza's ruling; none actioned unilaterally — D-18).** *All five ruled by Reza on 2026-08-19 — see the ruling line under the §2 table.*
- **P-1 (recommended).** Fold M1.3 into M2's PR rather than spending a separate Code session on docs. **→ ADOPTED.**
- **P-2 (recommended).** Treat **MON-161** (stale cached 404 on the bare URL after a flip) as an **M2 item, not a nice-to-have**: it directly undercuts the admin panel's ~30s promise that every R-stage return depends on. **→ ADOPTED.**
- **P-3 (defer, register only).** **MON-162** (admin portal and app cannot hold independent sessions in one browser). **→ ADOPTED (registered, deferred).**
- **P-4.** M3.4's scoreboard could reuse `/api/dashboard/charts`' NetWorth series. Recommended against until M2 proves the producers behind it. **→ ADOPTED AS RECOMMENDED: no reuse until the census.**
- **P-5.** Server-side session identity (cookie-based) would let layouts enforce overrides server-side and would also fix MON-162 — an auth-architecture change far outside v1's D-18 boundary. **→ ADOPTED AS RECOMMENDED: PARKED.**

**4. Cursor + §9 updated.** ✓ (this PR)

**Gate:** all boxes ticked or explicitly carried; both merged PRs shipped `changesNumbers: NO`; PROD verified in standing state from three viewpoints. **M1 CLOSED.**

### M2 — Kept-surface correctness **and depth** (P3) — 🔒 THE LAUNCH GATE — enhance-only (D-18 ✓ · D-20 ✓)
The validation/issue-tracker programme, resumed and filtered to the kept surface (by producer, never by tranche — held doctrine). **Brief: `docs/strategy/BRIEF_M2_CORRECTNESS.md`.**

> **M2.0 — THE SCOPING LAW (D-20, binding on every M2 session).** The census (M2.1) decides scope, not the issue list. A defect is in M2 **only if a producer or surface it reaches is on the §2.1 KEPT list**. Everything else is **HELD** — named in the PR, not fixed. Concretely: `/dashboard/expenses`, `/dashboard/income`, `/dashboard/cfo`, `/dashboard/tax` and every other §2.2 route are hidden, so a defect whose only surface is one of them does **not** enter M2 no matter how tempting the fix looks. Depth on what ships beats breadth on what doesn't.

- [x] M2.1 🟦 ✅ **done (M2 PR-2, 2026-08-19, Opus diagnosis at `0eab2e95`).** Census re-run + kept-surface filter re-verified per quantity. **Two corrections to the inherited scope docs:** (C-1) `moneyFlowService.ts` is NOT a kept consumer — the Activity Sankey fetches `/api/master-snapshot`; the service's runtime consumers are all gated/instrumentation → HELD. (C-2) `contextBuilder.ts` fetchers DO feed the kept pack and were its #1 defect (→ MON-164). The scope table (kept consumer · convergence · issue per quantity):

  | Quantity | Kept consumer | Convergence at diagnosis | Issues |
  |---|---|---|---|
  | loanCost (30) | properties list/detail, balances dialog, master snapshot | ✅ converged on kept half (`resolveLoanMonthlyCost`) | MON-130✓/143✓; MON-156 HELD |
  | deductions (104→103) | pack Total Deductions | ❌ pack re-derived raw → **fixed #1595** | MON-164/129/037 |
  | depreciation (15→12) | detail page, dep page, both kept reports | ❌ FOUR divergent producers, pack wrong (1.95×) → **fixed #1595** | **MON-165** (new) |
  | incomeTax (76) | computed on pack path, **no dollar renders** | n/a — HELD as rendered quantity | — |
  | taxableIncome (38) | pack Total Taxable Income | ❌ pack declared-only → **fixed #1595** | MON-164/001-R1 |
  | propertyCashflowYield (6) | tiles, detail, list | duplication only (numerically identical) | unregistered §12.2.1 note |
  | propertyEquity (11) | list ✅ canonical; detail + pack inline → **pack fixed #1595** | partial | — |
  | lvrGearing (39) | list/detail/pack | ❌ 3 kept inline producers → **pack fixed #1595**; pages queued | MON-136 |
  | propertyValuationGrowth (6) | pack Capital Growth + pages | ❌ 3 inline, no canonical home | MON-136 |
  | taxOffsetsFranking (48) | computed on pack path, no dollar renders | n/a — HELD as rendered quantity | — |
  | loan interest (sub) | detail tax line, loan dialog | converged on `resolveLoanMonthlyCost`; basis labelled #1595 | MON-145 |
  | loan min repayment (sub) | via canonical resolver only | ✅ CLEAN | — |

  T5–T7 fall out as predicted (netWorth partial: pack per-entity section, ≥2-entity households only).
- [ ] M2.2 🟩🌐 T2 (loan cost) Ring-3 on live data — first Ring-3 through R0's override. **Status 2026-08-22: still pending** (the pack-canonical and pack-fix Ring-3s ran first; T2 is next in the Matrix queue). **⚠️ 2026-08-25 — CONTESTED, for M2.7 to settle:** `MON-131_TRANCHE_LEDGER.md` records T2's **G8 Ring-3 as ✅ CLOSED** on VR-047 + VR-047B (2026-08-03/04), with **MON-143 and MON-130 both VERIFIED** on that evidence. The open question is whether this box means something DIFFERENT today — the surfaces those runs read (`/dashboard/expenses`, the old Home budget tile) are now HIDDEN, so a v1-era re-verification would have to read the KEPT surfaces (properties list/detail, balances dialog, master snapshot). Gate review decides: satisfied-by-evidence, or re-scoped and re-run. **→ DECIDED 2026-08-25 (M2.7): RE-SCOPE, proposal P-6 awaiting Reza's ruling.** The T2 evidence is real but covers the `masterFinancialService → resolveLoanCostsForUser` leg read on Home + `/dashboard/expenses`, both unreachable today; the KEPT property surfaces use `resolveLoanMonthlyCost` via `computePropertyCashflow` — never Ring-3'd. The single kept consumer of the verified leg is the `/dashboard/activity` Sankey, absent from VR-047's verified list. **→ RE-SCOPED RUN EXECUTED 2026-08-25 under Reza's GO** — handout `docs/verification/briefs/RING3_T2_KEPT_LOAN_COST.md` (committed BEFORE the run), verdict on #1612: **8/8 checks PASS, verdict PARTIAL** (K6 the activity Sankey did not render — it appears stripped by Lever 2, so the T2-verified leg may have NO kept consumer at all). **B1 CLEARED for the cashflow number** — the property detail renders `−$2,790` and the canonical leg returns `$2,789.71`; they agree because the ACTUALS path binds, not because the offset arrives. **B1's mechanism confirmed as LATENT:** `/api/properties` carries the loan with `offsetBalance` FIELD_ABSENT, so any loan whose interest floor binds WILL render wrong. **AND the run caught a bigger one — see the MON-151 row in §5.** **→ K6 CORRECTED 2026-08-25 (P-9 sweep):** the activity money-flow widget DOES render on both viewports with live figures and fires no `/api/money-flow` request — the kept consumer exists, reading a different producer (master-snapshot leg). The "no kept consumer" inference is withdrawn; which producer feeds the widget is a P-10 source question. |
- [x] M2.3 🟦(build)→🟩(Ring-3 pending) **Fixes BUILT on #1595** — MON-164 (pack rows canonical: actuals-first + one-off-once), MON-165 (ONE depreciation producer), MON-129 kept slice (entityBreakdown + propertyCashflow gates), MON-166 (100× dep-rate render), MON-145 M2 slice (honest basis label; real fix schema-bound → Reza decision), MON-001 re-cut (R1 = MON-164; income-chip check returns at R3), MON-146 HELD (hidden-only). **→ Ring-3 OUTCOME (Matrix, 2026-08-19, #1595 comment): FAIL on the D-12 pack** (perProperty `[]` on 387 txns · 97.9% of rows silently absent from ATO labels · transfers inside totals) **— the gate held, exactly as designed. The PR-2 legacy-path fixes themselves verified GOOD** (one-off counts once; DIV43 consistent; 2.5% renders). The FAIL produced the M3 PR-1 pack fix, whose own Ring-3 **PASSED 2026-08-22** (#1601 comment) → MON-164/165/166 evidence stands; MON-168/169/170 VERIFIED.
- [x] M2.4 🟦 ✅ done (M2 PR-2, `0eab2e95` + follow-ups): BOTH inline budget/actual blocks in the property dialog deleted — the dialog reads `computePropertyCashflow` + basis chips; ratchet test + source-lock exception 4→3; census producers fell (income 126→123 · expense 79→76 · cashflow 57→55 · deductions 104→103 · depreciation 15→12 across the PR-2 fixes)
- [x] M2.5 🟦 **CLOSED (punch-list PR-2, 2026-08-22) — the MON-131 five-condition done applied to the KEPT quantities, evidence per condition:** (1) one producer per kept quantity — `census:producers:check` green at HEAD, every count AT seed after the M2/M3 producer deletions (pack fetchers→canonical · ONE depreciation producer · entityBreakdown/propertyCashflow gates · properties-dialog SSOT · properties-list inline re-derivation · and the LAST kept-surface breach, the portfolio-LVR pair, closed as MON-182 in punch-list PR-2); (2) surfaces read the producer — `lint:source-lock` (ratchet-down held) + `lint:financial-surfaces` + Neomatrix A3 convergence green at HEAD; (3) declared `expectedMoves` landed exactly — T1 VR-045 PASS, T2 VR-047/047B PASS (G8 ✅), M3 pack-fix movements to the cent (#1601 verdict, mustNotMove held); (4) Ring-3 on live data — the pack-fix Ring-3 PASS (2026-08-22, #1601), the named unblocking precondition. **Honest boundary:** the punch-list PR-2 D-21 movements (MON-182/184) verify at the NEXT Ring-3 (`RING3_M3_PUNCH_FIXES.md`), and condition (5) — the complete Matrix sweep — closes MON-131 the programme, not M2.5; it stays with the Matrix (M2.2 T2 Ring-3 · M2.7 gate review). **→ The named Ring-3 RAN 2026-08-25: all 17 checks PASS (#1606 verdict) — the MON-182/184 movements landed exactly as declared.**
- [x] M2.6 🟦 **DEPTH SWEEP DONE (M2 PR-3, 2026-08-19)** — 49 findings catalogued in `docs/strategy/M2_DEPTH_SWEEP_CATALOGUE.md`: 11 fixed in PR-3 (no numbers move), 12 registered (MON-168…179), the rest = the M3.6 depth backlog + 10 LIVE-CHECK items for the Matrix. 🟩 **LIVE HALF RUN 2026-08-25 (P-9):** items 1/2/9 covered by the pack Ring-3s, **6 of the 7 outstanding executed** (`RING3_M2_6_LIVE_SWEEP.md` + envelope, verdict PARTIAL, findings F1–F10 → the P-10 brief). Outstanding: item 7 fresh-account onboarding ONLY (Reza runs it after the intake fix — B3 discharges then). ORIGINAL SPEC: **KEPT-SURFACE DEPTH SWEEP (D-20 — the quality half of the launch gate).** Walk every §2.1 kept route as a user, on PROD data, and catalogue what is *shallow, broken or dead-ended* — separately from whether the numbers are right (M2.1–M2.5 own that). Mandatory checks: **(a) dead links** — no kept surface may link to a hidden route (**live example already found: `/dashboard/properties/[id]` links to `/dashboard/tax`, `/dashboard/income` and `/dashboard/cfo/what-if/sellProperty` — all 404 today → MON-163**); (b) empty states that hide missing capability rather than explain it; (c) every displayed number traceable to ONE producer (no screen arithmetic — SSOT); (d) intake paths end-to-end (CSV/QIF · manual · cash · receipt OCR · reconcile→link · managed-rental) each actually reaching a property row; (e) documents: upload → OCR → auto-link → visible on the property; (f) the reports pack generates on real data; (g) mobile nav + tab bar; (h) error/loading states on every kept page. Output: one catalogued gap list → registry issues → **the M3 depth backlog**. This is the "working perfectly in PROD" test.
- [x] M2.7 🟩 **GATE REVIEW RUN 2026-08-25 → ⛔ VERDICT: THE M2 GATE DOES NOT CLOSE.** Full record: **`docs/strategy/M2_7_GATE_REVIEW.md`** (P-11 satellite pattern). Four blocking findings: **B1 (NEW, unregistered)** the offset is never threaded into the property pages' cashflow engine — `resolveLoanMonthlyCost` nets `principal − offsetBalance` (`propertyCashflow.ts:246`) but NO `offsetBalance` is passed anywhere under `app/dashboard/properties/` (`page.tsx:496`, `[id]/page.tsx:180`), so a kept-surface number is computed on the FULL balance while Guildford carries a $303,889.96 offset — same class as the registered MON-151 · **B2** M2.2 mis-stated: T2's Ring-3 DID pass (ledger `:419` G8 ✅) but on surfaces now unreachable, while the kept property path uses a DIFFERENT producer that has never had a Ring-3 · **B3** 0 of the M2.6 sweep's 10 LIVE-CHECK items executed (3 incidentally covered by the pack Ring-3s; **7 outstanding**) · **B4** 21 OPEN/FIXING critical-or-high issues cite KEPT surfaces, including a crash (MON-087); of the 7 criticals NONE is hidden-only. **Six proposals await Reza's ruling: P-6** re-scope M2.2 to the kept path · **P-7** rule on depth-sweep #19 (two pack generators, awaiting since 08-19) · **P-8** correct §7's reform row to separate ATO-confirmed from secondary-source claims · **P-9** run the 7 outstanding live-checks as one Matrix+Chrome sweep · **P-10** group the kept-surface depth debt into ONE Code brief · **P-11** Matrix satellite docs. **→ ALL SIX ADOPTED by Reza 2026-08-25; P-6 executed the same day (#1612).**
**Gate:** Ring-3 PASS on live data across kept quantities **AND** the M2.6 sweep's launch-blocking gaps closed. Nothing publishes before this — automation on wrong numbers is wrong numbers, faster; and a shallow surface with dead ends is not a product.

### M3 — The accountant pack becomes the product (D-12/D-13/D-16/D-19) — enhance-only (D-18 ✓: the pack, exports, vault and engines all EXIST; this perfects them)
- [ ] M3.1 🟦 Pack restructured to ATO rental-schedule line headings, per property per FY: income · interest (per loan) · repairs vs capital vs Div 40/43 (from the depreciation schedules) · other deductions by schedule line · linked evidence (vault docs) per row · PDF + CSV
- [ ] M3.2 🟦 EOFY completeness nudges (D-13): missing-receipt / missing-category / no-rates-this-FY style checks surfaced before pack export
- [ ] M3.3 🟦 Full-data CSV export verified/added (anti-lock-in trust requirement — PropertyChat's #1 stated reason for staying on Excel)
- [x] M3.4 🟦 **BUILT (M3 PR-2, 2026-08-19)** — `lib/dashboard/tileRegistry.ts` (TileDef + the verbatim visibility law + suppress-only toggles riding the GlobalFeatureFlag store as `TILE_SUPPRESS_*`), `app/dashboard/ScoreboardClient.tsx` (5 v1 tiles from kept engines: portfolio via /api/portfolio/snapshot · per-property cashflow via THE computePropertyCashflow engine · EOFY readiness via the D-12 pack's PR-1 reconciliation counts · documents · intake queue; 9 stage tiles registered DARK as link tiles), admin "Dashboard tiles" section (label · stage · module · computed state · suppress), 8 law tests. HomeClient untouched (returns R4). **Deviation noted for Reza:** portfolio tile renders net worth (a direct snapshot field), not equity — no portfolioEquity producer exists server-side and deriving it in the tile would mint a second producer (§12.2.1). **Stitch backfill DUE (§18.2.1)** at the MODULE_HOME acceptance pass — **OVERDUE as of 2026-08-25: acceptance passed 08-22 and the punch-list PRs since modified the tiles code-first; Reza re-affirmed (2026-08-25) that ALL tiles/UI-UX go through Stitch. The backfill (screens matching the AS-SHIPPED post-punch-list scoreboard → `.stitch/designs/` HTML+PNG → screen IDs in ScoreboardClient JSDoc) is the next Code slice's FIRST item; all future tile/section work is Stitch-FIRST per the standing law, not backfill. **SCOPE FENCE (D-22, Reza 2026-08-25): the backfill RECORDS the shipped composition — it is NOT a redesign brief. The artefact reproduces the five tiles, their figures and their layout as they ship today; any delta the render suggests is a FINDING for Reza, never auto-work.** **Ownership (ruling 2026-08-25a): Matrix generates + shows previews; Code commits only after Reza's nod.** **BLOCKED 2026-08-25 — tooling: two `generate_screen_from_text` runs (in-app project `5991501424852019479`, then canonical `1859462351962811110`) both hit the 60s MCP timeout and produced no retrievable screen; `list_screens` returns EMPTY for the in-app project entirely and the canonical project still shows its same 82 screens. Backfill cannot complete from the Cowork session until this is resolved — options for Reza in the next relay.** 🧑 **ACCEPTED by Reza 2026-08-22 — MODULE_HOME + MODULE_HOUSEKEEPING flipped ON live (no deploy); the redirect is retired and the scoreboard is the shipping `/dashboard`.** Matrix acceptance pass on Reza's data: portfolio $4,990,000 ≡ properties banner · cashflow strip ≡ tile figures ÷12 to the dollar (SSOT held) · vault count real. **Punch list (MON-180…183, registered punch-list PR-1 — items 1/2/4 FIXED there; item 3 = MON-182, fix in punch-list PR-2, D-21):** (1) EOFY tile empty state misleading — "All rows Tax-ready" is vacuously true on a near-empty FY2026-27 while 35 unmapped FY2025-26 rows exist; needs a no-rows state + the just-ended-FY framing; (2) intake-queue tile renders "—" instead of a count (both accounts — fetch/render bug); (3) "Portfolio LVR 41.3%" beside the properties page's "AVG LVR 40.8%" — two metrics, labels must earn the difference or one producer wins; (4) the cashflow strip shows 4 of 6 properties with no stated rule. **→ All four punch items + labelling VERIFIED LIVE by the 2026-08-25 Ring-3 (#1606).** ORIGINAL SPEC: D-16 scoreboard + D-19 tile registry, built from the M1.4 inventory (below), not from the old HomeClient.

<details open>
<summary><b>M1.4 — D-16 dashboard dependency inventory (Matrix, 2026-08-19, at <code>bea895f1</code>)</b></summary>

**Verdict: the old Home is NOT flip-on-able.** `app/dashboard/page.tsx` is already the correct thin server wrapper (force-dynamic · redirect when hidden · `ModuleOverrideGate` in an override window · never 404s, D-4) — the problem is entirely `HomeClient.tsx` (1,826 lines), which tells the pre-simplification wealth-OS story.

*API surface it fetches (`HomeClient.tsx:375-415`) — gate status from `moduleRegistry.ts`:*

| Endpoint | Gate status | Usable by the v1 scoreboard? |
|---|---|---|
| `/api/portfolio/snapshot` | KEEP-OPEN | **Yes** — property portfolio value/equity/LVR |
| `/api/accounts` | KEEP-OPEN | Yes (kept intake) |
| `/api/settings/balance-upgrade-nudge` | KEEP-OPEN | Yes (nudge plumbing — reusable for M3.2) |
| `/api/dashboard/insights` | KEEP-OPEN **but** | **Partly.** Reads `canonicalCashflow` (household — hidden D-1), `financialIndependence` + `lib/health` (CFO — hidden D-3), `moneyStoryTrend`, `masterFinancialService`. The route stays open (kept surfaces use it); its *content* is the household/CFO story. |
| `/api/dashboard/charts` | KEEP-OPEN **but** | **Partly.** Reads `masterFinancialService`, `moneyStoryTrend`, `entityValueBreakdown` (entities — hidden D-5/R5), `netWorthHistory`. **P-4 ruled: no reuse until M2's census.** |
| `/api/wealth-graph` (via `WealthUniverseWidget` → `useWealthExplorerData`) | **GATED → MODULE_ENTITIES (R5)** | **No** — 503s in v1. Widget must not ship on the scoreboard. |

*Outbound links into HIDDEN modules (would 404 today):* `/dashboard/investments/accounts` (R5) · `/dashboard/debt-planner` (R3) · `/dashboard/cfo` (R4) · `/dashboard/income` (R3) · `/dashboard/expenses` (R3) · `/dashboard/entities` (R5) · `/dashboard/budget-analysis` (R3) — **7 dead links.**

*Widgets rendered today:* FinancialHealthScore · EmergencyFundTracker · MoneyBleedingCard · SpendingByCategory · ActionableInsights · MonthlyBudgetSummary · DebtQualityWidget · EntityCashflowSummary · WealthUniverseWidget · InvestmentIncomeDisplay · LinkageHealthIndicator · QuickActionsBar · Editorial{Chart,Donut,EntityBars,Line}. **Every one is household/CFO/entity-scoped except the portfolio and property tiles.**

**D-19 — the stage-aware tile registry (spec; built in M3.4).**
```ts
// lib/dashboard/tileRegistry.ts — ONE source of the tile decision (SSOT)
export interface TileDef {
  id: TileId;                 // 'portfolio' | 'eofy-readiness' | 'tax-position' | …
  label: string;              // admin panel display name
  requires: ModuleKey | null; // null = v1 core, always eligible
  stage: 'v1' | 'R2' | 'R3' | 'R4' | 'R5';
  suppressible?: boolean;     // admin may hide it early; may NEVER force-show it
}
```
**Visibility law (the iron rule):** `visible = (requires === null || isModuleEnabled(requires)) && !suppressed(id)`. A tile toggle can only **suppress**; it can never show a tile whose module is off. That single constraint is what stops the old Home's failure mode (widgets rendering against gated APIs) from ever coming back. Fail-closed like `moduleGate`: unreadable ⇒ hidden. The client already has everything needed — `useEnabledModules()` returns the session user's effective map (global ∥ R0 override), so an override holder previewing a stage sees its tiles too, and nobody else does.

**The staging ladder (each tile appears by itself when its stage's module flips):**
| Stage | Tiles | `requires` |
|---|---|---|
| **v1 (now)** | Portfolio summary (value · equity · LVR) · per-property cashflow strip · **EOFY readiness** (M3.2 nudges) · documents/evidence status · intake queue | `null` |
| **R2** | Tax position slice · housekeeping review | `MODULE_TAX` · `MODULE_HOUSEKEEPING` |
| **R3** | Household cashflow · safety net · debt freedom | `MODULE_HOUSEHOLD` · `MODULE_SAFETY_NET` · `MODULE_DEBT_PLANNER` |
| **R4** | CFO actions · strategy | `MODULE_CFO` · `MODULE_STRATEGY` |
| **R5** | Wealth universe · investments | `MODULE_ENTITIES` · `MODULE_INVESTMENTS` |

**Admin surface:** a "Dashboard tiles" section under the Modules panel — one row per tile: label · stage · owning module · computed state (`LIVE` / `HIDDEN — module off` / `SUPPRESSED`) · the suppress toggle. Reza can see at a glance which tiles the current stage puts on the dashboard, and why any tile is dark.

**M3.4 build rule (from this inventory):** the scoreboard is a NEW client composed from `/api/portfolio/snapshot` + the per-property cashflow engine + the M3.2 completeness checks. `HomeClient.tsx` is **not deleted** (hidden ≠ deleted) — it stays for the R4 wealth-OS Home. Do not lift its widgets wholesale; do not add the WealthUniverse tile; every link must point at a KEPT route (MON-163's lesson).
</details>
- [ ] M3.5 🧑🟩 **The pilot = the user research:** Reza's accountant + 2–3 friendlies run the pack on real FY2025-26 data. Acceptance: the accountant needs NOTHING else to complete the rental schedule. Findings → registry issues, fixed, re-run.
- [ ] M3.6 🟦 **The M2.6 depth backlog** — the launch-relevant gaps the sweep catalogued, closed here (D-20: this is the milestone's substance, not a leftover).
- [ ] M3.7 🟩 **GATE REVIEW** (D-17 ritual) recorded here — **includes the D-18 GO decision: Reza rules which M4 tasks (if any) are authorised to build, one by one.**
**Gate:** accountant sign-off captured in this doc; dashboard live as the scoreboard with its stage-aware tiles. **This gate closes the flagged user-voice research gap.**

### M4 — AI intake (P4 rescoped by D-11; pain rows 1, 9, 10, 11) — ⛔ D-18 HOLD: planned + sequenced here, NOT pre-authorised. Each task below starts only on Reza's explicit GO (taken at the M3 gate review or later). Until then, all four actors treat M4 as read-only roadmap.
The automation design law made real: AI does the keying, matching, classifying and filing; the user confirms. (Builds ON the existing Vision OCR + analyze pipeline + LinkingRules cascade — extensions of what exists, but they are new capability surfaces, hence the GO gate.)
- [ ] M4.0 🧑 **D-18 GO recorded per task** (which of M4.1–M4.6, in what order) — becomes the next D-number.
- [ ] M4.1 🟦 Statement agent v1: upload PM statement / bank statement (PDF/CSV) → existing Vision OCR + analyze pipeline → classify + property-link via LinkingRules cascade → **propose→confirm queue** (one review moment per statement; accept-all-correct in one tap); every confirmed row carries its source-document link
- [ ] M4.2 🟦 **AI reconciliation (pain 9):** every imported/uploaded row auto-matched — property link, loan match, recurring-item match, duplicate detection — proposed for confirm; unmatched rows queue with best-guess, never silently dropped
- [ ] M4.3 🟦 **AI categorisation (pain 10):** ATO category + deductibility proposed per row with confidence; learns from corrections; low-confidence rows surfaced first; bulk accept
- [ ] M4.4 🟦 **Docs auto-filed to transactions (pain 11):** any uploaded document OCR'd, auto-filed to its property AND auto-linked to the matching transaction row (extends the existing vault cascade)
- [ ] M4.5 🟦 QS depreciation schedule upload → OCR → schedule lines
- [ ] M4.6 🟦 Onboarding rebuilt to 3 steps (property → loan → rent+agent); hidden-module wizard steps removed; `isTaxDeductible` defaults per category
- [ ] M4.7 🟩🧑 Time-to-first-correct-number < 5 min on a fresh account; taps-to-done measured per flow; zero silent writes (audit: every AI row has a confirm event)
- [ ] M4.8 🟩 **GATE REVIEW** (D-17 ritual) recorded here.
**Gate:** a new user reaches a correct per-property number in one sitting, with AI doing the work and the user only confirming. AFSL note: extraction/classification only — no advice surface, in code or copy.

### M5 — Publish 🚀
- [ ] M5.1 🧑🟩 Positioning/site rewrite to the §1 story (the queued row-66 follow-up — a marketing defect if skipped)
- [ ] M5.2 🧑 Pricing ruled (research anchors: TaxTank $15/mo · The Property Accountant $3.99/property/mo · Etax $59.90/schedule/yr status-quo cost · category clears A$10–20/mo; "tax-deductible" is the category selling line)
- [ ] M5.3 🟩🌐 Pre-launch verification sweep: golden self-diff CLEAN on PROD · route/nav smoke · pack generation on PROD data · health/monitoring
- [ ] M5.4 🧑 Go-live + first-users plan (friendlies → public)
- [ ] M5.5 🟩 **GATE REVIEW** (D-17 ritual) — includes the full pre-launch market/pricing re-check.
**Gate:** live, first external users, support loop agreed.

### M6 — Retention fast-follow + returns
- [ ] M6.1 🧑 Basiq feeds ON (built, dark — GTM/cost ruling; "tax pack = why they pay, feeds = why they stay"). NOTE: first CDR data in PROD permanently sunsets the D-7 dev-copy exception (runbook law).
- [ ] M6.2 R-stage returns, ONE at a time (D-8), each gated on producers converged + Ring-3 PASS live + Reza's switch: **R2** tax module as property-tax slice + 2027 reform readiness (cost-base register, 1-Jul-2027 valuation slot, quarantined-loss ledger — the legislated wedge; P5.4) + housekeeping → **R3** household/budget/debt/safety-net (needs Basiq) → **R4** CFO/strategy/full wealth-OS Home (highest AFSL bar) → **R5** entities/investments/social/labs/portal (commercial call each). Variance loop (P5.2) + Propsight import (P5.3) slot post-launch as the moat. **Each return also lights its D-19 dashboard tiles automatically — no separate dashboard work per stage.**
- [ ] M6.3 🟩 **GATE REVIEW** (D-17 ritual) per R-stage return — each return gets its own review before AND after the switch.

---

## 5. Launch-blocking issues (the M2 working set)

**Law:** `docs/issues/ISSUES.md` + `docs/issues/ISSUES.json` remain the REGISTRY OF RECORD (all ~147 issues, full history). This table is the launch-blocking SUBSET only; status changes land in BOTH (same PR — Neo-sync discipline). Hidden-module issues stay HELD (P0.2 freeze + D-20) and are NOT listed.

| Issue | What | Status at 2026-08-19 |
|---|---|---|
| MON-001 | Rent frequency handling (kept surface) | **FIXING** (critical) → M2.3 |
| MON-129 | Producer-class sweep — 23 `lib/` producers convert rows to run-rates with no one-off gate | **OPEN** (critical) → M2.3, scoped by M2.0 to kept-surface producers only |
| MON-143 | Offset netting | **VERIFIED already** — closed; the earlier "OPEN" entry here was plan drift, corrected 2026-08-19 |
| MON-145 | `Loan.interestRateAnnual` is an undated scalar while repayments are dated | **OPEN** (high) → M2.3 (feeds the pack's per-loan interest, pain 3) |
| MON-146 | Loan rate rendered 100× too small | **OPEN** (medium) — cited surface `/dashboard/expenses` is **HIDDEN**. **M2.0 scoping question:** does the same render path appear on a KEPT surface? If not → HELD, do not fix (D-20). |
| MON-160 | Static-prerender flag gating (build-time-baked module guards) | **VERIFIED** — registry flipped 2026-08-19 (M2 PR-1) citing the M1.5 live flip test |
| MON-161 | **NEW (M1.5 run):** stale **cached 404** on a gated route's bare URL for ~2 min after a flag flip — a cache-busted request gets the correct verdict immediately, so request-time gating works; the artefact is response/browser caching of the 404 for that exact URL. Undercuts the panel's ~30s promise every R-stage return relies on. Fix: `Cache-Control: no-store` (or equivalent) on gated-route 404 responses. | **REGISTERED + FIXED (M2 PR-1, 2026-08-19):** `Cache-Control: no-store, must-revalidate` stamped at the ONE middleware chokepoint for every registry-derived gated prefix; locked by `tests/featureFlags/mon161NoStoreCache.test.ts`. Ring-3 re-check = re-run the M1.5 flip on the BARE url. |
| MON-162 | **NEW (Reza, M1.5 run):** the admin portal and the app **cannot hold independent sessions in one browser** — signing into either signs the other out (shared auth storage, same origin). | **REGISTERED (M2 PR-1, 2026-08-19)** — OPEN in the registry; **fix DEFERRED** by Reza's ruling (P-3/P-5) |
| MON-168/169/170 | The D-12 pack trio (propertyId never stamped · transfers counted · silent label drops) | **FIXED #1601 · backfill applied on PROD 2026-08-22 (46 rows, idempotent) · Ring-3 PASS → VERIFIED (registry flipped, punch-list PR-1)** |
| MON-180…186 | Scoreboard punch list ×4 (MON-180 EOFY empty state · MON-181 intake "—" · MON-182 LVR producer pair · MON-183 strip 4/6 rule) + MON-185 data findings (register only, Reza runbook in the issue) + MON-184 pack ATO labelling zero-match + MON-186 MODULE_HOME re-key sweep | **Ring-3 RUN 2026-08-25 (#1606 verdict): 180/181/182/183/184 → VERIFIED · 186 → VERIFIED** (tile evidence live; the authenticated money-flow 503 Ring-1-only — accepted, no kept consumer). Registry flip lands with the next Code PR. **185 waits on 🧑 Reza's cleanup** |
| **MON-151** | **CONFIRMED LIVE + QUANTIFIED on a KEPT surface (Ring-3, 2026-08-25, #1612).** The property-detail **Tax position card** computes loan interest on the **FULL balance**, ignoring the linked offset. Guildford (principal $377,821.91 @ 6.24%, offset **$303,889.96**): full-balance interest **$23,576.09/yr** vs canonical offset-netted **$4,613.35/yr** → **interest overstated by $18,962.73/yr**. The card's own number proves the basis: rent $26,089 − $23,576.09 = $2,512.91 and it renders "$2,513/yr"; offset-netted it would read ~$21,476/yr. Control holds exactly — Broadbeach (no offset) 228,000×6.69%÷12 = $1,271.10 = canonical `monthlyInterest`. Basis IS labelled ("contractual estimate (balance × today's rate)" — MON-145's slice working), but an honest label on a materially wrong number is still a defect when the app already holds the offset. This is the ATO's #1 rental error class on our own kept surface. | **OPEN → promoted to the M2 working set.** Fix belongs in the P-10 kept-depth Code brief. NOT fixed in passing (§23.2.1). Scope note: the run establishes the figure differs from interest ACTUALLY INCURRED; whether offset-netted interest is the correct DEDUCTIBLE figure is a tax-agent determination, outside Monitrax's lane. |
| **F1–F10 (P-9)** | **NEW (P-9 live sweep 2026-08-25, envelope on the PR; MON ids assigned by Code in the P-10 PR-1):** Smart Inbox intake pipeline display-only — select/approve dead both viewports (F1), edits don't persist (F2/F3) · vault tree count 2× (F4) · OCR vendor picks mis-transcribed doc-type word (F5) · scoreboard $0-as-truth during real 503s (F6) · upload 413 at 6.2MB vs 10MB promise (F7) + raw parse error surfaced (F8) · help drawer uninvited/undismissable (F9) · basiq 403 every load (F10) | **OPEN → the P-10 brief (`BRIEF_M2_KEPT_DEPTH.md`): PR-1 = F1/F2/F3/F7/F8/F5/F4 · PR-3 = F6/F9/F10.** Reza's real expense (Bunnings $203.78, FY2026-27) stranded pending on F1 |
| MON-163 | **NEW (Matrix scan 2026-08-19, LIVE IN PROD):** the KEPT property-detail page `/dashboard/properties/[id]` links to **hidden** routes — `/dashboard/tax` (:556), `/dashboard/income` (:746, :758) and `/dashboard/cfo/what-if/sellProperty` (:403, :548). A v1 user clicking any of them gets a 404. The P2.1 route sweep checked that hidden routes hide; it never checked that **kept** routes stop pointing at them. | **REGISTERED + FIXED (M2 PR-1, 2026-08-19):** every link site gated on its target module (the Matrix's 2 files became **8 kept-reachable files / 14 link sites** once the repo-wide guard ran) + the permanent class guard `tests/featureFlags/deadLinkGuard.test.ts` (kept-reachable import-graph walk, registry-derived) now fails CI on any recurrence. |
| MON-164 | **NEW (M2.1 census):** the pack re-derived income/expenses from raw declared rows — no actuals, no one-off gate (MON-001-R1 + MON-129 kept #1/#2 on the same lines) | **FIXING on #1595** (changesNumbers YES — expected movements in the PR; Ring-3 = M2.2) |
| MON-165 | **NEW (M2.1 census):** FOUR divergent depreciation producers; the pack + dep page held a first-year-only formula (1.95× on aged DV assets) | **FIXING on #1595** (changesNumbers YES) |
| MON-166 | **NEW (M2.1 census):** dep rate rendered 100× too large on the properties dialog (MON-146's kept twin) | **FIXING on #1595** (display-only) |
| MON-167 | **NEW (M2.1 census):** dead-link guard missed `to:` link fields (editorial hero) | **FIXING on #1595** (guard + component gated) |
| MON-168 | **NEW (M2.6 sweep):** reconcile→link never stamps `propertyId` — the D-12 pack's per-property P&L can be EMPTY on a fully-linked year | **FIXING on M3 PR-1** — 17 write sites → ONE resolver (`propertyLink.ts`) + guard test + admin backfill (dry-run first); VERIFIED gates on `RING3_M3_PACK_FIX.md` |
| MON-169 | **NEW (M2.6 sweep):** the pack counts transfers + loan repayments as income/expense | **FIXING on M3 PR-1** — both diverted to counted informational buckets; totals property-scoped |
| MON-170 | **NEW (M2.6 sweep):** pack ATO-label totals silently drop unmapped rows | **FIXING on M3 PR-1** — every `continue` now a counted exclusion; identity `included + Σexcluded = total` hard-asserted + printed |
| MON-171…179 | **NEW (M2.6 sweep):** wizard parallel engine · CASH invisible on balances · HALF_YEARLY 6× · OCR invents 5% · raw minRepayment rows · lint-laundered RENTAL sums · recurring approximations · "Net Taxable Income" framing · HELD hidden-only trio | OPEN — triaged per the catalogue |
| **CENSUS CORRECTION** | The D-12 pack (`buildTaxPackSummary`) was OUT of the M2.1 census — the census + handout covered the legacy generator only. **M2.5 cannot close** until MON-168/169/170 land + their own Ring-3 | recorded in the handout §Scope-correction |
| + M2.1 / M2.6 additions | Whatever the kept-surface census and the depth sweep surface | → triaged into this table as found |

Registry counts at last full count (P0.6, 2026-08-04, `e588a837`): 65 OPEN/FIXING · 5 critical · 146 total (147 with MON-160). Re-count at M2.1; never quote these numbers later without a re-pull.

---

## 6. Verification law (pointers, not copies)

Golden baseline self-diff CLEAN on every phase-gate (`.audit/golden-baseline-12954ff.json`; relay endpoints + runbook: project memory `matrix-relay-runbook`) · Ring-3 on LIVE data via R0 override = the bar for M2 and every R-stage return · never fix a number in passing (§23.2.1 — registry issue) · §20.6 tri-axis + §16.5 doc-sync in every PR body · SSOT audit is step 0 of every change · preview data refreshes ONLY per `docs/operations/PREVIEW_DATA_REFRESH_RUNBOOK.md` · **no kept surface may link to a hidden route (MON-163's rule) — check it whenever a kept page changes.**

---

## 7. Market evidence (condensed; full agent reports in the 2026-08-19 session record)

- **Invariant core globally** (Stessa, Landlord Studio, Hammock, Baselane, REI Hub all launched with exactly): property-tagged ledger in local tax categories + ONE low-friction ingestion + accountant-accepted tax output + simple dashboard. All excluded tenant ops/banking/budgeting at launch. Landlord Studio v1 was manual+receipt-scan only; feeds came ~2 yrs later. Monetisation: tax pack is the paywall (Stessa Schedule E at $12/mo — "the exit door at tax time has a toll booth"); feeds drive retention (Stessa/Unit: banking users 4× LTV, 3.5× retention). [stessa.com/pricing · landlordstudio.com/pricing · accountingstack.co.uk/…/hammock]
- **AU field:** TaxTank $15/mo, Basiq feeds, live tax position — the benchmark; The Property Accountant $3.99/property/mo, feeds + tax-ready reports + accountant portal — "Monitrax with feeds already built"; Moorr free, 43k users, Property Couch funnel, NO tax pack; 5+ new entrants since 2024-25 (propkt $49/yr, Propva = statement-AI wedge — watch it, Opulo, WealthStacker, Lolli), nearly all pre-traction. ATO myDeductions has NO rental category — the true incumbent is spreadsheet+shoebox. [taxtank.com.au/pricing · thepropertyaccountant.com.au/pricing · moorr.com.au · propkt.com · propva.com.au]
- **Pain points ranked:** see §3.5 (the map is the operative version). [propertychat.com.au threads · ato.gov.au media releases · cpaaustralia.com.au]
- **Regulatory:** NG/CGT reform = LAW (Assent 26 Jun 2026, eff. 1 Jul 2027); deemed disposal + quarantined residential losses + indexation; ATO compliance guidance still unpublished; Treasury estimates $88.4M/yr extra compliance cost. **OPEN VERIFICATION: per-property vs pooled loss quarantining conflicts between Corrs (per-property) and PwC/William Buck (pooled) — read the Act before building any loss-ledger logic (R2).** "9-in-10 wrong" stat: operative but last ATO-primary 2023 — re-verify before using in marketing copy. Victoria = dated-obligation-rich state (VRLT 15 Feb, AOS 15 Jan, short-stay levy). [ato.gov.au/about-ato/new-legislation · corrs.com.au · pwc.com.au tax alerts]
- **Coverage caveat:** Reddit sentiment unreachable (proxy-blocked) — uncovered. The M3.5 pilot is the real user-voice closure.

---

## 8. Doc map — where every fact lives (SSOT)

| Question | Read |
|---|---|
| Programme state, roadmap, who does what next | **THIS FILE (§0 boot → cursor → §4)** |
| Repo laws binding every session | `CLAUDE.md` |
| Module-gate rulings D-1…D-9 + gate/route/API design | `PROD_SIMPLIFICATION_PLAN.md` (archive; binding design) |
| Every issue, full history | `docs/issues/ISSUES.md` / `.json` (registry of record) |
| MON-131 tranche mechanics + ledger | `docs/implementation/MON-131_TRANCHE_LEDGER.md` |
| Cross-programme cursor ("you are here" for ALL work) | `STATE.md` |
| Preview/dev data refresh | `docs/operations/PREVIEW_DATA_REFRESH_RUNBOOK.md` + `PREVIEW_BRANCH.md` |
| Golden-baseline / relay mechanics | `scripts/matrix/` + project memory `matrix-relay-runbook` |
| v1 scope four-lens analysis (archive) | `PRODUCT_SCOPE_V1_RECOMMENDATION.md` (Q-SCOPE-1, DECIDED) |
| Milestone gate reviews (D-17 records) | `docs/strategy/M2_7_GATE_REVIEW.md` and siblings (P-11 satellite pattern — the plan carries the verdict + pointer, the doc carries the evidence) |
| P-9 live-sweep result (F1–F10 evidence) | `docs/verification/briefs/RING3_M2_6_LIVE_SWEEP.md` + the envelope on its PR |
| Code-session briefs | `docs/strategy/BRIEF_*.md` (M2: `BRIEF_M2_CORRECTNESS.md`; M1 archive: `BRIEF_SIMP_P2R_R0.md`) |

## 9. Session log

- 2026-08-19 · Code (Fable 5) · **M3 PR-2 — the v1 scoreboard (§C+§D, changesNumbers: NO).** D-19 built to the plan's spec block verbatim: `tileRegistry.ts` (TileDef + `visible = (requires===null || isModuleEnabled(requires)) && !suppressed(id)`; suppress flags ride the existing GlobalFeatureFlag store as `TILE_SUPPRESS_*` — no second vocabulary; fail-closed on module state, suppression failure cannot force-show by construction). `ScoreboardClient.tsx`: five v1 tiles from kept engines only (portfolio = direct snapshot fields; per-property strip = THE computePropertyCashflow engine with the list page's identical inputs; EOFY readiness = PR-1's reconciliation counts — 'N rows aren't tax-ready yet'; documents count; intake-queue count); nine stage tiles registered dark, rendering as link tiles the moment their module flips; per-feed graceful degradation with LoadFailedState only on total failure. Admin 'Dashboard tiles' section per spec (label · stage · module · computed state · suppress toggle; flags created lazily on first suppress). §D: balances hero relabelled 'Accounts net — excludes property' (label states its basis; zero calc changes). 8 law tests (module-off ⇒ hidden regardless of toggles · suppress-only · fail-closed · registry-driven rows · kept-route hrefs). Scoreboard surfaces modelled in the Neomatrix with their sources' semanticKeys (A3). Honest deviations surfaced: portfolio tile shows net worth not equity (no server-side portfolioEquity producer; a tile-side derivation would be a second producer); Stitch artefact backfill DUE per §18.2.1 at the MODULE_HOME acceptance pass.
- 2026-08-19 · Code (Fable 5) · **M3 PR-1 — the D-12 pack fix (§A of BRIEF_M3_PACK_AND_SCOREBOARD.md, changesNumbers: YES).** Booted on the Ring-3 FAIL verdict (#1595 comment): perProperty:[] on 387 tx, 8/387 rows in ATO labels, transfers inside totals — all three root causes re-verified in source before code. MON-168: producer census found 16 link-write sites; the new Ring-1 guard test (`mon168PropertyStampGuard.test.ts`) then caught a 17th (`duplicateMerge.ts` repoints without re-stamping) — all 17 now stamp `propertyId` via the ONE resolver (`lib/bookkeeping/propertyLink.ts`), 5 clear-sites null it, the PATCH editor derives it, and the admin backfill route ships dry-run-by-default + idempotent (§12.11 answers in the PR). MON-169/170: `buildTaxPackSummary` rewritten property-scoped — every window row classified into exactly one of included/transfers/loanRepayments/notPropertyScoped, the identity `included + Σexcluded = total` HARD-ASSERTED in code and printed in the XLSX/PDF, label-path `continue`s became counted buckets (`atoLabelling`), the per-tx mapping N+1 killed. Ring-0 worked example locked (7-row fixture, 5 tests). D-21 honoured: expected movements written FIRST in `RING3_M3_PACK_FIX.md` (P0-P7 + mustNotMove; the dry-run JSON captured before apply is the movement record). Neomatrix: the D-12 pack modelled (engine + packIncomeGross/packExpenseTotal + reconciliation-identity law + export surface + resolver engine) — the census-correction blind spot closed. Census phantom note: a `bucket` arrow helper minted insuranceAdequacy 15→16 (the PR-2 function-unit class); fixed by inlining + renaming labelCoverage→atoLabelling ('coverage' matched the insurance signature) — ratchet green, no reseed. VERIFIED for all three gates on the Ring-3 run; M2.5 unblocks on its PASS.
- 2026-08-19 · Code (Fable 5, session pre-dating this plan) · #1590 built (P2 gate: MON-160 dynamic-gating fix · P2.3/P2.4/P2.6 · close-out ticks in the old plan · §13.6/§7 amendment · set-module-flags.mjs; suite 4,509 green) + #1591 built (R0 overrides, stacked; layout-user deviation surfaced; suite 4,523 green). Both DRAFT, awaiting Reza.
- 2026-08-19 · Matrix HQ (Fable 5) · Deep market/user/analogue research (3 agent sweeps); v1 focus ruled D-10…D-18 (identity · AI propose→confirm · pack-not-portal · nudges · no-planning-side-door · one doc · dashboard-as-scoreboard · living-plan gate reviews · perfect-what-exists build law); §3.5 pain map rows 1-8 research-ranked + rows 9-12 Reza-added + §1 automation + build laws; §0 boot protocol; M4 under D-18 HOLD; GATE REVIEW ritual on every milestone; plan created at `380a526a`; then #1590/#1591 discovered and folded into the M1 cursor with merge order #1590→#1591→#1592.
- 2026-08-19 · Matrix HQ (Fable 5 → Opus 5) · **M1 CLOSED.** R0 acceptance PASS on live PROD (override holder sees `/dashboard/tax`; other account + signed-out 404; teardown verified from all three viewpoints — #1591 comments) · MON-160 verified live (flip → page + nav within cache window, no redeploy) · M1.4 dashboard inventory run at `bea895f1` and recorded under M3.4 (verdict: old Home not flip-on-able — 7 dead links, gated `wealth-graph` widget, household/CFO-scoped data APIs) · M1.7 gate review recorded with 5 upgrade proposals · MON-161 + MON-162 raised for registration · `BRIEF_M2_CORRECTNESS.md` cut. Cursor → M2, the launch gate.
- 2026-08-19 · Code (Fable 5; Opus §E sweep subagent) · **M2 PR-3 — the depth sweep + fixes (changesNumbers: NO).** 49 findings at `be6ad808`, catalogued in `M2_DEPTH_SWEEP_CATALOGUE.md`. Fixed in-PR (11): onboarding steps module-gated fail-closed (the six hidden-module steps a v1 user was walked through are gone) · shared LoadFailedState on properties/assets/documents/depreciation (an outage no longer claims 'you have no data') · detail-page Edit deep-link (?edit= honoured by the list) + real confirmed Delete · receipt-confirm links evidence to the PROPERTY (the dead resolveAutoLinks chain bypassed) + idempotent re-confirm · activity fresh-vs-filtered empty state with Import CTA · reports empty state + error-parse guard · depreciation breadcrumb · pack comment. Registered (12): MON-168 (propertyId never stamped — pack per-property P&L can be EMPTY), MON-169 (transfers counted), MON-170 (labels drop unmapped), MON-171…179. CENSUS CORRECTION: the D-12 pack was outside M2.1's scope — handout amended; M2.5 blocked on the pack fix PR + its Ring-3. Sweep finding #38 corrected in review (documents family deliberately rides report.* permissions — not a defect). ALSO this window: the CI playwright hang isolated to the apt half of --with-deps (39s → 40-min stall, twice) → #1597 hardening (job timeout, per-attempt kill+retry with dpkg recovery, apt timeouts, browser cache), authorized by Reza lifting the workflows hard line for that one PR.
- 2026-08-19 · Code (Fable 5; Opus B-1 diagnosis subagent) · **M2 PR-2 (#1595, changesNumbers: YES).** B-1 census at `0eab2e95`: 12-row kept table (above) + two scope corrections (moneyFlowService NOT kept — type-only imports; contextBuilder IS the pack's producer). Four findings registered THEN fixed (§24.2 rule 2): MON-164 pack rows canonical (`annualContribution`: one-off ONCE per salaryBanked/MON-094 convention · recurring actuals-first via `calculateMonthlyAverage` over the same unifiedTransaction join the routes use · declared fallback) — worked examples: rent −54% pack row dead, $11,385 one-off no longer +$136,620 deductions; MON-165 depreciation converged on `calculateDepreciationAnnual` (first-year-only formula deleted from dep page + both pack fetchers; $10k DV@10% 3yrs: $1,024 not $2,000); MON-166 dep-rate ×100 render; MON-167 guard `to:` hole + editorial hero drill links gated by moduleKey. MON-129 kept slice: entityBreakdown + propertyCashflow declared-rent one-off gates (isRecurring on CashflowIncome). MON-145: diagnosed schema-bound (no honest no-schema fix; rate-from-repayments points the wrong way per VR-046) — M2 slice = honest basis label on the detail tax card; dated-rate model queued as a Reza decision. MON-146 HELD (hidden-only). M2.4 both dialog blocks dead. Ratchets: 7-test suite + census reseed (5 quantities fell) + source-lock −4 exceptions + 5 pack lineage edges modelled. Suite 4,539 green.
- 2026-08-19 · Code (Fable 5) · **M2 opened — PR-1 (§A+§C+§D+§B-5, changesNumbers: NO).** §D FIRST per kickoff order: MON-163 dead links killed — the property-detail page's five hidden-module links gated on their target flags, and the repo-wide guard (`tests/featureFlags/deadLinkGuard.test.ts`: kept-reachable import-graph walk from every non-gated route file, registry-derived prefixes, module-awareness check) surfaced the FULL class: 8 kept-reachable files / 14 link sites (PropertyTile sell-what-if, PropertyExpensesCard 'View all in Spending', LoanDetailDialog what-if panel, LinkedDataPanel CTAs, TrailStagePill de-linked to badge, HelpDrawer + help layout portal links, recurring View-Expense action, EntityStrategyTab self-gated) — all fixed; hidden-only files exempt via the reachability walk and auto-join when their module's registry entry drops at its R-stage. §A: all six M1.3 carry texts landed (STATE.md pointer, 0·SIMP → pointer, old plan frozen+banner+§1 pointer, CLAUDE.md boot line, hub bump, MON-162 registered). §C: MON-161 fixed at the ONE middleware chokepoint (`Cache-Control: no-store, must-revalidate` on every registry-derived gated prefix; Edge-safe pure-data import; kept routes untouched, locked by test). §B-5: MON-160 → VERIFIED citing the M1.5 live flip. Also carried into main: the Matrix's unmerged plan-v2/brief-v2 commits (D-19/D-20/M2.0/M2.6/MON-163) — they existed only on the merged PR's branch.
- 2026-08-19 · Matrix HQ (Opus 5) · Reza rulings: **D-19** stage-aware dashboard tile registry (module-derived, suppress-only toggles — no second flag vocabulary) · **D-20 depth before surface** (kept-surface depth outranks any hidden-module work; hardens the P0.2 freeze) · P-4 and P-5 adopted as recommended (no NetWorth reuse until the census; cookie sessions parked). M2 gained **M2.0 scoping law** and **M2.6 depth sweep**; gate review renumbered M2.7; M3 gained M3.6 (depth backlog). **MON-163 found by static scan and confirmed live: the kept property-detail page links into three hidden routes (404s in PROD today)** — the P2.1 sweep never checked the kept→hidden direction. Brief rewritten accordingly.
- 2026-08-19 · Matrix HQ (Opus 5) · **M2.2 Ring-3 on the pack: FAIL — the gate held.** Both packs exported for FY2025-26 and parsed: D-12 pack perProperty `[]` on 387 txns, 8/387 rows in ATO labels ($6,830 of $192,933), $1,000 transfer inside totals; legacy report = calendar-YTD window counting salary/ATO-refunds/gifts as taxable income (packs disagree by $271,546) and exporting hidden entity data. MON-168/169/170 root causes independently re-verified in source. PR-2 legacy fixes verified good. Verdict + `matrix-result/v1` on #1595. Rulings relayed to Code: hide legacy tiles · MON-145 defer with stated limitation · playwright required after 3 consecutive greens.
- 2026-08-22 · Matrix HQ (Fable 5) · **The pack fix VERIFIED — Ring-3 PASS** (#1601 comment): backfill dry-run 293/46 with per-property preview → Reza's apply → idempotence proven (re-run examined 247=293−46, stamps 0) → fresh FY2025-26 export: perProperty 4 entries Σ35≡included · identity 35+39+0+313=387 EXACT, dollars to the cent vs baseline gross · transfers 39/$55,318 out of totals · atoLabelling accounts for every included row (labelled 0 / noAtoMapping 35 — visible, not silent) · property tiles byte-identical (mustNotMove). Findings: ATO mappings unseeded (M3.1 item) · duplicate-Guildford + stray-Thornlands records + 2 orphaned links (register, never auto-fix — Reza confirms one Guildford; Thornlands is a duplex). MON-168/169/170 → VERIFIED.
- 2026-08-22 · Matrix HQ (Fable 5) · **Next Code brief cut: `BRIEF_M3_PUNCHLIST_AND_CLOSEOUT.md`** (same-turn plan PR per the amended §0). Every acceptance finding root-caused in source before briefing: intake tile fetches `review-queue` with no `band` param — the route 400s, so "—" renders for every account (`ScoreboardClient.tsx:103` vs `route.ts:31`) · EOFY tile reads only the current-FY window (no `fy` param) — "All rows Tax-ready" is vacuous on near-empty FY2026-27 · portfolio LVR has TWO live producers (snapshot `gearing.portfolioLVR` all-liabilities/property-value vs properties-page owned-only screen arithmetic, 41.3 vs 40.8) · strip is a bare `slice(0,4)`. ATO labelling zero-match DIAGNOSED: `SYSTEM_TAX_MAPPING_SEEDS` (~60 AU defaults) exists and `seedSystemMappings` runs on every export — the failure is exact-triple resolution (`level1|level2|subcategory` vs seeds registered at subcategory-null) → brief orders hierarchy-fallback at the ONE producer + seed vocabulary extension, D-21 movement confined to the atoLabelling partition (totals byte-identical). NEW exposure find: **the MODULE_HOME flip changed the key's meaning** — legacy Financial Overview report tile resurfaced and `/api/money-flow` is now open; brief §D re-keys both + hides the legacy Tax-Time tile per the standing ruling + adds a re-key guard test. §E: M2.5 close-out + MON-168/169/170 registry flips.
- 2026-08-22 · 🧑 Reza + 🟩 Matrix · **First two module returns shipped: MODULE_HOUSEKEEPING + MODULE_HOME ON** (live switches, no deploy — MON-160/161 machinery holding). **Scoreboard ACCEPTED on Reza's data** with the 4-item punch list (M3.4 note). D-21 row added to §2 at this catch-up (ruled 2026-08-19, recorded in the brief; the plan table lagged — drift closed). Cursor → M3 remainder + M2 residue (T2 Ring-3, M2.5 close, M2.7 gate review).
- 2026-08-22 · Code (Fable 5) · **Punch-list PR-1 (BRIEF_M3_PUNCHLIST_AND_CLOSEOUT §A+§C-1/2/4+§D, changesNumbers: NO).** §A: MON-180…186 registered (185 = data findings with the Reza runbook, register-only; 182/184 DIAGNOSED for PR-2) + MON-168/169/170 → VERIFIED citing the Ring-3 PASS (#1601). §C-1: EOFY tile rule extracted to `lib/dashboard/scoreboardDisplay.ts` — honest 'No property rows yet this FY' on an empty current FY, and in the Jul–Oct season the tile also fetches the just-ended FY (existing `?fy=` param, same producer) and leads with it. §C-2: intake tile fetches band=medium + band=low on the SAME route and sums; 0 renders as 0, '—' only on genuine failure. §C-4: strip renders ALL properties, worst monthly figure first (stable sort, no mutation) — `slice(0,4)` deleted. §D: Financial Overview tile MODULE_HOME→MODULE_CFO · Tax-Time tile (was UNKEYED) →MODULE_TAX · `/api/money-flow` MODULE_HOME→**MODULE_ENTITIES (deviation from the brief's MODULE_HOUSEHOLD recommendation, flagged for Reza: the route's own contract is the per-entity money-flow shape for the entities canvas; no household surface ever consumed it)** · MON-186 guard test pins the reports-tile keys + the money-flow gate to an explicit expected map (P2.3 test re-cut to the new law). Census ratchet green (no phantom); neomatrix Layer-0 allowlist +3 (graphify unavailable in-session); no producer touched anywhere — every §C fix is display-window/param/order only. 
- 2026-08-22 · Code (Fable 5) · **Punch-list PR-2 (§B + §C-3 + §E, changesNumbers: YES).** D-21 honoured: `RING3_M3_PUNCH_FIXES.md` committed BEFORE any fix code (totals/identity/perProperty byte-identical; movement confined to the atoLabelling partition; LVR 41.3→40.8 on the scoreboard, page unchanged). §B: hierarchy fallback `(l1,l2,sub)→(l1,l2)→(l1)` at the ONE lookup in `buildTaxPackSummary` + `SYSTEM_TAX_MAPPING_SEEDS` extended with the link-route enum vocabulary (RENTAL/RATES/INSURANCE/UTILITIES/MAINTENANCE/STRATA/LAND_TAX/LOAN_INTEREST/PROPERTY_MANAGEMENT → rental-schedule lines; **MODIFICATIONS and RENT deliberately stay unmapped** — capital-vs-repair is the agent's call, and RENT collides across the income/expense enums). Ring-0 fixture extended to 9 rows (a (l1,l2)-fallback label + a genuinely-unmapped row still counted). §C-3: `lib/calculations/portfolioLvr.ts` is THE producer (owned-only, attached principal, producer-owned 2dp rounding — MON-154 lesson); the snapshot's all-liabilities inline basis and the page's screen arithmetic DELETED; both surfaces read the engine and name the basis ('Portfolio LVR — owned'); Ring-0 worked example + Ring-1 one-producer source guard. Census: lvrGearing back AT seed — the +1 lib producer offset by removing a text-match phantom (`const lvr = calculateLVR(...)` consumption locals renamed; never a reseed). Neomatrix: engine + `number.portfolio.ownedPortfolioLvr` + hero surface modelled, pack worked-example refreshed, 4 anchored-file hashes re-pinned, parity KNOWN_UNRESOLVED +1 with growth path. §E: M2.5 CLOSED with per-condition evidence (see the M2.5 box); hidden-family LVR producers (portfolioEngine:493 · health metricAggregation:402 · testing exporter:334) recorded HELD under D-20, untouched. 
- 2026-08-25 · Code (Fable 5) · **Merge observation (§0: the observing session records).** 🧑 Reza merged #1605 (02:54Z) and #1606 (03:05Z); production deploy `dpl_5n4yW4…` READY — the punch-list fixes are LIVE on PROD. In between: #1606's Vercel build failed once on a stale grandfathered financial-math baseline anchor (import shifted snapshot-route entry 885→886; re-pinned, count unchanged, no new debt) and the branch was updated to main post-#1605 (auto-retarget held). §17.2 deploy checks done; runtime-log comparison deferred to the armed check-in (fresh deploys, zero traffic at 3am). Hand-back to the Matrix is live: `RING3_M3_PUNCH_FIXES.md` is runnable now.
- 2026-08-25 · Matrix HQ (Fable 5) · **Ring-3 `RING3_M3_PUNCH_FIXES.md` run account-first on Reza's live data — all 17 checks PASS; envelope verdict PARTIAL solely for the un-runnable authenticated `/api/money-flow` 503 clause** (Bearer-only route; unauth GET observed 401 serving no data; gate Ring-1-verified — recorded in sectionsNotRun, validated `matrix-result/v1` on #1606). Evidence highlights: totals/identity/perProperty byte-identical to #1601 · labelled 0→23/$13,864.90 with 21Q/21S/21V/21F/21M populated and Σ≡labelled.amount exact · noAtoMapping residue explained to the cent (MODIFICATIONS ×8 deliberate · expense OTHER ×1 outside vocabulary — seed decision for Reza · 3 income rows consistent with deliberately-unmapped RENT) · LVR 41.3→40.8 on the scoreboard, banner unchanged, both basis-labelled, hand-check exact · EOFY tile ≡ pack noAtoMapping (12) · intake 48 · strip 6/6 worst-first · legacy report tiles gone · property tiles + scoreboard figures byte-identical (mustNotMove). **MON-180…184 + 186 → VERIFIED** (registry flip = next Code PR). SAME SESSION, 🧑 Reza directive: **all tiles/UI-UX go through Stitch** — the §18.2.1 scoreboard backfill (recorded DUE at M3 PR-2, honestly flagged but not yet executed, and now spanning the punch-list changes) is OVERDUE → first item of the next Code slice; future tile work Stitch-FIRST.
- 2026-08-25 · Matrix HQ (Opus 5) · 🧑 **Reza ruled four open questions (all as recommended)** — Stitch pass owned by Matrix with Reza's nod gating Code · the `OTHER` $130 row stays honestly unmapped · `/api/money-flow` keeps MODULE_ENTITIES (brief recommendation withdrawn) · Matrix proceeds to the M2.7 gate review. 🧑 **Reza directive → D-22 (refinement-only, depth-first sequencing)**, raised after he caught a real drift risk in the Stitch step: the backfill prompt carried the §18.7.2 "polished tile recipe", which — if Stitch rendered something richer than what ships and Code built toward it — converts a paper-trail backfill into an unauthorised redesign. Scope fence added to M3.4: the artefact records the SHIPPED composition; any delta is a finding for Reza. **Stitch tooling BLOCKED and recorded honestly:** two generations attempted, both timed out at the 60s MCP boundary with no retrievable screen (in-app project `5991501424852019479` enumerates ZERO screens via `list_screens` despite `metadata.inapp-wealth.json` naming several; canonical `1859462351962811110` enumerates 82 and did not gain a scoreboard). No design was invented to work around it — the backfill waits for working tooling rather than shipping a guess. Evidence sweep this session also surfaced a **plan-vs-ledger discrepancy now recorded on the M2.2 box for M2.7 to settle:** the plan reads "T2 Ring-3 still pending", but `MON-131_TRANCHE_LEDGER.md` records T2's **G8 Ring-3 as ✅ CLOSED** on VR-047 + VR-047B (2026-08-03/04) with MON-143 and MON-130 both VERIFIED on that evidence — the live question is whether M2.2 means something different today, since the surfaces those runs read are now hidden.
- 2026-08-25 · Matrix HQ (Opus 5) · **M2.7 GATE REVIEW — ⛔ the M2 launch gate DOES NOT CLOSE** (`docs/strategy/M2_7_GATE_REVIEW.md`; the first P-11 satellite doc). Evidence-audited, nothing recalled. **B1 — NEW live-number defect on the most important kept surface:** `resolveLoanMonthlyCost` nets the offset (`lib/calculations/propertyCashflow.ts:246`) but **no `offsetBalance` is passed anywhere under `app/dashboard/properties/`** — both call sites (`page.tsx:496`, `[id]/page.tsx:180`) hand `computePropertyCashflow` loans without it, so the properties list, the property detail page and the scoreboard cashflow strip compute on the FULL balance while Guildford carries a $303,889.96 offset. Unregistered; same class as MON-151. No number is claimed — the re-scoped Ring-3 confirms or clears it. **B2 — M2.2 mis-stated:** T2's G8 Ring-3 genuinely PASSED (ledger `:419`, VR-047 + VR-047B), but on Home's budget tile and `/dashboard/expenses`, both unreachable today (MODULE_HOUSEHOLD hidden; HomeClient has ZERO importers); the kept property surfaces use a different producer that has never been Ring-3'd, and the one kept consumer of the verified leg (`/dashboard/activity` Sankey) is absent from VR-047's verified list. **B3 —** 0 of 10 M2.6 LIVE-CHECK items executed; 3 incidentally covered by the pack Ring-3s, **7 outstanding** (pack generation time · Vision/OCR in PROD · empty-state-on-failure · mobile bottom bar 375px · fresh-account onboarding · Cash FAB→balances · global-FAB receipt→documents). **B4 —** of 39 OPEN/FIXING critical+high, **21 cite KEPT surfaces** (12 hidden-only, 6 mixed); of the 7 criticals NONE is hidden-only; kept highs include a crash (MON-087, Radix `Select.Item` empty value in the property-context Add Expense dialog). **Market re-check:** our TaxTank $15/mo anchor STANDS (the new "from $6" headline is the non-property tanks) — new mechanic recorded: 5 properties included, **+$36/property/yr** beyond, i.e. **$216/yr** on Reza's six; propkt now running category-comparison content. **Regulatory:** the ATO reform page is now published but HIGH-LEVEL — per-property-vs-pooled quarantining **still unaddressed**, deemed disposal **not mentioned at all**, no record-keeping guidance; cost-base indexation confirmed **plus a 30% minimum CGT rate we had not recorded**. Two of our four §7 reform claims are therefore secondary-source only → P-8. Six proposals raised (P-6 re-scope M2.2 · P-7 rule depth-sweep #19 · P-8 correct §7 · P-9 batch the live-checks · P-10 one kept-depth Code brief · P-11 satellite docs), none actioned unilaterally per D-18/D-22.
- 2026-08-25 · Matrix HQ (Opus 5) · 🧑 **Reza: GO on all six gate-review proposals (P-6…P-11).** **P-6 executed the same day** — `RING3_T2_KEPT_LOAN_COST.md` committed BEFORE the run (D-21 discipline applied to a verification, with the hypothesis stated falsifiably in both directions), then run account-first on Reza's data. **Verdict #1612: 8/8 checks PASS, PARTIAL** (K6 the activity Money-Flow Sankey did not render — consistent with Lever 2 having stripped that widget, which if confirmed means the leg VR-047/047B verified has NO kept v1 consumer at all). **B1 CLEARED for the cashflow number:** the property detail renders `Bankwest repayment −$2,790` and the canonical offset-aware leg returns `$2,789.71` — they agree because the ACTUALS path binds ($2,789.71 sits above the $384.45 interest floor; `flooredToInterest false`), NOT because the offset arrives. **B1's mechanism confirmed and reclassified as LATENT:** `/api/properties` carries Guildford's loan with `offsetBalance` FIELD_ABSENT and `resolvedCost` FIELD_ABSENT, so any loan whose interest floor binds renders wrong — and the floor path is already reachable in this account (the HECS loan shows `flooredToInterest true`). **THE BIGGER CATCH — MON-151 confirmed live and quantified on a KEPT surface:** the property-detail Tax position card computes interest on the FULL balance, ignoring Guildford's **$303,889.96** offset — **$23,576.09/yr charged vs $4,613.35/yr canonical, overstating interest by $18,962.73/yr**. The card's own figure proves the basis (rent $26,089 − $23,576.09 = $2,512.91, rendered "$2,513/yr"); offset-netted it would read ~$21,476/yr. The implied netted balance works back to an offset of $303,889.96, matching MON-143's Guildford figure **to the cent**, and the no-offset control holds exactly (Broadbeach 228,000×6.69%÷12 = $1,271.10 = canonical `monthlyInterest`). Promoted into the §5 working set for the P-10 brief; NOT fixed in passing. mustNotMove byte-identical ($4,990,000 · $3,401,782 · 40.8% · strip 6/6). Honest lane note recorded in the envelope: the run establishes the figure differs from interest ACTUALLY INCURRED; deductibility is the tax agent's call.
- 2026-08-25 · Matrix HQ (Fable 5) · **P-9 LIVE SWEEP** — handout `RING3_M2_6_LIVE_SWEEP.md` committed BEFORE the run (D-21); account-first on Reza's session (two sign-in false starts: the API is Bearer-token auth — `lib/auth/context.ts:56` — and an unauthenticated probe 401s AND appears to tear the session down; recorded, no more raw fetches). 6 of 7 outstanding items run, verdict **PARTIAL** (envelope validated, on the PR). **S3** pack export PASS — warm run bracketed 2–5s, well under the 10s line. **S4** Vision/OCR CONFIGURED and live ('Powered by Gemini AI'): on a real Bunnings receipt the amount was EXACT ($203.78), the date honestly READ from an internally-inconsistent paper (header misprint 20/03/2026 vs EFT footer 20/08/26 — header day-name proves August), category left empty, nothing auto-saved — but the VENDOR field took a mis-transcribed document-type word ('Invnice' from '** TAX INVOICE **') over the page-dominant BUNNINGS (F5). **S5** answered by a LIVE failure, no injection: portfolio/snapshot + linkage/health 503'd transiently and the scoreboard rendered healthy-looking $0/0.0%/empty-state copy for ≥4s, no LoadFailedState (F6). **S6** 375px PASS — Home/Track/Invest/More all kept targets, More-sheet kept, no overflow. **S8** MON-172 CONFIRMED + catalogue #40 confirmed (cash quick-add: no account selector, no property attach; no Cash Wallet row on balances). **S10** verified to the intake boundary: 6.2MB upload → 413 behind the UI's '10.0 MB' promise (F7, ~4.5MB platform body cap) surfaced as a raw JSON-parse error (F8); 2.7MB → 201 → analysed into Smart Inbox. **THE HEADLINE (F1–F3): the Smart Inbox is display-only** — checkboxes/Select-all never register on desktop OR mobile (Reza hit it first, Matrix reproduced both viewports), Approve permanently at '0 selected', and pencil-edit Done reverts on reload (two clean attempts). The intake counters can never clear; Reza's real Bunnings expense is stranded pending. **F4:** the vault folder tree count is exactly 2× the true count and increments +2 per upload (16=2×8 → 18=2×9 across the test upload) — the hero and dashboard tile agree on the truth. **K6 CORRECTION (#1612):** the activity 'Where your money goes' widget RENDERS on both viewports with live figures (income $304,159 · loans $153K 50% · surplus $95K 31%) and fires NO /api/money-flow request — the kept consumer exists, on a different producer; #1612's 'no kept consumer' inference is withdrawn. **S7 deferred by Reza** ('let's fix this issue first and then I will create a user') → runs after PR-1, Reza typing credentials. mustNotMove held; the single write is the authorized test document, named in the envelope.
- 2026-08-25 · Matrix HQ (Fable 5) · **P-10 BRIEF CUT** — `docs/strategy/BRIEF_M2_KEPT_DEPTH.md`, sequenced on Reza's direction: **PR-1 the intake pipeline first** (F1/F2/F3 + upload truth F7/F8 + vendor extraction F5 + count F4; Code assigns MON ids and registers all ten findings in the same PR) · **PR-2 the loan-cost truth** (MON-151 quantified fix through the canonical offset-aware leg + threading `offsetBalance` into `/api/properties` — no second producer, D-22) · **PR-3 kept display + sweep highs** (F6 skeletons/LoadFailedState · MON-087 crash · MON-171…178 · F9 · F10). Each PR carries its Ring-3 predictions written before the code; VERIFIED flips only on the Matrix's PASS. Sequencing law in the brief: PR-1 ships alone and first.
