{
  "_note": "Files deliberately excluded from Layer 0 (Graphify) structural coverage, each with a VERIFIED reason. The completeness gate treats these as covered. Keep this list SHRINKING and reviewed — every entry is a known graphify-0.8.45 limitation, NOT a financial calc engine (CLAUDE.md Part 22 / docs/blueprint/NEO_INVENTORY.md §8). Re-verify on each graphify upgrade.",
  "files": {
    "lib/gcp/credentials.ts": "graphify 0.8.45 skips files whose NAME matches its secret-file filter — VERIFIED: a byte-identical copy under a neutral name IS extracted, the original 'credentials.ts' is not. Auth/infra (GCP credential resolution), no financial calculation. Re-test on graphify upgrade.",
    "lib/share/tokens.ts": "Same graphify secret-name skip as credentials.ts (name contains 'tokens'). Share-link token helpers, no financial calculation.",
    "app/api/entities/[id]/accounting/snapshots/route.ts": "graphify 0.8.45 drops this in the full-corpus merge via node-id collision (the GET handler's symbol id collides with an identically-named symbol in another route.ts) — VERIFIED: the file extracts cleanly (3 nodes) when graphify runs on its directory in isolation; sibling [id] routes are all covered. A route handler, no financial calculation. Candidate for NI-2 collision-handling improvement.",
    "lib/health/buildHealthInput.ts": "MON-030 stage 1 (2026-07-12): the canonical FinancialHealthInput builder, EXTRACTED verbatim from the duplicated copies in app/api/financial-health/route.ts + app/api/dashboard/insights/route.ts (§12.2.1). It is a data-assembly SERVICE (fetches + maps to FinancialHealthInput), NOT a calc engine with new formulas — the scoring engine it feeds (lib/health/aggregateEngine.ts) is already modelled. The offline graphify tool that populates structural-graph.json is unavailable in this session; allowlisted pending the next graphify run, which will cover it and make this entry redundant (self-prunes). No new financial formula introduced.",
    "lib/calculations/propertyActualsWindow.ts": "MON-035 (2026-07-14): the ONE canonical property-actuals transaction WINDOW (a trailing-12-month `gte` date bound, DECISION 2) — pure date arithmetic, NOT a financial calc engine (it holds no money formula; the cashflow engine it feeds, propertyCashflow.ts, is already modelled). It exists to make the window single-source across the three fetch sites (propertyActuals / masterFinancialService / portfolio-snapshot). The offline graphify tool is unavailable in this session; allowlisted pending the next graphify run, which will cover it and self-prune this entry.",
    "app/dashboard/investments/page.tsx": "MON-046 (2026-07-14): a trivial Next.js redirect landing page (`redirect('/dashboard/investments/accounts')`) that fixes the bare-route 404 — NO financial calculation, no money formula, no rendered number. The offline graphify tool is unavailable in this session; allowlisted pending the next graphify run, which will cover it and self-prune this entry.",
    "lib/assets/valueChange.ts": "MON-041 (2026-07-14): a pure PRESENTATION helper for asset value-change — maps the signed `depreciation` to a direction enum (appreciation/depreciation/unchanged) and returns the display MAGNITUDE (Math.abs of an already-computed percent). NOT a financial calc engine — it holds no money formula and produces no monetary value (the depreciation/percent are computed upstream in app/api/assets/route.ts). It exists to make the 'never show -200% depreciation' display rule single-source across AssetTile + the Assets page dialogs. The offline graphify tool is unavailable in this session; allowlisted pending the next graphify run, which will cover it and self-prune this entry.",
    "lib/properties/activityFrequencyLabel.ts": "MON-048 (2026-07-14): a pure PRESENTATION helper for the property Cashflow-rhythm cadence badge — returns 'One-off' when isRecurring===false else the humanised declared frequency string. NO money formula, no monetary value (pure string label). Exists to stop the rhythm list showing one-off costs as 'Monthly'. The offline graphify tool is unavailable in this session; allowlisted pending the next graphify run, which will self-prune this entry.",
    "lib/properties/propertyCashflowVisibility.ts": "MON-039c (2026-07-14): a pure PRESENTATION-VISIBILITY rule — returns a boolean for whether a property surface shows its 'Cashflow / yr' figure (true for INVESTMENT, or any property with rental income incomeCount>0). NO money formula, no monetary value (the cashflow number itself is computed upstream by computePropertyCashflow, already modelled). Exists to make the show/hide rule single-source across the property list tile / detail / Home tile so the same number isn't present on 2 of 3 surfaces. The offline graphify tool is unavailable in this session; allowlisted pending the next graphify run, which will self-prune this entry.",
    "lib/income/unmatchedDeclaredIncome.ts": "MON-043 (2026-07-14): a PRESENTATION aggregation for the income data-completeness nudge — sums the GROSS annual (via the canonical toAnnual) of declared income rows that have NO matching transactions, to surface the declared-vs-actuals gap. NOT a canonical financial engine: it feeds NO downstream calc (the tax/net-worth engines read the income rows directly, never this sum); it introduces no new money formula (reuses toAnnual). Exists to make the nudge's 'unmatched declared income' figure single-source. Ratchet: tests/income/unmatchedDeclaredIncome.test.ts (§19.2 worked example). The offline graphify tool is unavailable in this session; allowlisted pending the next graphify run, which will self-prune this entry.",
    "lib/tax-engine/deductions/propertyLoanInterest.ts": "MON-045 STAGE 1 (2026-07-14): the canonical DEDUCTIBLE property-loan-interest calc (actuals-first Σ INTEREST_CHARGED → (principal−offset)×interestRateAnnual×deductibleFraction; reform-INDEPENDENT — the reform gates the loss-offset, not interest deductibility). This IS a financial calc, but it is UNWIRED in stage 1 — no engine consumes it yet (stage 2 injects it into calculateTaxPosition.deductions.property and DELETES the four rogue neg-gearing producers). Its SEMANTIC Neomatrix node + lineage (feeds deductions.property; fed by Loan.principal/interestRateAnnual/offset/deductibleFraction + the INTEREST_CHARGED ledger) lands in the stage-2 wiring PR, where it has real edges. Ring-0 §19.2 proof already exists: tests/tax/propertyLoanInterest.test.ts (9 cases incl. offset/deductibleFraction/actuals-vs-theoretical). Allowlisted for Layer-0 structural coverage only (graphify offline this session); NOT a claim that it is non-financial — it is a financial calc pending its stage-2 semantic model. Self-prunes on the next graphify run.",
    "lib/intake/classifyIntake.ts": "MON-078 KEYSTONE (2026-07-16): the ONE canonical intake classifier (frequency/recurrence/stream-reuse decisions for every Income/Expense producer — docs/architecture/INTAKE_INTEGRITY_GUARDRAIL.md §2). Allowlisted for Layer-0 structural coverage only (graphify offline this session — self-prunes on the next graphify run). NOT a financial calc: it produces classification decisions, never money numbers; its correctness is pinned by tests/intake/classifyIntake.test.ts (Ring-0) + the R1 source-lock tests/intake/intakeSourceLock.test.ts (Ring-1).",
    "lib/intake/detectors.ts": "MON-001 D2 (2026-07-16): the standing cadence-mismatch detector (stored frequency vs the cadence the row's linked transactions imply — INTAKE_INTEGRITY_GUARDRAIL.md §4 R2). Allowlisted for Layer-0 structural coverage only (graphify offline — self-prunes on the next run). NOT a financial calc: it flags a review nudge, never produces a money number; cadence itself comes from the ONE canonical detectFrequency (lib/utils/reconciliation.ts). Proven by tests/intake/detectors.test.ts.",
    "lib/calculations/rentalReconciliation.ts": "Phase 59 / MON-079 (2026-07-16): the ONE canonical managed-rental reconciliation engine (gap = cadence-normalised declared gross − net disbursement). This IS a financial calc — and it is FULLY modelled in the same PR: semantic node engine.rentalReconciliation.reconcileManagedRental (verified file:line) + R0 calc-audit fixtures (property.managedRentalGap) + R1 source-lock. Allowlisted for Layer-0 STRUCTURAL coverage only (the offline graphify tool is unavailable in this session); self-prunes on the next graphify run.",
    "lib/calc-audit/engines/rental-reconciliation.ts": "Phase 59 / MON-079 (2026-07-16): R0 fixture REGISTRATIONS for the managed-rental gap engine (calc-audit adapter — proof spine, not a calc engine itself; the engine lives in lib/calculations/rentalReconciliation.ts, modelled + proven in the same PR). Allowlisted for Layer-0 structural coverage only (graphify offline this session); self-prunes on the next graphify run.",
    "lib/services/managedRentalService.ts": "Phase 59 / MON-079 (2026-07-16): the suggest-and-confirm DECISION service — fetches stream context (rule / sibling dates / existing derived row) and applies fire-vs-silent; ALL arithmetic comes from the ONE engine lib/calculations/rentalReconciliation.ts (modelled + proven). NOT a calc engine — no money formula of its own. Allowlisted for Layer-0 structural coverage only (graphify offline this session); self-prunes on the next graphify run.",
    "app/api/rental-reconciliation/route.ts": "Phase 59 / MON-079 (2026-07-16): the single LOCKED producer of derived agent-cost Expense rows (R1 source-lock tests/tax/rentalReconciliationSourceLock.test.ts) — recomputes via the ONE engine and persists; no formula of its own (thin route per §12.3). Allowlisted for Layer-0 structural coverage only (graphify offline this session); self-prunes on the next graphify run.",
    "lib/neobrain/rentalStatement.ts": "Phase 59 / MON-079 (2026-07-16): the Neobrain rental-statement parser + GROUNDING validator (never emit a figure not on the statement; gross − Σ items must equal net). NOT a calc engine — perception + validation; the money maths lives in the modelled lib/calculations/rentalReconciliation.ts. Proven by tests/neobrain/rentalStatement.test.ts. Allowlisted for Layer-0 structural coverage only (graphify offline this session); self-prunes on the next graphify run.",
    "lib/services/loanCosts.ts": "graphify CLI offline in this build environment (spawnSync graphify ENOENT — same as prior sessions); SELF-PRUNE: remove this entry and run `npm run neomatrix:graphify` in a graphify-capable session. The file is fully modelled SEMANTICALLY (orchestrator.loanCosts.resolveLoanCostsForUser, verified file:line + edges) — all math delegates to the already-covered engine.propertyCashflow.resolveLoanMonthlyCost. Added 2026-07-17 (VR-013 fix).",
    "lib/calculations/liquidCash.ts": "NEW canonical producer (MON-031/064 VR-017 re-fix, 2026-07-19) — graphify CLI is offline in this build environment (ENOENT; same limitation recorded for prior new files). The file IS fully modelled in Layer 1 (engine.liquidCash.computeLiquidCash, verified file:line + worked example + 3 test suites). SELF-PRUNE: remove this entry the next time `npm run neomatrix:graphify` runs successfully — the structural graph will then cover it.",
    "lib/intake/duplicateMerge.ts": "Mechanism A Part 2 (2026-07-19) — graphify CLI offline in this build environment (ENOENT; same recorded limitation). Fully modelled in Layer 1 (engine.intake.findDuplicateGroups, verified file:line + 7-test suite). SELF-PRUNE: remove on the next successful `npm run neomatrix:graphify` run.",
    "app/api/intake/duplicates/route.ts": "Mechanism A Part 2 (2026-07-19) — graphify CLI offline in this build environment (ENOENT; same recorded limitation). Fully modelled in Layer 1 (thin route over engine.intake.findDuplicateGroups — no financial math; §12.3 wrapper). SELF-PRUNE: remove on the next successful `npm run neomatrix:graphify` run.",
    "app/admin/intake-duplicates/page.tsx": "Mechanism A Part 2 (2026-07-19) — graphify CLI offline in this build environment (ENOENT; same recorded limitation). Fully modelled in Layer 1 (admin display surface for the engine preview — renders engine output, no financial math). SELF-PRUNE: remove on the next successful `npm run neomatrix:graphify` run."
  }
}