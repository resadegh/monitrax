# Implementation Plan — Active Workstreams

> **Spoke of the Implementation Plan SSOT.** Hub + navigation: [`docs/IMPLEMENTATION_PLAN.md`](../IMPLEMENTATION_PLAN.md).
> What is being built right now. Sorted by priority; top = in flight. CLAUDE.md §15 governs this file.
> Update rules (incl. the per-spoke size budget) live in the hub under "How to update this document".

---

## 🟡 Active Workstreams

### 0·CASHFLOW-ACTUALS. Headline cashflow correctness — actual vs declared (Phase 1)

- **Status:** 🟡 ACTIVE — **Phase 1 shipped**; **Phase 2 SSOT-convergence in flight** (this PR).
- **Started:** 2026-06-22.
- **Owner:** Reza (decisions) + Claude (build).
- **Last touched:** 2026-06-23 — Phase 2 convergence on branch `claude/cashflow-ssot-convergence-shk180`. Reza: *"this is very critical issue … find the correct numbers and source of truth and compare all other numbers based on that."*
- **Phase checklist:**
  - [x] **Audit:** confirmed headline tiles compute from DECLARED records and silently drop uncategorised/unlinked OUT transactions → falsely optimistic surplus/margin/runway.
  - [x] **Phase 1A — foundation:** new pure engine `lib/calculations/actualCashflow.ts` (`computeActualCashflow()`, 10 tests) + new actual fields on master `quickMetrics` (`actualMonthlyOutflow`/`Inflow`/`actualNetCashflow`/`actualAvgMonthlyOutflow`/`actualOutflowByCategory`/`hasActualData`) computed from ALL non-transfer `UnifiedTransaction` (trailing ~4 mo). Declared fields untouched (back-compat).
  - [x] **Phase 1B — repoint 5 worst offenders:** cashflow waterfall + budget-vs-actual (`/api/cashflow/intelligence`), Money-Story kept/margin (`/api/dashboard/insights`), safety-net survivability (`/api/safety-net`), Gemini cashflow narrative (`/api/cashflow/summary`).
  - [x] **Phase 2 — SSOT convergence (root-cause: SSOT documented but never *enforced*):**
    - [x] **Canonical accessor** `lib/calculations/canonicalCashflow.ts` (`getCanonicalMonthlyCashflow` + pure `resolveCanonicalCashflow`) — the ONE place the actuals-vs-declared rule lives (CLAUDE.md §19.1). 4 tests.
    - [x] **`/cashflow` HERO fixed** — `buildForecastSummary` + the cashflow health-score input in `/api/cashflow/intelligence` previously read DECLARED records (the +$10,505 surplus / 51.9% saving-rate fiction) while the SAME page's money-flow waterfall already showed actuals → two numbers on one page. Both now resolve through the canonical accessor.
    - [x] **Emergency-fund tile internal mismatch fixed** — `/api/dashboard/insights` displayed declared `totalMonthlyExpenses` as the "/month" denominator while `monthsCovered` used the actual trailing-avg; now displays `snapshot.emergencyFund.monthlyExpenses` (the engine's own denominator).
    - [x] **Divisor fix** — `actualCashflow.ts` trailing average changed from a fixed `/3` to a **data-driven divisor** (months with NO transactions are missing data, excluded from sum + divisor; a populated low-spend month still counts). Fixes the false $938 emergency figure for recently-connected users.
    - [x] **Enforcement gate** — `tests/calculations/cashflowSurfacesUseCanonical.test.ts` structural guard fails the build if a converged surface drops the canonical accessor.
  - [ ] **Phase 2b — Home page convergence (DEFERRED, needs frontend + Stitch):** the Home "Monthly Cash Flow / Annual Outgoings / Saving Rate" tiles read declared `portfolio/snapshot.cashflow` + `insights.kpiTiles.outgoingsAnnual`. Converging the **headline** to actual breaks the **drill-down** tie-out (it recomposes net from a declared Income−Expenses−Loans breakdown; actuals only give Inflow−Outflow). Doing it right needs a section-level drill-down change (§18.2.1 Stitch-first) — scoped as a separate workstream, NOT a backend-only overwrite.
  - [ ] **Phase 2c — open items:** reconcile the rolling-30-day analytics window vs current-calendar-month (label honestly); trace the `/activity` donut "20% kept" YTD source; remaining declared-only surfaces (budget page, debt-freedom runway, CFO baseline); plan-vs-actual shown explicitly in UI.
- **Risk:** silent semantic shift (kept/margin/surplus now mean "after actual spend"); mitigated by `hasActualData` gate (falls back to declared for no-transaction users).
- **Blocking:** Phase 2b gated on a Stitch drill-down design (drill-down decomposition can't be done backend-only without re-introducing a contradiction).
- **Why this matters:** the headline numbers were lying — a user with uncategorised spending saw a surplus they don't have, and two tiles on one page showed different numbers because SSOT was documented but never enforced. Financial-adviser lens: never quote a number we can't trace to reality; the canonical accessor + enforcement gate make the headline honest *and* keep it honest.

### 0·FIN-LOGIC-INDEX. Financial Logic Index — master reference for all calc engines

- **Status:** 🟡 ACTIVE — **template slice shipped** (hub + Core Calculations spoke, 4 engines); domain-by-domain backfill queued.
- **Started:** 2026-06-23.
- **Owner:** Reza (decisions) + Claude (research + write).
- **Last touched:** 2026-06-23 — hub `docs/financial-logic/00_INDEX.md` + spoke `01_CORE_CALCULATIONS.md` on branch `claude/financial-logic-index-shk180` (stacked on the #1201 cashflow-SSOT branch so it documents the converged state). Reza directive: *"build the document for every phase [after] complete research on the document for that section, the code, understand the logic, then document. Never guess or assume … only to keep a live index and reference … so you can understand Monitrax without guess or assumption."*
- **Why this exists:** the doc-coverage audit (2026-06-23) found ~27% of ~112 financial-engine files have proper inline headers, tax-divisions/CFO-scenarios largely undocumented, and NO canonical engine→file→formula→authority index. Drift (the #1201 cashflow contradiction) happens when a surface is changed without tracing to the canonical source. This index is the anchor an engineer/AI reads before touching any number, and the reference for correctness audits.
- **Hard constraints (Reza):** **documentation only** — never change logic/law/formula/threshold without explicit confirmation; **research-first** — full source read + phase doc + input-unit trace + caller trace before writing each entry; never guess (`⚠️ UNVERIFIED`/`⚠️ SUSPECTED ISSUE` instead).
- **Phase checklist:**
  - [x] Hub + status legend + operating rules (`00_INDEX.md`).
  - [x] **Core Calculations spoke** — net worth, declared cashflow, actual cashflow, canonical cashflow (4 of ~10, each from a complete read + verification evidence).
  - [ ] Core Calculations — remaining (aggregators incl. loan-interest decimal gotcha, money-story trend, asset valuation, net-worth history, entity breakdown).
  - [ ] **Tax engine spoke** (highest risk — divisions/CGT/super, with ATO authority citations).
  - [ ] CFO + scenarios spoke (what-if formula + assumptions).
  - [ ] Intelligence + reports spoke.
  - [ ] Services/orchestration spoke (`masterFinancialService`, portfolio snapshot, emergency fund).
  - [ ] (Phase A follow-up, separate decision) machine-derived registry generator + CI freshness gate so the index can't drift.
- **Risk:** the index documenting stale behaviour if an engine changes without updating the entry — mitigated by the same-PR sync rule (§16) and (future) the CI freshness gate.
- **Blocking:** none for the doc backfill. The generator/CI-gate sub-phase awaits Reza's go-ahead on the Phase-A enforcement approach.

### 0·NEOMATRIX. Neomatrix — Financial Logic Knowledge Graph (Phase 53)

- **Status:** 🟡 ACTIVE — **design spec shipped; build handed to a dedicated new session.**
- **Started:** 2026-06-23.
- **Owner:** Reza (direction) + a dedicated build session (execution). This session authored the spec only.
- **Last touched:** 2026-06-23 — **N0 Graphify trial executed** (build session, branch `claude/eloquent-archimedes-jqahjw`): code-only/offline extraction validated (zero egress), 9/9 `00b` orchestration edges reproduced. Go/no-go report shipped; paused for Reza. Prior: full build spec `PHASE_53_MONITRAX_NEOMATRIX.md` (branch `claude/neomatrix-design-spec-shk180`). Reza named it **Neomatrix**.
- **What it is:** the machine-readable + navigable + auditable evolution of the markdown Financial Logic Index — a typed directed knowledge graph of every calc engine/number/formula/law (nodes) + dependencies/data-flow (edges), with sliceable projections (the "cube" axes: layer / domain / TRAIL / regime) and CI validation so no number can drift from its canonical source without the build failing. Reza's "3D model / chemical structure" in its correct CS form (graph, not literal cube).
- **Architecture (4 layers):** L0 structural skeleton (Graphify auto AST/call-graph — trial first), L1 semantic graph `financial-graph.json` (the canonical model — formulas/authority/units/lineage, research-verified), L2 views (markdown generated from JSON + interactive 2D explorer + optional 3D), L3 CI audit checks (the correctness guarantee). Division of labour: Graphify = bones (auto edges), our graph = meaning (no tool can derive formulas/laws).
- **Phase checklist (for the build session):**
  - [x] Design spec (`PHASE_53_MONITRAX_NEOMATRIX.md`).
  - [x] **N0 — Graphify trial on `lib/` + §13.6 security pre-check** — report [`docs/financial-logic/graph/N0_GRAPHIFY_TRIAL.md`](../financial-logic/graph/N0_GRAPHIFY_TRIAL.md). **Result: GO (conditional, code-only/offline).** Graphify 0.8.45 over `lib/` (539 files) ran with all LLM keys unset → 6120 nodes / 12866 edges, **100% AST-extracted, zero egress** (§13.6 pass); reproduced **9/9** of the `00b §2` verified orchestration call-edges at exact `file:line`; no false core edges. ⏸️ **Awaiting Reza's go/no-go before N1** (and confirm per-phase branch convention).
  - [~] **"Smartest model" enhancements (PHASE_53 §14)** — Tier A spine (**A1 executable law-referenced audit — first slice SHIPPED**: `tests/neomatrix/financialAudit.test.ts` runs the REAL engines against **law-derived** expected values (net worth = assets−liabilities; investment fallback; Phase 39.5 SMSF rule; canonical actuals-vs-declared savingsRate) — 7/7 pass, model refs the code. Reza directive 2026-06-23: audit-as-we-go, model is reference, **never change Monitrax logic without confirmation** — a mismatch → `suspected-issue` raised, not fixed. · A3 convergence-contradiction (live) · A2 drift sentinel + A4 unit edges = N3); Tier B reach (regime layer + law nodes live); Tier C interface (coverage dashboard live; MCP + 3D later). Every audit slice follows the §20.4 3-review/10-10 rule. **A1 tax slice SHIPPED:** modelled + audited `calculateIncomeTax` (core/incomeTaxCalculator.ts:21) + `calculateMedicareLevy` (core/medicareLevyCalculator.ts:38) against **ATO FY24-25 Stage 3 brackets** — 9 cases incl. **bracket-boundary regression locks** ($45,001→4,288.30, $135,001→31,288.37: the historical $0-at-boundary P0 bug, now provably locked), $100k→20,788, $200k→56,138, Medicare 2% → 2,000. All pass; **no suspected-issue** (engine agrees with ATO law). 10/10 §20.4. **A1 GST/Div293/SG SHIPPED:** `calculateGst` (s9-70 10%: $1k sale→$100, net-of-ITC→$50, GST-free→$0), `calculateHighIncomeSuperTax` (Div 293 s293-15: $300k/$30k→$4,500, $260k→$1,500, <$250k→$0), `calculateSuperGuarantee` (SGAA 11.5%: $100k→$11,500, locks the P1 cap fix). All pass; no suspected-issue. 10/10 §20.4. **A1 SMSF Div 295 + CGT discount SHIPPED:** `calculateSmsfIncomeTax` (Div 295: contributions 15%→$15k, investment 15%→$7.5k, NALI s295-550 45%→$4.5k) + `calculateCgtDiscount` (Div 115: individual 50%→$50k, SMSF 1/3→$60k, company 0→$100k, <12mo→$100k). All pass; no suspected-issue. 10/10 §20.4. **9 engines now A1-audited** (core + income/Medicare/GST/Div293/SG/SMSF/CGT). **Health engine MODELLED + AUDITED:** `calculateAggregateScore` :106 + `generateHealthScore` :315 + `generateHealthReport` :338 (the §12.3 health SSOT) + methodology law node; audited `calculateAggregateScore` (Σ score×weight − penalty, clamp 0-100: 70/60/clamp-100/clamp-0) + `scoreToRiskBand` boundaries (9 cases: ≥80 EXCELLENT … <20 CRITICAL). All pass; no suspected-issue — proves the audit works for INTERNAL methodology, not just ATO law. Graph now spans **core + tax + health** (72 nodes/81 edges); **11 engines A1-audited**. 10/10 §20.4. **CFO score MODELLED + AUDITED:** `calculateOverallScoreDecimal` :730 + `calculateCFOScore` :33 (§6.4 CFO SSOT) + the 6-component weighting law (.25/.20/.15/.15/.15/.10); audited the weighted overall (all-100→100, mix→62.5, all-0→0). All pass; no suspected-issue. Graph now spans **core+tax+health+cfo** (77 nodes); **11 engines A1-audited**. 10/10 §20.4. **CFO what-if scenario (cutSpend) MODELLED + AUDITED:** `cutSpendCategoryScenario` :18 + what-if-annualisation law (annual = monthly delta × 12, capped at actual spend); audited annual saving ($200/mo→$2,400; cap $800→$500→$6,000; unknown category→$0). All pass; no suspected-issue. 10/10 §20.4. **Intelligence domain MODELLED + AUDITED:** `calculateCashflowHealthScore` :248 (5-category weighted) + `calculateLinkageHealth` :306 (GRDCS severity) + weighting/threshold law nodes; audited the cashflow-stability sub-score (surplus ratio ≥20%→100, ≥10%→80, volatility penalty min(20,vi×0.2): 100/80/90). All pass; no suspected-issue. Graph now **5 of 6 domains** (core+tax+health+cfo+intelligence, 83 nodes); **13 engines A1-audited**. 10/10 §20.4. **Reports domain MODELLED + AUDITED — ALL 6 DOMAINS now represented:** `generatePropertyPortfolioReport` :8 + report-aggregation law node; audited capital-growth % (Σvalue−Σcost)/Σcost×100: 2 props→25%, 1 prop→20%. All pass; no suspected-issue. **Milestone: full domain breadth** (core+tax+health+cfo+intelligence+reports, 85 nodes); **14 engines A1-audited**. 10/10 §20.4. **Next:** depth (more engines per domain) + N2 2D explorer + A2/A4 audit enhancements.
  - [x] **N1 — `financial-graph.json` schema + proof slice (4 core engines) + markdown generator** (PR #1204). Schema `docs/financial-logic/graph/schema/financial-graph.schema.md`; graph = 23 nodes / 26 edges, **all `verified`** (4 core engines + resolver + orchestrator + inputs + laws + numbers + ui), each citing file:line. Pure-Node generator `scripts/neomatrix/generate-financial-logic.mjs` (`npm run neomatrix:generate|check`) renders `GENERATED_CORE.md` FROM the JSON. **Layer-3 seed already enforced in CI** via `tests/neomatrix/financialGraph.test.ts` (schema · A3 orphan-number + convergence/contradiction invariants (the #1201 class) · file:line resolution · markdown freshness; 6 assertions verified via dependency-free harness — node_modules not installed in this env). ⏸️ **Awaiting Reza's schema sign-off before domain backfill (N4).**
  - [ ] N2 — interactive 2D explorer.
  - [ ] N3 — CI audit checks (generalise the #1201 drift-guard).
  - [~] **N4 — backfill domains (tax first).** **N4.1 income-tax spine shipped** (PR #1204): graph 23→36 nodes / 26→38 edges. Tax nodes (all `verified`, file:line read this session): orchestrator `buildMasterTaxPosition` :186 → `calculateEntityTaxPosition` :300 → income-tax `calculateTaxPosition` :92; PAYG `processSalary` :46; thresholds SSOT `getCurrentTaxYearConfig` :370 ← brackets/Medicare/LITO config inputs; **law nodes** ITAA 1997 income tax · Medicare Levy Act 1986 · 2026-27 reform cut-over (`REFORM_CUT_OVER_UTC`, B8 anchor for N4.2 CGT); `number.taxPayable` → tax UI. `neomatrix:check` + harness green. **N4.2 reform-gated CGT/negative-gearing shipped** (graph 36→41 nodes / 38→43 edges): `calculateCgtDiscount` :166 (Div 115 pre-reform 50% ↔ Measure 2 post-reform, FW-2 default), `applyCgtIndexation` :87 (post-reform, UNCOMPUTED-until-commencement), `applyCgtMinimumRate` :80 (30% floor, post-reform), `deriveNegativeGearingRegime` :147 (Measure 1 classifier) + ITAA Div 115 law; all `governed-by` the reform cut-over. **Regime axis now live + sliceable** (`regime=post-reform` returns the 3 reform nodes). **N4.3 superannuation shipped** (graph 41→57 nodes / 43→62 edges, all verified, self-review 10/10 §20.4): SG `calculateSuperGuarantee` :61 + `calculateSuperContributions` :115 (15% taxed-in-fund s295-485); caps `calculateCarryForward` :96 (s291-20) + `calculateBringForward` :148 (s292-85); `calculateHighIncomeSuperTax` :77 (Div 293 + Div 296 pending/FW-2); `calculateSmsfIncomeTax` :151 (Div 295 + ECPI s295-385/390 + NALI s295-550) — all fed by the FY-config SSOT + `governed-by` ITAA law nodes. **N4.4 land tax / stamp duty / GST shipped** (graph 57→64 nodes / 62→70 edges, all verified, 10/10 §20.4): `calculateLandTax` :374 → `calculateCrossStateLandTax` :105 → orchestrator (:200); `calculateStampDuty` :285 → orchestrator (:213); `calculateGst` :104 → orchestrator (:226, s9-70 10% / s23-15) + state Land Tax / Duties Act + GST Act law nodes. **Tax domain backfill substantially complete** (income/PAYG, CGT/reform, super, land/stamp/GST). **Next N4.5:** health engine → CFO → intelligence → reports.
  - [ ] N5 (optional) — literal 3D view.
- **Constraints:** documentation/model only — never change a formula/law/threshold; never guess an edge or formula (cite file:line or mark `unverified`); derive don't hand-maintain (markdown renders from JSON; CI checks freshness); no CDR data in the graph.
- **Risk:** maintenance drift if hand-kept (mitigated by derive-from-code + CI freshness); Graphify egress (mitigated by N0 security pre-check); gold-plating the 3D (mitigated by value-before-visuals ordering).
- **Why this matters:** drift is a graph problem; this is the artefact that makes every number traceable + provable-correct, and machine-enforces it. The structural fix for the recurring correctness failures.

### 0·LOAN. Loan Ledger, Repayment Matching & Low-Effort Categorisation (Phase 51)

- **Status:** 🟡 ACTIVE — **design phase** (research complete; Stitch-first surfaces next, no code yet).
- **Started:** 2026-06-21.
- **Owner:** Reza (decisions) + Claude (research, design direction, build).
- **Last touched:** 2026-06-21 — research + Phase doc shipped (PR #1166); the two core 51.1 designs (Repayments tab + folded-in import) **approved by Reza** ("looks good, ship it").
- **Phase checklist:**
  - [x] Deep research (categorisation UX · transfer detection · loan modelling · AU tax · CDR/Basiq) + codebase audit.
  - [x] Phase doc + recommendation ([`PHASE_51_LOAN_LEDGER_AND_CATEGORISATION.md`](../blueprint/PHASE_51_LOAN_LEDGER_AND_CATEGORISATION.md)).
  - [x] **Decision:** import folds into existing surfaces (Repayments tab + create-page auto-fill) — **no standalone dialog** (Reza, 2026-06-21).
  - [x] **51.1 design (all 3 core surfaces approved):** Repayments tab (`309e4b0c`) + import affordance (`f160bad2`) + matching confirm queue (`ea91d6e3`).
  - [ ] **51.1 design (remaining):** create-page handoff + full light/dark × desktop/mobile variant matrix (§18.7.2).
  - [x] **51.1 build #1 — schema foundation:** `LoanTransaction` ledger (quarantined) + `LoanTransactionKind`/`LoanMatchStatus` enums + `Loan.deductibleFraction` + migration `20260621000000_add_loan_ledger` (additive). Build green.
  - [x] **51.1 build #2 — loan-scoped import:** `importLoanStatement()` (QIF/CSV → quarantined `LoanTransaction`; keyword+direction kind classification; dedup by contentHash) + `POST /api/loans/[id]/ledger/import` + 13 tests. Build green.
  - [x] **51.1 build #3 — matching engine:** `matchLoanRepayments()` (actual-vs-actual; amount+date+direction; confidence tiers; ambiguous → review, never auto-applied) + `suggestMatchesForLoan()`/`resolveRepaymentMatch()` (LINKED sets offset `isTransfer`) + `POST`/`PATCH /api/loans/[id]/ledger/match` + 8 tests. Build green.
  - [x] **51.1 build #4 — Repayments tab UI:** `GET /api/loans/[id]/ledger` (rows + FY summary; interest from actual INTEREST_CHARGED) + `LoanRepaymentsTab` (inline import + summary strip + ledger with Linked/Confirm/Unmatched + inline one-tap confirm) wired into `LoanDetailDialog` (7 tabs). Semantic tokens → light+dark. Build green.
  - [ ] **51.1 polish (4b, optional):** standalone "Awaiting confirmation" queue in My Accounts (`ea91d6e3`) + create-page "import transactions too?" handoff + variant matrix.
  - **51.1 is functionally complete** — import → match → confirm → tax-correct interest/principal, all live behind the loan's Repayments tab.
  - [ ] **51.2:** review-exceptions categorisation inbox + "apply to past+future" rules + confidence gate + ATO-label mapping.
  - [ ] **51.3 (CDR):** swap ingestion to Basiq; TR 2000/2 mixed-purpose apportionment.
- **Risk:** tax correctness (offset vs redraw; never show estimated interest as a tax figure); §12.14 reform gate on negative-gearing projections.
- **Blocking:** Stitch designs awaiting generation/Reza nod; confirm the two-records transfer model (§7 recommended).
- **Why this matters:** directly fixes Reza's two stated pains — "which loan is this repayment?" (all loans paid from one offset, same payee) and "categorisation is overwhelming." Builds the CDR destination now, hand-fed by QIF until Basiq.

### 0·MOB. Mobile Companion App (Phase 15)

- **Status:** 🟡 ACTIVE — **design phase.** Realign the mobile design system to the §18.7 glass vocabulary (Stitch-first) before any RN build, then produce the MVP screen set.
- **Started:** 2026-06-19.
- **Owner:** Reza + Claude (chat = PM + Stitch design direction · Code = repo writes · future `monitrax-mobile` = RN build).
- **Last touched:** 2026-06-19 — governance scaffolding (this PR): stood up the `05_MOBILE_WORKSTREAM.md` spoke, STATE.md sub-cursor, continuity-gate watch.
- **Detail:** **lives in [`05_MOBILE_WORKSTREAM.md`](05_MOBILE_WORKSTREAM.md)** (phase checklist, decision log D1–D5, open-for-Reza items, cross-references). This entry is the registry pointer only — to keep this over-budget spoke (§15.5) from growing, the live detail is in the dedicated spoke.
- **Scope boundary:** this workstream is the **main-repo half** (design docs + Stitch artefacts, the `/api/v1/mobile/*` backend, the `@monitrax/core` shared package). The **React Native app** is a **separate `monitrax-mobile` repo** (blueprint §3.4) that gets its OWN STATE/CLAUDE/continuity-gate when stood up — this PR does NOT fork a second cursor inside `resadegh/monitrax`.
- **Risk:** Low — design-phase docs + Stitch only; no code/schema/calc surface yet.
- **Blocking:** D4 notification-copy compliance sign-off (content only — does NOT block the visual design work); the *build* gates on Basiq accredited+live + §15.1 P0 backend pre-reqs (API versioning, Cloud Armor, FCM).
- **Why this matters:** the mobile design-system + blueprint §13 visual sections predate the §18.7 glass vocabulary (2026-06-01) and the Phase 45 glass migration — the documented mobile look has drifted from the current web app. Realigning it Stitch-first is the design-phase goal before any RN build.

### 0·DOC. AI Document Router (recognise → attach-or-create → file)

- **Status:** 🟡 ACTIVE — **Phases A–C shipped & live; Phase D (engine intelligence) IN PROGRESS.** The end-to-end document workflow is live in prod: recognise (Vision OCR via REST transport) → attach to the right asset/property OR create a linked expense (dedup'd) → file in the right Vault folder → auto-name → thumbnail → edit/add-as-expense any time. **D.1 (document-level dedup) ✅ shipped (PR #1144).** D.2–D.5 are the remaining engine-intelligence steps (Reza go-ahead 2026-06-17).
- **Started:** 2026-06-16 (Reza vision: *"every document/receipt should be recognised by AI, attached to the correct item/asset OR used to create a new item/expense, and filed in the right Vault folder for reporting/extraction."*)
- **Owner:** Reza (direction + Stitch sign-off + GCS provisioning) + Claude (build).
- **Last touched:** 2026-06-18 — **Phase D engine-intelligence: D.2 ✅, D.3 ✅ (auto-exec rejected per autonomy decision), D.4 ✅ (suggest-only learned routing, PR #1151 MERGED), D.5b ✅ (retention clock — advisory) shipped.** Recorded the load-bearing **autonomy model** (AI suggests, user always confirms + can edit; bulk-approve the only convenience; no silent auto-execution). Prior: **the full doc workflow shipped & prod-verified across PRs #1135–#1148:** Vision OCR fixed (gRPC→REST, #1135); scan→asset/property link + Vault Assets folder (#1136); per-item AI-on-upload (#1137); mobile asset-detail Stitch redesign + overflow/crash fixes (#1138–#1140); auto-rename (#1141); expense dedup + per-expense delete + doc↔expense link (#1142); propagation to Properties/Investments + DME 2.0 design (#1143); **D.1 document-level dedup at the `processUpload` chokepoint (#1144)**; real thumbnails + persistent add-as-expense (any category) + full edit dialog + tag de-clutter (#1145–#1146); asset deep-link from a doc's Asset badge (#1147); instant Expenses-tab refresh after add-as-expense (#1148). **Single-engine clarification (Reza challenge):** ONE DME (filing) + ONE DIE (reading), separate concerns — Phase D *extends* them, not a new engine.
- **Spec:** `docs/blueprint/PHASE_50_AI_DOCUMENT_ROUTER.md` (Phase D §159+ = the engine-intelligence roadmap).
- **Phases:**
  - [x] **Phase A — Foundation + fix the live scan bug (PR #1123).**
  - [x] **Phase B — Intelligence + storage** — Vision OCR live (REST transport, the real fix #1135); keyless GCS auth + GCS-aware download; per-user quota.
  - [x] **Phase C — Per-item Documents** — `DocumentsSection` on Assets/Properties/Investments; AI-on-upload; auto-rename; thumbnails; edit dialog. (Tax-pack export + ATO retention still queued under Phase C.)
  - [~] **Phase D — Engine intelligence (DME judgement layer, INSIDE the single DME/DIE):**
    - [x] **D.1 — document-level dedup** (SHA-256 hash at `processUpload`; #1144).
    - [x] **D.2 — record reconciliation** ✅ — canonical `reconcileSuggestedAction()` the confirm flow calls for expense/income/loan; on a match it links the doc to the existing record (`reconciled:true`) instead of duplicating + skips the receipt-matcher. 7 tests.
    - [x] **D.3 — confidence-gated autonomy** ✅ — `confidencePolicy.ts` SSOT shipped (bands AUTO/CONFIRM/ASK + earned-autonomy TRAIL downgrade; unifies the scattered 0.9/0.7 thresholds; 6 tests). **Auto-EXECUTION REJECTED, not deferred** — per the **2026-06-18 autonomy decision** (Reza: *"the user should confirm any ai recognition… the confirmation and the option for user to edit the ai findings should always be there"*), the AUTO band is a display/priority cue only; the sanctioned convenience is **bulk-approve** (each item still an explicit editable confirm), a Stitch-first Smart Inbox follow-up.
    - [x] **D.4 — learned routing** ✅ **suggest-only** — `VendorEntityHint` table (migration `20260618090000_add_vendor_entity_hints`) + canonical `lib/documents/intelligence/learnedRouting.ts`. Records vendor→entity routings at the link API + confirm chokepoints; the scan flow's "What is this for?" selector **pre-selects** the usual entity (with a "Suggested for you" cue) but **never auto-applies** — the user always confirms (autonomy decision). `GET /api/documents/vendor-hint`. 14 tests.
    - [x] **D.5 — lifecycle** ✅ — **D.5b retention clock** (`retentionClock.ts`, pure ATO 5yr-from-FY-end rule; "Safe to archive" pill; advice-only; 10 tests) + **D.5a renewal → reminder** (Stitch-first; see next row).
    - [x] **D.6 — bulk-approve Smart Inbox** ✅ (Stitch-first) — new `components/documents/SmartInbox.tsx` replaces the inline inbox: multi-select with D.3 AUTO band pre-selected, per-row inline edit of AI findings, "Approve N selected" looping the SSOT confirm path per item (no batch shortcut, no silent writes), Track glass design, lives in My Vault. 4 Stitch variants + 6 tests. Removed the dead inline JSX + 3 page helpers (§12.1).
    - [x] **D.1.1** ✅ — funnelled the scan/form path (`/api/documents/analyze-for-form`) through the DME `processUpload` chokepoint (was legacy `uploadDocument`, no dedup); a byte-identical re-scan now returns the existing doc + inherits the storage quota. No schema change.
    - [x] **D.5a renewal → reminder** ✅ (Stitch-first) — `analyze-for-form` now extracts an optional `expiryDate` (policy/rego/warranty only); the scan flow shows the new `RenewalReminderCard` (calm sky→teal, date editable, explicit Set/Not-now) and on confirm **creates a custom `Reminder`** via the existing `POST /api/reminders/custom` (CREATE-only — §12.11-safe, no clobber); `computeCustomReminders()` lights up the bell. No schema change. 4 Stitch variants + 6 tests. **Phase D engine-intelligence layer COMPLETE.** v2 (typed entity-column write) needs a safe partial-update endpoint first.
  - [x] **Tax-pack export — Unified Accountant Pack** ✅ (2026-06-19) — added `format=pack` to the existing FY tax-pack export: ONE ZIP = PDF + XLSX summary + all FY receipts/invoices/tax docs + README. New `accountantPackBuilder.ts` (orchestration only; reuses the canonical summary + exporters + a shared `addReceiptDocuments` helper extracted from `zipBundleBuilder`). `TaxPackExportButton` "Complete pack" option (default). 6 tests. ATO retention = the D.5b retention clock. **0·DOC is now feature-complete** (remaining: D.5a v2 typed-column write + expense-list parity — both low-priority follow-ups).
- **Risk:** Low — Phase D steps are additive, behind the existing confirm/upload flow; engines stay pure (§6.4).
- **Blocking:** none (GCS provisioning done; key restored as the stable storage/telemetry credential).
- **Why this matters:** the Vault is no longer a folder of photos — a scanned receipt is recognised, routed to the right asset, turned into an expense, de-duplicated, and filed. Phase D adds the *judgement* (reconcile, confidence-gate, learn, lifecycle) that makes it feel like the app understands the paperwork.

### 0·CONT. Continuity System (cross-session memory — STATE.md + SYSTEM_MAP + gate)

- **Status:** ✅ **Phase 0+1 COMPLETE; Phase 2 governance audit IN PROGRESS.** The cross-session continuity rails that let any surface (chat · Cowork · Code) resume Monitrax in <1 min without the user re-explaining context. Phase 0 (rails) + Phase 1 (deep ingestion) are live on main; Phase 2 consumes the F-1…F-8 drift findings.
- **Started:** 2026-06-14 — built to stop every session reconstructing context from scratch and to make "read live, never recall" structurally enforceable.
- **Owner:** Reza (direction) + Claude (build).
- **Last touched:** 2026-06-15 — Phase 2 **PR-C** (this PR): **F-8** split the 884 KB `IMPLEMENTATION_PLAN.md` into a thin hub + four `docs/implementation/` spokes (this file is `01_ACTIVE_WORKSTREAMS.md`) + relocated the 86 KB preamble narrative to `IMPLEMENTATION_CHANGELOG.md`; **F-1** added two soft-launch doc-hygiene CI checks (`scripts/check-plan-freshness.sh` + `scripts/check-index-paths.sh` via `.github/workflows/docs-hygiene.yml`). Prior: **PR-B** (#1106) F-5/F-6; **PR-A** (#1105) F-3/F-4/F-7; F-8-prelim (#1104) deferred plan edits; Phase 1 ingestion (#1102) SYSTEM_MAP.md; Phase 0 rails (#1100/#1101) STATE.md + hook + continuity-gate.yml.
- **Phases:**
  - [x] **Phase 0 — Rails (PRs #1100 / #1101)** — `STATE.md` (the "you-are-here" cursor every session reads first), `.claude/hooks/session-start.sh` (prints cursor + HEAD at Code session start, skip-on-failure, never blocks), `.github/workflows/continuity-gate.yml` (soft-launch — warns when a PR changes workstream files without updating STATE.md + this plan in the same PR). RESUME CHECK ritual re-landed in #1101.
  - [x] **Phase 1 — Deep ingestion (PR #1102)** — `SYSTEM_MAP.md` (pointer / what-owns-what map); STATE.md RESUME CURSOR filled from a live range-read of this plan; Q-GTM-3 (OPEN) + Q-DEC (DECIDED) recorded; drift / SSOT findings logged in `docs/audits/PHASE1_INGESTION_FINDINGS_2026-06-14.md` (F-1…F-8).
  - [x] **F-8 deferred plan edits + cursor re-pin (PR #1104)** — header date bump (F-1), §0·WI Q-DEC checkbox reconcile to `[x]` (F-2), Recently-Completed entry; STATE.md cursor re-pinned to live HEAD `6c0ff92`.
  - [~] **Phase 2 — Governance audit (IN PROGRESS)** — consume the F-1…F-8 findings.
    - [x] **PR-A (PR #1105)** — F-3 backfill PR numbers ✅, F-4 refresh `00_INDEX.md` ✅, F-7 add this workstream ✅.
    - [x] **PR-B (#1106)** — F-5 de-dup `docs/audit/` → canonical `docs/audits/` ✅, F-6 reconcile CLAUDE.md §12.4 ↔ §12.2 on `/api/portfolio/snapshot` (§12.2 wins; never delete as a dup) ✅.
    - [x] **PR-C (this PR)** — **F-8** plan split into hub + `docs/implementation/` spokes (preamble narrative → changelog; hub stays canonical entry; CLAUDE.md §15.5/§1.5, SYSTEM_MAP, 00_INDEX, continuity-gate, STATE all updated) ✅; **F-1** soft-launch CI: `check-plan-freshness.sh` (header-date) + `check-index-paths.sh` (index coverage) via `docs-hygiene.yml` ✅. **Also fixed a latent `continuity-gate.yml` shell-injection bug** — the gate interpolated `${{ github.event.pull_request.body }}` straight into bash and crashed (`syntax error near '('`) on any PR body containing parens/backticks; moved title/body/SHAs to `env:` so they're treated as data (surfaced by this PR's own CI run). **Branch-currency rail added** (Reza directive 2026-06-15): new soft-launch `branch-currency.yml` flags a PR branch that is behind its base; the authoritative control is GitHub native branch protection ("Require branches to be up to date before merging") — operator setting documented in the workflow header + STATE.md next-action.
    - [ ] **Follow-ups (queued)** — (a) hygiene pass: retire completed §0x workstreams + roll >30-day completions to the changelog to bring the two over-budget spokes under §15.5 (Backlog row 33); (b) index-completeness: clear the 5 residual `check-index-paths.sh` warnings; (c) **operator/repo-admin actions:** enable branch protection "Require branches to be up to date before merging" on `main`, grant GitHub `workflow` scope, then arm all three soft-launch workflows (`continuity-gate.yml`, `docs-hygiene.yml`, `branch-currency.yml`) to blocking (`--strict` / `exit 1`).
- **Risk:** Low — documentation / governance only; no code, schema, or calc-engine surface. The gate is soft-launch (warns, never blocks) until GitHub `workflow` scope is granted.
- **Blocking:** GitHub `workflow` scope NOT granted — blocks arming `continuity-gate.yml` (and the Phase 4 CI test-runner). Until then the gate warns only.
- **Why this matters:** every prior session reconstructed context by re-reading CLAUDE.md + changelogs + the user's messages — queued items got forgotten, reverted decisions re-attempted, and the user had to repeat themselves. The continuity system is the persistent memory between sessions: STATE.md holds the cursor, SYSTEM_MAP holds what-owns-what, this plan holds the detail, and the gate keeps them in sync. It makes "read live, never recall" enforceable instead of aspirational.

### 0·EOF. Phase 47 — Entity Ownership Fabric (the "golden feature")

- **Status:** 🟢 ACTIVE — Q-EOF-1…5 ✅ decided 2026-06-10 per recommendation + §4A personal-tier completeness matrix added (binding). **Stages A, B, C, D, E, F ✅ COMPLETE.** Stage D's last build item — **D6 What-If lever per-entity CGT — ✅ shipped 2026-06-15** (`sellProperty` now splits CGT across joint owners + applies each entity's Div 115 discount). Phase 47 is **feature-complete**; the only residual is the registered-tax-agent sign-off scheduled at Basiq prep (a review gate, not a build item).
- **Started:** 2026-06-10 (Reza directive: *"I want to have everything (financial portfolio) of the user captured and can be shown on the entity universe… tax implications, dashboard, reports and everything on the app also needs to understand these relationships… This will be the golden feature of Monitrax."*)
- **Owner:** Reza (direction + §8 design gates) + Claude (audit, design, build).
- **Last touched:** 2026-06-15 — **Stage D · D6 COMPLETE** (this PR): the `sellProperty` What-If lever now computes CGT through the canonical per-entity path — new pure core `lib/cfo/scenarios/propertyDisposalCgt.ts` composes `calculateCgtDiscountDecimal` (Div 115) + the AD-2 `attributeAsset` weighting, so the gain splits across joint owners (TR 93/32) and applies each entity's discount (individual/trust 50%, complying SMSF 33⅓%, company 0%). Honesty boundary: only the user's own share is estimated to a tax dollar (current marginal rate, labelled); co-owners show taxable gain only; FW-2 post-reform → `UC-CGT-POST-REFORM`; additive (no context → prior CGT flag verbatim). 16 D6 tests. **Also flipped stale tracker hygiene:** Stage F `[~]`→`[x]` (all F1–F4/F-LAW/F-G merged #1090–#1092, golden test green — was the abandoned PR #1107's intent). Prior: 2026-06-13 — **Stage E COMPLETE** (E1 per-entity report sections #1098 + E2 universe tax-flow overlay) + **Stage D AD-2 shipped** (stake & beneficial attribution in the `entityTaxFactsAssembler`). Prior: 2026-06-12 — **Stage F capture surfaces COMPLETE**: F1+F2 merged (#1090), F3 merged (#1091, prod verified), F4 shipped (wizard two-tier picker + template-driven roles + More-detail disclosure; F-LAW review with partnershipSubtype gap fixed incl. §12.12 migration; F-G Renew golden test passes — the advisor chart is reproducible node-for-node). Stage F polish shipped same day: universe-popover completeness chip + OWED Stitch variants (picker dark `77f18d26`, picker+correction mobile `40930f57`; residual mobile-dark cells documented as token-derivable). Next big rock: Stage D (gated on Reza's Part 2 doc review).
- **Approach:** Five gated stages — **A Capture** (entity picker on create, "correct ownership record" on edit, bulk re-attribution, entity-create hook), **B Complete** (retail-super attachment + wealth-graph super coverage, derived-ownership helper), **C Consume** (additive `byEntity` master-snapshot breakdown + dashboard entity lens + universe value parity), **D Compute** (execute the review-gated `PHASE_44_PART_2_MONEY_FLOW_TAX_REWIRE.md` — EntityTaxFacts assembler from persisted facts), **E Report** (per-entity report sections + universe tax-flow overlay). Non-breaking guarantees: golden tests pin flat household totals per stage; progressive disclosure (no new chrome below 2 entities); correction-≠-transfer framing throughout (§12.11 gated).
- **Audit headline (2026-06-10):** schema layer ~complete (Phase 44 Part 1) but **write path has no entity picker anywhere** (everything auto-assigns PERSONAL_NAME, immutable after); `SuperannuationAccount.ownerEntityId` nullable + unselected in wealth graph; `masterFinancialService`/reports entity-blind flat sums (`netWorthCalculator` accepts the entity filter — never passed); tax engine fully per-entity-ready but starved — companies/trusts/SMSFs return `UNCOMPUTED` because nothing assembles `EntityTaxFacts` from persisted rows.
- **Phases:**
  - [x] **Stage 0 — Audit + phase plan (PR #1040 + this PR, 2026-06-10)** — full ownership-coverage audit with file:line evidence; `PHASE_47_ENTITY_OWNERSHIP_FABRIC.md` v1; Q-EOF-1…5 raised → ✅ all decided per recommendation same day; v2 adds the binding §4A personal-tier completeness matrix (P1–P9 Australian co-ownership forms, TR 93/32 income-split treatment, warm-words capture UX, co-ownership-≠-partnership guard).
  - [~] **Stage A — Capture (IN FLIGHT)** — Stitch pass: v1 rejected by Reza (Stitch editorial defaults leaked — design-principles violation); v2 re-driven from the canonical Phase 45.2.5 glass-dialog artefact per the documented Phase 45.2.1 lesson (generate from source screens, never redescribe) — **Reza approved v2 ("ship it"), screen `8e6fc018886e4d54b18abff0f7c80c13`**. First build PR ships: `lib/services/ownershipSelectionService.ts` (canonical picker→Phase-44-models translation layer: parse/validate selection, joint=equal stakes+survivorship per TR 93/32, shared=TIC fractions sum-100, INDIVIDUAL quick-create for named co-owners, group creation via `ownershipService`), `components/ownership/OwnershipPicker.tsx` (the §4A ownership picker — "Just me / Joint / Shared / An entity", household-member chips, progressive disclosure: entity card hidden without entities), properties POST accepts the `ownership` payload (absent = sole, behaviour unchanged), Add-property dialog renders the picker (create only, not RENTAL, not edit — edits are Stage A2 correction flow). 13 tests in `tests/ownership/`. **A1 rollout complete (PR #1043):** picker live on ALL five group-capable create surfaces — properties, accounts (AccountFormDialog), loans (LoanFormDialog), investment accounts, assets — via the uniform `resolveOwnershipForCreate`/`applyOwnershipSelection` route pattern; INDIVIDUAL co-owners render as people beside YOU on the universe, group nodes carry "$X held" + warm naming. **Build sweep codified as §4B (D1–D9, 2026-06-10, Reza: "nothing missed")**: D1 co-owner dedup ✅ fixed (service reuses INDIVIDUAL by name + picker chips existing people); D2 onboarding-wizard capture → Stage A5; D3 Income/Expense slim-select → OPEN (recommend: no picker, attribution derives from asset in C/D); D4 ownership-evidence link → Stage E; D5 HouseholdMember↔entity FK → tech debt; D6 What-If single-taxpayer → Stage D scope note; D7 CDR joint auto-suggest → parked (Basiq trigger); D8 estate lens → parked; D9 lint-baseline rebase process note. **D2 + D3 ✅ DECIDED 2026-06-10 (Reza):** D2 — ownership stays OUT of onboarding (wizard light; ReviewStep gains the "set who owns each item anytime" note, shipped); D3 — no Income/Expense picker v1 (attribution derives from the owning asset in C/D). All remaining tactical decisions delegated to Claude. **Delivery cadence DECIDED 2026-06-10 (Reza: "build each stage in 1 PR each"):** Stage A remainder (A2+A3+A4+variants) = ONE PR; Stage B = ONE PR; Stage C = ONE PR (golden-test gate unchanged); Stage E = ONE PR. **Stage D exempted** — stays split per `PHASE_44_PART_2_MONEY_FLOW_TAX_REWIRE.md`'s own review gates (legal-exposure surface; an unreviewable mega-diff defeats the gate). **Stage A FINAL PR shipped (this PR):** A2 correction flow (canonical `correctOwnershipRecord` + generic `PATCH /api/ownership/[type]/[id]` + `CorrectOwnershipDialog` on all five edit surfaces), A3 bulk re-attribution (`BulkAssignDialog`, single entities-page surface), A4 entity-create hook (auto-offer after structure entity creation). **OWED: dark+mobile Stitch variants for the picker + correction dialog (§18.7.2 matrix) — queue before Stage E ships. WX.5 focused-scene composition screen ✅ delivered (screen `7ad05a712f4e4c7eadcff8eb2dd23d43`, artefact `.stitch/designs/wealth-universe-zoom/universe-focused-scene-dark.{html,png}`, prompted from the canonical L2 screen — Reza compliance check 2026-06-10).** **Phase WX.5 shipped 2026-06-10 (Reza: 'microscopic zoom'): single-expansion now returns a FOCUSED SCENE (bubble re-centres + enlarges, items ring it, everything else leaves the stage) and both canvases animate between layers with a camera zoom (magnify toward tap point, appleEase 0.6s, reduced-motion safe). **WX.5.1 same day (Reza feedback ×4): mobile zoom-back pill + toggle (was a real bug — no exit), iOS-safe scale-only camera (blur frame-dropped), sheet no longer auto-rises on zoom / card-close stays in layer / explicit minimise chevron, ownership trail visible on every layer ('You › Other assets').** **WX.5.2 shipped 2026-06-11 (Reza: "minimal navigation on the widget as well"): the dashboard widget navigates in place — bubble tap zooms the widget itself with the same camera (scale+opacity only, 0.5s), '‹ Universe' pill / re-tap zooms back, asset taps + header arrow + footer link carry `?focus=` to the full page; chip/footer totals stay pinned to universe level. Stitch SoT: screen `77b13314b96a4423975b3e89782efa46`, artefact `.stitch/designs/wealth-universe-zoom/universe-widget-focused-dark.{html,png}`.** **WX.5.3 shipped 2026-06-11 (Reza prod-testing screenshots ×2): (1) the redundant first zoom layer removed in cluster mode — tapping YOU no longer unfolds the all-holdings ring (busy on mobile, faded behind the card on desktop); the type clusters carry the way in, and the entity tap opens the card instead. New layout-computed `WealthNode.isExpandable` flag is the SSOT all three surfaces' tap handlers + deep-links obey. (2) Focused-scene centre lowered y 42→52 so the top satellite clears the breadcrumb text.** **WX.5.4 shipped 2026-06-11 (Reza prod screenshot: asset bubble → "Failed (404)"): desktop EntityDetailPanel now gates the `/api/entities` fetch to real entities (mobile already did); asset/group bubbles render from the in-memory graph (value + "Held by <owner>" + click-through CTA). `assetHrefFor` consolidated into canonical `wealthExplorerTypes.ts` — per-component copies had drifted (investments/super now route to their Asset Spotlight detail pages).** **Stage A ✅ merged (PR #1046, prod verified). Stage B shipped as ONE PR (this PR): B1 super-attachment backfill migration + create-route fix-forward + universe coverage (new `'super'` asset kind end-to-end), B2 `resolveOwnerEntityId` derived-ownership helper. Stage C shipped as ONE PR (this PR): C1 `entityBreakdown` canonical calculator + additive snapshot `byEntity` (additivity invariant tested), C2 entity lens v1 = EntityBreakdownWidget (global filter deferred until Stage D payoff — documented scope decision), C3 parity-by-construction note. Stake-splitting deliberately Stage D. **UX fixes from Reza's live testing all shipped same day (PRs #1042–#1053): ownership row on edit surfaces, picker discoverability hint, My Structure always renders the universe (stale personal-only gate removed), widget bubble deep-links (`?focus=`). Next: Stage D — execute `PHASE_44_PART_2_MONEY_FLOW_TAX_REWIRE.md` per its own review gates (multi-PR, exempt from the one-PR cadence) — GATED ON REZA's review of that doc.**
  - [ ] **Stage B — Complete** (~2 PRs; B1 carries a §12.12 migration)
  - [ ] **Stage C — Consume** (~3 PRs; golden tests gate)
  - [x] **Stage D — Compute (✅ COMPLETE 2026-06-15)** — gate resolved: dedicated adversarial review agent verdict READY WITH ADDENDA (AD-1..AD-4, doc §14); **Reza approved in writing**. PR-1 honesty hardening ✅ shipped (Div 5A subtype dispatch, residency pass-through, deed-power streaming gate, no-false-silence flags for all 12 extended types). **PR-2 ✅ shipped same day**: trust/SMSF `cgtEvents` FIFO feed (per-lot holding periods, unmatched-sell honesty, UC-CGT-PARCEL-ID) + Div 207 dividend-register feed (UC-DIVIDEND-REGISTER dedup note) + `assemblerNotes` no-false-silence channel. **AU-law conformance audit ✅ run + all 4 critical drifts fixed same day** (s292-85 bring-forward bands, Div 207 explicit franking, MLS tiers, FY25-26 $2.0M TBC indexation) + 6 minors (streaming deadlines s115-228, Div 6E residual pools, s17A severity, Decimal parity, CGT 12-month boundary, citations). Audit register in CHANGELOG_2026_06_12. **Trust spot-check fixes shipped 2026-06-13** (Reza screenshots): wizard now surfaces PEOPLE (You + INDIVIDUALs + quick-add) as role candidates; new `TrustDistributionsSection` — the missing capture UI for per-FY trustee resolutions with percentages (engine/API existed since Part 2a, curl-only until now); discretionary-trust teaching line. **AD-2 ✅ shipped 2026-06-13** (this PR): the `entityTaxFactsAssembler` now reads `OwnershipStake` (TR 93/32 — JT equal split / TIC fractional) + `BeneficialOwnershipOverride` (bare-trust / nominee / LRBA → income follows beneficial owner) instead of attributing flat by `ownerEntityId`; new pure core `lib/services/ownershipAttribution.ts`; no-false-silence flags (`UC-OWNERSHIP-SPLIT[-UNKNOWN]` / `UC-BENEFICIAL-OWNED` / `UC-BENEFICIAL-REDIRECTED`); additive (no group/override → byte-for-byte prior mapping); 23 attribution tests, 924-suite green. **D6 ✅ shipped 2026-06-15** (this PR): the `sellProperty` What-If lever consumes the per-entity path — `lib/cfo/scenarios/propertyDisposalCgt.ts` (pure) composes `calculateCgtDiscountDecimal` (Div 115 per-entity discount) + the AD-2 `attributeAsset` weighting (JT equal / TIC per share / beneficial-override redirect); the lever surfaces an "Estimated CGT (your share)" impact + after-CGT net worth + per-owner taxable-gain breakdown in the assumptions panel; honesty discipline — only the user's share → a tax dollar (current marginal rate, labelled), co-owners show taxable gain only, SMSF/company show the entity discount + a basis note (never fabricated), FW-2 post-reform → `UC-CGT-POST-REFORM`; the run route builds the per-property ownership/cost-base/marginal-rate context (lazy — only for sellProperty); additive (no context → prior CGT flag verbatim); 16 D6 tests, full sweep green. **Stage D build items complete** — only the registered-tax-agent sign-off (a review gate, scheduled at Basiq prep) remains.
  - [x] **Stage E — Report** (~2 PRs) — **✅ COMPLETE 2026-06-13.** **E1** (#1098, merged): per-entity sections on financial-overview + tax-time reports. **E2** (this session): universe tax-flow overlay — `wealthGraphService` computes per-entity `taxStatus` (engine-backed, parallel), flows to the Money-Flow-lens entity tiles as an honest emerald-✓/amber-N pip + legend pill; status-only (no tax dollar leaves the engine); Stitch backfill per §18.2.1 (screen `8e2871ee…`); 2 layout tests; additive (no schema/endpoint change). Directive met: structure + value + money flow + tax flow, one canvas.
  - [x] **Stage F — Structure Capture Completion (✅ COMPLETE 2026-06-12; tracker flipped 2026-06-15)** (4 PRs: **F1 ✅ shipped 2026-06-12** — full entity-type grammar unlocked: canonical `lib/entities/entityTypeCatalog.ts` SSOT, API whitelists → all 19 types + CORPORATE_TRUSTEE role + per-type conditional fields (companySubtype/DOB/deed/vesting/estate-status) with trustType auto-derive (§12.14-safe), two-tier picker in the entity dialog (Stitch light `eb5dd4a3`/dark `c3c62699`), universe classification for the extended types → F2 "Roles & People" editor on My Structure (restores the relationship add/end/delete capability deleted with the legacy EntityCanvas 2026-05-31, now in universe vocabulary, + beneficial-ownership "actually held for…" row) **+ F2a per-type role templates (Reza 2026-06-12: type-aware "Entity File" rows — company shows Director/Shareholder/Secretary, trust shows Trustee/Beneficiaries; new canonical `lib/entity-graph/roleTemplates.ts`; completeness chip; same edges consumed by panel, popover, accountant-review, Stage D tax facts, Stage E reports)** → F3 ShareParcel writer (service+API+UI — schema-only since Phase 44 1a) → F4 onboarding-wizard fine-tune (two-tier type picker + optional "More detail" per entity; D2 unchanged — asset ownership stays out)). Spec: `PHASE_47_ENTITY_OWNERSHIP_FABRIC.md` §4 Stage F (added 2026-06-12). Golden acceptance: Reza's advisor-drawn Renew Group chart reproducible node-for-node (`tests/entity-graph/renewStructure.golden.test.ts`). **Origin: Reza 2026-06-12 advisor-chart comparison — "plan the build to add all possible entities and their relationship… wizard captures most, fine tuning through my structure page." Independent of Stage D; can interleave.**
- **Risk:** Stages A-C low (additive, flagged, golden-tested). Stage D high (legal-exposure surface — its own doc's external-review gates apply unchanged).
- **Blocking:** Q-EOF-1…5 (Reza sign-off on the five design gates).
- **Why this matters:** who owns an asset changes its AU tax treatment by construction (CGT discount 50%/33⅓%/0% by entity type, negative gearing, land tax aggregation, trust distribution). Today ownership is recorded but unreachable from the UI and ignored by every number except the Wealth Universe canvas. Closing capture→completion→consumption makes the universe the product moat: structure + value + money flow + tax flow, one navigable surface.


> Sorted by priority. Top of list = work in flight right now.

### 0·DG. Dashboard Glass migration (`/dashboard` Home — §18.7.2 polished sub-pattern)

- **Status:** 🟡 IN FLIGHT — opened 2026-06-09. Phase 45.3 design pass starting this session.
- **Started:** 2026-06-09 — Reza directive after the Phase 45.2.5 ship: *"properties and investment tiles on the dashboard page which are still old design. Can you also check other tiles on the dashboard to see which ones are changed and aligned with the new design and which ones are missed"* + after the §18.7 audit: *"document all these changes completely and add to the plan, make sure the design follows the design principles and all goes through stitch. There should be no backend changes to the tiles, they are only design UI changes. then continue to 45.3"*.
- **Owner:** Reza (direction + visual sign-off) + Claude (Stitch design pass + four-lens review + React port).
- **Last touched:** 2026-06-09 — workstream opened; documenting full scope below + starting Phase 45.3 Stitch design pass for the Properties + Investments dashboard tabs.
- **Why this workstream exists (relationship to 0·StD):** The `0·StD` workstream shipped a complete editorial redesign of `/dashboard` (Phase R1-R5) on the **Phase 38 "Restrained Editorial"** design system (warm ivory / paper / slate, no glass, no backdrop-blur). Subsequently, Phase 39 + Phase 45.2.x introduced the **§18.7.2 My Wealth glass vocabulary** (`bg-card/70 backdrop-blur-xl` + 1px hairline + 3-tier float shadow + 1px inner-top white highlight + 3px sub-palette gradient top-accent + luminous icon badge + sub-palette tinted bg). The glass vocabulary lives on `PropertyTile.tsx`, the Asset Spotlight detail pages (`/dashboard/properties/[id]`, `/dashboard/investments/accounts/[id]`, `/dashboard/investments/super/[id]`), and partially on `TrailStageIndicator`. **The two systems coexist on `/dashboard` today** — Reza identified the discontinuity in the screenshots: the Properties + Investments tabs are vanilla shadcn `<Card>` (oldest design), the KPI row + Net Worth hero + most diagnostic widgets are Phase 38 editorial (middle generation), and only a couple of surfaces use the §18.7.2 glass vocabulary. This workstream is the **visual consolidation** — sweep the dashboard so every tile reads as one design family.
- **Scope cap (Reza directive 2026-06-09):** **NO BACKEND CHANGES.** Every tile in the dashboard already receives data from the canonical snapshot (`useFinancialSnapshot` / `getMasterFinancialSnapshot` / `/api/dashboard/insights` / `/api/dashboard/charts`). Phase 45.3-45.6 are pure presentational migrations — swap component internals, preserve data contracts. No new endpoints, no schema changes, no calc engines, no new state. Reviewer rejection trigger: any PR in this workstream that touches `app/api/`, `lib/calculations/`, `lib/services/`, `prisma/`, or any other non-presentational layer.
- **Stitch-first per §18 (MANDATORY):** Every phase generates a full 4-variant Stitch matrix (desktop light + dark + mobile light + dark) BEFORE the React port. Sub-palette mapping for each tile follows the §18.7.2 money-signal + per-entity convention (see per-phase rows below). The §18.7.2 dark-mode reviewer enforcement applies — no PR ships with only light variants.
- **Phases:**
  - [~] **Phase 45.3 — Properties + Investments dashboard tabs (the two tiles Reza screenshotted).** Stitch design pass in flight this session. Sub-palette: Properties = sky→indigo (matches the Properties Asset Spotlight + the in-app Stage I Invest convention); Investments = indigo→violet (matches the investments Asset Spotlight). Tile anatomy = the §18.7.2 polished sub-pattern verbatim — glass + 1px hairline + 3-tier shadow + 1px inner-top white highlight + 3px gradient top-accent + sub-palette tinted bg + luminous icon badge + tabular-nums numerals. Co-locate two new components at `components/dashboard/tiles/DashboardPropertyTile.tsx` + `DashboardInvestmentTile.tsx` so future dashboard surfaces can reuse them. Replace the inline `<Card>` blocks in `app/dashboard/page.tsx:980-1113`. UI-only. Owns its own PR.
  - [ ] **Phase 45.4 — 5-tile KPI row + Net Worth `PairedMetricCard` hero.** Migrate `EditorialKpiCard` (Cashflow / Income / Outgoings / Saving Rate / LVR) + `PairedMetricCard` (Net Worth Assets / Debts split) to the §18.7.2 polished sub-pattern. Per-tile sub-palette per the §18.7.2 money-signal row: Cashflow = emerald (positive) / amber (negative caution, NEVER red), Income = emerald (growth), Outgoings = slate (calm not alarm), Saving Rate = sky (informational), Portfolio LVR = indigo (system-level diagnostic). Sparkline + band affordances stay — vocabulary swap only. Net Worth hero gets the polished-tile look + an internal 1px navy hairline divider between Assets + Debts halves. UI-only. Owns its own PR.
  - [ ] **Phase 45.5 — Insight + diagnostic widgets.** `FinancialHealthScore` + `EmergencyFundTracker` (both in `InsightWidgets.tsx`) + `DebtQualityWidget` + `EntityCashflowSummary` + `RenewalsCard` (currently 4× shadcn `<Card>` — needs the deepest internal restructure) + `PendingActionsPrompt`. Each widget keeps its existing data API + behaviour — surface vocabulary swap only. Per-widget sub-palette: Financial Health = emerald + amber zones (existing zone semantics stay), Emergency Fund = sky (informational, months-of-cover band), Debt Quality = indigo (system-level diagnostic), Entity Cashflow = sky→indigo (Properties-adjacent), Renewals = amber (cautionary, pending actions), Pending Actions = sky. UI-only. Owns its own PR (probably split into 45.5a + 45.5b given the surface count).
  - [ ] **Phase 45.6 — Hero + heavyweight widgets.** `MoneyStoryHeroV2` (Phase 38/R3 "Freedom Horizon" with Recharts ribbon — preserve the chart + animation + reduced-motion contract; the §18.7.2 swap is the SURROUND only, not the chart), `WealthUniverseWidget` (wrap the SVG canvas in the glass container; canvas stays), `DailyPulseCard` (small heartbeat tile — clean glass swap). UI-only. Final dashboard surface to migrate. Owns its own PR.
- **Risk:** Low blast radius — visual-only iteration; nothing touches data / API / engines. Each phase is its own reversible PR. Stitch dark-mode requirement adds 2x screen generation cost vs light-only but catches contrast bugs before code lands.
- **Blocking:** none.
- **Why this matters:** The dashboard is the highest-traffic surface in the app and the first thing every returning user sees. Today it reads as three different design eras coexisting — that's exactly the "tax-spreadsheet" reaction the §0 designer lens flags. The §18.7.2 vocabulary is what makes My Wealth feel premium; until the dashboard speaks the same language, the gap between "where I land" and "what the product can be" is glaring. Sequencing by what Reza screenshotted first (Phase 45.3) lifts perceived quality the most per LOC changed (designer + behaviour-psychology lens).
- **Follow-up after the workstream completes:** the `0·StD R6/R7` checkboxes are SUPERSEDED by this workstream — R6 (Holdings tabs) = Phase 45.3 in §18.7.2 glass instead of editorial; R7 (final verify) folds into Phase 45.6 close-out.

### 0·StD. Stitch Dashboard "Restrained Editorial" redesign (Home / `/dashboard`)

- **Status:** 🟡 BUILDING — design system created, 3 of 14 locked sections rendered.
- **Started:** 2026-05-27
- **Owner:** Reza (direction, taste, lock decisions) + Claude (Stitch generation + DESIGN.md curation).
- **Last touched:** 2026-05-27 — **Stitch composition (passes 1-8) complete + React-port Phase R1 (foundation) shipped.** Phase R1 adds editorial-* tokens + 7 canonical primitives + canonical doc updates without migrating any consumer yet. Subsequent React-port phases (R2-R7) queued in the phase checklist.
- **Approach:** Stitch-first composition (NOT direct React rewrite). Build the visual reference in Stitch via small, isolated section seeds + iterative `edit_screens` composition, then port the approved composition into `/dashboard/page.tsx` once the entire 14-section inventory is laid out and Reza signs off.
- **Phases:**
  - [x] Lock the redesign brief (Restrained Editorial inspired by Mercury × Apple Wallet; warm ivory + deep navy + emerald accent + TRAIL spectrum chips for sky/amber/indigo/emerald/violet).
  - [x] Lock the 14-section tile inventory of the current `/dashboard/page.tsx` (TRAIL stage indicator, Pending Actions banner, Daily Pulse, Money Story Hero, utility row, 6 metric tiles, Health + Emergency, Debt Quality + Entity Cashflow, Net Worth Trend + Entity Comparison, Insights + Budget, Money Bleeding + Categories, Asset Allocation + Insights, Holdings tabs).
  - [x] Author the corrected `DESIGN.md` (warm ivory tokens replacing the cool-blue placeholder, TRAIL spectrum named colors, navy-tinted shadows, paired metric tile + Money Story Hero patterns).
  - [x] Land the corrected DESIGN.md as a tracked artifact: `docs/design/MONITRAX_STITCH_DESIGN_SYSTEM.md`.
  - [x] Create a new Stitch design system "Monitrax — Restrained Editorial" (asset `5eb40c25ecd946828ee9ba4d60c0662c`) — left the 4 existing design systems (Monitrax / Monitrax Core / Deep Cosmos / Deep Cosmos Editorial / Deep Cosmos OS) untouched so the 45 existing website screens don't drift.
  - [x] First seed screen — Money Story Hero card alone (Stitch screen `81e67b3e78934fd1aad6a8e81ab2cb2a`, title "Monitrax Money Story - Dashboard Hero").
  - [x] First edit pass — add TRAIL Stage Indicator above + 6 Metric Tiles grid below (Stitch screen `127693673f554c199966d52d3a1db8a3`, title "Monitrax Dashboard - Expanded Wealth View"). **3 of 14 sections complete.**
  - [x] **Second edit pass ✅ 2026-05-27** — appended Health Score + Emergency Fund pair (row 1) and Debt Quality + Entity Cashflow pair (row 2) below the 6-tile grid (Stitch screen `9d0d72acf1ad4a1c8b041651bab3966a`, title "Monitrax Dashboard - Diagnostic Deep Dive", 4612px tall). The 5-min `MCP_TOOL_TIMEOUT` from PR #899 was sufficient — edit completed first try. Restrained Editorial discipline preserved: navy text, sparing emerald, amber for the cashflow-negative SMSF entry (not red). Screenshot saved at `.stitch/designs/dashboard-edit-pass-2.png`. **5 of 14 sections complete.**
  - [x] **Third edit pass ✅ 2026-05-27** — appended Net Worth Trend area chart + Entity Comparison horizontal bar chart pair below the Debt Quality / Entity Cashflow row (Stitch screen `b4fd60be40f64fec947fd20c660f1625`, title "Monitrax Dashboard - Wealth Trajectory Pass", 5866px tall). 280px area chart on the left with 1Y segmented selector; 4-row entity comparison on the right (Personal $412,800 / Trust $258,400 / ABC Pty Ltd $132,920 / SMSF $43,200). Screenshot saved at `.stitch/designs/dashboard-edit-pass-3.png`. **7 of 14 sections complete.**
  - [x] **Fourth edit pass ✅ 2026-05-27** — appended Actionable Insights (8-col wide, 5 TRAIL-coloured insight rows with severity chips HIGH/WIN/MEDIUM/MEDIUM/LOW) + Monthly Budget Summary (4-col narrow, 4-row list + REMAINING block + 86% progress bar) (Stitch screen `e70424a8aa2743f6a7801aa24946e285`, title "Monitrax Dashboard - Priorities & Budgeting Pass", 7158px tall). Screenshot saved at `.stitch/designs/dashboard-edit-pass-4.png`. **9 of 14 sections complete.**
  - [x] **Fifth edit pass ✅ 2026-05-27** — appended Money Bleeding (5 amber-tinted "Quiet money leaks" rows with Lucide icon tiles + percentage-vs-avg) + Spending by Category (5-segment horizontal stacked bar + 5-row legend with vs-plan helpers) (Stitch screen `8bef96ac9de74985b50143847fae4086`, title "Monitrax Dashboard - Spending Deep Dive Pass", 8414px tall). Screenshot saved at `.stitch/designs/dashboard-edit-pass-5.png`. **11 of 14 sections complete.**
  - [x] **Sixth edit pass ✅ 2026-05-27** — appended Asset Allocation (8-col, 220px donut with TRAIL-spectrum 4-segment composition + 4-row breakdown with $-amount and %-of-total) + Portfolio Observations (4-col, 3 insight blocks with TRAIL-coloured Lucide icons) triplet (Stitch screen `126531e6a60b488aa8953fcfb4ff0b55`, title "Monitrax Dashboard - Asset Allocation Pass", 9904px tall). First attempt hit Stitch 503 service-unavailable; succeeded on retry. Screenshot saved at `.stitch/designs/dashboard-edit-pass-6.png`. **13 of 14 sections complete.**
  - [x] **Seventh edit pass ✅ 2026-05-27** — appended full-width Holdings section: "WEALTH IN DETAIL" eyebrow header + segmented tab strip (Properties active / Investments inactive) + 2-col panel grid with 3 property cards (chip + name + 2×2 metric grid + monthly-cashflow strip) and a dashed "Add property" 4th tile (Stitch screen `f723372ebbd943b197770129eff849a2`, title "Monitrax Dashboard - Holdings Detail Pass", 11704px tall). Screenshot saved at `.stitch/designs/dashboard-edit-pass-7.png`. **All 14 content sections composed.**
  - [x] **Eighth edit pass ✅ 2026-05-27 (with caveat)** — appended app shell (left sidebar 240px wide with 8 TRAIL nav items + "Start here" 2FA hint + user profile, topbar with "Welcome back, Reza" + date + search pill + bell + settings, Pending Actions banner with amber tile + emerald "Review actions" CTA, Daily Pulse strip in surface-container-low). Stitch capped the composition at 2048px (working-context limit), so the content below the Money Story Hero was truncated in this screen. **This is treated as a natural split — shell composition locked at screen `2543c8240b944c8fa6b6e89d20ac8e77` ("Monitrax Dashboard - Full Application Shell"), full 14-section content stack lives at screen `f723372ebbd943b197770129eff849a2`.** React port will compose both. One known mojibake nit: em-dash in "Stage 2 — Reduce" rendered as `â€"` — fixable at port time. Screenshot saved at `.stitch/designs/dashboard-edit-pass-8.png`.
  - **Stitch composition complete.** All 14 content sections + app shell composed and locked. Ready for Reza review → React port to `/dashboard/page.tsx`.
  - [ ] Reza review of full 14-section composition; lock or redirect.
  - [x] **Port to React — Phase R1 ✅ 2026-05-27 (foundation only).** Reza directive "Procees" (Proceed) — start the React port. Phase R1 ships the foundation: `editorial-*` Tailwind tokens (warm ivory / paper / navy / emerald / amber / TRAIL spectrum + navy-tinted shadows) in `app/globals.css` + `tailwind.config.ts`; `@layer utilities` typography utilities (eyebrow, data-xl, data-lg, headline-md/sm, tabular-nums-data); seven canonical primitives in `components/editorial/` (`Eyebrow`, `DataValue`, `DeltaChip`, `EditorialCard`, `MetricTile`, `PairedMetricCard`, `EditorialMoneyStoryHero`) with barrel export. **No consumer is migrated yet** — the existing dashboard continues unchanged; the editorial namespace lives alongside cosmos-* (public) and brand-* (legacy app) without mutating either. CLAUDE.md §16.3 row "UI / design system change" docs updated: `06_UI_UX_FOUNDATION.md` §16.x (full tokens + primitives + ban-list + reviewer enforcement) + `08_BRAND_UI_DESIGN.md` (three brand-level rules + coexistence rules). Pre-existing `tsconfig` deprecation warning only — primitives type-check clean. Subsequent phases queued below.
  - [~] **Port to React — Phase R-Charts (interactive charts Stitch design + components) — IN PROGRESS 2026-05-27.** Reza directive: "the most important thing is the interactive graphs." Generated desktop charts suite Stitch screen `82390998efbe4d3e96fd304ec8ec1ba1` covering 6 surfaces: (1) Net Worth Trend bezier line + emerald area fill + time-period selector + hover tooltip, (2) Asset Allocation Donut with 4 TRAIL segments, (3) Cashflow Bars 6mo paired income/expense, (4) Entity Comparison horizontal bars, (5) Spending Sparklines 3×2 small-multiples by category, (6) Debt Freedom 8-year stacked area. Audit clean — 0 glass / 0 gradient-bg / 0 red. Files at `.stitch/designs/dashboard-interactive-charts.{html,png}`.
    - [x] **R-Charts-1 — chart primitives + 3 real-data charts ✅ 2026-05-29 (this PR, labs preview).** Reza greenlit "Yes". Built 5 primitives in `components/editorial/charts/`: `EditorialChartCard` (shared eyebrow+headline+slot card shell), `EditorialChartTooltip` (styled popover wrapper — Recharts v3 dropped `payload` from `TooltipProps`, so each chart defines a minimal local payload type), `EditorialDonutChart` (Recharts PieChart donut + centre total + legend; Property=sky / Investments+super=emerald / Cash=indigo / Other=muted), `EditorialBarChart` (Recharts grouped vertical bars — emerald earned vs amber spent, 6mo), `EditorialEntityBars` (CSS diverging horizontal bars — positive emerald right, negative amber left, never red). Wired to **live data** via new dedicated endpoint `app/api/dashboard/charts/route.ts` (`report.read`) — distinct concern from `/insights` (§12.4). New canonical aggregation `lib/calculations/entityValueBreakdown.ts` reuses `calculateTotalAssets`/`calculateTotalLiabilities` per `ownerEntityId` (SSOT — per-entity figures sum to the Net Worth tile). ALL arithmetic server-side (donut pct, cashflow avg net) so the chart components stay presentational + linter-clean (Phase 41i.6b: 0 new violations). Preview route `/dashboard/labs/charts` reads real `/api/dashboard/charts`, each chart ships a quiet empty-state. Theme-aware via editorial-* tokens. **Net Worth Trend, Spending Sparklines, Debt Freedom NOT in this PR** (R-Charts-2/3).
    - [x] **R-Charts-2 — Net Worth Trend (real monthly-snapshot store) ✅ 2026-05-30 (this PR).** Replaced the legacy `NetWorthTrend` component (Math.random backfill) with an honest editorial line chart reading a new `NetWorthSnapshot` table. Architecture decision: **stored monthly snapshots, not compute-on-read** (compute-on-read needs per-asset historical values the app doesn't maintain). Recorder `lib/services/netWorthSnapshotRecorder.ts` upserts one row per (userId, first-of-month UTC) fire-and-forget on every `/api/dashboard/charts` load; canonical reader `lib/calculations/netWorthHistory.ts` returns empty when <2 months accrue (consumer shows "Trend unlocks after 2 months" empty state, mirrors Money Story v2). New `EditorialLineChart` primitive (Recharts bezier `<AreaChart>` + emerald area fill + styled tooltip). Legacy `NetWorthTrend.tsx` deleted (~270 lines, including `generateNetWorthTrendData` + `CompactNetWorthTrend` + local Sparkline). Additive migration `20260530030000_phase_r_charts_2_net_worth_snapshots` (CREATE TABLE only; §12.11 + §12.12 checklists pass). Existing months stay empty for current users (chart unlocks once two visits across two calendar months accrue); future R-Charts-2.1 hardening: Cloud Scheduler job to snapshot end-of-month instead of last-visit time.
    - [ ] **R-Charts-3 — Spending Sparklines (per-category small-multiples) + Debt Freedom stacked area.** Spending sparklines need per-category historical aggregation; Debt Freedom needs an amortisation projection. Both deferred behind R-Charts-1/2.
    - [~] **R-Charts consumer swap — Asset Allocation + Entity Comparison ✅ 2026-05-29 (this PR).** Wired 2 of 3 R-Charts-1 primitives onto `/dashboard`: the Asset Allocation card (was: local `DonutChart` SVG + hard-coded blue/green/purple progress-bar legend) and the Phase-3/4 row's Entity Comparison (was: `EntityComparisonChart` showing cashflow-by-entity). Dashboard now does 3 parallel API calls (`/portfolio/snapshot` + `/insights` + `/charts`) — chose separate call over folding into `/insights` to preserve §12.4 "one concern per endpoint" and keep the labs route contract. Dead code removed in same PR: local `DonutChart` (~80 lines), local `ProgressBar` (~25 lines), `getAssetAllocationData` helper. **Cashflow Bars NOT swapped** — adding a new dashboard section bloats the page; placement is a separate UX decision worth its own review. Stays in `/dashboard/labs/charts` preview.
    - [ ] **R-Charts consumer swap — Cashflow Bars placement.** Decide whether the 6-month earned/spent bars deserve a new dashboard section, replace the existing 12-month Cashflow KPI sparkline, or stay labs-only. The KPI sparkline gives long-context trend; bars give short-window monthly comparison — they're complementary, not duplicate.
    - **Next:** Reza review of `/dashboard/labs/charts`; optional mobile Stitch pass for the charts suite; then R-Charts-2.
  - [x] **Port to React — Phase R2b (DashboardLayout consumer swap) ✅ 2026-05-27 PR #904.** Flipped the existing 707→441 line `DashboardLayout.tsx` to compose `EditorialSidebar` + `EditorialTopBar` + `EditorialBottomNav`. All providers / modals / floating buttons / onboarding wiring preserved. 33 consumer pages unchanged.
    - [x] **R2b follow-up — desktop sub-nav regression fix ✅ 2026-05-29 (this PR).** Reza caught it on desktop: under My Wealth only Properties was reachable — no way to get to Investments / Assets (mobile was fine via `SectionTabsRow`). Root cause: the R2b swap dropped the legacy sidebar's active-section child expansion, and `SectionTabsRow` is `md:hidden` (defers desktop sub-nav to the sidebar). Fix: `EditorialSidebar` now renders the active section's `children` (SSOT `trailNavItems`) as indented sub-tab links on desktop. `EditorialNavRow` already had the `dense` nested-row mode — sub-nav was always intended, just never wired.
  - [x] **Port to React — Money Story v2 "Freedom Horizon" (Ribbon variant) ✅ 2026-05-27 (component build + labs preview).** Reza locked Variant B "The Ribbon" from PR #907's 3-variant Stitch exploration (sources: 75f9abb1c1d54fe9baaa0e01755348cd lock, 888f0c4fa9944222b5cf44e1ed7fcfd4 ribbon variant). Built 4 React primitives in `components/editorial/money-story/`: `MoneyStoryHeroV2` (full tile), `FreedomRibbonChart` (Recharts stacked-area ribbon — Spent slate base + Kept emerald band on top, widening over time), `TimePeriodPill` (framer-motion layout-animated segmented control), `BreathingDot` (8px emerald pulse next to hero). framer-motion count-up on the hero years number (1.1s ease-out), Recharts default area-path draw on the ribbon, all respecting `prefers-reduced-motion`. Theme-aware via the editorial-* CSS variables (R2c). Preview route at `/dashboard/labs/money-story` renders the tile with synthetic data + a labelled "synthetic preview" banner + a design-rationale `<details>` block. Shipped via PR #908.
  - [x] **Port to React — Money Story v2 consumer swap ✅ 2026-05-27 (real-data wiring + dashboard swap).** Reza approval "I like it let's ship it" → wired V2 onto `/dashboard`. New service `lib/calculations/moneyStoryTrend.ts` aggregates the user's own `prisma.transaction` records into a 12-month spent/kept ribbon series (HONEST — never simulated; existing `generateNetWorthTrendData` uses simulated data, that's a separate pre-existing concern). Returns empty trend when <2 months of activity so the consumer can gracefully hide the ribbon. `app/api/dashboard/insights/route.ts` extended `moneyStory` block with `trend` + `marginDeltaPoints` + `freedomYears` fields. `app/dashboard/page.tsx` consumer flipped `EditorialMoneyStoryHero` (Phase R3) → `MoneyStoryHeroV2` (the Freedom Horizon Ribbon). Empty-trend handling: V2 now renders a quiet dashed-outline placeholder ("Your ribbon unlocks after 2 months of recorded activity") so users without history still get the hero + KPI strip. R3's `EditorialMoneyStoryHero` stays in the codebase for any future surface that wants the 3-row Earned/Kept/Free version.
  - [x] **KPI tiles — Stitch 3-variant exploration ✅ 2026-05-28 PR #910.** One canvas (screen 2294f8b6a64d4e2db78b10275ce53e4f), three design languages applied to 5 dashboard KPI tiles (Monthly Cash Flow, Annual Outgoings, Annual Income, Saving Rate, Portfolio LVR — Net Worth intentionally excluded). A = Sparkline-Forward (Mercury), B = Paired Metric (Copilot), C = Band/Context (iOS Health). All compliance-clean (0 glass, 0 gradient, 0 red).
  - [x] **KPI tiles — component build (hybrid A + C) ✅ 2026-05-28 (this PR).** Reza locked direction "for monthly cash flow go with A, for the rest go with option C". Built 3 primitives + zone constants in `components/editorial/kpi/`: `EditorialKpiCard` (unified card with `variant: 'sparkline' | 'band'` discriminated union), `EditorialKpiSparkline` (inline SVG path with Catmull-Rom smoothing, 60px, emerald/amber/slate tones), `EditorialBandTrack` (6px segmented zone bar with white thumb + 2px coloured ring at the active zone), `zones.ts` (canonical AU-financial-planning bands: LVR_ZONES, SAVING_RATE_ZONES, INCOME_YOY_ZONES, OUTGOINGS_YOY_ZONES with `resolveZone()` + `thumbPosition()` helpers). Preview route at `/dashboard/labs/kpi-tiles` with synthetic data + labelled banner + design-rationale block. Theme-aware via editorial-* tokens (R2c). Net Worth tile intentionally untouched. **Tweaked PR #912:** Reza moved Income + Outgoings from band → sparkline (flow metrics all sparkline; only Saving Rate + LVR stay band). Also added a `/dashboard/labs/*` route error boundary + hardened the sparkline gradient ID for React-19 `useId()`.
  - [x] **KPI tiles — consumer swap (live dashboard) ✅ 2026-05-28 (this PR).** Reza "Looks good ship them" → wired the 5 KPI tiles onto `/dashboard` with real data. Extended `lib/calculations/moneyStoryTrend.ts` to also return raw monthly series (`monthlyEarned`, `monthlySpent`, `monthlyNetCashflow`) + precomputed deltas (`cashflowDeltaMonthly`, `incomeDeltaPct`, `outgoingsDeltaVsAvg`) — ALL arithmetic in the service (the Phase 41i.6b surface linter scans only `app/dashboard`, `app/portal`, `components`, not `lib` or `app/api`). `app/api/dashboard/insights/route.ts` exposes a new `kpiTiles` block (series + deltas + precomputed `outgoingsAnnual`/`outgoingsMonthly`/`incomeMonthly` so the page does ZERO inline math). `app/dashboard/page.tsx` swapped the 5 StatCard tiles → `EditorialKpiCard` (Cash Flow/Income/Outgoings = sparkline, Saving Rate/LVR = band). Net Worth tile kept as StatCard. Drill-down detail modals preserved via the `onClick` wrappers; `CalculationTooltip` + cashflow period toggle dropped (the sparkline + detail modal cover that context). Financial-math baseline updated: grid income/12 violation removed (tile gone), 7 downstream violations repositioned. Theme-aware (R2c). Headline values from canonical `snapshot.*`; sparkline series from honest transaction aggregation.
  - [~] **Port to React — Phase R2c (audit + dark-mode fix) — IN PROGRESS 2026-05-27.** Reza audit on mobile (iOS Safari) found editorial chrome rendered LIGHT-ONLY against a dark-mode legacy body — devastating contrast (washed-out text, dark navy cards on ivory page, floating buttons in old chrome). **Root cause:** all `--editorial-*` CSS variables defined only on `:root`; no `.dark` overrides. Legacy `--background` / `--card` / `--foreground` tokens DO flip via `.dark`, so legacy components inside pages went dark while the new chrome stayed ivory. **Fix shipped here:** added full `.dark` override block in `app/globals.css` (deep-navy ground `#050913`, navy-tinted card `#0E1424`, near-white text `#FAFAF7`, brighter emerald `#22C55E` for AA contrast on dark, navy/white-rim shadows). Tailwind config converted to `hsl(var(--editorial-X) / <alpha-value>)` format so slash-opacity utilities (`bg-editorial-amber/10`) work in both themes. `DeltaChip` switched from hardcoded `bg-amber-100` / `bg-slate-100` (light-only) to `bg-editorial-amber/10` / `bg-editorial-slate/15` (theme-aware). **Out of scope for this PR (queued):** (a) Properties hero card + property tiles look broken on mobile — they're legacy dark-mode-only components, fix lands with Phase R6 (Holdings tab restyle); (b) "Where your wealth is" font contrast — existing Balances page styling, looks worse on ivory than the old neutral bg, full re-style in Phase R5; (c) Money Story `5255 days of life` label — math is correct (existing data wiring from Phase 43 cosmos hero), label rewording deferred to a copy pass; (d) SectionTabsRow + legacy Tabs.tsx render as dark navy pills — migrate to editorial in a follow-up.
  - [x] **Port to React — Phase R3 (Money Story Hero swap-in) ✅ 2026-05-27 (PR pending).** Replaced `MoneyStoryHero` consumer at `app/dashboard/page.tsx:611` with `EditorialMoneyStoryHero`. Existing `MoneyStoryHero` (cosmos / glass variant) stays in the codebase. `taxWithheld` + `surplus` props dropped (editorial hero omits the MoneyStoryBar segmentation). 14-line diff. Type-checks clean.
  - [~] **Port to React — Phase R-Mobile (mobile Stitch sweep) — IN PROGRESS 2026-05-27.** Reza directive: "make sure that the mobile view is also optimised via stitch and is not missed out." Generated two mobile Stitch screens via `mcp__stitch__generate_screen_from_text` (project `1859462351962811110`, design system asset `5eb40c25ecd946828ee9ba4d60c0662c`, `deviceType: MOBILE`, model `GEMINI_3_1_PRO`): v1 (`dc038cd19aea4cfc8d0300f4d9122ffd`) had `glass-card`+gradient defects from a loose prompt; v2 (`466dc8550c1f4b1898459a8359696584`) clean — 0 glass / 0 gradient / 0 multi-colour. **Defect surfaced in the design-system asset itself:** the `namedColors` block contains pre-redesign greenish placeholders (`#f4fcf0`, `#006b2c`) that leak into the inline Tailwind config of every generated screen. Currently harmless (markup uses `bg-surface-ivory` from `colors:` which IS `#FAFAF7`), but should be fixed via `mcp__stitch__update_design_system` before the next sweep. Both HTML+PNG files committed at `.stitch/designs/dashboard-mobile-{content-stack,v2}.{html,png}`. **Awaiting Reza approval on v2 before further mobile generations.** Desktop screens (`f723372ebbd83b197770129eff849a2` content, `2543c8240b944c8fa6b6e89d20ac8e77` shell) remain locked.
  - [x] **Port to React — Phase R2 (app shell primitives) ✅ 2026-05-27 (foundation only).** Built 5 editorial shell primitives in `components/editorial/shell/`: `EditorialNavRow` (44px row with 3px emerald left accent on active), `EditorialSidebar` (240px desktop left rail using `trailNavItems` SSOT from `lib/navigation/trailNav.tsx`), `EditorialTopBar` (responsive 56px mobile / 64px desktop, with AEST date eyebrow + welcome name + search pill + bell + avatar), `EditorialBottomNav` (mobile-only 5-cell fixed bottom nav: Home / Accounts / Wealth / Guide / More with safe-area-inset padding + 3px emerald top accent on active), `EditorialAppShell` (orchestrator wrapping all three around `<main>`). Barrel exports through `components/editorial/index.ts`. **No consumer is migrated yet** — the existing 707-line `DashboardLayout.tsx` continues unchanged. Consumer swap planned for Phase R2b once Reza confirms the visual direction on preview.
  - [x] **Port to React — Phase R4 (metrics grid migration) ✅ 2026-05-30 (this PR).** Completed in two waves: (a) **Wave 1 (R-KPI, 2026-05-28 — PRs #912/#913)** migrated 5 of the 6 metric tiles (Cash Flow / Income / Outgoings / Saving Rate / LVR) to `EditorialKpiCard` with sparkline + band variants. (b) **Wave 2 (this PR)** elevated the remaining Net Worth `StatCard` out of the 6-tile grid into a full-width `PairedMetricCard` hero (Copilot Assets / Debts split) between the Money Story hero and the now-5-tile KPI strip. IA: forward-looking hero → current-position hero → drill-down strip. Click-through detail modal preserved; legacy `StatCard` + `CalculationTooltip` imports removed (orphaned by the swap).
  - [x] **TRAIL palette unification ✅ 2026-05-30 (this PR).** Reza directive: TRAIL stage indicator on `/dashboard` was showing the wrong colour per stage (T=orange, R=red, A=teal, I=blue, L=yellow) because its local `stages` array hard-coded a bespoke palette that bypassed the canonical `TRAIL_STAGE_TONES` SSOT. Fix: rewrote `stages` to consume `TRAIL_STAGE_TONES[<letter>].hex` (added `hex.{base,soft,veil}` to the SSOT for non-Tailwind use), switched per-stage Tailwind tokens to single-hue gradients matching the canonical palette, and reverted `TRAIL_STAGE_TONES.L` from fuchsia → violet (locks behavioural-psychology mapping: mastery/aspiration). Documented colour psychology rationale in both the SSOT JSDoc and the changelog (T=sky·trust/clarity / R=amber·attention-not-alarm / A=indigo·depth/security / I=emerald·growth/money / L=violet·mastery/freedom). Downstream consumers (SectionTabsRow, MobileTabBar, MoneyStoryHero) pick up the L=violet change automatically via the SSOT; no per-consumer edits needed.
  - [x] **Port to React — Phase R5 (paired diagnostic rows) ✅ 2026-05-30.** All paired diagnostic rows now fully editorial. Shipped across 5 PRs: Asset Allocation + Net Worth Trend + Entity Comparison via the R-Charts work (PRs #929 / #930); **R5 PR1 (PR #932)** restyled `InsightWidgets.tsx` end-to-end — 6 widgets covering Health + Emergency / Insights + Budget / Money Bleeding + Spending; **R5 PR2 (PR #934)** restyled `DebtQualityWidget.tsx` — editorial score arc, category cards in emerald/sky/amber (Bad Debt → amber, NEVER red), warm action-oriented language; **R5 PR3 (this PR)** restyled `EntityCashflowSummary.tsx` — surgical color migration (green→emerald, red→amber, blue→sky) + Card → EditorialCard wrapper rewrite, preserving the 6-tab structure + helper functions. Bonus cleanup across the three PRs: 3 grandfathered `0.15` HARDCODED_FINANCIAL_CONSTANT violations in MonthlyBudgetSummary eliminated (PR1); unused `getLoanTypeIcon` + 5 lucide imports + 2 legacy multi-color helpers removed (PR2); 5 more unused lucide imports + legacy `Button` import removed (PR3). **Out of scope (future hygiene):** legacy `@/components/ui/tabs` base component still renders with `bg-muted` / `text-muted-foreground` ground — its editorial restyle is a separate piece, not blocking R5 close.
  - [ ] **Port to React — Phase R6 (Holdings tabs)** — restyle the Properties / Investments tabbed view with editorial tab strip, property cards with chip + 2×2 metric grid + cashflow strip, dashed "Add property" empty-state tile.
  - [ ] **Port to React — Phase R7 (final compose + verify)** — full dashboard verification in browser (CLAUDE.md "For UI or frontend changes, start the dev server and use the feature in a browser"), regression sweep against existing data wiring (`getMasterFinancialSnapshot`, `/api/dashboard/insights`), mobile responsive pass.
  - [ ] Update canonical design system docs (`docs/architecture/06_UI_UX_FOUNDATION.md` + `08_BRAND_UI_DESIGN.md`) with the Restrained Editorial palette + Money Story Hero + paired metric tile patterns (§16.3 row "UI / design system change").
- **Risk:** Low blast radius — visual-only iteration; nothing ships to prod until the React port lands. Stitch-side timeouts on big edits force small incremental edits, but each step is reviewable.
- **Blocking:** none — but session-level MCP timeout (60s default) makes large `generate_screen_from_text` / `edit_screens` calls unreliable. **Fix already merged in PR #899** (raises `MCP_TOOL_TIMEOUT` to 300000ms via `.claude/settings.json` env block) — effective for sessions started AFTER the merge. Resume the remaining edit passes in a fresh session for the 5-min window.
- **Why this matters:** the current `/dashboard/page.tsx` is functional but visually a tax-spreadsheet — paired metrics rendered as 6 disconnected stat cards, no editorial whitespace, no calm hierarchy. The Money Story Hero + restrained palette pivot is the foundation for the "this feels premium" reaction we want returning users to have — and Apple/Mercury/Linear are the calibration references. Three of 14 sections are now in the new language; remaining 11 layer in over subsequent sessions.

### 0·Φ44. Phase 44 — Entity Graph & Structure Modelling

- **Status:** 🟢 BUILDING — **Part 1 complete** (1a + 1b + 1c + 1d merged). **Part 2 build complete** — 2a + 2b + 2c-i + 2c-ii merged; 2d shipping. Remaining: the non-blocking Part 2 follow-ups.
- **Started:** 2026-05-20 (Reza brief: "enrich the entity function … capture all combinations possible for financial/company structures … make sure the rules and logic and connections are done correctly so all the calculations in monitrax will be done correctly").
- **Owner:** Reza (direction + structural review) + Claude (research, design, build).
- **Last touched:** 2026-05-22 — Parts 1a–1d + Part 2a–2c-ii merged (PRs #864–#874); Part 2d (`MoneyFlowSankey` distributions upgrade) shipping.
- **Phases:**
  - [x] Deep research — AU entity/relationship rules + Phase 41 codebase gap analysis (Explore agent)
  - [x] Design doc `PHASE_44_ENTITY_GRAPH.md` — node + edge taxonomy, validity matrix, proposed schema, tax-treatment + SSOT commitment, legal-positioning, migration, phased build
  - [x] Open questions Q1-Q4 resolved (Reza, 2026-05-20)
  - [x] Adversarial design review #1 (internal review agent, source-verified vs ATO/ASIC) — 4 critical issues; incorporated → v2
  - [x] Adversarial design review #2 (external AI / ChatGPT) — found v2 still over-claimed completeness, the SMSF corporate-trustee rule still too loose, `Float`-not-`Decimal`, legal-title/beneficial-ownership/control blurred, binary accept/reject too rigid; **ALL incorporated → v3** (new §3A three-dimensions, §6.1 three-state model, §6.3 corrected SMSF rule, §6.5 cycle detection, `OwnershipGroup` split, `BeneficialOwnershipOverride`, executor/LPR/family/associate edges, residency/jurisdiction metadata, §8 tax corrections). See doc §14 + §15 acceptance criteria.
  - [x] Reza reviewed + approved the v3 design doc 2026-05-20; accountant sanity-check deferred to Basiq-onboarding prep (D-Day Bundle T3.4)
  - [x] **Part 1a — schema + migration ✅ MERGED (PR #864, 2026-05-22)** — `prisma/schema.prisma`: 5 new models (`EntityRelationship`, `ShareParcel`, `OwnershipGroup`, `OwnershipStake`, `BeneficialOwnershipOverride`) + 8 new enums; `LegalEntityType` +12 / `LegalEntityRole` +1 / `TrustType` +2; 22 additive `LegalEntity` columns. Migration `20260522030000_phase_44_entity_graph_part_1a` — purely additive, includes the `parentEntityId`→`TRUSTEE_OF` data migration. All financial fields `Decimal`. A follow-up commit reconciled the type mirrors broken by the widened enums (type-level only, no behaviour change).
  - [x] **Part 1b-i — rules engine ✅ MERGED (PR #865, 2026-05-22)** — `lib/entity-graph/{types,validityMatrix,queries}.ts` — the §6 grammar (edge legality, the three-state classifier, the corrected SMSF rules, entity validity, cycle detection) + pure graph traversal (`getTrusteesOf`, control, ownership chains, the associate test). Pure functions, zero I/O, zero tax arithmetic (§8.3 / §8.4). 21 tests encode the §14.1 acceptance cases.
  - [x] **Part 1b-ii — service layer ✅ MERGED (PR #866, 2026-05-22)** — `lib/services/entityRelationshipService.ts` (the only writer of `EntityRelationship`; loads the graph, classifies via `validityMatrix` inside the write transaction, persists the §6.1 state to the edge + every affected entity, audited) + `ownershipService.ts` (`OwnershipGroup`/`OwnershipStake`, joint-tenancy survivorship) + `beneficialOwnershipService.ts` (`BeneficialOwnershipOverride`, §3A). 9 helper tests. **`parentEntityId` repoint:** no `lib/calculations/` engine reads it; the only reader is the tax-entity route (`app/api/tax/entity/[entityId]/route.ts`), whose repoint belongs to Part 2's tax-engine rewire (§11 — "Part 1 does NOT touch the tax engine").
  - [x] **Part 1c — entity-section UI ✅ MERGED (PR #867, 2026-05-22)** — the §11A entity-structure canvas (`components/entities/EntityCanvas.tsx` on **React Flow + `@dagrejs/dagre`** — library choice confirmed with Reza): Control / Ownership / Money-flow / All lens toggle, role-coloured Apple-glass boxes, §6.1 three-state badges, layered dagre auto-layout + persisted manual nudge, click-through entity + relationship detail dialogs. The money-flow Sankey is folded in as a lens (the separate Money Flow tab is removed). Plus the API routes exposing the Part 1b-ii services (`/api/entities/graph`, `/relationships*`, `/ownership-groups*`, `/beneficial-ownership*`, `/[id]/verify`, `/relationships/[id]/verify`); the joint-ownership UI (`OwnershipGroupsDialog`, Q1); and the accountant-review share-pass (`/dashboard/entities/accountant-review`, Q4). Presentational-only canvas — reads `lib/entity-graph/queries.ts` + `validityMatrix`, computes nothing financial (§8.3 / §8.4); `parentEntityId` stays frozen (tax-route repoint is Part 2). `EntityTree.tsx` retained — still used by the accountant portal client view. Branch `claude/phase-44-part-1c-entity-canvas-ONspp`.
  - [x] **Part 1d — onboarding wizard relationship sub-step 🟡 SHIPPING** — a new optional wizard step `entity-relationships` ("Who runs what", `components/onboarding/wizard/steps/RelationshipsStep.tsx`) straight after the entity step; `getStepsForProfile` hides it unless the user has ≥1 non-personal entity. Per-entity collapsed cards capture the load-bearing edges (directors + shareholders, trustee + beneficiaries/unitholders, members + trustee, partners, sole-trader operator) via toggle-chip pickers; pre-seeds `TRUSTEE_OF` from the entity step's trustee link. `WizardData.relationships` + `lib/onboarding/relationshipsSync.ts` persist the skeleton via the Part 1c `/api/entities/relationships` route — best-effort (never traps the user, §11 "finishable later"), idempotent on resume. Capture depth "control + ownership edges" confirmed with Reza. 9 tests. Branch `claude/phase-44-part-1d-wizard-relationships-ONspp`.
  - [ ] **Part 2 — money-flow + tax-engine rewire 🟡 BUILDING** — design doc `docs/blueprint/PHASE_44_PART_2_MONEY_FLOW_TAX_REWIRE.md` v2 (external AU-tax-law review incorporated, doc §13) ✅ MERGED (PR #870).
    - [x] **2a — schema ✅ MERGED (PR #871, 2026-05-22)** — 4 new models (`DistributionResolution`+`DistributionAllocation`, `DividendDistribution`+`DividendPayment`, `PrivateCompanyBenefit`, `TrustDeedRule`) + 3 enums; migration `20260522100000_phase_44_part_2a_money_flow` — purely additive; applied to dev + prod.
    - [x] **2b — model services + routes 🟡 SHIPPING** — 4 SSOT service writers (`distributionResolutionService`, `dividendDistributionService`, `privateCompanyBenefitService`, `trustDeedRuleService` — audited, §12.11-compliant, digital-twin warn-not-reject) + 8 thin `/api/tax/*` routes (`withPermission`, `tax_data.*`). 9 data-quality tests. Branch `claude/phase-44-part-2b-services-ONspp`.
    - [x] **Open questions decided (Reza, 2026-05-22):** Q-SMSF → YES (build `smsfIncomeTax.ts`); Q-UPE → YES (engine stays `UNCOMPUTED` on contested sub-trust UPEs); Q-PARTNERSHIP → DEFER (partnerships `UNCOMPUTED`; a `PartnershipDistribution` follow-up later).
    - [x] **2c-i — `smsfIncomeTax.ts` 🟡 SHIPPING** — the new SMSF fund-earnings income-tax module (Div 295 — 15% concessional rate / ECPI with the §13-F9 carve-outs / NALI top-rate / non-complying top-rate; `UNCOMPUTED` honesty gate when the ECPI proportion is unknown for a pension-phase fund). Wired into `entityTaxRouter`'s SMSF branch; reachable via `POST /api/tax/entity`. 9 tests. Branch `claude/phase-44-part-2c-i-smsf-income-tax-ONspp`.
    - [x] **2c-ii — assembler + route repoint 🟡 SHIPPING** — `lib/services/entityTaxFactsAssembler.ts` (the SSOT for building `EntityTaxFacts`) feeds `trustDistribution` from `DistributionResolution` + `div7aLoans` from `PrivateCompanyBenefit` `LOAN` rows (Q-UPE — UPE/sub-trust excluded). `GET /api/tax/entity` repointed → real numbers, not `UNCOMPUTED`. Dead `parentEntityId` read removed (route + `EntityTaxFacts`). 10 mapper tests. Branch `claude/phase-44-part-2c-ii-assembler-ONspp`.
    - [x] **2d — `MoneyFlowSankey` upgrade 🟡 SHIPPING** — `getMoneyFlow()` gains a `Distributions` outflow column fed by CONFIRMED `DistributionResolution` (trust → beneficiary) + `DividendDistribution` (company → shareholder); the §11A money-flow lens now shows recorded inter-entity distributions. Pure mapper `buildDistributionDetail()` (exported from `moneyFlowService.ts`, 6 unit tests); surplus recomputed net of distributions; trust/company keeps its row even with no `Income` rows (warn-not-reject §6.1). `MoneyFlowSankey` gains an indigo `Distributions` node + per-recipient detail list + an entitlement-not-cash caveat (review §13-F22); `ConsumerMoneyFlowSankey` updated for the new field (`Distributions: 0`, `distributions: []`). `SuperContribution` member→SMSF limb deferred — double-count risk needs its own design pass (§6.4). Branch `claude/phase-44-part-2d-money-flow-ONspp`.
    - **Part 2 follow-ups** (post-2d, not blocking): `SuperContribution` money-flow limb (member→SMSF, double-count-safe design); `smsfIncomeTax` GET-feed (needs pension-phase/ECPI facts captured); `trustMinimumTax`→`TrustDeedRule` consumption (when the reform skeleton leaves Stage 1); `PartnershipDistribution` (Q-PARTNERSHIP deferral); the non-loan Div 7A limbs; `s99` (G-S99); per-asset CGT-event assembly.
    - **Part 2 overview** — opens with a full 45-module tax-engine audit. Central finding: the engine is already pure + built + correct for current law — the gap is the **data layer** (trust/Div7A/SMSF dispatch data only reaches the engine via a `curl` POST body; nothing persists it). Part 2 is therefore a data-layer build: 4 new models (`DistributionResolution`, `DividendDistribution`, `PrivateCompanyBenefit`, structured `TrustDeedRule`) + a canonical `entityTaxFactsAssembler` + repoint `GET /api/tax/entity` to real numbers + `MoneyFlowSankey` upgrade. Engine compute modules unmodified (§8.3 holds) bar one flagged gap (`super/smsfIncomeTax.ts`). Build sequence 2a–2d, each review-gated; 2a blocked on design-doc approval.
- **Risk:** High legal weight — wrong entity/relationship modelling → wrong tax numbers. Mitigated by: design-doc review gate; the SSOT rule (Phase 44 adds ZERO calc logic — all tax stays in `lib/tax-engine/`, §8.3 of the doc); `accountantVerified` provenance flags; estimates-not-determinations positioning (§9 of the doc). Schema is additive throughout (§12.11 N/A; §12.12 migration-in-PR).
- **Blocking:** none — design approved 2026-05-20; build under way.
- **Why this matters:** Phase 41 shipped the entity *type* layer but explicitly deferred the *relationship* layer — shareholding, directorship, beneficiary entitlements, partnership interests, SMSF membership. The current model (a single `parentEntityId` self-FK) literally cannot represent a normal multi-entity AU structure. Every per-entity tax figure and the Phase 41E reform dispatch read ownership/control off this layer — so until it can represent the real graph, those numbers are wrong by construction.
- **Open questions:** see `PHASE_44_ENTITY_GRAPH.md` §12 — Q1 joint ownership in Part 1?, Q2 Director-ID store value vs presence, Q3 wizard-vs-entity-section capture split, Q4 accountant-review share-pass timing.

### 0·WX. Wealth Universe Explorer (consumer-facing canvas on `/dashboard/entities`)

- **Status:** ✅ COMPLETE — **Phase 7 semantic zoom shipped 2026-06-10 (this PR).** Workstream rolls into `✅ Recently Completed` on next plan archive (>30d).
- **Started:** 2026-05-30 (Reza brief: "tell the user the full story of their wealth and entities in one screen … hover/zoom magnifying-glass feel … Apple Dock × Apple Maps × Minority Report").
- **Owner:** Reza (direction + visual sign-off) + Claude (Stitch design + React port + service).
- **Last touched:** 2026-06-19 — **Phase WX.6.1 tap-to-zoom-first** — first tap on a bubble zooms in only (no panel); the detail file opens on the second tap of the centred bubble (a cluster zooms back out). Fixes the panel greying out the universe on zoom-in (Reza prod screenshots). Prior same day: **Phase WX.6 entity → class → asset zoom (Level-2 bundles)** — focusing a multi-asset entity now groups its holdings into per-class bundle bubbles that zoom into their assets at L3; `expandedEntityIds` became a nav stack + tappable breadcrumb (PR #1158). Prior same day: Level-1 balanced-orbit recomposition + detail-panel-close-no-longer-ejects fix (PR #1156). Earlier: WX.5–WX.5.4 focused-scene camera + fixes (2026-06-10/11); Phase 7.1 cluster level (2026-06-10).
- **Approach:** Stitch-first per CLAUDE.md §18 (screen `a3b43b9164d74f1c8ec53bc20f319cbd` is the visual SoT), iterative React port. Pure-shape aggregator (no calc, no tax math); calc engines untouched per §12.2/§12.3.
- **Phases:**
  - [x] **Stitch v1–v5 visual exploration** — five Stitch iterations converging on the spatial "Wealth Universe" direction (Apple Dock × Apple Maps × Minority Report). PR #939 committed the v5 artefact to `.stitch/designs/wealth-explorer-v5-universe-dark.{html,png}`.
  - [x] **React port v1 — surface preview** (PR #940, merged) — `/dashboard/wealth-explorer` route with fixture data + static HOME hover demo.
  - [x] **Relocation to canonical home** (PR #941, merged) — moved the canvas into the My Structure tab at `/dashboard/entities`; desktop-only swap, mobile keeps the legacy `EntityCanvas`. Standalone preview route deleted (§12.1 dead-code rule).
  - [x] **React port v2 — interactive surface** (PR #942, merged) — replaced fixture with `/api/entities` live data, Apple Dock magnification on hover, ribbon highlighting tracking the hovered node, popover follows cursor, click → `EntityDetailPanel` slide-in (Framer Motion spring), search box + filter chips wired, loading / error / empty states.
  - [~] **Phase 1 — Wealth Graph SSOT + full-object rendering (this PR)** — new canonical service `lib/services/wealthGraphService.ts` (single SSOT for canvas-shape data, distinct from `getMasterFinancialSnapshot()` financial breakdowns and `/api/portfolio/snapshot` SnapshotV2 GRDCS health — see CLAUDE.md §12.2 "Two snapshot SSOTs, not duplication"; this is the third — graph SSOT). New `/api/wealth-graph` route (`withPermission('entity.read')`). Layout function extended to position every owned Property / Loan / Account / InvestmentAccount / Asset as its own node in a radial satellite around the owning entity; EntityRelationship edges drive typed ribbons (BENEFICIARY_OF → violet, SHAREHOLDER_OF → emerald, DIRECTOR_OF → sky-controls, MEMBER_OF → emerald-soft, etc.). **Reza Q1 answer "every relevant item"** honoured — no clustering thresholds. **Reza Q2 answer "go with the group"** — `OwnershipGroup`s render as synthetic `ownership-group` nodes positioned between member entities; jointly-owned assets connect via the group, not directly from `ownerEntityId`. **Reza Q3 answer** — `BeneficialOwnershipOverride` renders a violet beneficial-owner ribbon alongside the dimmed legal chain; lens-switcher toggle to come. Pure aggregator (no tax math, no calc); engine modules unmodified per §12.3.
  - [x] **Phase 2 — Money Flow lens ✅ THIS PR (2026-05-31)** — extended `WealthGraphSnapshot` with `moneyFlows: WealthGraphMoneyFlow[]` (current FY, CONFIRMED status only) derived from `DistributionAllocation` (amount = `presentlyEntitledShare × (distributableIncome ?? trustNetIncome)`) + `DividendPayment` (amount = `payment.amount` directly). New primary lens toggle on the canvas: **Structure / Money flow** (sky-tint vs emerald-tint pill, renders only when ≥1 flow exists per behaviour-psychology lens — no anticipated-disappointment empty toggle). In Money flow mode, structural ribbons dim to 0.06; flow ribbons render at 0.55 opacity with marching-ants emerald dash + $ amount label at midpoint. Direction matches cash flow (trust → beneficiary, company → shareholder). **§12.14 FW-1 / FW-2 discipline implemented:** new `classifyDistributionRegime(fy, trustType)` function (exported for unit tests) returns one of `PRE_REFORM` / `POST_REFORM_VERIFIED` / `POST_REFORM_PENDING` + a verbatim `reformNotice` when pending — never computes post-reform math speculatively. `POST_REFORM_PENDING` flows surface a per-ribbon amber dot (tooltip = the full notice) + a chrome-level "N pending Royal Assent" pill. The Legal/Beneficial pill from Phase 1.1 hides in Money flow mode (its emphasis is about ownership reading; pointless when structural ribbons are dimmed). Dashboard widget filters flow ribbons out at render time (the widget is a structural preview, not a money-flow surface — restraint over richness per §0.4). 12 new tests in `tests/wealth-graph/classifyDistributionRegime.test.ts` pin the §12.14 dispatch (non-discretionary out-of-scope, pre-2028-29 PRE_REFORM, 2028-29+ POST_REFORM_PENDING, malformed-FY safe-fallback). Pure-shape aggregator — no tax math, no calc engine touched. No schema change, no migration.
  - [x] **Phase 2 enhancement — FY-slider for Money Flow lens ✅ THIS PR (2026-05-31)** — service aggregator now fetches **every** CONFIRMED distribution/dividend (not just current FY); new `moneyFlowFyOptions: string[]` lists every FY with data (descending); `moneyFlowFy` resolved to the most recent FY with data. Each flow ribbon carries its own `financialYear`. Canvas renders a ghost-glass FY strip in money-flow mode (replaces the Level breadcrumb at the same canvas position — they're alternatives, not stacked). Each FY pill toggles which flows render at full strength; off-FY flows dim to 0.06. The chrome-level pending-Royal-Assent count + per-ribbon amber dots automatically retrack the selected FY (§12.14 FW-1 per-FY regime discipline — when the user scrubs from FY 2025-26 to FY 2028-29 on a discretionary trust, the pending pill appears). Mobile parity: horizontal scroll strip in the bottom sheet below the Structure/Money-flow toggle. Pure-presentation + minor aggregator scope expansion; no schema change, no migration, no calc-engine touch. Existing 12 `classifyDistributionRegime` tests cover per-FY classification.
  - [x] **Phase 3 — Click-to-zoom ✅ THIS PR (2026-05-31)** — Level 2 ecosystem dim: when a tile is selected, all tiles + ribbons NOT directly connected to the focal entity recede to ~0.22 opacity (`connectedToSelectedIds` set computed from any ribbon endpoint). The focal entity + its direct neighbours stay at full brightness — the user reads the entity's ecosystem at a glance, then can drill into Level 3. Breadcrumb advances from "Level 1 · Universe" → "Level 2 · {entity name}" with a back chevron (tap = back to Level 1). Pure presentation pass — no re-layout, no spatial reorganization (the layout is already informative; dim is the right move per CLAUDE.md §0.4 restraint). Level 3 (`EntityDetailPanel` + mobile `SelectedEntityCard`) extended with a **Linked assets** list rendered below the asset-counts grid: one row per held asset with glyph + name + subtype/context + formatted value + chevron + click-through. URL map: property → `/dashboard/properties/{id}`, loan → `/dashboard/loans/{id}` (individual routes exist), account/investment-account/asset → list page (individual routes can be added later as per-asset detail pages are built). Hook `useWealthExplorerData` now exposes `snapshot` alongside `layout` so the panel reads source assets without a re-fetch. Removed the dead-end "Open full entity file →" CTA on both desktop + mobile (it pointed back to `/dashboard/entities` — circular). Pure-presentation + lightweight hook surface expansion; no schema change, no migration, no calc-engine touch, no §12.14 surface change (the lens-toggle + reform-pending pill already track the canvas state correctly).
  - [x] **Phase 4 — Mobile Apple Maps hybrid ✅ MERGED (PRs #947 / #948 / #949 / #950, 2026-05-31)** — `WealthUniverseMobile` built from Stitch screen `72ea8d79fa7e4a0c865a2c2a9d73d198`. Compact spatial canvas at top + draggable bottom sheet (peek 96px / half ~55% / full ~92% via Framer Motion drag). Sheet content: filter chips + selected-entity detail card (mirrors desktop EntityDetailPanel — Identity / Controlled-by / 2×2 asset counts grid) + all-entities list with "Holds N" chip on rows that own assets. Tap-tile ↔ list-row sync via shared `selectedId`. Search dropped (filter chips are enough at typical entity counts). Replaced legacy `EntityCanvas` on the `md:hidden` path of `/dashboard/entities`. Doc updates in PR #946 / §16.6.
  - [x] **Phase 6 — Legacy EntityCanvas retirement ✅ MERGED (2026-05-31)** — §12.1 zero-tolerance dead code triggered now that desktop + mobile are both on the new canvas. Deleted `components/entities/EntityCanvas.tsx` + `canvas/EntityCanvasNode.tsx` + `canvas/layout.ts` + `EntityDetailDialog.tsx` + `RelationshipDetailDialog.tsx` (5 files, all verified zero-consumer via grep). Removed the now-orphaned dynamic import from `app/dashboard/entities/page.tsx`. Trimmed the dagre-layout describe block (3 tests) from `tests/entity-graph/canvasMeta.test.ts` — other tests in that file retained (they still serve the accountant-review page). Files retained: `MoneyFlowSankey.tsx` (used by ConsumerMoneyFlowSankey), `canvas/entityGraphClient.ts` + `canvas/graphMeta.ts` (used by accountant-review), `types.ts` (used by EntityTree + MoneyFlowSankey + accountant-review), `EntityTree.tsx` (used by accountant portal client view), `OwnershipGroupsDialog.tsx` (used by entities page).
  - [x] **Phase 5 — Simplified dashboard widget ✅ MERGED (PR #952, 2026-05-31)** — `components/wealth-explorer/WealthUniverseWidget.tsx` ported from Stitch screen `c84ff8ac53b74f04a8d07e5b61b53848` (`.stitch/designs/wealth-universe-widget-v1.{html,png}`). 340px dark-glass tile slotted into a new diagnostic-grid row on `/dashboard` above `DebtQualityWidget`. Renders the wealth-graph at `COMPACT_SIZE_SCALE = 0.42` (no particle stream, fewer dust motes for performance); header with eyebrow + title + node-count chip + ArrowUpRight glyph; footer with total $held + "Open Wealth Universe →" link. Whole card wrapped in `<Link href="/dashboard/entities">` (single canonical home per Phase 4). Skeleton on load, silent-fail on error (§13.3 — no error details surfaced to consumers), celebratory empty state when graph is empty. Financial-surface lint baseline bumped +20 lines on `app/dashboard/page.tsx` via jq (PR #953, pre-existing inline-arithmetic violations shifted by the new widget row — not new violations).
  - [x] **Phase 1.1 — Legal · Beneficial lens toggle ✅ THIS PR (2026-05-31)** — Phase 1 (PR #944) shipped both legal ribbons (typed EntityRelationship edges) AND beneficial-owner override ribbons (violet `boo-*` edges) drawn simultaneously, which mid-density graphs read as ribbon-soup. This PR adds the lens switcher Phase 1 promised. New `lensMode: 'legal' | 'beneficial'` state on `WealthUniverseCanvas.tsx` + `WealthUniverseMobile.tsx`. A 2-segment pill toggle ("Legal · Beneficial", sky-tint when Legal active, violet-tint when Beneficial active) renders ONLY when `hasBeneficialOverride === true` (memoised on `relationships.some(r => r.id.startsWith('boo-'))`) — no UI noise on simple structures. Legal mode dims the `boo-*` ribbons (legal chain emphasised). Beneficial mode dims the `holds` ribbons (beneficial chain emphasised). Implemented as a pure dimming pass in `isRibbonDimmed()` — no re-layout, no relationship-array mutation, no service change. Reza Q3 answer ("show both, but with a lens toggle so the user can switch emphasis") now fully delivered. Desktop pill sits between the filter chips and the settings icon in the top chrome; mobile pill sits above the filter chips in the bottom sheet. Pure-presentation change — no schema, no migration, no calc-engine touch.
  - [x] **Phase 7 — Semantic zoom ✅ THIS PR (2026-06-10, code tag `Phase WX.4`)** — the structural fix for Reza's report *"the entities are so small specially when there are a number of them connected to the user it will become unreadable"* (desktop + mobile dashboard-widget screenshots, 20 nodes rendered as an overlapping smudge). Root cause: percentage-positioned nodes in fixed zones + fixed-pixel tile sizes + a fixed 9%-radius satellite arc — space constant, population growing, overlap inevitable; the dashboard widget compounded it by rendering ALL nodes at 0.42× in a 340px card. Fix = the Apple Maps principle (what is shown changes with zoom level, not just how big it is): **Level 1 (default)** emits NO asset satellites — `layoutWealthExplorer(snapshot, options?)` gains `assetDetail: 'collapsed' | 'all'` + `expandedEntityIds`; entities carry `assetSummary { count, totalValue }` rendered as a docked count-badge pill + accent "$X held" line (totalValue excludes loan principal — debt is not held wealth, financial-adviser lens). **Level 2** — selecting an entity unfolds its constellation (one arc ≤6 satellites, two concentric rings beyond; staggered `wealth-satellite-pop` entrance with scoped `prefers-reduced-motion` fallback); selecting it again / back-chevron / panel-close folds it back. **Mobile** keeps the bottom-sheet list on the full graph (`assetDetail: 'all'`) — granular browsing lives in the list, the canvas stays calm. **Widget** renders Level 1 always (hook default), tile scale 0.42 → 0.58, footer total now sums raw `assetSummary` aggregates instead of re-parsing formatted strings (also stops counting loan principal as "held"). **Deterministic collision-relaxation pass** added as the layout's final step (anchor immovable, positions clamped to safe area) — safety net for crowded zones. **Dead zoom chrome removed** — the +/− / fit buttons never had handlers (§12.1); they return only when true viewport pan/zoom ships (queued follow-up, only if semantic zoom proves insufficient at real node counts). Stitch-first per §18: 4 new screens in project `1859462351962811110` (L1 desktop `770687a1c73c42f0b4fd5686782bf5f3`, L2 desktop `068403f1296440508b601c2fc32d5e20`, L1 mobile `e5ecb8d170cc4fbdbc336413cd9948d2`, widget `80c21d51c38242d883bec3d6875fabe6`), artefacts committed at `.stitch/designs/wealth-universe-zoom/`. 11 new tests in `tests/wealth-explorer/semanticZoomLayout.test.ts` pin the contract (collapsed default, loan-exclusion, expansion scoping, two-ring placement, collision floor, determinism). Pure-presentation + pure-layout change — no schema, no migration, no calc-engine touch, no service change.
  - [x] **Phase 7.1 — Cluster level for ≤2-entity universes ✅ THIS PR (2026-06-10, code tag `Phase WX.4.1`)** — Reza caught the Phase 7 edge case within minutes of the prod deploy (mobile screenshot): his structure has ONE entity holding all 19 assets directly, so entity-level collapse rendered a single YOU tile with a "19" badge — technically correct, completely useless. Fix: with ≤2 entities (`CLUSTER_MODE_MAX_ENTITIES`), Level 1 clusters each entity's holdings BY TYPE into aggregate tiles (`tier: 'cluster'`, id `cluster-<entityId>-<kind>`, label "3 Properties", value "$2.1M" / "$600K owing" for loans), fanned in the upper arc above the anchor (`fanAbove`). Singleton kinds render the real asset directly. Tapping a cluster unfolds that type's assets (same expansion mechanic); clusters never open the entity detail panel (guarded on desktop; mobile card self-guards because clusters don't exist in the list layout). Entity keeps its "$X held" total but drops the redundant count badge. Widget chip copy now reads "19 holdings" / "N entities · M holdings" instead of "N nodes", and the footer total sums entity-tier aggregates only (clusters are sub-totals of the same holdings — counting both would double-count). The canvas rule is codified in `06_UI_UX_FOUNDATION.md`: **always target the 3–9 tile sweet spot; the layout picks the aggregation level that achieves it.** 6 new tests (17 total in the suite). No Stitch pass — the cluster tile IS the established L1 aggregate-tile vocabulary (count badge + value line) applied to a type group; zero new visual primitives.
  - [x] **Phase WX.6 — Entity → Class → Asset zoom (Level-2 bundles) ✅ THIS PR (2026-06-18)** — Reza prod screenshot (his Renew structure, 8 entities): focusing an entity that holds many assets dumped ~25 raw tiles at once — *"the first layer is messy … each entity or asset class should be bundled up into 1 bubble and after I click … it can zoom in to the next layer."* Reza chose the **Entity → class → asset** 3-level model. The dormant cluster-by-type mechanism (gated to ≤2 entities) is now also applied **inside a focused entity** for multi-entity universes: tapping an entity unfolds **per-class bundle bubbles** (Properties / Loans / Vehicles / Shares / Cash), each with count + class total; a class with a single asset renders directly (a "1 Property" bundle is noise); loans read "$X owing" + stay out of the held total. Tapping a bundle opens the existing L3 `cluster-<entityId>-<kind>` focus scene (its individual assets). `expandedEntityIds` is now a **navigation STACK** (last id = active focus); the breadcrumb renders every layer as a tappable crumb (`Universe ‹ Reza ‹ Properties`) and the centred bubble pops ONE layer. Both canvases (desktop + mobile). Stitch-first per §18.2.1: desktop-dark `24620f695c144a0b91ce43ac70a2497b` + mobile-dark `4bcd4f111f504ffc8d601ddb2219eca4` (project `1859462351962811110`), artefacts at `.stitch/designs/wealth-universe-l2-bundles/`. 6 new tests (36 in the suite). Pure-presentation + pure-layout — no schema, no migration, no calc-engine touch.
  - [x] **Phase WX.6.1 — tap to zoom first, details second ✅ THIS PR (2026-06-19)** — Reza prod screenshots (×3): clicking YOU on L1 zoomed into its bundles AND opened the entity detail file at once, greying out the universe just zoomed into. *"First click on a layer change needs to only zoom in; second click should open the node details."* Fix: split across two taps — first tap on an expandable bubble zooms in only (`selectedId` cleared, no panel); second tap on the centred bubble opens its file (entity/group); a cluster (no file) zooms back out instead; a leaf asset / holding-less entity opens its file on the first tap (unchanged). Deep-link `?focus=` mirrors a first tap. Both canvases. §18.2.1 true tweak (interaction only, no new composition) — code-first, no Stitch. Interaction-only change in the components (no layout test harness); layout tests 36/36 unchanged.
- **Risk:** Low — Phase 1 is pure-shape aggregation, no tax math, no schema change, no migration. Phase 2 is the regime-aware piece (§12.14 gate); separate PR keeps the bar high.
- **Blocking:** none — Phase 1 builds clean against the existing Phase 41 / Phase 44 Part 1 schema; every required column already exists.
- **Why this matters:** every owned asset already carries its `ownerEntityId` (Phase 41 backfilled), every entity-to-entity relationship already lives in `EntityRelationship` (Phase 44 Part 1a), every joint ownership lives in `OwnershipGroup`/`OwnershipStake` (Part 1a), every beneficial-owner override in `BeneficialOwnershipOverride` (Part 1a). The data substrate is already correct — the canvas just wasn't drinking from it. This PR closes that gap: every property, loan, account, investment, vehicle now appears as its own node on the canvas, attached to the legal entity that owns it. The user can finally SEE their full structure — the prerequisite for trusting the per-entity tax + cashflow numbers the rest of the app computes.

### 0·WI. Phase 45 — "What If?" scenarios + Q-DEC migration as gate

- **Status:** ✅ **v1 STRUCTURALLY COMPLETE 2026-06-09** — workstream opened 2026-06-01. Reza decision (AskUserQuestion 2026-06-01): **sequence Q-DEC (Float→Decimal) FIRST as the gate**, then Phase 45 v1 with all 5 levers in §18.7.2 My Wealth glass vocabulary (anchor decision 2026-06-04 swapped editorial → glass mid-pass). Stitch-first design pass for every Phase 45 surface per CLAUDE.md §18. **v1 ship summary:** Q-DEC migration (PR 1/1.5/2.A-E/3.A-E/4 + cleanup hotfix) fully shipped; Phase 45 PR 1 (engine composition) + PR 2.A/2.A.1/2.B/2.C (UI port — all 5 levers wired) shipped; Phase 45.1 (3 contextual entry points) + 45.1.1 (PropertyTile sparkles + income banner) + 45.1.2 (§18.7.4 Cremorne codification) + 45.1.3 (first Cremorne on sellProperty lever) shipped; Phase 45.2 / 45.2.1 / 45.2.2 (Asset Spotlight detail pages — properties / investments / SMSF) + 45.2.3 (income-banner Cremorne, PR #1035) + 45.2.5 (user-uploadable property hero photo, PR #1030) shipped. Parallel workstream `0·DG` (dashboard glass migration + §18.7.6 Compact Dashboard) — Phase 45.3 + 45.4-6 + 2 mobile hotfixes — also shipped 2026-06-09. **v2 triggers** (when user evidence demands): Monte Carlo confidence bands, saved scenarios, multi-lever scenarios, auto-recommendations, probabilistic projections (intentionally deferred per `PHASE_45_WHAT_IF_SCENARIOS.md` §3).
- **Started:** 2026-06-01 — Reza directive: "Let's go with phase 45."
- **Owner:** Reza (direction + visual sign-off) + Claude (engineering + Stitch design + four-lens review).
- **Last touched:** 2026-06-09 — **Phase 45 v1 polish queue CLOSED.** Seven PRs shipped this session in sequence: **#1030** Phase 45.2.5 (user-uploadable property hero photo, v1 properties only); **#1031** Phase 45.3 (dashboard Properties + Investments tabs glass migration); **#1032** Phase 45.4 + 45.5 + 45.6 (full Compact Dashboard mobile reflow + glass migration of every tile on `/dashboard` — KpiSwipeStrip + BentoPair primitives, 11 widget migrations, §18.7.6 pattern codified in CLAUDE.md); **#1033** mobile Bento cell text-wrap + entity-bars overflow hotfix; **#1034** Bento chip truncation + Pending Actions spacing hotfix; **#1035** Phase 45.2.3 (income-banner Cremorne — Reza directive overrode the "evidence-driven, lower priority" tag). All 7 prod deploys verified READY per §17.2. **Phase 45 v1 polish queue is now structurally complete:** every §18.7.4 Cremorne replicate surface ticked, every §18.7.5 Asset Spotlight surface shipped, §18.7.6 Compact Dashboard pattern documented + first canonical application live on `/dashboard`. Earlier 2026-06-08: Phase 45 polish trio (#1020 / #1021 / #1022). Earlier (2026-06-07): Q-DEC PR 3 COMPLETE (#1004 + #996/#997/#998/#999/#1000/#1001/#1003). Earlier (2026-06-06): Q-DEC PR 2 COMPLETE (16 sub-PRs). **`0·MA` maths audit + Q-DEC PR 4 column drop** remain as the only Phase-45-adjacent active items — both running on their own cadence.
- **Approach:** Two-stage build.
  - **Stage A — Q-DEC migration (~10-15d, 4 PRs)** — the load-bearing precision fix. 10-year What-If projections compound Float error visibly; the entire product promise is "regulator-grade accuracy, every number traceable." Pre-revenue is the cheap time. See Up Next row 69 for the detailed scope; that row now blocks-points at this workstream entry.
  - **Stage B — Phase 45 v1 (~5-7d, ~2-3 PRs)** — UI surface + salary-sacrifice scenario (only new calc, 4 of 5 levers already exist in `lib/cfo/scenarios/`: refinanceLoan, sellProperty, payDownLoan, addInvestment + redirectToOffset close cousin) + 10-year trajectory composer (the new piece — existing scenarios are 1-year impact only).
  - **Stitch design pass runs in PARALLEL with Q-DEC PR 2-3** — Reza reviews `.stitch/designs/what-if-*.{html,png}` while engine work proceeds; React port begins once Q-DEC adapter layer (PR 2) lands.
- **Phases:**
  - [x] **Q-DEC PR 1 — additive Decimal schema ✅ MERGED (PR #974, 2026-06-03)** — `*_decimal` columns on the 10 core money-bearing models that Phase 45's five levers compose: Property, Loan, Account, Income, Expense, InvestmentAccount, InvestmentHolding, PurchaseLot, SuperannuationAccount, Asset (~45 columns total). Migration v2 fix during the PR (table-name typo on Income — singular `income` not plural `incomes`; added `IF NOT EXISTS` for idempotency on retry after partial-apply failure). Reza-side `prisma migrate resolve --rolled-back` ran via Cloud SQL Studio (lesson captured in §17.3 active-debugging log).
  - [x] **Q-DEC PR 1.5 — supplementary Decimal columns ✅ MERGED (PR #975, 2026-06-03)** — same additive pattern for the 7 supplementary money-bearing models: Transaction, InvestmentTransaction, CapitalGainEvent, CapitalGainLotAllocation, RecurringPayment, SmsfAnnualReturn, SuperContribution (~23 columns). Phase 41E models (DistributionResolution/Allocation, DividendDistribution/Payment, PrivateCompanyBenefit) excluded — built with Decimal from day one per CLAUDE.md §0. TaxPosition cache layer deferred — can ship in a follow-up if engine cutover (PR 3) needs it. All 7 @@map values verified before SQL written (PR 1 lesson). `IF NOT EXISTS` from the start. Result: every Float money column the engines compose now has a Decimal sibling. **PR 2 (adapter layer) is unblocked.**
  - [x] **Q-DEC PR 2 — adapter layer (Reza decision 2026-06-04: split by directory + start with `netWorthCalculator` as proof)** — back-compat strategy: engines return BOTH Float and Decimal during PR 2 so callers don't break; PR 3 swaps the primary to Decimal; PR 4 drops Float DB columns. Five sub-PRs:
    - [x] **PR 2.A — foundation + `netWorthCalculator` proof (current branch `claude/qdec-pr2-adapter-layer-LIlK9`)** — new `lib/decimal/` module (`toDecimal`, `fromDecimal`, `decimalDiff`, `isCloseEnough` with policy-driven rounding: currency 2dp, rates 4dp, units 4dp, percentages 4dp); new `lib/calc-audit/shadowComparison.ts` extending the Phase 41I harness to run BOTH Float and Decimal paths on the same fixture input and surface any diff > tolerance; `MoneyValue = number | Prisma.Decimal` discriminated type in `lib/decimal/types.ts`; `netWorthCalculator` converted to return both paths (proof-of-pattern); tests: convert.test.ts + shadowComparison.test.ts + netWorthCalculator.decimal.test.ts.
    - [x] **PR 2.B — rest of `lib/calculations/*` (IN FLIGHT this PR)** — 4 pure-compute engines extended with Decimal siblings (`expenseAggregator`, `incomeAggregator`, `loanAggregator`, `cashflowOrchestrator`) + `toAnnualDecimal`/`toMonthlyDecimal` added to `lib/utils/frequencies.ts`. The 3 I/O readers (`entityValueBreakdown`, `moneyStoryTrend`, `netWorthHistory`) deferred to PR 3 because their math is either trivial subtraction or delegates to PR 2.A's `netWorthCalculator` Decimal siblings. **Two latent bugs discovered + fixed:** (1) `Decimal(0).isPositive()` returns truthy in some Prisma.Decimal builds — replaced all `.isPositive()` div-by-zero guards with `.gt(0)`. (2) Cashflow ratio fields use `'currency'` shadow tolerance (not `'percentage'`) because the Float path pre-rounds via `Math.round(n * 100) / 100`.
    - [x] **PR 2.C — `lib/cashflow/incomeNormalizer.ts` (IN FLIGHT this PR)** — Decimal siblings for `calculateTakeHomePayDecimal` (the PR 2.B Float-bridge unblocker), `normalizeIncomeStreamDecimal`, `normalizeAllIncomeDecimal`, `getEffectiveMonthlyIncomeDecimal`, + private `calculateNetSalaryDecimal`. `cashflowOrchestrator.calculateCashflowDecimal` swapped off the Float-bridge — now calls `calculateTakeHomePayDecimal` directly. The 4 larger `lib/cashflow/` files (`forecasting`, `optimisation`, `stressTesting`, `insightGenerator`) deferred to PR 3 — they're consumer-layer decision-support, not money-producers. **One harness improvement landed:** `runShadowComparison` skips non-numeric leaves (strings, Dates, booleans) so engines returning composite results (`IncomeStream` spread with money fields) shadow-compare cleanly without the harness throwing on string leaves.
    - [x] **PR 2.D — `lib/tax-engine/*` (largest sub-PR; salary-sacrifice depends on this for Phase 45 PR 1)** — Reza split decision 2026-06-04: Phase-45-driven 2-PR plan (D.1 critical path + D.2 rest), no CGT/trust reform engines in D.1. **Operational refinement** mid-session: given `lib/tax-engine/*` is 12,238 lines / 45 files, the "Phase 45 critical path" itself splits into three cohesive sub-PRs to keep each review tractable:
      - [x] **PR 2.D.1 — `lib/tax-engine/core/*` (IN FLIGHT this PR)** — 4 engines (paygCalculator, medicareLevyCalculator, taxOffsets, incomeTaxCalculator, ~970 lines). Unblocks the inner Float-bridge in PR 2.C's `calculateTakeHomePayDecimal`. After D.1 merges, swap incomeNormalizer.ts's inner TaxEngine calls to the Decimal siblings.
      - [x] **PR 2.D.2 — `lib/tax-engine/super/* (3 files)` (IN FLIGHT this PR)** — Decimal siblings for the salary-sacrifice critical path: `capTracker` (H3 hardening anchor from Phase 45 spec §4.1 — concessional cap headroom guard with carry-forward + bring-forward logic), `contributionCalculator` (SG + salary sacrifice + Div 293 detection), `highIncomeSuperTax` (Div 293 + Div 296 gated + TBC). `super/smsfIncomeTax` deferred to PR 2.D.3 (SMSF-specific, not on salary-sacrifice path).
      - [x] **PR 2.D.2b — `income/taxabilityRules + position/taxPositionCalculator` (IN FLIGHT this PR)** — Decimal siblings for `calculateFrankingCredits`, `determineTaxability`, `calculateTaxPosition`, `calculateQuickTaxPosition`. Composes Decimal core (PR 2.D.1) + Decimal taxability classification end-to-end. **Scope refinement:** `income/salaryProcessor` (uses binary-search `calculateGrossFromNet`) and `orchestrator/masterTaxPosition` (depends on D.3 engines: divisions/, landTax/, stampDuty/, gst/, entity/) deferred to a follow-up PR alongside D.3 dependencies.
      - [x] **PR 2.D.3 — rest of `lib/tax-engine/*` (IN FLIGHT, split into 4 sub-PRs)** — given the ~8400-line scope (25 files), splits into cohesive sub-PRs:
        - [x] **PR 2.D.3a — state taxes (`landTax/` + `stampDuty/` + `gst/`) (IN FLIGHT this PR)** — 4 engines (calculateGst, calculateStampDuty, calculateLandTax, calculateCrossStateLandTax). Self-contained, no reform dependencies.
        - [x] **PR 2.D.3b — CGT division engines (IN FLIGHT this PR)** — Decimal siblings for `calculateCgtDiscount` (Div 115 entity-rate dispatch with §12.14 FW-1 Measure 2 reform guard preserved), `applyCapitalLossNetting` (s100-50 + s115-100 ordering + blended discount), `applyCgtIndexation` (stage-1 UNCOMPUTED + stage-2 throw preserved), `applyCgtMinimumRate` (same), `applyForeignResidentCgt` (same + >$50M notification flag).
        - [x] **PR 2.D.3c — loss-treatment engines (IN FLIGHT this PR)** — Decimal siblings for `negativeGearing` (real arithmetic + §12.14 FW-1 Measure 1 regime preserved), `companyLossRules` + `trustLossRules` (Div 165 / Sch 2F test-outcome dispatch with Decimal-typed `deductibleLossAmount`), `trustMinimumTax` + `lossRefundability` (FW-2 skeletons). **Scope refinement:** original PR 2.D.3c was 13 files; given session tractability, narrowed to the 5 "loss treatment / quarantine" engines as a cohesive theme. Defer to follow-up: `trustDistribution`, `div7aLoanClassifier`, `div152SmallBusinessConcessions`, and the 5 categorical classifiers (fteIee, psi, s100aZone, smsfTriumvirate, trustDeedValidation, negativeGearingRegime) — they don't return money values so no Decimal sibling needed.
        - [x] **PR 2.D.3c2 — beneficiary allocation + business concessions ✅ MERGED (PR #988, 2026-06-04)** — Decimal siblings for `trustDistribution.allocateTrustDistribution` (Div 6 + Div 6E streaming + s99A penalty), `div7aLoanClassifier.calculateMinimumYearlyRepayment` (exact annuity formula via `Decimal.pow`) + `classifyDiv7ALoans` (surplus cap pro-rata), `div152SmallBusinessConcessions.applyDiv152` (15-yr + 50% + retirement + rollover stacking).
        - [x] **PR 2.D.3d — composers + remaining ✅ MERGED (PR #989, 2026-06-06)** — Decimal siblings for `calculateSmsfIncomeTax` (Div 295 / NALI / ECPI / refundable franking — ECPI honesty discipline preserved: pension phase + undefined `ecpiExemptProportion` returns `tax: null` + UC-SMSF-ECPI-PROPORTION, never guesses), `processSalary` (NET→GROSS reverse binary-search + PAYG NAT 1004 + Medicare + super), and supporting `calculateGrossFromNetDecimal` on `paygCalculator`. **Scope decision:** `entityTaxRouter` and `masterTaxPosition` deliberately deferred to PR 3 (cutover) — they're pure aggregating routers, no marginal benefit to a composer-tier Decimal sibling when the underlying engines they route to are already Decimal-capable. 22 tests (11 shadow + 11 contract); both engines reform-agnostic (§12.14 FW-1/FW-2 outcome (a)).
    - [x] **PR 2.E — `lib/cfo/*` (IN FLIGHT — split into sub-PRs)** — `lib/cfo/*` is ~6.6 k LOC across 19 files; given prior sub-PR tractability lessons, splits into cohesive sub-PRs by theme:
      - [x] **PR 2.E.1 — scenarios + supporting utils ✅ MERGED (PR #991, 2026-06-06)** — Decimal siblings for all 6 deterministic What-If scenarios (`sellProperty` / `payDownLoan` / `refinanceLoan` / `redirectToOffset` / `cutSpendCategory` / `addInvestment`) + 3 supporting primitive Decimal siblings on `lib/utils/calculations.ts` (`calculatePIRepaymentDecimal` exact amortising annuity via `Decimal.pow`, `calculateInterestForPeriodDecimal`, `calculateEffectivePrincipalDecimal`). New `ScenarioResultDecimal` + `ScenarioImpactDecimal` types. This is the Phase 45 What-If critical path — every lever the UI exposes runs through one of these engines, and the 10-year projection composer (Phase 45 PR 1) will consume the Decimal outputs directly without Float-bridge.
      - [x] **PR 2.E.2 — health + risk ✅ MERGED (PR #992, 2026-06-06)** — Decimal siblings for `scoreCalculator` (6 component sub-scores + weighted-overall composer; step functions with linear interpolation preserved bit-for-bit) + `riskRadar.calculateSummary` (counts unchanged; `totalImpact` accumulated in Decimal). Per-detector Decimal siblings deferred: each of the 10 detectors is categorical (ratio → conditional risk object) with shallow money math; the Decimal value lives at the summary layer where impact aggregation accumulates without Float-rounding drift. New `CFOScoreComponentsDecimal` + `RiskSummaryDecimal` types.
      - [x] **PR 2.E.3 — decision support ✅ MERGED (PR #993, 2026-06-06)** — Pure-math Decimal siblings across all 4 decision-support files: `propertyDecisionSupport.calculatePortfolioSummaryDecimal`; `loanDecisionSupport.{calculateMonthlyPayment, calculatePayoffMonths, calculateTotalInterest}Decimal` (amortising annuity + log-bridge payoff + interest accumulator); `investmentDecisionSupport.{calculateDividendYield, calculateMaxConcentration}Decimal`; `taxIntegration.{calculateUnrealisedCGT, calculateNegativeGearingBenefit}Decimal`. Async `calculateCFO*Insights` wrappers + categorical alert/risk generators deferred to PR 3 cutover.
      - [x] **PR 2.E.4 — actions + AI advisor + intelligence (IN FLIGHT this PR)** — `intelligenceEngine`: `calculateProjectedMonthEndBalanceDecimal` (liquid − dailyBurn × daysRemaining) + `calculateMonthlyProgressNetWorthDecimal` (accounts + properties + holdings × price − debt). `actionEngine` + `aiAdvisor` SHIPPED WITHOUT Decimal siblings — `actionEngine` is categorical (recommendation generator with hard-coded display amounts as priority thresholds; no compounding math); `aiAdvisor` is Gemini tool-call dispatcher + prompt construction (the 3 numeric helpers `bucket`/`round`/`clamp` are all display-side; the actual money math the advisor narrates over is the scenario engine output which already has Decimal siblings shipped in PR 2.E.1). Scope rationale documented in `lib/calc-audit/engines/decimal-cfo-actions-ai-intel.ts` header. **After this lands, all 4 PR 2.E sub-PRs are complete → entire PR 2 of Q-DEC migration is done.**
  - [x] **Q-DEC PR 3 — engine-by-engine cutover (IN FLIGHT — split into sub-PRs)** — route handlers consume Decimal results; components format via `formatCurrency()` (already type-abstract). Touches every engine consumer; cutover sub-divided like PR 2:
    - [x] **PR 3.A — tax composer-tier Decimal siblings ✅ MERGED (PR #996, 2026-06-06)** — `calculateEntityTaxPositionDecimal` + `buildMasterTaxPositionDecimal` shipped (deferred from PR 2.D.3d per scope decision; ships now as foundation for the consumer swaps). Each downstream branch in the entity router dispatches to the `*Decimal` sibling already shipped by PR 2.D; the orchestrator aggregates totals in Decimal across entities. `MasterTaxPositionDecimal` + `EntityTaxPositionDecimal` + `CrossCuttingTaxResultDecimal` types exported.
    - [x] **PR 3.B — tax route handlers cutover (entity + position) ✅ MERGED (PR #997, 2026-06-06)** — `/api/tax/entity/[entityId]` (GET + POST) + `/api/tax/position` (GET) swapped from Float composers to `*Decimal` siblings. New `lib/decimal/serialize.ts:serializeDecimalsForJson` boundary walker is the canonical Decimal → JSON conversion path. Dual-call to Float on `/api/tax/position` sources `warnings` + `recommendations` (presentational; no Decimal sibling).
    - [x] **PR 3.B.2 — salary + super route handlers cutover ✅ MERGED (PR #998, 2026-06-06)** — `/api/tax/salary` (POST) swapped from `TaxEngine.processSalary` → `processSalaryDecimal`; dual-call for the `calculations[]` narration. `/api/tax/super` (GET) swapped from `TaxEngine.trackContributionCaps` → `trackContributionCapsDecimal`; per-field `.toNumber()` at the JSON boundary preserves the existing integer-rounded response shape. `TaxEngine.calculateOptimalSalarySacrifice` stays Float (recommendation engine, not money math).
    - [x] **PR 3.C — cfo scenarios route cutover ✅ MERGED (PR #999, 2026-06-06)** — `/api/cfo/scenarios/run` (POST) swapped from `runScenario` → new `runScenarioDecimal` dispatcher (added to `lib/cfo/scenarios/index.ts`). The dispatcher routes each of the 6 scenarios to its `*Decimal` sibling shipped in PR 2.E.1; the route serializes Decimal → number via `serializeDecimalsForJson` at the JSON boundary. The dashboard wrapper routes (`/api/cfo`, `/api/cfo/advice/*`) defer to follow-up sub-PRs because their async wrappers (`getCFODashboardData`, `generateOrFetchAdvice`) don't have Decimal siblings yet — they compose many engines that PR 2.E.2/3/4 made Decimal-capable at the inner-helper level, but the wrapper-level Decimal sibling is its own work.
    - [x] **PR 3.D — AI advisor tools cutover ✅ MERGED (PR #1001, 2026-06-06)** — All 9 tax-engine-consuming tools in `lib/ai/tax-advisor/tools/*` swapped from Float engines to `*Decimal` siblings: `getEntityTaxPosition` / `getContributionCapHeadroom` / `getCgtExposure` / `getDiv7aRisk` / `getLandTaxPosition` / `runContributionScenario` / `runCgtScenario` / `runDiv7aRefinanceScenario` / `runLandTaxScenario`. Per-field `.toNumber()` conversion at the `numericFields[].value` boundary preserves the AI-facing `number` shape; `raw` field wrapped in `serializeDecimalsForJson`. Also fixed one PR 2.D.3a Decimal-sibling parity gap (`calculateLandTaxDecimal` was missing the `UC-MULTI-STATE-LAND-TAX` flag the Float sibling emits unconditionally).
    - [x] **PR 3.E — UI consumers cutover (IN FLIGHT this PR)** — `formatCurrency` widened from `(amount: number)` → `(amount: number | { toNumber(): number } | null | undefined)` with `—` empty-state placeholder for null / NaN / Infinity. Defensive forward-compat so any future direct-Decimal pass-through (e.g. server component reading a Prisma Decimal column without the JSON-serialize hop) renders correctly without an extra `.toNumber()` call site. The 5 UI consumer pages (`/dashboard/tax`, `/dashboard/income`, `/dashboard/entities/[id]/tax`, `/dashboard/investments/super`, `components/cfo/AIAdviceSection`) need no code changes — they read `number` from JSON-parsed responses (route handlers serialize Decimal → number via `serializeDecimalsForJson`, PR 3.B). Last 3.x sub-PR; after merge **all of Q-DEC PR 3 is complete** and PR 4 (Float column drop) is gated only on the 7-day parallel-run window. Also: **new workstream `0·MA` added** for the comprehensive maths/calc/data-relationship sanity audit before Phase 45 PR 1 ships.
  - [x] **Q-DEC PR 4 — DRAFT (this PR) — drops the DORMANT `*Decimal` columns (INVERSE of original plan)** — Audit 2026-06-07 ahead of the planned Float-drop revealed Q-DEC's actual code state: engines read Float from Prisma, immediately convert via `toDecimal()` at the engine boundary, and do all arithmetic in Decimal. The `*Decimal` schema columns added by PR 1/1.5 were POPULATED by the backfill but NEVER READ (Grep over `lib/`/`app/`/`components/`/`tests/` → zero `.<field>Decimal` reads, zero `select: { *Decimal: true }` clauses). The originally-planned "drop Float" would have broken every engine. Reza directive 2026-06-07 (AskUserQuestion) approved inverting the scope: drop the dormant Decimal columns instead. 63 columns across 17 tables removed via `prisma/migrations/20260613120000_q_dec_pr4_drop_dormant_decimal_columns/migration.sql`. §12.11 destructive-write checklist N/A by structural argument (removing unused data cannot break callers — verified by zero typecheck errors + 2,321 passing tests post-change). **DRAFT pending merge 2026-06-13** to preserve the 7-day parallel-run window even though it's a no-op for this scope. After merge, Q-DEC structurally complete; MA.2-001 also resolved structurally (Float `Math.round` HALF_AWAY_FROM_ZERO remains since Float path is canonical at the data layer; Decimal `ROUND_HALF_EVEN` at the engine boundary — both layers internally consistent).
  - [x] **Phase 45 Stitch design pass ✅ MERGED (PR #977, 2026-06-04)** — both canonical surfaces locked in the full 4-variant matrix per CLAUDE.md §18.7.2 dark-mode reviewer enforcement: (1) lever picker `/dashboard/cfo/what-if` (Screen A v3 desktop+mobile, light+dark), (2) lever detail `/dashboard/cfo/what-if/[lever]` (Screen B v1 Salary-sacrifice as the showcase lever, desktop+mobile, light+dark). 8 approved artefacts in `.stitch/designs/`. Vocabulary shift mid-pass from Restrained Editorial flat → §18.7.2 My Wealth glass after Reza overruled v1 (lesson recorded in PHASE_45_WHAT_IF_SCENARIOS.md §6.0). Other 4 levers' detail variants deferred to PR 2 — React component parameterises by lever, no separate Stitch designs needed. §4.1 spec amendment in same PR adds 3 hardening items (H1/H2/H3) for PR 1 to consume.
  - [x] **Phase 45 PR 1 — engine composition (this PR)** — Shipped immediately after Q-DEC PR 4 merged 2026-06-07. New `lib/cfo/scenarios/salarySacrificeToSuper.ts` (~440 LOC) composes the existing `calculateSuperContributionsDecimal` (SG + sacrifice + Div 293 + contributions-tax) + `concessionalCapHeadroomDecimal` (cap + carry-forward) + `taxYearConfig` (Div 296 regime flags). New `lib/cfo/scenarios/tenYearProjection.ts` (~155 LOC) — pure-function deterministic composer: per-year compounding of non-super assets (4% real default) + super (6% real default) + cashflow delta (2.5% wage-indexed default). PR 1 simplification: per-year scalar compound, not full `cashflowOrchestrator` × `masterTaxPosition` re-run (rationale documented in file header — gold-plating for v1; PR 2 may revisit). **Hardening items shipped:** H1 `SliderSource` discriminated union (`snapshot:path` / `default:value+rationale`) on `types.ts` + `salarySacrificeSliderSources` helper returning per-slider source declarations the UI consumes; H2 `getRegimeStatus` per CLAUDE.md §12.14 FW-2 returning UNAFFECTED / DIV296_UNCOMPUTED / DIV296_VERIFIED — scenario returns UNCOMPUTED ScenarioResult when super balance ≥ $3M and `div296CommencementVerified === false`; H3 `concessionalCapGuard` exported as a named utility (pure function) that thin-wraps `concessionalCapHeadroomDecimal` and produces a scenario-shaped `hardStopReason` when proposed contribution exceeds cap + carry-forward — the slider UI in PR 2 calls this on every change. Wired into both `runScenario` (Float wrapper for AI advisor parity) and `runScenarioDecimal` dispatchers + `SCENARIO_TYPES`. `lib/cfo/aiAdvisor.ts:validateScenarioParams` extended with `salarySacrificeToSuper` case. 34 new tests in `tests/cfo/salarySacrificeToSuper.test.ts` (21) + `tests/cfo/tenYearProjection.test.ts` (13) covering cap-guard math (under/at/over/with-YTD/with-carry-forward), regime status (3 cases + edge), scenario result shape (empty income / over-cap UNCOMPUTED / regime-locked UNCOMPUTED / normal case / Div 293 trigger / Float-Decimal parity), slider sources, projection determinism, and assumption surfacing. Full sweep: **2,355 passing, 69 skipped, 0 failures** (+34 net from 2,321 baseline). ZERO new calc engines per CLAUDE.md §12.3 and Phase 45 §4.
  - [~] **Phase 45 PR 2 — UI port** (split: 2.A picker shipping; 2.B detail queued)
    - [x] **PR 2.A — lever picker (this PR)** — `/dashboard/cfo/what-if` page + `[lever]/page.tsx` route stubs. 5 lever tiles (refinanceLoan / salarySacrificeToSuper / sellProperty / payDownLoan / addInvestment) matching the locked Stitch artefact (PR #977) — §18.7.2 glass vocabulary, per-lever gradient top-accent strips + watermark glyphs + hover-lift micro-motion. TRAIL-stage labels per §6.2 sub-palette (REDUCE·DEBT / INVEST·SUPER / LIVE·EXIT / ANCHOR·DEBT FREEDOM / INVEST·PROPERTY). GAW card at the bottom AFSL-disciplined ("Monitrax shows what changes. It doesn't tell you what to do."). Sidebar nav entry added under My Guide (`/dashboard/cfo/what-if`). Stub route on `[lever]` validates against `SCENARIO_TYPES` whitelist + renders "coming in PR 2.B" placeholder with back-to-picker link. 12 smoke tests covering scenario list, TRAIL labels, AFSL discipline (no "you should" phrasing), GAW copy, static Tailwind classes (defence against future dynamic-class regression), §18.7.2 vocabulary tokens, lever routing, breadcrumb, stub validation, unknown-lever handling. Full sweep: **2,367 passing, 69 skipped, 0 failures** (+12 net from 2,355 baseline).
    - [x] **PR 2.A.1 — Div 296 TSB aggregation fix (PR #1016, merged 2026-06-08)** — surfaced by Reza question 2026-06-08 ("Is salary sacrifice designed for both SMSF and super account?"). Pre-fix bug: H2 regime check read TSB from `snapshot.netWorth.assets.superannuation` which by Phase 39.5 excludes SMSF member balances. Fix: extended `ScenarioContext` with `superAccounts?: SuperAccountView[]`; salary-sacrifice scenario now prefers the un-deduplicated sum when supplied. `/api/cfo/scenarios/run` populates `superAccounts` via new `fetchSuperAccounts` helper. 5 regression tests covering the bug case ($1.5M APRA + $2M SMSF → DIV296_UNCOMPUTED) + back-compat + APRA-only + edge cases. FW-2 commencement gate HARDENED.
    - [x] **PR 2.B — lever detail (this PR)** — interactive `/dashboard/cfo/what-if/[lever]/page.tsx` (~720 LOC). React port of v1-desktop Stitch artefact in §18.7.2 in-app glass vocabulary (dark mode derived via token substitution per Reza directive 2026-06-08 — v1-dark cosmos artefact treated as superseded). **Salary-sacrifice fully wired** as the showcase lever: slider 0..auto-cap-max with $/mo value pill + H1 source-line attribution; H3 `ConcessionalCapCard` with red/emerald state + hard-stop on slider; H2 `RegimeLockedBadge` when Div 296 fires; super-account destination selector with SMSF-specific note (consumes `ctx.superAccounts` from PR 2.A.1); quick-pick chips ("Use my current sacrifice", "Max within cap"); 250ms-debounced re-run of `/api/cfo/scenarios/run` on slider change; client-side `tenYearProjection` composition for instant chart re-render (NO extra server roundtrip); recharts `LineChart` with current-path baseline (dashed grey) + with-sacrifice (emerald solid); hover tooltip; result pills (Annual tax saved, Concessional cap used %, TSB > $3M Div 296 watch); collapsible Assumptions panel (default closed); GAW footer linking `/legal/general-advice-warning`. **Other 4 levers** parameterised by the same React shell — stub inputs + projection placeholders explaining "PR 2.C wires this lever using the same surface". New `GET /api/cfo/scenarios/context` endpoint returns the snapshot-derived defaults + un-deduplicated TSB + super accounts + loans + properties — single SSOT for all lever-detail input rendering, `report.read` auth-gated. 20 smoke tests covering: API endpoints, debounce, auth-header discipline, AFSL copy (no prescriptive phrasing, GAW footer), H1/H2/H3 surfacing, SMSF note, assumptions collapsed-by-default, stub PR 2.C reference, §18.7.2 vocabulary defence (NO cosmos tokens), fund-selection default, Div 296 watch pill, context API SSOT + Universal Response Format. Full sweep: **2,392 passing, 69 skipped, 0 failures** (+20 net from 2,372 PR 2.A.1 baseline).
    - [x] **PR 2.C — wire the remaining 4 levers (this PR)** — refinanceLoan / payDownLoan / sellProperty / addInvestment all wired into the shared shell shipped in PR 2.B. Per-lever inputs: each scenario has its own `*Inputs` component (`RefinanceInputs`, `PayDownInputs`, `SellPropertyInputs`, `AddInvestmentInputs`) with per-lever palette gradient value pills (amber→rose, indigo→blue, violet→fuchsia, teal→cyan matching the picker tile palette) + per-lever AFSL-disciplined source-line copy. Shared `EntityPicker<T>` component for loan + property selection (subtitles surface principal/rate/remaining months for loans and value/equity for properties). New `GenericLeverProjection` component handles the 4 non-sacrifice levers — per-lever headline copy keyed off the scenario's impacts (refinance: "$X/mo + $Y lifetime savings", payDown: "$X interest saved + payoff in N months", sellProperty: "$X freed up + net worth Δ (after CGT)", addInvestment: "$Xm portfolio · $Y from compounding"). Generic 10-year `SimpleProjectionChart` reuses recharts primitives from PR 2.B; `GenericResultPills` surfaces the top 2 non-zero currency impacts. Per-lever defaults wired on context load (largest loan principal → refinance + payDown; largest property → sellProperty; refinance default newRate = current rate − 50bp). Scenario-run effect dispatches via `buildRequest()` returning the right `{type, params}` shape per lever — returns null when entity not yet picked (no doomed requests). PayDown surfaces a defensive cashflow check (warns when extraMonthly > snapshot's `monthlyCashflow`). 12 new tests covering: per-lever input components, defensive copy (HIGHER rate flag, cashflow check, industry-default explanations), per-lever headline keys, request guards, TRAIL-stage labels, AFSL discipline (negative grep against "you should refinance/pay/sell/invest"), result-pills filtering. Full sweep: **2,404 passing, 69 skipped, 0 failures** (+12 net from 2,392 PR 2.B baseline). **Phase 45 v1 UI complete** — all 5 levers live; entry-point integration (Phase 45.1 contextual entry points) is the next workstream.
  - [x] **Phase 45.1 — contextual entry points (this PR)** — "What if?" affordance deep-linking from entity-detail pages into the lever-detail screen with the right entity pre-populated. Three surfaces wired: (1) `LoanDetailDialog` Overview tab — `WhatIfLoanAffordances` card with two CTAs (Refinance + Pay extra), each deep-linking `?loanId=X` with per-lever sub-palette gradient (amber→rose, indigo→blue); (2) `/dashboard/properties/[id]/strategy/page.tsx` — "What if you sold this property?" card deep-linking `?propertyId=X` with violet sub-palette; (3) `/dashboard/investments/super/page.tsx` — "What if you salary-sacrificed?" CTA placed beneath the `SuperCapMeter` (cap-aware context) with emerald/indigo gradient. The lever-detail page extended to read `?loanId=X` / `?propertyId=Y` from URL searchParams via `useSearchParams()`; deep-linked IDs override the largest-balance default. AFSL discipline preserved across all 3 entry points (negative grep against "you should refinance/sell/sacrifice"). 19 new tests covering: searchParam reading + deep-link override of defaults + per-CTA copy + per-CTA palette + per-CTA AFSL discipline + per-surface positioning. Full sweep: **2,423 passing, 69 skipped, 0 failures** (+19 net from 2,404 PR 2.C baseline). Income-page entry-point deferred (overlaps with super page; would add complexity without unique value — revisit if user feedback demands it).
  - [x] **Phase 45.1.1 — polish backlog (this PR, Stitch-first per §18)** — picks up the two follow-ups noted as "potential" at the end of Phase 45.1: (1) **PropertyTile inline sparkles affordance** — violet-tinted `Sparkles` icon in the hover-reveal action cluster (INVESTMENT type only), deep-links into `/dashboard/cfo/what-if/sellProperty?propertyId={id}`. More discoverable than the strategy sub-page CTA — surfaces the "I could model selling this" affordance on the main properties grid. (2) **Income page salary-sacrifice CTA** — contextual banner between `<PageHeader>` and `<ListFilter>`, only rendered when the user has at least one `SALARY` income (`hasSalaryIncome` guard keeps the page calm for non-salary households). Emerald glass card (border-emerald-500/25, bg-emerald-500/[0.08], `dark:` variants per §18.7.2), gradient `PiggyBank` badge, AFSL footnote underneath. Deep-links to `/dashboard/cfo/what-if/salarySacrificeToSuper`. Reverses the 2026-06-08 Phase 45.1 "income-page entry-point deferred" decision after Reza approved the Stitch design pass. **Stitch artefacts** locked in `.stitch/designs/polish/` (4 files, light+dark per surface per §18.7.2 reviewer enforcement) under project `5991501424852019479`. Stitch screen IDs documented inline at both insertion points. Build clean (`tsc --noEmit` ✅, eslint ✅ 0 new warnings). **Merged as PR #1020 (2026-06-08).** Required one CI follow-up commit (`0f05a9e`) to refresh `.audit/financial-math-baseline.json` after the CTA insert shifted a pre-existing grandfathered entry from line 1904 → 1948.
  - [x] **Phase 45.1.2 — codify the "Cremorne pattern" into design principles (this PR)** — Reza directive 2026-06-08 after seeing the PropertyTile what-if Stitch design: *"document this design in design principles as one that I like with the background picture and other elements to be used later in other parts of the app."* Adds a new row to CLAUDE.md §18.7.2 (the canonical design-principles digest table) covering "Contextual decor", plus a full new sub-section CLAUDE.md §18.7.4 documenting the three-layer atmospheric system (L1 contextual photo bleed + L2 atmospheric halo + L3 next-item ghost), with when-to-use / when-NOT-to-use rules, canonical Stitch artefact reference, and a queued list of next surfaces to replicate the pattern (property detail, investment detail, SMSF detail, what-if lever detail when an entity is selected). §18.7.3 reviewer enforcement extended with a new rule rejecting misuse of the pattern. Matching implementation pointer added to `docs/architecture/06_UI_UX_FOUNDATION.md` per §16.3 doc-sync requirement (the foundation doc defers to CLAUDE.md as the SSOT — quick-reference table + when-to-use + reference artefact + replicate queue).
  - [x] **Phase 45.1.3 — first Cremorne-pattern application: sellProperty lever (this PR)** — picks the smallest-scope highest-payoff item from the §18.7.4 replicate queue. **Stitch-first per §18**: full 4-variant matrix locked under `.stitch/designs/polish/sellproperty-lever-cremorne{,-dark,-mobile,-mobile-dark}.{html,png}` (canonical project `1859462351962811110`, screen IDs documented in commit). **React port**: `app/dashboard/cfo/what-if/[lever]/page.tsx` — adds L1 (photo bleed via `next/image fill` against `/public/decor/cremorne-apartment.jpg`) at the page-container level bottom-right (33% × 50% desktop / 66% × 50% mobile), responsive opacity (40 light / 30 dark via `dark:`); adds L2 (violet→fuchsia halo, `blur-[40px] md:blur-[60px]`, opacity step-down for dark) wrapping the left-column protagonist GlassPanel; L3 skipped (page has 3 protagonists already, per §18.7.4 rule). Both layers gated on `lever === 'sellProperty' && sellPropertyState.propertyId` so they only render when a property is actually picked. **Per-surface tuning learnings** (captured in §18.7.4 replicate queue): photo placement is page-container level NOT card-level on desktop because the lever's two-column grid has its own visual gravity; mobile keeps it page-level too because the photo behind the last stacked card grounds the long-scroll experience better than burying it in the inputs card; halo only on the protagonist column (right column is the projection card, not a protagonist). Build clean (`tsc --noEmit` ✅, eslint ✅ 0 warnings after `<img>` → `next/image` conversion).
- **Why this matters:** What If? is the wedge feature for the wealth-builder ICP (Q-ICP-1 decision 2026-05-24). It justifies the new positioning (row 66) and gives the friendlies cohort (workstream 0f) something to play with that's structurally different from any AU PFM. Q-DEC is the precision foundation — without it, 10-year horizons compound to visible cents-wrong; with it, every projection is traceable to the canonical engine output to-the-cent.
- **Risk:** Medium. Q-DEC PR 4 (Float drop) is the only destructive piece — §12.11 checklist mandatory + 7-day parallel-run gate. Phase 45 build is mostly composition (4 of 5 levers already exist); only salary-sacrifice + the 10-year composer are new code.
- **Blocking:** none — Reza green-lit sequencing 2026-06-01.

### 0·MA. Maths / Calc / Data-Relationship Sanity Audit — pre-Phase-45 correctness gate

- **Status:** 🟡 IN PROGRESS — **MA.1 first-pass merged (PR #1005)**; **MA.1b authority re-verification IN FLIGHT (this branch)** per Reza directive 2026-06-07: *"The audit should also check the calculations against the tax laws and other related accounting rules. Don't guess, cross check everything against the real rules to make sure they are fact checked and exact."* Audit doc anchored at `docs/audits/2026-06-MATHS-AUDIT.md`.
- **MA.1 findings (merged PR #1005):** Stage 3 brackets ✅, PAYG NAT 1004 coefficient values ✅, LITO ✅, SAPTO ✅, super constants ✅, CGT 50% ✅, REFORM_CUT_OVER_UTC + M1/M2/M3/M5/M7/M8/M9 ✅; MA.1-002 cosmetic PAYG integer bracket boundaries (added code comment); MA.1-003 Medicare Levy thresholds may need indexation bump (deferred); MA.1-004 FOREIGN_PURCHASE_BAN 24h off (fixed).
- **MA.1b authority-cross-check findings (this branch, in flight):**
  - ✅ **Medicare Levy FY24-25 thresholds — FIXED.** Verified via ATO `tax-table-weekly-with-no-and-half-medicare-levy`: single $27,222 / family $45,907 / dependent child increase $4,216 / shade-out multiplier 1.25. Patched in `claude/ma1-003-fix-medicare-thresholds-LIlK9`. Updated `lib/tax-engine/config/taxYearConfig.ts:53-55` (canonical SSOT) + `lib/tax/auTax.ts:36-37` (legacy parallel engine, kept consistent for safety; flagged with `@deprecated` JSDoc + Dead Code #29 for MA.4-001 retirement). FY25-26 inherits via `TAX_YEAR_2024_25.medicareThresholds` reference (`taxYearConfig.ts:157`). Full vitest sweep: `2,309 passing, 69 skipped, 0 failures`.
  - ✅ **SAPTO FY24-25** — verified single $2,230, couple-each $1,602 against ATO published `Tax offsets — seniors and pensioners`.
  - ✅ **MA.1-005 CRITICAL — PAYG formula adjustment missing — FIXED.** Per ATO Schedule 1 (NAT 1004) `working-out-the-weekly-earnings` + `using-a-formula` pages, the canonical formula is `y = a × x - b` where **`x` = (number of whole dollars in the weekly earnings) + 0.99**. Pre-fix code at `lib/tax-engine/core/paygCalculator.ts:145` used raw `weeklyEarnings`. Fix shipped in `claude/ma1-005-fix-payg-formula-LIlK9`: both `calculatePAYG` (Float) and `calculatePAYGDecimal` (Decimal sibling) now apply `xWhole = floor(weeklyEarnings) + 0.99` immediately before the per-band formula. Period-conversion helpers (`toWeeklyAmount`/`toWeeklyAmountDecimal`) unchanged — their ratios were already mathematically equivalent to ATO Schedule 1 §3. 5 new MA.1-005 contract tests added covering bracket boundaries ($361.99, $362.00), cents-invariance ($1500.00 ≡ $1500.50 ≡ $1500.99), top bracket $4000 → $1205, and the divergence point $869.39 → $101 (was $100 pre-fix). Q-DEC shadow comparison Float ≡ Decimal still holds (both engines patched consistently). Full vitest sweep `2,309 passing, 69 skipped, 0 failures`.
- **MA.1b CLOSED on `claude/ma1b-authority-recite-LIlK9` — ZERO new bugs surfaced.** All MA.1 constants re-cited against primary authority under LFC rule:
  - Stage 3 brackets (rates + base amounts) ✅ — ATO `tax-rates-australian-residents` + Treasury Budget 2026 fact sheet, retrieved 2026-06-07.
  - Super Guarantee 11.5% + concessional cap $30,000 + Div 293 $250,000 + TBC $1.9M ✅ — ATO `key-superannuation-rates-and-thresholds` pages.
  - LITO ($700 max / $37,500 / $45,000 / $66,667 / 5c/1.5c phase-out) ✅ — ATO `low-income-tax-offset`.
  - CGT 50% + 12-month rule ✅ — AustLII ITAA 1997 s115-25 + ATO TD 2002/10.
  - REFORM_CUT_OVER_UTC `2026-05-12T09:30:00Z` ✅ — ATO `tax-reform-boosting-home-ownership` + Treasury Budget 2026-27 tax reform page.
  - M1-M9 commencement dates ✅ — each cited per measure via ATO new-legislation pages + Treasury + LFC-5 redundancy from independent practitioner firms.
- **Confidence in MA.1 baseline (post-MA.1b): MAXIMUM.** Every numeric assertion in MA.1 is now backed by a primary-authority URL, retrieval-dated 2026-06-07, with two-source redundancy.
- **MA.2 / MA.3 / MA.4 / MA.5 + MA.4-001 retirement — ALL CLOSED (this PR `claude/ma2-5-full-audit-LIlK9`).**
  - **MA.4-001 (legacy parallel engine):** ✅ retirement complete. `/api/calculate/tax/route.ts` + `lib/tax/auTax.ts` deleted; `_note` field in snapshot route repointed at canonical `/api/tax/position`; Dead Code #29 closed.
  - **MA.5-001 (CLAUDE.md §12.14 NON-NEGOTIABLE violation — hard-coded cut-over timestamp):** ✅ FIXED in 4 files (`app/api/properties/route.ts`, `app/api/properties/[id]/route.ts`, `lib/onboarding/propertiesSync.ts`, `components/onboarding/wizard/steps/PropertiesStep.tsx`) — all now import canonical `REFORM_CUT_OVER_UTC`.
  - **MA.4-002 (divergent `calculateNetWorth` in `portfolioEngine.ts`):** ✅ FIXED on `claude/ma-finish-items-LIlK9`. `portfolioEngine.calculateNetWorth` now delegates to canonical `lib/calculations/netWorthCalculator.ts` SSOT. `generatePortfolioSnapshot` now fetches `SuperannuationAccount` + `Asset` and feeds them through `PortfolioInput`. 6 regression tests added. AI advisor + strategy engine now return the same net-worth number as the user's dashboard. Dead Code #30 closed.
  - **MA.2-001 (Float `Math.round` HALF_AWAY_FROM_ZERO vs Decimal `ROUND_HALF_EVEN`):** ✅ RESOLVED-BY Q-DEC PR 4 (Float column drop). Q-DEC PR 4 retires the entire Float path; after that ships, only the Decimal `ROUND_HALF_EVEN` path remains and the rounding-mode drift disappears structurally. No standalone fix needed.
  - **MA.2/3/5 otherwise clean** — period-conversion ratios (× 52 / × 26 / × 12 / × 4) match ATO Schedule 1 §3; GRDCS schema FK design (Cascade/Restrict/SetNull) consistent; FW-1 regime input + FW-2 commencement gate verified across all reform consumers.
- **Phase 45 PR 1 (engine composition) is now unblocked from the audit-gate side.** Remaining blockers (MA.4-002 + MA.2-001) are LOGGED follow-ups, not gates.
- **Why this matters (load-bearing for Phase 45):** Q-DEC fixed the *precision* foundation (Float → Decimal). It did NOT audit whether the underlying formulas are correct, whether engine A reads the right column from engine B, or whether a data relationship that *looks* right under sparse fixtures breaks at scale or under edge cases. Phase 45's 10-year What-If projections compound any formula bug or relationship mismatch into visibly-wrong cents. **Better to find these now, pre-revenue, with no users to refund, than after Phase 45 ships.**
- **What this is NOT:** a Q-DEC re-run. Q-DEC validated Float ≡ Decimal *for the same formula*. This audit validates that the *formula itself* is right against the canonical authority (ITAA, ASIC, ATO publications, accounting standards).
- **Trigger:** before Phase 45 PR 1 (engine composition) ships. Q-DEC PR 4 (Float drop) does not gate on this — they're orthogonal.

#### Law fact-check rule — **NON-NEGOTIABLE for all MA passes** (Reza directive 2026-06-07)

> *"Don't guess, cross check everything against the real rules to make sure they are fact checked and exact"* — every assertion in this audit MUST be backed by a primary-authority citation, retrieval-dated, before it ships.

This rule applies retroactively to MA.1 (the original first pass; MA.1b is the re-verification arm) and prospectively to MA.2 → MA.5. Process discipline:

| Rule | What it means in practice |
|---|---|
| **LFC-1. No memory-based assertions.** | Every constant, threshold, rate, formula-coefficient, and effective date in the audit doc MUST be paired with a `Verified-via:` line citing the primary authority URL **fetched in this session** (not "I remember reading it"). |
| **LFC-2. Primary > secondary > tertiary.** | Authority hierarchy: (1) the Act / Regulation / ATO Schedule itself (e.g. ATO `tax-rates-and-codes/...` pages, AustLII Acts, Treasury fact sheets, state-revenue-office published tables); (2) ATO secondary publications (ATO web guidance + tax tables); (3) reputable practitioner sites (Thomson Reuters / CCH / `atotaxrates.info`) ONLY as corroboration, never as the sole source. |
| **LFC-3. Retrieval-dated.** | Every citation MUST include the retrieval date (when the URL was fetched). Tax law indexes annually; an undated citation rots silently. Format: `Verified-via: <URL> — retrieved 2026-06-07`. |
| **LFC-4. Anchor to the FY.** | Every numeric assertion MUST name the FY it applies to (e.g. "FY 2024-25 Medicare Levy single threshold = $27,222"). Mis-applying a prior-year threshold is the most common silent bug — being explicit prevents it. |
| **LFC-5. Authority redundancy for load-bearing constants.** | The cut-over date, every bracket boundary, every rate, every threshold MUST be confirmed by **at least two independent authority URLs** (e.g. the ATO Schedule page + the ATO tax-table page, OR the Act + the ATO publication). Single-source assertions are a yellow flag. |
| **LFC-6. Fail loud on uncertainty.** | If a primary source cannot be retrieved (e.g. ATO returns 403 to WebFetch), the auditor must use WebSearch for the literal quote, capture it in the audit doc, AND flag the entry as `🟨 SECONDARY-ONLY` until a primary fetch succeeds. NEVER assert "verified" without primary-or-secondary confirmation. |
| **LFC-7. Each fix PR re-cites.** | A PR that ships a maths fix (e.g. MA.1b-fix-payg) MUST re-quote the authority text in the PR body — `>` blockquote of the literal source language — proving the fix matches the canonical rule. Reviewers reject "I read it once two passes ago." |

**Operational impact on the five MA passes:**
- **MA.1 (already shipped + re-verifying):** every constant/formula gets a `Verified-via:` line in the audit doc. MA.1b is the catch-up pass.
- **MA.2 (cashflow + frequency math):** ATO conversions (weekly→annual via ×52, fortnightly→annual via ×26, etc.) cited against the ATO Schedule 1 § "Working out the equivalent weekly earnings" — NOT against developer convention. Rounding policy cited against ATO `Statement of formulas` (`HALF_EVEN` is the ATO default per APRA's accounting standard; verify before asserting).
- **MA.3 (GRDCS hygiene):** the law lens applies less directly here — but where data relationships are driven by legal definitions (e.g. "spouse" for CGT main-residence exemption per ITAA s118-178), the relationship audit MUST cite the section. Pure CRUD relationships need no citation.
- **MA.4 (cross-engine consistency):** where two engines diverge, the audit MUST cite which one matches the canonical authority. The wrong one becomes the fix target.
- **MA.5 (reform-aware Phase 41E):** every measure's commencement date, every grandfathering rule, every per-asset test MUST cite the Treasury exposure draft / Bill / explanatory memorandum / ATO LCR — whichever is the most authoritative document available as of the audit retrieval date. Status flag per `commencementVerified` gate.

- **Scope — five passes, each its own sub-PR:**
  - **MA.1 — Tax formulas vs ATO authority.** Walk every formula in `lib/tax-engine/**/*.ts` against its citation. PAYG NAT 1004 brackets verified against 2024-25 schedule. Medicare shade-in formula vs s8 Medicare Levy Act. Div 293 / Div 296 commencement rules vs ITAA. CGT 50% discount math vs Div 115. ECPI proportion math vs s295-385. Stamp duty thresholds per state. Land tax brackets per state. **Output:** annotation per file + diff report. **Each finding stamped with LFC-1 through LFC-7.**
  - **MA.2 — Cashflow + frequency math.** `lib/calculations/cashflowOrchestrator.ts`, `lib/calculations/expenseAggregator.ts`, `lib/utils/frequencies.ts`. Walk every `toMonthly` / `toAnnual` / period-conversion call. Verify the `weeklyToAnnual = ×52` vs `monthlyToAnnual = ×12` assumptions hold across all consumers. Check rounding policy is consistent (`HALF_EVEN` ATO standard, not silently `HALF_UP` somewhere). **Output:** annotation per file + diff report. **Each conversion ratio cited against the ATO Schedule per LFC-1/LFC-2.**
  - **MA.3 — Data-relationship audit (GRDCS hygiene).** Every entity-to-entity reference. `LegalEntity.id` → `Property.ownerEntityId` / `Loan.ownerEntityId` / `Income.ownerEntityId` etc. Cascade-on-delete behaviour. Orphan-row detection (e.g. a `Loan` whose `propertyId` is now stale because the property was deleted but the FK constraint was off). `prisma/schema.prisma` invariants vs run-time. Per CLAUDE.md §6.5 (GRDCS entity contract) + §12.2 SSOT. **Where relationships are driven by legal definitions (e.g. "spouse" per ITAA s995-1), cite the section per LFC-1.**
  - **MA.4 — Cross-engine formula consistency.** Three places the app computes "net cashflow"; do they agree? Net worth in `netWorthCalculator` vs the snapshot summary vs the intelligence-engine local aggregation (see CHANGELOG_2026_06_06 PR 2.E.4 entry — already flagged that the intelligence-engine local NW is a smaller composition). Any divergence is either intentional (with a comment) or a bug. Audit every "same metric, different code path." **Where engines diverge, cite the authority that resolves which one is correct per LFC-2.**
  - **MA.5 — Reform-aware formula correctness (Phase 41E).** `docs/blueprint/PHASE_41E_REFORM_2026_27.md` lists the 8 measures. For each: is the FW-1 regime branch correct? Is the FW-2 commencement gate honoured? Is the `REFORM_CUT_OVER_UTC = 2026-05-12T09:30:00Z` constant used consistently? Audit all 8 measures' code paths against the Phase 41E doc spec. **Each measure's commencement + grandfathering rule cited against Treasury fact sheet / exposure draft / Bill / EM per LFC-1 + LFC-3 (retrieval-dated).**
- **What good looks like:** A `docs/audits/2026-06-MATHS-AUDIT.md` summary doc + per-pass annotated source files + one PR per discovered bug + every finding stamped with primary-authority citation + retrieval date. **Bugs found ship as fix PRs immediately; the audit doc is the meta-trail.**
- **Risk:** Medium. The audit will find bugs (it always does at this scale). Each bug is its own remediation PR — could add 1-2 days of fix work depending on severity. **MA.1-005 PAYG `+ 0.99` adjustment is the first confirmed material finding** (it surfaced under the LFC rule itself — MA.1's original pass missed it because the formula was matched against memory not against the literal ATO text).
- **Blocking:** Q-DEC PR 3.E + PR 4 + cfo dashboard/advice follow-ups can run in parallel — those are precision-layer, this is correctness-layer.

### 0. Production Readiness — gate to first paying user

- **Status:** 🟡 In flight — Reza directive 2026-05-09: *"Leave gate 1 and continue with the rest, let's get it a prod ready system."* Compliance bedrock chunk in this PR; subsequent chunks queued below.
- **Started:** 2026-05-09
- **Owner:** Reza (GCP-console + external dependencies) + Claude (engineering)
- **Why this matters:** Platform is **demo-ready** (lighthouse pitch runs end-to-end) but **not production-ready** (cannot accept paying users with real CDR data flow). Gate-1 external lead times (pen test ~6 wk + cyber insurance ~3 wk + Stripe live-mode review ~2 wk) are Reza-managed; the engineering work below runs in parallel and converges with the external dependencies.
- **External dependencies (Reza-side, NOT in this workstream's scope):**
  - Pen test commission (~6 wk lead time, ~AU$15-25k) — Basiq accreditation hard requirement
  - Cyber insurance binding (~3 wk lead time) — Basiq accreditation hard requirement
  - Stripe live-mode review (~2 wk) — required for real revenue
- **Engineering chunks (queued, will ship one PR at a time):**
  - [x] **Compliance bedrock (this PR)** — Conversation 7-yr archive sweeper (`lib/services/conversationRetentionService.ts` + `POST /api/conversations/retention-sweep` with `CRON_SECRET` auth, mirrors `/api/cdr/lifecycle` pattern). New `CONVERSATION_MESSAGES_PURGED` `AuditAction` enum + matching additive migration. Operational runbook `05_RETENTION_SCHEDULERS.md` documenting the GCP Cloud Scheduler config for both retention crons (CDR consent-expiry at 02:00 + conversation sweep at 03:00, both in the `Australia/Sydney` timezone — updated 2026-05-12). Reza Tier-1 GCP-console TODOs (CMEK / Cloud Armor / SCC) documented in §6 of the same runbook so they don't fall through the cracks.
  - [x] **Real alert engine v1** — replaces the `LIGHTHOUSE_ALERTS` fixture with real triggers (CASHFLOW_NEGATIVE, EMERGENCY_FUND_LOW, LVR_REFINANCE_WINDOW stateless; HEALTH_DROP, TRAIL_ADVANCED stateful). **#9a (PR #745):** schema (`ClientAlert` + `ClientSnapshotMarker` + `ClientAlertStatus` + migration) + pure engine `lib/portal/alerts/alertEngine.ts` + 20+ tests + cron sweep `POST /api/portal/alerts/sweep` (`CRON_SECRET` auth, per-org per-client snapshot → scope-gated `computeAlerts` → upsert/resolve/re-arm persistence). **#9b (this PR):** org-scoped `GET /api/portal/alerts` + `POST /api/portal/alerts/[id]/dismiss` + Practice dashboard wiring (`/portal/dashboard` fetches real alerts → `<PracticeAlertStream>` with the dismiss button; falls back to `LIGHTHOUSE_ALERTS` as the empty-state preview when the org has no real alerts). See `docs/blueprint/PHASE_32B_PR3_ALERT_ENGINE.md`. **Closes Phase 32B PR3 item #9** — the last PR3 item. Reza-side: wire the GCP Cloud Scheduler job (`monitrax-portal-alert-sweep`, `0 4 * * *` in the `Australia/Sydney` timezone, `Authorization: Bearer <CRON_SECRET>`) — until then the engine is dormant and the dashboard shows the fixture preview. **Post-#9b polish:** ① admin "run sweep now" — **✅ shipped (PR #749)**: sweep core extracted to `lib/portal/alerts/sweepRunner.ts`; the cron route is a thin CRON_SECRET wrapper; SUPER_ADMIN-only `POST /api/admin/portal-alert-sweep` (audit-logged, dry-run) + a "Portal alert sweep" card on `/admin/scheduler`. ② hero KPI strip + client book on real data — **✅ SHIPPED (this PR)**: `ClientSnapshotMarker` + `previousHealthScore`/`previousTrailStage` (migration `20260514100000_phase_32b_pr3_marker_prev`); new aggregate-only `GET /api/portal/clients?organizationId=…` (`activeClients`/`needsAttention`/`trailAdvancedThisWeek`/`averageHealth`/`averageHealthDelta` + a thin per-client array — read from `OrganizationClient` + `ClientSnapshotMarker` + ACTIVE `ClientAlert`, no live snapshot); `/portal/dashboard` now shows the real book once the sweep has run (`hasRealClients` master switch — else the `LIGHTHOUSE` fixture preview, never half-and-half), with the fixture client-book table replaced by a slim "N active · M need attention · → Open the full client book" card in real mode. ③ richer real-data **client-book table** on `/portal/clients` — **✅ SHIPPED 2026-05-18 (PR D)**: `app/portal/clients/page.tsx` fetches the aggregate endpoint in parallel; `<ClientList>` gains optional `aggregateMap` prop that, when supplied, inserts 3 columns between Consent and Assigned To — TRAIL (5-stage pill), Health (score + delta indicator, colour-toned), Alerts (count pill). Failures non-blocking. Aggregate-only privacy posture (§13.3). Full §6b polish backlog now empty (① + ② + ③).
  - [x] **Email-in hardening** — SendGrid Inbound Parse webhook (`/api/conversations/inbound`) hardened with, fail-closed: (1) timing-safe shared secret (`INBOUND_EMAIL_SECRET`, `?secret=` / `x-inbound-secret`; missing-in-prod = 503); (2) DKIM/SPF strict mode (`INBOUND_EMAIL_REQUIRE_DKIM_SPF=true` → SPF=pass + `pass` in the SendGrid `dkim` field); (3) sender allowlist (exact match on the conversation's consumer participant, OR domain in `INBOUND_EMAIL_ALLOWED_DOMAINS`); (4) per-conversation rate limit (`INBOUND_RATE_LIMIT_PER_HOUR = 20`). Every reject writes a `BLOCKED` audit row. Pure verification primitives + 20+ unit tests in `lib/email/inboundSecurity.ts`. Still deferred (not blocking): Cloud DLP attachment scanning, HTML→plaintext sanitisation, a proper email-reply parser, X-Monitrax-* tracing-header round-trip. **⏸ Activation DEFERRED (Reza decision 2026-05-12):** the hardening *code* is shipped (PR #747) and stays dormant until *turned on*. Turning it on requires: (a) a SendGrid Inbound Parse account, (b) an MX DNS record on a dedicated subdomain (e.g. `inbound.monitrax.com.au` → `mx.sendgrid.net`), (c) the env vars (`INBOUND_EMAIL_SECRET`, `INBOUND_EMAIL_REQUIRE_DKIM_SPF=true`, `MONITRAX_INBOUND_DOMAIN`, optional `INBOUND_EMAIL_ALLOWED_DOMAINS`) + redeploy, (d) the SendGrid destination URL set to `https://www.monitrax.com.au/api/conversations/inbound?secret=…`. **Trigger to activate:** a real client actually wants to reply to an adviser by email. Until then it's parked — the in-app conversation feature works without email-in, it'd add a new vendor (SendGrid) alongside the already-chosen Resend (Tech Debt #16), and the MX DNS change is risky-for-no-current-benefit. (Note: outbound email = Resend, not SendGrid; inbound-parse code currently expects SendGrid's format — revisit consolidating onto Resend's inbound support when this gets activated.)
  - [ ] **Stripe live-mode flip** — when Reza's risk review approves: env-var flip + price ID swap + webhook signing-secret swap + dunning + invoice PDFs (use Stripe-hosted Customer Portal — don't roll our own). Bundled with `OrganizationPlan` enum rename `STARTER/PROFESSIONAL/BUSINESS → STUDIO/PRACTICE/ENTERPRISE` (Tech-Debt #14). ~3 days code work after Stripe approves.
  - [ ] **WIF Phase 11** — drop legacy `buildStandardPrisma()` branch + remove `DATABASE_URL` from runtime env scope. Trigger ≥ 2026-05-31 (24h soak window). 1 day code change.
  - [ ] **WIF Phase 12** ⏸ PARKED 2026-05-20 — NOT a 15-min config. The `CDR_WIF_AUTHENTICATION_EVIDENCE.md` §8 path is flawed (see Up Next #2 for the full finding). Real path is a ~1-2 day infra PR (move `prisma migrate deploy` to an IAM-authenticated Cloud Run Job). Parked until a real trigger.
  - [x] **Profession-aware scope presets** — `LENDING` (LOANS+PROPERTIES+TRANSACTIONS) / `TAX` (TAX+TRANSACTIONS+DOCUMENTS) / `ADVISORY` (FULL minus DOCUMENTS, enumerated) presets. **SHIPPED (this PR).** SSOT at `lib/portal/scopePresets.ts`; quick-pick chips wired into `components/portal/team/InviteModal.tsx` (the actual scope-picking surface — the plan originally named `ConsentRequest.tsx`, but that's the consumer-side *approve* UI; presets belong on the adviser-side *request* UI). Chip de-highlights automatically when the selection no longer matches a preset (`matchScopePreset` on each render). **Closes Phase 32B PR3 item #10.**
  - [x] **Phase 36 Phase 2b/2d/2e** — `/dashboard/accounts` + `/dashboard/loans` bare list pages retired, `?action=` + `?id=` handlers on `/dashboard/balances`, `routeMap.ts` flipped, 7 source-side hrefs flipped. **SHIPPED (PR #742, merged 2026-05-09).** Closes Tech-Debt #9 + 2 dead routes. Phase 36 Phase 2c (Import Transactions UI migration) shipping in the follow-up PR — **closes Phase 36 entirely.**
  - [x] **Basiq integration master toggle (this PR)** — Reza directive 2026-05-14: while Basiq accreditation is in flight, every "Connect bank" affordance must be HIDDEN (not deleted) so users don't see buttons that lead nowhere. New canonical reader `lib/featureFlags/basiqGate.ts` (server, 30s in-process cache + invalidation hook) + `BasiqGateContext` (client React hook). Public read endpoint `GET /api/feature-flags/basiq` (unauthenticated — value isn't sensitive; renders or hides UI immediately). UI sweep: `BasiqHeroCard` self-hides; `AccountsEmptyState` reworded to "Add your accounts" + manual-add CTA; `SetupNextActionPanel` swaps the "Connect your bank" priority to "Add your accounts"; `/dashboard/balances` Connect-Bank toolbar button hidden + `?action=connect-basiq` deep-link silently strips; `AccountsDataSourceTiles` (onboarding wizard) hides the tier-1 Basiq tile + "or" divider; `/dashboard/settings/connections` page replaced with "not available yet" notice; "Bank connections" sidebar entry under My Settings hidden. Defense-in-depth: new `basiqRouteGuard()` short-circuits ALL 5 `/api/basiq/*` routes (`connect`, `connections`, `connections/[id]`, `sync`, `webhook`) with `503 BASIQ_DISABLED` when the flag is OFF — protects against stale UI, curl, hostile probes. New seed `npm run seed:feature-flags` (idempotent) creates the `BASIQ_INTEGRATION` row in `GlobalFeatureFlag` (defaults `enabled=false`). Admin `PATCH /api/admin/feature-flags/[key]` invalidates the in-process cache when the BASIQ flag flips so the change is instant on the same Vercel instance + ≤30s on warm peers via the TTL. Operator runbook `06_BASIQ_INTEGRATION_TOGGLE.md` documents every gated surface, toggle procedure, verification commands, and the compliance evidence pack. Reuses existing Phase 33 admin feature-flag UI — no new admin surface.
  - [x] **Backup/restore drill + IRP tabletop + observability SLOs** — operational readiness. **Doc-authoring portion SHIPPED (this PR — see §0b).** Three new runbooks under `docs/operational/runbooks/`: `06_BACKUP_RESTORE_DRILL.md` (quarterly non-destructive restore drill — verify backups → restore latest into a throwaway instance → verify schema/row-counts/integrity → tear down; annual extension covers PITR + `pg_dump`/`pg_restore` round-trip; Drill Log + PASS/FAIL definition), `07_IRP_TABLETOP_EXERCISE.md` (annual tabletop with 4 scenarios — CDR breach / prod DB unreachable / auth-provider outage / runaway cost — each walked through the IRP phases with `DECISION:` markers + After-Action Report template + Exercise Log), `08_OBSERVABILITY_SLOS.md` (app-level SLOs: availability 99.5% + p95/p99 latency + 5xx error-rate targets per route group; Cloud Monitoring alert-policy specs A1–A9 each with a runbook link; synthetic-canary plan; Service Health dashboard tiles; "live vs spec-only" status table). **Reza-side (GCP console, tracked in `08_OBSERVABILITY_SLOS.md` §9):** create alert policies A1–A9 + notification channels (Slack `#ops` + SMS/PagerDuty); set up the Vercel → Cloud Logging log drain (unlocks per-route 5xx/latency log-based metrics + A4/A6); stand up the authenticated synthetic monitor for the dashboard path; assemble the "Monitrax — Service Health" dashboard. **Reza-side (run the exercises):** first backup/restore drill (per `06`); first IRP tabletop (per `07`). The runbooks are now the spec/script to execute against.
- **Risk:** Each chunk is bounded (1-5 days). Heavy-touch areas (Stripe, alert engine) ship to staging-equivalent (Vercel preview against `monitrax-db-dev`) before any prod deploy. CDR sweepers are guard-railed by where-clauses that can only delete already-expired data — §12.11 destructive-write checklist N/A by structural argument.
- **Wall-clock estimate:** ~3 weeks of engineering, mostly in parallel with the 6-week external lead time.

#### 📋 Reza-side operational checklist — current state (last updated 2026-05-12)

> The GCP-console / Vercel / external activities that aren't in the code path. Tracked here so they don't get lost. ✅ = done, 🟡 = in progress, ⬜ = not started, ⏸ = deliberately deferred, 🚧 = blocked on an external dependency.

| # | Activity | State | Notes |
|---|---|---|---|
| 1 | Cloud Scheduler — `monitrax-cdr-lifecycle` | ✅ | Created (region `australia-southeast1`, target `https://www.monitrax.com.au/api/cdr/lifecycle`, `Authorization: Bearer <CRON_SECRET>`). **Schedule changed 2026-05-19** from `0 2 * * *` → `30 3 * * *` Australia/Sydney to dodge Cloud SQL maintenance-window collisions. Two intermittent TLS-handshake 500s observed (2026-05-17, 2026-05-19) at the original 02:00 AEST slot — root cause = `monitrax-db-prod` maintenance window was "any day, any hour, notifications off" so could overlap silently. Maintenance window also tightened to Sunday 04:00–05:00 AEST same day. Force-run after re-schedule = 200. See `docs/operational/security/04_WIF_TROUBLESHOOTING.md` §3.G "Variant — intermittent TLS-42" + `CHANGELOG_2026_05_19.md` Session 6 Quick-Click #4 for full diagnostic. |
| 2 | Cloud Scheduler — `monitrax-portal-alert-sweep` | ✅ | Created (`0 4 * * *` Australia/Sydney, target `https://www.monitrax.com.au/api/portal/alerts/sweep`). The `www.` matters — the apex `monitrax.com.au` redirects and turned the POST into a GET → HTTP 405 (now fixed). Last Force-run **200**. |
| 3 | Cloud Scheduler — `monitrax-conversation-retention-sweep` | ✅ DONE 2026-05-19 | Created (region `australia-southeast1`, `0 3 * * *` Australia/Sydney, target `https://www.monitrax.com.au/api/conversations/retention-sweep`, `Authorization: Bearer <CRON_SECRET>`). Force-run → 200. The 7-yr conversation-message purge (AFSL recordkeeping) is now enforced daily. See `CHANGELOG_2026_05_19.md` Quick-Click #3. |
| 4 | Email-in (SendGrid Inbound Parse) activation | ⏸ | Deferred (see "Email-in hardening" chunk above) — code shipped & dormant; activate when a real client wants to reply by email. Requires a SendGrid account + an MX subdomain DNS record + env vars. |
| 5 | Observability — confirm alerts **A1** (app-down on `/api/health`) + **A9** (GCP budget overrun) are live & notifying | ✅ DONE 2026-05-19 | **A1, A7, A9 all live + 2 cleanups completed.** **A1 ✅** — created `A1 — Monitrax app down (/api/health)` policy in Cloud Monitoring on the (NEW HTTPS) `Monitrax API Health Check` uptime check; trigger = uptime metric `< 1` over 2 min; channels = `Reza-Email` + `Reza-SMS`; severity Critical; labels `category=availability` / `severity=p0` / `runbook=01_incident_response`. **A9 ✅ DONE + gap discovered** — existing "Development Budget" was $50/mo with default Email recipients only; inbox audit confirmed **no budget alert email ever fired** (50% threshold should have triggered when spend crossed $25 weeks ago — silent overrun risk). Edit applied: renamed to `Monitrax monthly budget`, bumped to $100/mo, added 120% threshold, **explicitly linked `Reza-Email` notification channel** (the actual fix). GCP Billing budget alerts don't support SMS (platform limitation — email-only). **A7 ✅** — discovered already existed as `Cloud scheduler job failed` (Logs-based, scope `resource.type="cloud_scheduler_job" AND severity="ERROR"` correctly covers all 3 crons). Hardened: renamed to `A7 — Cloud Scheduler cron failure`, added `Reza-Email` + `Reza-SMS` (was previously only `GCP Monitrax Alert`), added documentation with runbook reference. **C1 cleanup ✅** — deleted 2 duplicate uptime-check alert policies (`Monitrax API Health Check uptime...` orphaned + `Notify on failure uptime failure` older). Policies list now clean: A1 + A7 only. **C2 fix ✅** — discovered old `Monitrax API Health Check` uptime check was misconfigured as HTTP/port 80 (causing intermittent regional failures because Vercel redirects HTTP→HTTPS and the check didn't follow redirects); GCP doesn't allow editing the protocol of an existing check, so DELETED the broken HTTP check and CREATED fresh HTTPS uptime check (port 443, contains "healthy" validation, 1-min frequency, all 6 regions). A1 alert automatically catches the new check via `host=monitrax.com.au` filter. |
| 6 | Observability — Vercel → Cloud Logging log drain | ⏸ DEFERRED 2026-05-19 | **Foundation work done (confirmed saved 2026-05-19 ~14:03 AEST), full pipeline deferred.** Created `vercel-log-drain@monitrax-479700.iam.gserviceaccount.com` with `roles/logging.logWriter` (project-level). WIF binding **saved and confirmed** on the SA's "Principals with access" tab: principal = `principalSet://iam.googleapis.com/projects/87218209262/locations/global/workloadIdentityPools/vercel-pool/attribute.project_id/prj_UYQF3GpGAkeFo4ZhMhch4Q0btCAU`, role = `Workload Identity User` (`roles/iam.workloadIdentityUser`) — same pattern as the WIF Phase 9 Cloud SQL binding. **But discovered the snag:** Vercel's Log Drain feature uses HMAC-signed POSTs (`x-vercel-verify` header), NOT OIDC bearer tokens — so the WIF binding we created doesn't help the log-drain pipeline directly. **Two paths forward:** (a) ship a tiny `vercel-log-receiver` Cloud Function PR that accepts Vercel's HMAC POSTs and writes to Cloud Logging via the SA we just created (~2 hr code work) — queued as Up Next #15 below; (b) use a third-party intermediate (Better Stack / Datadog) which has native Vercel integration but adds a vendor dependency. **Not a Basiq blocker** — `auditLog` table + retention sweep already handle the compliance artifact; this is operational debug-log mirroring. The SA + WIF binding remain in place — not wasted, just dormant. |
| 7 | Observability — authenticated synthetic monitor (Playwright → load `/dashboard` as a synthetic user) | ⬜ | Catches "auth + DB up but dashboard blank". `08_OBSERVABILITY_SLOS.md` §4. |
| 8 | Observability — A2–A6, A8 alert policies + notification channels + "Monitrax — Service Health" dashboard | ⬜ | The rest of `08_OBSERVABILITY_SLOS.md` §3/§5. |
| 9 | Backup/restore drill | ⬜ | Run `06_BACKUP_RESTORE_DRILL.md` end-to-end (~45 min, non-destructive). Log it in the Drill Log. |
| 10 | IRP tabletop exercise | ⬜ | Run one scenario from `07_IRP_TABLETOP_EXERCISE.md` (~60–90 min). Log it in the Exercise Log. |
| 11 | Prod schema-drift audit | ✅ DONE 2026-05-19 | Reza ran `GET /api/admin/schema-drift` (after PR #806 fixed the endpoint). Audit surfaced 3 missing tables + 4 missing enums (all CDR Phase L Fix G18 / G43 — `cdr_consents`, `cdr_complaints`, `cdr_disclosures` + their enums). Corrective migration `20260519100000_fix_pre_migration_cdr_tables_drift` shipped — purely additive, idempotent, `DO $$ ... EXCEPTION` on the CREATE TYPEs + `IF NOT EXISTS` on the tables/indexes so it's a no-op on dev. Tech Debt #18 ✅ closed. Pre-Basiq blocker resolved. |
| 12 | Rotate `CRON_SECRET` | ✅ DONE 2026-05-19 | **Rotated atomically across 4 surfaces** — generated new value via `openssl rand -base64 48` (NOT pasted into chat), updated Vercel Production env var, updated `Authorization: Bearer …` header on all 3 Cloud Scheduler jobs (`monitrax-cdr-lifecycle`, `monitrax-portal-alert-sweep`, `monitrax-conversation-retention-sweep`), redeployed Vercel Production to activate, force-ran all 3 jobs → all 200. The old secret (the one that appeared in chat 2026-05-12) is now invalid. |
| 13 | Restrict the Gemini API key | ✅ DONE 2026-05-19 | **Reza applied both restrictions** in GCP Console → APIs & Services → Credentials → the Gemini key in project `Monitrax` (organisation `monitrax.com.au`). **(a) Application restrictions** = HTTP referrers: `https://monitrax.com.au/*`, `https://www.monitrax.com.au/*`, `https://*.vercel.app/*` (the Vercel wildcard covers preview deploys). **(b) API restrictions** = restricted to **Gemini API** only (`generativelanguage.googleapis.com`). The other two Gemini-named APIs in the dropdown (`Gemini Cloud Assist API`, `Gemini for Google Cloud API`) were intentionally NOT selected — those are GCP-internal AI helpers, not the consumer Gemini API the app calls. **Rollback if anything breaks (e.g. Gemini 403s in Vercel logs):** GCP Console → same key edit page → either widen the HTTP referrers list (add missing domain) or temporarily flip **API restrictions** back to "Don't restrict key". The key value itself was NOT rotated — only restrictions were added, so any code calling Gemini continues to use the same key. |
| 14 | CMEK on Cloud SQL | 🟡 | Reza Tier-1 — see `05_RETENTION_SCHEDULERS.md` §6. CDR best-practice; ~1h + the maintenance window. |
| 15 | Cloud Armor (WAF/DDoS) | ⬜ | Reza Tier-1 — `05_RETENTION_SCHEDULERS.md` §6. Required for Basiq accreditation. ~1-2 days (2-wk PREVIEW window). |
| 16 | Security Command Center (Standard tier triage) | ✅ DONE 2026-05-19 | **SCC Standard already activated; first-scan findings triaged to Active = 0.** Findings dropped 4 → 0: (a) 2 × "Public SQL instance" (Cloud SQL `0.0.0.0/0` on dev + prod) → MUTED with documented exception linking to CLAUDE.md §13.6 + WIF Phase 12 resolution path; (b) "Open RDP port" → RESOLVED by deleting `default-allow-rdp` firewall rule (unused default VPC rule, zero Compute Engine VMs in our architecture); (c) "Open SSH port" → RESOLVED by deleting `default-allow-ssh`. Bonus housekeeping: also deleted `default-allow-icmp` + `default-allow-internal` (not SCC-flagged but equally pointless given no VMs). **Premium activation deferred to D-Day Bundle Tier 1.1** (30-day free trial timed for fresh scans when Basiq reviewers look). |
| 17 | Pen test commission | ⏸ PARKED 2026-05-20 | ~6 wk lead time, ~AU$15-25k. Basiq accreditation hard requirement. **Parked pending Q-BASIQ-1** (Reza weighing Basiq initial + ongoing cost — see Open Questions). Do NOT commission until Basiq go/no-go is decided. If Basiq proceeds, this becomes time-critical again (6-wk lead). |
| 18 | Cyber insurance binding | ⏸ PARKED 2026-05-20 | ~3 wk lead time. Basiq accreditation hard requirement. **Parked pending Q-BASIQ-1.** Note: cyber insurance is good practice for any SaaS handling financial data even without Basiq — but the *urgency* is Basiq-driven, so parked with the rest. |
| 19 | Stripe live-mode review | ⏸ PARKED 2026-05-20 | ~2 wk. **Parked 2026-05-20 — Reza: "can wait until later."** Monetisation-timing decision, not Basiq-gated. When Reza is ready to take real payments → submit the live-mode application, then ping Claude for the live-mode flip code PR (~3 days). |
| 20 | Anthropic API key + budget cap | ✅ DONE 2026-05-17 | `ANTHROPIC_API_KEY` added to Vercel Production env vars (reza-onboarding-api-key, Default workspace). Spend limits set at `console.anthropic.com` per `cost-control/01_BUDGET_ALERTS_SETUP.md` §4. **Verification still needed:** confirm the $50/mo cap is on the SAME workspace as the key (Default, not a separate `monitrax-prod` workspace — Anthropic caps are per-workspace). Key name `reza-onboarding-api-key` is cosmetically misleading (it covers BOTH consumers: Phase 33g.2 feedback chat + Phase 12 Track E onboarding) — optional rename to `monitrax-prod`. |
| 21 | WIF Phase 11 / Phase 12 | 🚧 | Trigger-gated (Phase 11 ≥ 2026-05-31; Phase 12 trigger-based). Claude does the code; Reza does the env/network bits. |
| 22 | Delete `DIRECT_URL` env var from Vercel (if present) | ✅ DONE 2026-05-19 | **Confirmed clean** — Reza checked Vercel Project Settings → Environment Variables; `DIRECT_URL` is not present in any scope (a "direc" search only matched `GOOGLE_REDIRECT_URI`, the OAuth callback URL — must stay). Either never set, or removed in an earlier cleanup. Tech Debt #6 fully closed at both code AND infra layer. |

#### 📅 D-Day Bundle — pre-Basiq submission checklist (executed the day before Reza replies to the Basiq accreditation email)

> **Why this exists:** Reza decision 2026-05-19 — defer all cost-incurring security activations to **the day before Basiq submission** so they're fresh when reviewers look (recently-activated SCC Premium with a "last scanned: 2 hours ago" timestamp reads more credible than a 4-month-old setup), and Reza doesn't pay for months of "waiting for Basiq." This bundle is the single source of truth for "what to do that morning."
>
> **Sequencing:** Tier 3 items MUST start ~3-6 weeks before submission (external lead times). Tier 2 ships in code PRs the week before. Tier 1 + Tier 4 are pure D-Day actions.

##### Tier 1 — Cost-incurring activations (D-Day, ~2 hours)

| # | Activity | Cost | Effort | Console path |
|---|---|---|---|---|
| T1.1 | **SCC Premium 30-day free trial** | $0 for 30 days → ~$15-40/mo after | 5 min activation + 25 min triage | GCP Console → Security → Security Command Center → Settings → upgrade to Premium → start free trial |
| T1.2 | **Cloud Armor WAF policy** on Vercel-fronted endpoints | ~$5/policy/mo + per-request fees | 30 min create + 30 min tune | GCP Console → Network Security → Cloud Armor → create policy → attach to backend service. Per `05_RETENTION_SCHEDULERS.md` §6 |
| T1.3 | **CMEK on Cloud SQL** | ~$0.06/key/mo + ~$0.03/10k operations | 30 min (requires maintenance window — schedule for Sunday 04:00 AEST already-configured maintenance window) | GCP Console → KMS → create keyring + key → SQL → instance → Customer-managed encryption keys → assign. Per `05_RETENTION_SCHEDULERS.md` §6 |

##### Tier 2 — Code PRs that ship the week before D-Day

| # | Activity | Where queued | Notes |
|---|---|---|---|
| T2.1 | **WIF Phase 11** — drop legacy `buildStandardPrisma()` from `lib/db.ts`; remove `DATABASE_URL` from runtime env scope (keep build scope for `prisma migrate deploy`); disable / drop `monitrax_user` | Up Next #1 | Trigger ≥ 2026-05-31 (30 days post-Phase-9). After this, the codebase has NO static-credential code path at all — IAM is the only auth mechanism. |
| T2.2 | **WIF Phase 12** — Vercel Static IP add-on + Cloud SQL authorized networks restricted to that single IP (removes `0.0.0.0/0`) | Up Next #2 | 15-min cutover, but needs Vercel Pro Static IP add-on (~$20/mo) provisioned + the code change to land first. After this, the 2 muted "Public SQL instance" SCC findings can be UNMUTED → they'll auto-resolve. |
| T2.3 | **Retry-on-TLS-error wrapper** for Prisma proxy in `lib/db.ts` | Up Next #6b | Defensive belt-and-braces for the Cloud SQL Connector handshake. Small PR. |
| T2.4 | **`vercel-log-receiver` Cloud Function shim** | Up Next #15 | Closes the Vercel → Cloud Logging gap; SA + WIF binding already created 2026-05-19 ready for the Cloud Function to impersonate. |

##### Tier 3 — External lead-time items ⏸ PARKED 2026-05-20

> **PARKED pending Q-BASIQ-1.** Reza is weighing whether to onboard Basiq at all, given the initial + ongoing cost. The whole D-Day Bundle above (Tiers 1-4) only executes IF the Basiq go decision is made. If Basiq proceeds, Tier 3 reverts to "commission NOW" because of the 3-6 week external lead times — the bundle's sequencing assumes Tier 3 starts ~6 weeks before submission. **This subsection is preserved, not deleted, so it's ready to re-activate the moment Q-BASIQ-1 resolves to "go".**

| # | Activity | Lead time | Cost | Notes |
|---|---|---|---|---|
| T3.1 | **Penetration test** | ~6 weeks | ~AU$15-25k | ⏸ Parked. Basiq accreditation hard requirement. Phase 0 row #17. |
| T3.2 | **Cyber insurance binding** | ~3 weeks | $TBD | ⏸ Parked. Basiq accreditation hard requirement. Phase 0 row #18. |
| T3.3 | **Stripe live-mode review** | ~2 weeks | $0 (application) | ⏸ Parked (monetisation timing — Reza "can wait until later", NOT Basiq-gated). Phase 0 row #19. |
| T3.4 | **Accountant sanity-check of the Phase 44 entity model** (`PHASE_44_ENTITY_GRAPH.md` §3A–§6 — node taxonomy, edge taxonomy, validity matrix) | schedule a short call | nominal | ⏸ Deferred to Basiq-onboarding prep — **Reza decision 2026-05-20: skip for now, do it at Basiq-onboarding time.** Phase 44 build proceeds without it (the structural model was hardened by two independent AI adversarial reviews — see `PHASE_44_ENTITY_GRAPH.md` §14). The accountant check becomes diligence evidence for the Basiq submission: have the accountant confirm the model correctly reflects real AU structures + the user's actual deeds before Basiq go-live. |

##### Tier 4 — D-Day morning verifications (15-20 min total)

| # | Verification | Expected result | How to verify |
|---|---|---|---|
| T4.1 | Production schema-drift audit | `summary.hasDrift: false`, all `missing*` arrays empty | `GET /api/admin/schema-drift` (admin auth) |
| T4.2 | All 3 Cloud Scheduler jobs healthy | All "Last status" = Success / 200 | GCP Console → Cloud Scheduler → check + force-run all 3 |
| T4.3 | Prisma migrations applied | `_prisma_migrations` table count matches `prisma/migrations/` folder count on `main` | Cloud SQL Studio → `SELECT COUNT(*) FROM _prisma_migrations;` vs `ls prisma/migrations/ \| wc -l` |
| T4.4 | Vercel deploy is green | Most recent Production deploy = Success | Vercel → Deployments → most recent Production = green |
| T4.5 | SCC active-findings count | 0 (after Tier 1.1 trial activation reruns the scan) | SCC → Findings → Active filter |
| T4.6 | **Submission screenshots captured** | Folder of evidence for Basiq email | SCC Compliance dashboard, Cloud Armor policy attached, IAM principal list, MFA-on-owner-account screenshot, WIF Phase 9 setup (already in `CDR_WIF_AUTHENTICATION_EVIDENCE.md`), pen-test executive summary cover page, cyber insurance certificate of currency |
| T4.7 | **Reply to Basiq with submission package** | Email sent | Compose reply to the Basiq onboarding email with the evidence package attached |

##### What happens DAY OF the reply

1. **Morning (08:00 AEST)** — run Tier 4 verifications T4.1 → T4.5 in sequence
2. **Mid-morning (10:00 AEST)** — execute Tier 1 activations T1.1 → T1.3 (SCC Premium trial → Cloud Armor policy → CMEK)
3. **Afternoon (14:00 AEST)** — capture submission screenshots (T4.6)
4. **Late afternoon (16:00 AEST)** — compose + send the Basiq reply (T4.7)
5. **Evening** — monitor Cloud Logging for any anomalies in the 24h post-activation window; SCC findings may spike briefly as new scanners light up

##### Rollback if any Tier 1 activation breaks production

- **SCC Premium** — no production impact; can downgrade back to Standard tier at any time
- **Cloud Armor** — if a rule blocks legitimate traffic, edit/disable the offending rule (the policy itself doesn't break anything by existing; only specific rules do). Worst case: detach the policy from the backend service to fully revert.
- **CMEK** — most disruptive to roll back. Mitigation: schedule the activation for Sunday 04:00 AEST in the same maintenance window; have the rollback path (revert to Google-managed keys) verified before activation. If anything breaks: `gcloud sql instances patch monitrax-db-prod --no-customer-managed-encryption-key` (reverts to Google-managed in-place, ~30s downtime).



### 0b. Phase 32B PR3 — post-#9b polish, part 2: hero KPI strip + client book on real data

- **Status:** 🟡 SHIPPING in this PR (`claude/phase-32b-pr3-post9b-real-kpis-MG8mr`)
- **Started:** 2026-05-10 (immediately after PR #749 merged)
- **Owner:** Reza (direction) + Claude (code + docs)
- **Last touched:** 2026-05-10
- **Why this matters:** Reza directive 2026-05-09 *"continue"* — the second post-#9b polish item from `PHASE_32B_PR3_ALERT_ENGINE.md`. After #9b the alert *stream* read real data but the hero KPI *strip* (active clients / need-attention / TRAIL advanced / avg health) was still on the `LIGHTHOUSE` fixture, so the dashboard was half-real / half-demo. This PR makes the dashboard show **either** the real book **or** the demo preview — never a hybrid — keyed on whether the alert sweep has actually computed anything for the org. Combined with PR #749's admin "run sweep now" button, an org can be flipped from preview → real today, before the Cloud Scheduler job is wired.
- **Phases (this PR):**
  - [x] **Schema — `ClientSnapshotMarker` + `previousHealthScore Int?` + `previousTrailStage String?`** + migration `prisma/migrations/20260514100000_phase_32b_pr3_marker_prev/migration.sql` (additive — `ADD COLUMN`×2, nullable, no backfill; §12.11 N/A). `runPortalAlertSweep`'s marker upsert now rolls forward on the `update` branch: `previous* := last*` (the value the row had going in, from `client.alertMarker`), then `last* := current`. Null until the second sweep for a given client — so the "change since last sweep" delta reads 0/"stable" for ~24h after this lands, then it's live.
  - [x] **`app/api/portal/clients/route.ts`** (NEW) — `GET ?organizationId=…`, `withPermission('org.read', …)` + inline active-membership check (mirrors `/api/portal/alerts`). Returns `{ hasRealClients, lastSweptAt, kpis: { activeClients, needsAttention, trailAdvancedThisWeek, averageHealth, averageHealthDelta }, clients: [{ id, name, initials, trailStage, healthScore, healthDelta, activeAlertCount }] }` — built from `OrganizationClient` (status ACTIVE) + `ClientSnapshotMarker` + ACTIVE `ClientAlert` (one `findMany` each, bucketed in JS); **no live `getMasterFinancialSnapshot()` per client**. `hasRealClients = (≥1 active client has a marker with a non-null health score)`. `id` = `organizationClientId` so it joins cleanly with `/api/portal/alerts`. Privacy: aggregate scalars + display name/initials only — no balances, no CDR data (§13.3).
  - [x] **`app/portal/dashboard/page.tsx`** — fetches `GET /api/portal/clients` + `GET /api/portal/alerts` on mount. `usingRealData = hasRealClients` is the master switch: when real → hero KPIs from `clientSummary.kpis`; alert stream shows the real ACTIVE alerts (even if empty → genuine "all quiet", not the demo); the fixture `PracticeClientBookTable` (which has the demo's financial columns) is **replaced** by a slim "Your client book — N active · M need attention · last refreshed … → Open the full client book" card linking to `/portal/clients`. When not real → everything is the `LIGHTHOUSE` fixture preview (unchanged from #9b). The `Avg client health` KPI cell's sub flips "vs 30d" → "vs last sweep" in real mode and shows "stable" (neutral tone) when the delta is 0.
  - [x] **Doc-sync (CLAUDE.md §16):** `docs/blueprint/PHASE_32B_PR3_ALERT_ENGINE.md` (§6b "part 2" added; §6 table row "computeKpis real input" → ✅; header status line updated; §8 references); `docs/architecture/03_DATA_MODEL.md` §9.3 (`client_snapshot_markers` row gains the `previous*` columns + the roll-forward note + the `GET /api/portal/clients` paragraph; migration named); `docs/architecture/07_API_STANDARDS.md` §15 (new sub-section "Org-scoped aggregate portal endpoints" distinguishing `?organizationId=` aggregate GETs — `/api/portal/alerts`, `/api/portal/clients` — from the per-client `verifyAdviserClientAccess`-gated `/api/portal/clients/[id]/*` drill-ins); `docs/blueprint/MASTER_BLUEPRINT.md` §4 (Phase 32B PR3 row gains the post-#9b-polish summary); `docs/IMPLEMENTATION_PLAN.md` (the "Real alert engine v1" chunk's post-#9b note updated — ① ✅ PR #749, ② ✅ this PR, ③ 📋 queued; §0b → this workstream; PR #749 → Recently Completed; top header refreshed); `docs/changelog/CHANGELOG_2026_05_09.md` session entry.
- **Risk:** Low–moderate. **Schema change** — but minimal (two nullable columns, additive migration, §12.11 N/A; the Vercel preview applies it to `monitrax-db-dev` before building, so a bad migration fails the preview, not prod). **`GET /api/portal/clients`** — read-only; the consent gate is already applied upstream (the marker/alert rows are written by the sweep, which applies `scopeAllowedTriggers` per client), so it returns nothing the org isn't entitled to; aggregates only, no balances. **Dashboard rewiring** — touches #9b's fetch logic: `usingRealAlerts` (gated on `alerts.length > 0`) → `usingRealData` (gated on `hasRealClients` from the new endpoint), and the fixture client-book table is conditionally replaced by a slim card in real mode. The fixture path is byte-for-byte the #9b behaviour for an org with no swept clients. `tsc --noEmit` is a no-op in this sandbox — code reviewed against the existing patterns (`withPermission('org.read', …)` + membership check matches `/api/portal/alerts/route.ts`; `alertMarker` relation + `select` shape matches the sweep route; `GlassHeroKpiCell tone="neutral"` already used on this page); the Vercel preview build is the canonical type + migration check.
- **Closes / opens:**
  - Closes: post-#9b polish item ② (hero KPI strip + client book on real data — the dashboard is no longer half-fixture).
  - Closes: post-#9b polish item ③ — **richer real-data client-book table on `/portal/clients`** — **✅ SHIPPED 2026-05-18 (PR D, session 19)**: `app/portal/clients/page.tsx` now fetches `GET /api/portal/clients?organizationId=…` in parallel with the granular client list and builds a `Map<clientId, ClientAggregateRow>` (`{ trailStage, healthScore, healthDelta, activeAlertCount }`). `<ClientList>` accepts an optional `aggregateMap` prop and, when present, inserts three new columns between Consent and Assigned To — **TRAIL** (warm-toned pill per stage: Track=sky, Reduce=amber, Anchor=emerald, Invest=indigo, Live=fuchsia), **Health** (score colour-toned — emerald ≥70 / amber 40–69 / rose <40 — plus a small delta indicator: `↑ +N` / `↓ -N` / `stable` — neutral grey for `stable`, ↑ emerald, ↓ rose), **Alerts** (red-toned pill when >0, slate otherwise). Aggregate fetch failures are **non-blocking**: the granular table still renders, the three new columns just collapse out (the existing 5 columns stay). Privacy-first (CLAUDE.md §13.3): no balances / no CDR data flows through this map — aggregate scalars + display labels only, byte-for-byte the same data the dashboard hero strip already shows. Behavioural-psychologist + designer lenses: the warm "human dimension" reads alongside the system fields, instead of forcing the adviser to click each client to see if anything's changed since the last sweep. Reza-side, unchanged: wire the `monitrax-portal-alert-sweep` Cloud Scheduler job (`0 4 * * *` in the `Australia/Sydney` timezone, `Authorization: Bearer <CRON_SECRET>`) — until then, run the sweep via `/admin/scheduler` → "Portal alert sweep" to populate the real data. With ① + ② + ③ all shipped, the full §6b polish backlog from `PHASE_32B_PR3_ALERT_ENGINE.md` is now empty.
- **Why-this-matters (4-lens):** Architect lens — keeps the SSOT honest (the dashboard now reads the same `ClientSnapshotMarker` rows the alert engine maintains, via a thin aggregate endpoint — no parallel computation) and avoids the worst failure mode (showing demo numbers next to real alerts) by making "real vs preview" a single, explicit switch. Behaviour-psychologist lens — a half-real / half-demo dashboard erodes trust the moment the adviser notices; "either your book or the sample, never a blend" is the trustworthy default. Financial-adviser lens — the hero strip is the first thing an adviser reads each morning ("how many of my clients need me today?"); having it reflect *their* book the moment a sweep has run (rather than a frozen demo) is the difference between a dashboard they glance at and one they ignore. Designer lens — restraint over density: rather than fake the demo's financial columns for real orgs, the client-book table is replaced by a slim summary card that points to the proper `/portal/clients` page where the detail (and the per-client drill-in audit trail) lives.

### 0c. Settings overhaul (consumer / admin / portal)

- **Status:** 🟡 SHIPPING in this PR (`claude/review-monitrax-settings-LL4bQ`)
- **Started:** 2026-05-08
- **Owner:** Reza (direction) + Claude (code)
- **Last touched:** 2026-05-08
- **Why this matters:** A 2026-05-08 four-lens audit of consumer / admin / portal Settings surfaced 12 trust-breaking gaps. Several were UI lying to users — Delete Account button with no `onClick`, Appearance API silently dropping toggles, `/api/settings/status` hardcoding values, mock API Keys / Billing pages pretending to be real, dead Storage folder switches. Several were schema-orphans — `/api/settings/categorization` (Phase 29) had no UI; `/components/portal/settings/OrganizationSettings.tsx` was 333 lines of working component with zero importers. And several were missing-but-needed: account deletion lifecycle (CDR §3.2 right-to-erasure), data export (Privacy Act APP 12), trusted contact (Phase 32B advisor handover precondition), country / tax-year UI exposure (Phase 20 + Phase 41 sensitive). Behavioural-psychology lens: settings is the surface where users go when something feels wrong — every lie there compounds the original anxiety.
- **Phases (this PR):**
  - [x] Schema migration — `UserPreference` +4 appearance columns, `User` +5 trusted-contact + 2 deletion-lifecycle columns, `AuditAction` +5 enum values, partial index on `users.deletionScheduledFor`. Additive, §12.11-clean.
  - [x] `/api/settings/status` reads real notification + profile state from `UserPreference` + `User`.
  - [x] `/api/settings/appearance` persists theme / showCents / compactMode / financialYearStart / taxYear / country.
  - [x] `/api/account/delete-request` GET / POST / DELETE — 30-day soft-delete grace, audit-trailed, cancellable.
  - [x] `/api/account/export` GET — JSON document of every row the user owns (right to portability).
  - [x] `/api/account/trusted-contact` GET / PUT — optional second-line contact, never auto-shared.
  - [x] `/dashboard/settings/categorization` consumes the orphaned Phase 29 API.
  - [x] `/dashboard/settings/connections` mounts Basiq disconnect on Settings (was buried in `/dashboard/accounts`).
  - [x] `/dashboard/settings/trusted-contact` form.
  - [x] `/portal/settings` mounts the orphaned `OrganizationSettings` component; sidebar feature-gate removed.
  - [x] Delete Account button wired with banner state for pending deletions.
  - [x] Privacy & CDR page gains "Download my data" card.
  - [x] Appearance UI gains Country + Tax year fields + persistence.
  - [x] API Keys + Billing pages replaced with honest "Coming Soon" placeholders.
  - [x] Storage page dead `<Switch defaultChecked />` toggles replaced with honest "what we do today" list.
  - [x] Settings IA — header renamed `My Settings`; sidebar regrouped into 5 mental-model groups (Me / My money data / Privacy & safety / My notifications / My plan).
  - [x] Doc-sync — `06_UI_UX_FOUNDATION.md`, `03_DATA_MODEL.md`, `PHASE_19_DOCUMENT_MANAGEMENT.md`, `IMPLEMENTATION_PLAN.md`, `CHANGELOG_2026_05_08.md`.
- **Risk:** Low. Migration is purely additive, defaults preserve existing behaviour. New routes don't touch CDR boundaries beyond the existing `withPermission`/`withActiveConsent` patterns. Visual surface unchanged on the in-place pages.
- **Reversed-decision protection:** Mock API Keys + Billing pages were the worst offenders. They're now stripped to placeholders. If someone wants to re-build them as mock UI in the future, this entry says "no — that's why we deleted them last time".
- **Closes tech-debt / opens:** Closes `🗑️ Dead Code` entries for the API Keys mock + Billing mock + categorisation-API-no-UI orphan + portal-settings-component orphan. Opens follow-up: (1) ✅ **DONE 2026-06-01** — Cloud Scheduler hard-delete executor shipped (`/api/account/lifecycle` + `lib/services/accountDeletion.ts`; identity-first, abort-on-failure; see Recently Completed 2026-06-01). Note: the design is hard-delete (not anonymise) — the irreversibility is surfaced honestly in the UI. Two Reza-side console steps remain to arm it (IAM grant + Scheduler job). (2) Phase 39 GlassHero pass on Settings; (3) Folder-organisation Storage settings when Phase 26 ships.
- **Why-this-matters (psychology lens):** Reza's brief 2026-05-08: *"Yes fix them all and give me a PR url to merge"* — pragmatic, decisive. The audit identified 5 things the app was lying about and 4 things it didn't expose. Lying erodes trust faster than missing features; this PR closes the lies first.

### 0d. GTM Automation — B2B-led launch playbook (executable plan)

- **Status:** 🟡 IN FLIGHT — plan shipped 2026-05-11; **Phase 1 substantively underway** (Steps 1.1 + 1.3 + 1.5 DONE; 1.6 BUILT awaiting first live cron run). Execution running.
- **Started:** 2026-05-11
- **Owner:** Reza (sales calls + AFSL judgment + Basiq go/no-go + n8n operator) + Claude (automation, copy, prompts, schemas, code review, doc-sync)
- **Last touched:** 2026-05-14
- **Canonical docs:** `docs/marketing/GTM_EXECUTION_PLAN.md` — step-by-step playbook (phases 0–6, status tracker, "how to ask Claude to execute" protocol). `docs/marketing/GTM_TOOL_STACK.md` — living tool stack + cost register (per-tool breakdown, cost summary, tool-choice decision log). The cost register is the SSOT for tooling costs — update it, not the plan, when a tool changes.
- **Why this matters:** Monitrax is demo-ready and approaching production-ready but has no users, no revenue, no distribution. A 4+ lens architect-mode review (financial / growth-marketing / behavioural / architect / security) concluded the fastest path to **real revenue from a pre-Basiq position** is B2B (mortgage brokers) + a productised "Financial Health Review" service — NOT consumer subscriptions. Consumer comes after Basiq is paid for. The Basiq economics ($10k upfront + $2k/mo) make this gate non-negotiable: B2B has to fund Basiq, not the other way around.
- **The plan (one paragraph):** Sell to mortgage brokers first (60-day pilots → paid contracts; refinance-trigger engine is the moat). Run a paid Financial Health Review service (~$200–400 fixed-price) for cash-now and a warm subscription pipeline. Park consumer subscriptions until Basiq is live. Automate the volume layer (lead gen, enrichment, sequencing, content, support triage, lifecycle, reporting) on an n8n + Claude + Instantly + Airtable + Stripe spine so Reza spends ≤2 hours/day on the things only he can do (calls, judgment, product).
- **The Basiq gate (locked):** Do not start Basiq onboarding until BOTH (a) ≥ AU$3–5k committed broker MRR AND (b) ≥ AU$15k cash on hand. Until then, brokers + Reviews run on manual data entry / CSV import. Captured as Phase 5 of the execution plan.
- **Phases (per `GTM_EXECUTION_PLAN.md`):**
  - [ ] **Phase 0 — Pre-flight** (Day 0, ~2h): AFSL boundary doc + success metrics locked
  - [ ] **Phase 1 — Foundations** (Week 1, ~12h): Step 1.1 n8n DONE 2026-05-13 · **Step 1.2 Airtable CRM DONE 2026-05-15** — base `Monitrax CRM` (`appEDHNU0mtbWznHp`) with 7 tables (Leads / Contacts / Companies / Brokerages & Employer Orgs / Deals / Reviews / Activities), PAT `n8n-monitrax-crm` wired into n8n, digest workflow's Activities branch replaces the stub with a real Airtable Search-Records query (filter: created in last 24h); CRM ACTIVITY section added to Claude prompt; end-to-end production test successful · Step 1.3 sending-domain warming DONE 2026-05-12 (2–3wk warm ticking, ~early June) · Step 1.4 core SaaS (deferred — wire each only when actually needed) · Step 1.5 Anthropic API DONE 2026-05-14 (🟡 → 🟢) · Step 1.6 **Founder Daily Digest LIVE 2026-05-14 (evening)** — cron PUBLISHED, first scheduled run 2026-05-15 06:45 Sydney landed cleanly with both prompt-improvement candidates self-resolved (no edits needed), error notifications wired, Gmail self-send filter active. Runbook: `docs/operational/runbooks/09_GTM_FOUNDER_DAILY_DIGEST.md`. **"Done when" → 3 useful mornings in a row — Day 1 of 3 done 2026-05-15 ✅; Days 2 + 3 are tomorrow + Sunday morning.**
  - [ ] **Phase 2 — Outbound pipeline** (Week 2, ~10h): broker ICP + 1,000-lead list + 4-email sequence + Claude personalisation + reply routing + Cal.com call-prep + launch @ 30/day
  - [ ] **Phase 3 — Financial Health Review service** (Week 3, ~10h): `/review` landing + Stripe + intake + Claude-drafted report (Opus) + branded PDF + QA-and-deliver + VA hired
  - [ ] **Phase 4 — Broker onboarding** (Weeks 4–5, ~15h): `/for-brokers` landing + co-branded referral link (minimal viable, NOT full multi-tenancy) + pilot agreement (Documenso) + refinance-trigger engine spec + monthly broker value report
  - [ ] **Phase 5 — Basiq decision gate** (trigger-based, ~Month 3–4): go/no-go check against the two gates → kick off Basiq onboarding when both green
  - [ ] **Phase 6 — Consumer scale** (post-Basiq, ~Month 6+): SEO content engine + one creator partnership + founding-member pricing + lifecycle + affiliate (full plan written when Phase 5 closes)
- **Success metrics (success criteria, locked Phase 0):**
  - Outbound: ≥8% reply rate, ≥1 booked call per 50 sends
  - Reviews: ≥30% conversion from booked discovery → paid Review
  - Brokers: ≥1 in 3 pilots → paid contract
  - Founder time: ≤2 hours/day on calls + decisions + Review QA; everything else machine/VA
  - 90-day cumulative revenue target: AU$8–15k + first broker MRR
- **Tool stack + monthly burn:** `try-monitrax.com` domain (GoDaddy) + **Google Workspace** mailbox `reza@try-monitrax.com` + **Smartlead** (cold-outbound sequencing+warmup; connects the Workspace mailbox) + **Hetzner Cloud VPS `n8n-1`** (CPX22, Nuremberg, Ubuntu 24.04) hosting self-hosted n8n + Postgres + Caddy + future Documenso + Anthropic API + Apollo + Airtable + Cal.com + Loops + Stripe + PostHog + Sentry + Senja + Loom + Typefully + part-time VA. **Pre-Basiq (lean):** ~AU$560–910/mo · **(fuller):** ~AU$910–1,310/mo. **Post-Basiq:** ~AU$2,910–3,310/mo + ~AU$10k Basiq initial. Full per-tool breakdown + decision log in **`docs/marketing/GTM_TOOL_STACK.md`** (SSOT for tooling costs).
- **Open Questions:**
  - ✅ Q-GTM-1 DECIDED 2026-05-11 — Review price for first 5 friendlies = **$197** (public price TBD post Reviews #3–5; recommended ladder $197→$297→$397)
  - ✅ Q-GTM-2 DECIDED + Step 1.3 DONE 2026-05-12 — Outbound sending domain `try-monitrax.com` (GoDaddy, "Keep Separate"); mailbox = **Google Workspace Business Starter** → `reza@try-monitrax.com` (MX/SPF/DKIM `google._domainkey`/DMARC `p=quarantine` all configured via GoDaddy auto-connect, audited OK); connected to **Smartlead** via OAuth; **warmup ENABLED** (target 30/day, +1/day rampup, 35% reply). **The 2–3 week passive warm is now running (~early June 2026); no real cold mail until then.** Smartlead's own DFY-mailbox flow rejected — wrong tool, registers a new Smartlead-owned domain. See `GTM_EXECUTION_PLAN.md` Step 1.3 + `GTM_TOOL_STACK.md` Decision Log 2026-05-12.
  - Q-GTM-3 (open, before Step 2.2): First aggregator — Claude rec: Finsure first, Connective second
  - Q-GTM-4 (open, before Step 3.7): VA hire timing — Claude rec: hire mid-Phase 2, small scope, expand into Review intake from Review #2–3
  - Q-GTM-5 (open, before Step 3.1): AFSL boundary — Claude rec: DIY + AU fintech-lawyer review for v1 (factual-only), queue AFSL-holder partnership for the advice-session upsell
- **Risk / considerations:**
  - **AFSL line (Security/Compliance lens):** the Review must stay strictly factual / general-information — no personal recommendations. ASIC enforcement on unlicensed personal advice is active. Doc + lawyer review BEFORE selling to non-friendlies (Step 0.1).
  - **Spam Act 2003 (Security lens):** cold B2B outbound is fine done right, illegal/blacklisted done sloppily — separate sending domain, low daily volume, real unsubscribe, B2B-relevant offer. Step 1.3 enforces this.
  - **CDR scope (Architect lens):** until Basiq is live, NO CDR data flows. Reviews use manual entry / CSV import only. This is structural, not policy — there's nothing to test against yet.
  - **Solo-operator bottleneck (Behavioural lens):** sales calls + Review QA + product can't all be the founder forever. The VA hire (Step 3.7) and Founder Daily Digest (Step 1.6) exist specifically to keep the founder's daily plate at 3–4 decisions, not 30.
  - **Dropped option (load-bearing dissent):** consumer-first GTM (SEO + creator partnerships + paid ads) was considered and rejected for the next 90 days — would need Basiq live, would burn the $10k+$2k/mo before any validation, and consumer fintech CAC payback is too long for a pre-revenue runway. Captured in `↩️ Reversed Decisions` so a future session doesn't relitigate.
- **Why-this-matters (4-lens):** Financial-adviser lens — match the burn to validated revenue, not the other way around; the Basiq gate is the discipline. Growth-marketing lens — one broker = 50–500 onboarded clients; this is the only B2C-acquisition shortcut available pre-Basiq. Behavioural lens — automation buys back the founder's scarce cognitive budget for the work that compounds (relationships, judgment, product). Security/Compliance lens — AFSL boundary + Spam Act + CDR sequencing are all structurally enforced by the plan, not bolted on later.

### 0e. Monetisation model + Consumer Freemium Infrastructure (dormant build)

- **Status:** 📋 MODEL DECIDED 2026-05-12 (Reza + Claude architect-mode session) · INFRA BUILD QUEUED (after GTM Step 1.1/1.6; no urgency — enforcement is off until Basiq is live)
- **Owner:** Reza (direction) + Claude (build, when started)
- **The model (DECIDED):**
  - **Revenue priority order:** (1) adviser/broker **B2B subscriptions** — the engine, already designed (Phase 32B Org plans Studio/Practice/Enterprise + per-seat + per-client overflow); (2) **D2C premium subscriptions** — Phase 6 of the GTM plan, *post-Basiq*; (3) **marketplace lead fees** ($80/$150/$250 by user net-worth — already in schema); (4) **tasteful, disclosed affiliate** — a minor supplementary stream, never a pillar. **Display advertising = NO** — trust-killer + advice-conflict generator, contradicts CLAUDE.md §0 + §14; affiliate (only for products that pass the financial-adviser test, e.g. a high-interest savings account when the user is building an emergency fund — never a credit card to someone in debt, never inside the Guide's recommendation flow, always disclosed) is the acceptable form. Q-GTM-6 closed by this decision.
  - **Build-now vs build-later split (Reza directive 2026-05-12 — "build the paid structure + admin so I can review it, but everyone gets PREMIUM until Basiq, controlled via admin"):**
    - **Build now (foundational, DORMANT):** `lib/auth/entitlements.ts` → `checkEntitlement(userId, feature)` (companion to `withPermission()` — boundary only, never scattered in components); admin-portal controls (set a user's `UserSubscription.tier`; a **global "freemium enforcement: OFF" switch**; per-feature tier flags; a comp-a-friendly action); tag each gateable feature with its tier requirement (metadata, not enforced while the switch is off); a `UsageMeter` for the three cost centres (AI / storage / Basiq feeds). **Hard rule: dormant = provable no-op — enforcement OFF ⇒ everyone gets full access ⇒ zero behavioural change vs today; a user with no `UserSubscription` row defaults to PREMIUM, not FREE.** Tests pin this.
    - **Design now, build in Phase 6:** mockups / short design doc for the customer-facing pricing page + upgrade flow + paywall/quota states (premium, restrained, no manufactured urgency, no dark patterns).
    - **Build in Phase 6 (at/near Basiq go-live):** customer-facing pricing page, Stripe *consumer* checkout, paywall modals, quota prompts, "Upgrade" CTAs. Marketing site: an **Org/adviser pricing page** is appropriate now (real — advisers pay now); a **consumer pricing page** waits for Phase 6 (don't commit publicly to numbers that'll change).
  - **Free/Premium split (Phase-6 design work, not needed now — shape only):** FREE = manual entry, basic net worth + budget, TRAIL journey + stage indicators, Guide top-3 (rule-based), small AI quota (~10 advisor msgs/mo, lite model), small storage cap (~100 MB / ~25 docs), 1–2 connected bank accounts (post-Basiq). PREMIUM (~AU$9–14/mo or ~$79–129/yr, TBD) = unlimited bank feeds, unlimited AI advisor (fair-use, Opus), unlimited storage (within reason), advanced scenarios, full Tax module, full Properties/Investments, deeper Guide, Ask-a-Professional, priority support. **Cost-centre rule:** AI / storage / Basiq feeds get a hard low cap on FREE and a generous-but-finite cap even on PREMIUM, so one power user can't blow unit economics. Everything else (calc engines, dashboards, TRAIL) is ~free to serve — don't meter it.
- **Existing infra (do NOT rebuild):** `UserTier` enum (FREE/BASIC/PRO/PREMIUM) + `UserSubscription` (Stripe-linked) · `GlobalFeatureFlag.enabledForTiers`/`enabledForPlans` + `FeatureFlagOverride` (per-user/org, `expiresAt`) — *this is the entitlements + comp mechanism, already in the schema* · `OrganizationPlan` + `OrganizationLicense` (B2B) · `StripeCustomer`/`StripeSubscription` (live-mode flip pending — Phase 0) · marketplace `leadFeeTier*`.
- **Phase doc:** to be created (`docs/blueprint/PHASE_*_CONSUMER_FREEMIUM.md`) when the build workstream starts.
- **Risk:** building the dormant engine must not introduce any gating regression — if a feature that works today gets accidentally gated, that defeats the point. Default-to-PREMIUM + the global off-switch + tests are the guards. Also: don't let this jump the queue — it's off until Basiq (months away); the GTM machine generates the revenue that funds Basiq, so GTM Step 1.1/1.6 + Phase 2 come first.

### 0g. Phase 47 — Signup consent + legal-document foundation (compliance bedrock)

- **Status:** ✅ PR 1 SHIPPED 2026-05-24 (PR #876 merged — schema + migration + 3 draft docs + public route + register-page consent block + signup capture API). 🟡 PR 2 SHIPPING this session (existing-user migration modal — closes the friendlies-backfill gap). PR 3 queued (OAuth legacy-flow gate + settings page + version-bump middleware).
- **Started:** 2026-05-24
- **Owner:** Reza (direction + lawyer engagement + document copy review) + Claude (build, draft docs, schema)
- **Last touched:** 2026-05-24
- **Why this matters:** Friendlies have been invited and may be signing up RIGHT NOW. The current `app/register/page.tsx` collects no consent — no Terms tick, no Privacy tick, no AFSL boundary acknowledgement, and the legal documents themselves do not exist (`docs/policy/` has CDR + security policies but no consumer-facing ToS / Privacy / AFSL disclosure). This is an active Privacy Act APP 1 + APP 5 compliance exposure and an AFSL-line risk (s911A — unlicensed financial services). Reza directive 2026-05-24: *"all of the legal warnings and documentation to be added at the time of signup, and user to confirm they have read and agreed."* This workstream brings Monitrax to the same signup-consent posture as Up Bank / Frollo / Stake — bundled tick for mandatory documents + unbundled tick for marketing (Spam Act 2003) + audit-trailed acceptance pinned to document version.
- **PR 1 deliverables (this session):**
  - [x] **Schema**: new `UserConsent` model + `ConsentDocumentType` + `ConsentSource` enums + 6 new `AuditAction` enum values + relation on `User`. Additive only (§12.11 N/A).
  - [x] **Migration** `prisma/migrations/20260524100000_phase_47_signup_consent/migration.sql` — idempotent (DO blocks for `CREATE TYPE`, `IF NOT EXISTS` for table + indexes + ALTER TYPE ADD VALUE IF NOT EXISTS for enum extensions).
  - [x] **3 legal documents (DRAFT v0.1)** under `docs/legal/`: `terms-of-service.md`, `privacy-policy.md`, `afsl-boundary-disclosure.md`. Each carries a prominent DRAFT banner; each is pending counsel review per Q-HOOK-AFSL (now repurposed to cover this workstream's lawyer pass too — economies of scale).
  - [x] **Content loader** `lib/legal/content.ts` — frontmatter parser, canonical registry mapping URL slug → file → `ConsentDocumentType`, `getCurrentDocumentVersion()` helper.
  - [x] **Public route** `app/legal/[slug]/page.tsx` — reuses Phase 33a `renderMarkdown()` (§12.2 SSOT — no parallel renderer); statically generated; surfaces DRAFT banner inline; cross-links between the three docs.
  - [x] **Register-page consent block** — two checkboxes inside a stone-bordered card. Tick 1 mandatory (bundled ToS + Privacy + AFSL — gates BOTH email/password submit AND OAuth button); Tick 2 optional marketing opt-in (default OFF, separate label per Spam Act unbundling rule). Footer line rewritten to Privacy-Policy-only language now that consent is captured explicitly.
  - [x] **API** `POST /api/auth/consent` — `withAuth`-gated; Zod-validated; idempotent (skips inserts for already-current-version rows); writes 3 `UserConsent` rows + 3–4 `AuditLog` rows (one per `CONSENT_*` action); IP hashed (SHA-256), user-agent truncated to 500 chars.
  - [x] **Register flow integration** — `handleSubmit` + `handleGoogleSignUp` call `captureConsent()` immediately after Firebase signup; if consent POST fails, the user sees an error and can retry (the API is idempotent). Legacy OAuth (redirect) path defers to PR 2's migration modal — documented inline.
  - [x] **Cross-link sweep** — `app/signin/page.tsx` + `components/marketing/Footer.tsx` updated from dead `/legal/terms` + `/legal/privacy` to the new live routes + AFSL added.
- **PR 2 deliverables (this session — friendlies-backfill gap closed):**
  - [x] **`GET /api/auth/consent/status`** — read-only endpoint returning `requiresConsent` + `needed` array + `currentVersions` + per-doc `accepted` versions. Privacy-safe (flags + version strings only).
  - [x] **`<ConsentMigrationModal />`** at `components/auth/ConsentMigrationModal.tsx` — non-dismissible blocking modal (preventDefault on `onPointerDownOutside` / `onEscapeKeyDown` / `onInteractOutside`) with same two-tick UX as register page. Fail-safe: API trouble → skip modal, retry next page load (prefer letting the user through over locking them out on a flaky API).
  - [x] **Mounted in `DashboardLayout`** inside `BasiqGateProvider` so it fires for any authenticated user reaching any `/dashboard/*` route. Same component covers both the friendlies-pre-PR1 case AND the future version-bump case (the status endpoint compares accepted-version to current-version, so a re-acceptance ask is one line of doc-frontmatter away).
  - [x] **`POST /api/auth/consent`** Zod enum extended to accept `'EXISTING_USER_MIGRATION'` as a valid `consentSource` (was `SIGNUP | OAUTH_SIGNUP` only). Endpoint idempotency unchanged — partial-state users (e.g. accepted ToS but not Privacy from a previous failed run) handled cleanly.
- **PR 3 deliverables (this session — Phase 47 closeout):**
  - [x] **Settings → Legal page** at `app/dashboard/settings/legal/page.tsx` — surfaces user's current consent state per document (up-to-date / newer-version-available chip), Read links to each doc, marketing-comm toggle (revocable via new revoke endpoint), and "Download my consent record (JSON)" button. Tone calm, no shouty CTAs. Pointer to account-deletion for mandatory-doc withdrawal.
  - [x] **`POST /api/auth/consent/revoke`** — marketing-only revocation; sets `revokedAt` + `revokedReason` on every active MARKETING `UserConsent` row for the user; writes `CONSENT_MARKETING_OPTED_OUT` audit row. §12.11 checklist filled in the route's JSDoc — scoped to userId + documentType=MARKETING; safe.
  - [x] **`GET /api/auth/consent/record`** — Privacy Act APP 12 access-right fulfilment; returns user's complete consent history as a JSON file (Content-Disposition attachment); includes current document versions, every UserConsent row's full audit fields, generated-by + legal-notice header.
  - [x] **Version-bump operator playbook** at `docs/operational/runbooks/13_LEGAL_DOCUMENT_VERSION_BUMP.md` — when to bump, version-string convention (patch/minor/major), counsel-review gate for material changes, what NOT to do, verify-after-deploy steps. The PR-2 migration modal already structurally handles the re-prompt — this runbook closes the operator loop.
  - [x] **Settings nav** — `Legal` entry added under "Privacy & safety" group with `FileText` icon.
  - [x] **3 legal documents filled in** with company details — ReNew Holding Company Pty Ltd, ACN 675 267 311, 10 Fairview St Guildford NSW 2161, contact `admin@monitrax.com.au`. All `[TO BE INSERTED]` markers now resolved.
- **PR 4 queue (deferred — lower priority now):**
  - [ ] **Legacy-OAuth-redirect consent gate** for `/api/auth/oauth/google` server-side redirect (non-GCP-Identity legacy path). Caught structurally by the PR 2 modal on the first dashboard load, but the interstitial is the cleaner pattern. ~1 day.
  - [ ] **Friendlies remediation email** — Reza-side comms once docs are counsel-reviewed.
- **Risk:**
  - **PR 1 is half a flow without PR 2.** Existing friendlies who signed up before PR 1 ships will not be caught until PR 2's migration modal lands. This is intentional sequencing (PR 1 = new signups secured; PR 2 = backfill) but the gap window matters — ship PR 2 within ~1 week of PR 1.
  - **Documents are DRAFT** — not lawyer-reviewed. Footer of every doc + a prominent banner inline say so. Do NOT publish to general public traffic until counsel pass (Q-HOOK-AFSL).
  - **ACN placeholder + postal-address placeholder** in the docs. Reza-side: fill in `[TO BE INSERTED]` markers before counsel review.
  - **Privacy email** `privacy@monitrax.com.au` referenced in Privacy Policy — Reza-side: confirm this mailbox exists + is monitored, OR change to `support@`.
- **Why-this-matters (multi-lens):** Security/Compliance lens — closes the active APP 1 + APP 5 exposure; gives ASIC/OAIC the kind of point-in-time-version-pinned audit trail they expect; reuses the Phase 33a renderer (no parallel surface to govern). Behavioural-psychologist lens — the two-tick pattern (bundled mandatory + unbundled optional) is structurally honest with the user about what's required to use the service vs what's a separate choice; warm-words style preserved in the consent copy ("I have read and agree to..." not "I have read and accept the terms in full"). Financial-adviser lens — the AFSL Boundary Disclosure is the structural non-licence acknowledgement that protects every other surface (the AI advisor, the What If? scenarios, the marketing copy) by making clear what Monitrax is NOT. Architect lens — `UserConsent` as a separate table (not columns on `User`) preserves version history on re-acceptance, supports revocable marketing consent cleanly, and gives one queryable audit surface for the regulator.

### 0h. Phase 48 — Public Website Redesign (Dark Deep Cosmos)

- **Status:** 🟡 PR 1 (Foundation) SHIPPING this session. PRs 2-6 queued (each independently shippable).
- **Started:** 2026-05-26
- **Owner:** Reza (direction + visual approval) + Claude (Stitch generation, React conversion, PRs)
- **Last touched:** 2026-05-26
- **Why this matters:** The current public website (`monitrax.com.au`) suffers from four structural issues identified in the 2026-05-26 audit: (a) brand-identity drift — amber-led marketing surface vs the canonical navy + emerald + ivory brand system in `08_BRAND_UI_DESIGN.md`; (b) TRAIL stage hue contradiction — landing uses `T amber · R orange · A emerald · I sky · L yellow` while the app's `lib/navigation/trailNav.tsx` SSOT uses `T sky · R amber · A indigo · I emerald · L violet`, so a new user sees five different colours after signup; (c) pre-paint loading gate — every cold visitor + SEO crawler + social-preview bot sees a "Loading..." spinner as the LCP; (d) duplicate `/login` and `/signin` routes (282-line duplicate). Plus zero product imagery / app glimpse, which is the opposite of every premium-fintech reference (Mercury, Stripe, Linear, Ramp all front-load product proof). Reza brief 2026-05-26: redesign the entire public website experience to read as "Apple designed a private wealth operating system" — calm, premium, restraint-as-luxury — explicitly for the wealth-builder ICP (Q-ICP-1 decided 2026-05-24).
- **Design source (locked v4 direction):** Stitch project `1859462351962811110`. 8 canonical screens generated 2026-05-26. Visual language: dark Deep Cosmos `#0A0A14` background, Inter typography (700 hero / 600 sections / 400 body), emerald `#16A34A` CTA, 6 floating 3D symbolic icons (house / 3 columns / shield / `$` / upward chart / bank). Locked direction documented in `.stitch/SITE.md` §7 with full v1→v2→v3→v4 history. All TRAIL stage hues now match the canonical SSOT verbatim.
- **PR sequence (6 PRs, each shippable independently):**
  - [x] **PR 1 — Foundation ✅ MERGED (PR #891, 2026-05-26)** — `docs/blueprint/PHASE_48_PUBLIC_WEBSITE_REDESIGN.md` spec doc · Deep Cosmos public-side tokens additive to `app/globals.css` + `tailwind.config.ts` (zero existing token mutated, internal app surfaces unaffected) · `/login` → `/signin` permanent redirect (CLAUDE.md §12.1 dead-code consolidation) · `app/page.tsx` pre-paint loading gate replaced with post-paint redirect-via-useEffect (LCP fix; auth functionality unchanged). NO business / logic / functional code changed. Production deploy `dpl_7WAWZzRpTSBXAw2Mt8BupSGoRjoY` verified READY, `/`+`/login`+`/signin` all returning expected status codes.
  - [x] **PR 2 — Header + Footer ✅ MERGED (PR #892, 2026-05-26)** — `components/marketing/Header.tsx` + `Footer.tsx` rebuilt to Deep Cosmos. Refined geometric "M" mark (emerald-tinted square, no amber pill). Emerald `cosmos-cta` pill CTA. Footer `bg-cosmos-deeper` (#08080F). Dead-link audit removed `/pricing`, `/security`, `/about`, `/contact`, `/blog`, `/learn`, `/changelog` from header + footer (none of those routes exist in `app/`). 4-column footer (Brand / Product / Explore / Legal — "Company" column removed entirely, every entry dead). ACN line added per Phase 47 legal docs. Placeholder social-icon URLs removed. Production deploy `dpl_4g6qTFbQoY3Df9y6ADkczeVNyzXN` verified READY, all marketing routes returning 200, `/pricing` correctly 404 (dead link removed).
  - [x] **PR 3 — Hero + animations migration ✅ MERGED (PR #893, 2026-05-26)** — new `components/marketing/Hero.tsx` (dark Deep Cosmos full-screen hero, 6 floating 3D symbolic icons downloaded to `public/marketing/hero/`, "Your wealth, fully integrated." headline with emerald-gradient line 2, eyebrow chip, emerald `cosmos-cta` CTA, scroll affordance, `cosmos-float` keyframe gated by reduced-motion). `app/page.tsx` IA partial re-sequence. `animations.tsx` refactored to compose `components/shell/motion.ts` SSOT (exported API unchanged, 9 consumers untouched). Production deploy `dpl_FSHerQc7ioZzPZKcmHywkpowjToi` verified READY, hero + all 6 icon assets serving 200 from edge.
  - [x] **PR 4 — Body sections ✅ MERGED (PR #894, 2026-05-26)** — 4 new dark Deep Cosmos section components (`ProofStrip` · `OnePicture` · `FiveCapabilities` · `HowItWorks`) covering the entire middle of the landing page. TRAIL stage hue SSOT preserved. Strategic copy edits: Stitch's "+$14,280/yr" softened to "Clarity before commitment + Example" badge; "30+ entity types" softened to "Multi-entity native"; stock photos replaced with abstract cosmos-glass viz panels. 6 deleted v1 Trail* components (TrailHero/Problem/Bridge/Testimonials + TrailJourney/HowItWorks replacements). Production deploy `dpl_FeUSAgS4z8BafESMSKcSvq2SDyjq` verified READY, anchor `/#trail` working.
  - [x] **PR 5 — FAQ + FinalCTA ✅ MERGED (PR #895, 2026-05-26)** — 2 new dark Deep Cosmos sections close out the landing page. `FAQ` + `FinalCTA`. Last v1 component `TrailCTA` deleted. Pricing intentionally DEFERRED to Phase 6 per IMPLEMENTATION_PLAN.md §0e. Security claim softened in FAQ Q6 (CMEK not yet deployed per §13.9). Production deploy `dpl_DKwXVyEhD8WmCm7Yu1BS8ykKLS61` verified READY. Landing page now fully Deep Cosmos canonical end-to-end.
  - [x] **PR 6 — Auth surfaces 🟡 SHIPPING this session** — auth chrome on `/signin`, `/register`, `/verify-email`, `/resend-verification` rebuilt to dark Deep Cosmos via new shared `components/auth/AuthShell.tsx` (exports `AuthShell` + `AuthLabel` + `AuthSubmit` + `AuthGhostButton` + `AuthDivider` + `AuthError` + `AuthInfo`). New `.cosmos-input` utility class for native dark form inputs (replaces shadcn `<Input>` on public auth — shadcn is internal-app light-theme vocabulary). **ZERO auth logic touched** — all hooks, AuthContext usage, MFA flows, OAuth callbacks, Phase 47 consent block + `captureConsent('SIGNUP'/'OAUTH_SIGNUP')` + `acceptedBundle` gating preserved verbatim. Skipped: `/forgot-password` (route doesn't exist — functional addition, separate PR), `/welcome` (already uses Deep Cosmos Header/Footer; body is its own design exploration), `/portal/login` (separate org-portal route).
- **🟢 Phase 48 COMPLETE after PR 6 merges.** All 6 PRs sequenced + shipped within a single 2026-05-26 session. Public-facing surface unified dark Deep Cosmos end-to-end (landing → auth).
- **Phase 48.7 (post-main-sequence cleanup — 2026-05-26):** Reza visual check after PR 6 merge spotted `/trail-method` + `/wealth-check` still on v1 ivory/amber treatment (Phase 48 §5 had queued these as "secondary surfaces, refresh in follow-up"). Migrated both pages to cosmos-* tokens. Bonus fix: `/trail-method` had drifted TRAIL stage hues (T amber · R orange · A emerald · I sky · L yellow — completely scrambled) — corrected to the canonical SSOT (T sky · R amber · A indigo · I emerald · L violet). Pure presentational token swap; no business logic touched.
- **Open follow-ups (queued, not blocking the Phase 48 closeout):**
  - `/forgot-password` route — currently linked from `/signin` but returns 404. Functional addition.
  - `/welcome` page body rebuild — friendlies landing, currently uses Phase 47 ivory body.
  - `/trail-check` — secondary public surface, may need same Deep Cosmos token migration as `/trail-method` + `/wealth-check`. Audit pending.
  - Pricing teaser — Stitch design at `.stitch/designs/pricing.{html,png}` ready for Phase 6.
  - Real social handles — Footer placeholder LinkedIn / Twitter icons removed in PR 2; restore when real Monitrax accounts exist.
- **Strategic invariants preserved across every PR:** wealth-builder ICP language (never lifestyle-spending categories), AFSL boundary copy (never "AI advisor"), TRAIL stage hue SSOT, restraint over richness, motion via canonical primitives, accessibility (WCAG AA + reduced-motion). Documented in `PHASE_48_PUBLIC_WEBSITE_REDESIGN.md` §6.
- **What's NOT touched:** dashboard, entities, tax engine, calc engines, master snapshot service, auth context logic, MFA, OAuth handlers, API routes, Prisma schema, CDR/Basiq code, TRAIL framework SSOT, internal navigation, tests, build pipeline. PURELY VISUAL redesign of public surfaces.
- **Risk:** Near-zero. PR 1 changes are additive (tokens) or route-level (redirect) or render-order (loading gate). Each subsequent PR is a discrete component swap with no business-logic impact. Rollback via `git revert <merge-sha>` is clean per PR.
- **Why-this-matters (multi-lens):** Designer/Visual lens — premium fintech requires first-frame premium quality; the current "Loading..." LCP undermines every other design effort. Brand strategy lens — unified identity across marketing → auth → dashboard is the structural fix for "different products after I clicked Start free" (the brand-identity drift Reza flagged). Behavioural-psychology lens — wealth-builder ICP reads "for households outgrowing budget apps" and feels seen; current site reads as overwhelm-relief therapy and the wealth-builder politely closes the tab. Growth lens — the `/wealth-check` make-or-break funnel (Phase 46) needs a coherent public-site context to convert; without it, the funnel is a single page on a fragmented site. Security/compliance lens — auth pages get re-skinned to match the brand WITHOUT touching any auth logic, MFA path, or session handling.

### 0f. Friendlies private beta — onboard early users for feedback (near-term)

- **Status:** 🟡 PLAYBOOK + LANDING PAGE SHIPPED 2026-05-15 (v2) — operational doc at `docs/marketing/gtm/FRIENDLIES_INVITE_PLAYBOOK.md` (v2 invite template + 2-wk follow-up + re-engage + testimonial + referral; subject-line variants; 8-stage lifecycle; Airtable view spec; weekly 15-min cadence) + the `/welcome` landing page at `app/welcome/page.tsx` (real Next.js page using the existing `bg-stone-950` + amber-accent marketing design system; reads `?ref=<firstname>` for hero personalisation; auth-aware CTA; 4-item FAQ accordion including AFSL boundary callout). **The two-layer invite pattern:** email = warm personal voice (plain text, short, one link); `/welcome` page = polished visual surface (design system, screenshots, FAQ). Reza-side: pick the 5–10 friendlies, then execute the playbook on his own pace.
- **Owner:** Reza (picks the friendlies, sends the invite, runs the feedback calls) + Claude (drafted the playbook + invite templates; updates the docs as the cohort moves)
- **Canonical doc:** `docs/marketing/gtm/FRIENDLIES_INVITE_PLAYBOOK.md` — the operational guide. **Read this before sending the first invite.**
- **Goal (Reza directive 2026-05-12):** *"at the end of this build I need to be able to send an email to friendlies with a workable app to start testing and providing feedback while I work on Basiq onboarding."*
- **The stack (no new tools):** Reza's personal Gmail (NOT Smartlead — wrong tool, wrong domain) · Airtable `Contacts` table with `friendly` tag + new `Friendly stage` single-select field · Airtable `Activities` table for touch logging · app's built-in `/dashboard` → "Send feedback" → triaged at `/admin/feedback` (Phase 33g). **Operating principle: at 5–10 friendlies, automation is the enemy.**
- **The 6-step playbook (summary — full detail in canonical doc):**
  1. Pick the 5–10 friendlies (spread across TRAIL stages; quality > headcount; avoid politely-supportive non-users)
  2. Add to Airtable Contacts (tag `friendly`, set `Friendly stage = Invited`)
  3. Send 1:1 personalised invite from Reza's Gmail (template in playbook §6 — personalise first line, plain text, no HTML)
  4. Log the send as an Airtable Activity row (so the Founder Daily Digest's CRM ACTIVITY section can surface friendly engagement)
  5. Manage replies + sign-ups + 2-week follow-up (template §7) + re-engage (template §8) — update `Friendly stage` as they progress
  6. Sequenced asks: week 4–6 testimonial (§9), week 8–12 referral (§10) — only if positive engagement, never on day one
- **Scope (revised):**
  - [ ] Verify the new-user flow end-to-end on production (`monitrax.com.au`): sign up → onboarding wizard → first TRAIL "Track" win → can add accounts/income/spending manually → Feedback affordance works (`/dashboard` "Send feedback" + the consumer Feedback page, Phase 33g). Fix anything broken before sending invite #1.
  - [x] **Draft the invitation email** + 4 follow-up templates (2-wk check-in, re-engage, testimonial ask, referral ask) — DONE 2026-05-15 in `FRIENDLIES_INVITE_PLAYBOOK.md`. **Upgraded to v2 same day** — short email + ONE link → `/welcome` landing page; subject-line variants; landing page does visual heavy lifting
  - [x] **Build the `/welcome` landing page** — `app/welcome/page.tsx` Next.js client page, existing marketing design system (`bg-stone-950` + amber accents + `Reveal` Framer Motion), `?ref=<firstname>` personalises hero, auth-aware CTA, 4-item FAQ accordion. DONE 2026-05-15
  - [x] **Document the lifecycle stages** (Invited → Signed up → Active → Feedback given → Testimonial received → Referred someone → Lapsed → Declined) — DONE 2026-05-15
  - [x] **Document the failure modes + escalation** (sign-up-no-engagement, negative feedback, AFSL boundary crossing, etc.) — DONE 2026-05-15 in playbook §11
  - [ ] **Add `Friendly stage` single-select field to Airtable `Contacts` table** + create `🤝 Friendlies pipeline` view (filter: Tags contains friendly; group by Friendly stage) — 30 sec manual setup in Airtable when Reza is ready to launch
  - [ ] Pick **5–10 friendlies**, deliberately spread across TRAIL stages (someone in debt, someone with property, someone just starting). Quality of feedback > headcount. (Q-GTM-7 — who, open.)
  - [ ] Send invite #1 — Reza-side, from his personal Gmail
  - [ ] Brief them on the in-app Feedback system as the primary feedback channel; Reza triages at `/admin/feedback`
  - [ ] When 0e + Basiq enforcement land, comp friendlies via `FeatureFlagOverride` / `UserSubscription.tier = PREMIUM`, **time-boxed 6 months**, cohort + review date logged in the playbook
- **AFSL boundary (Step 0.1 cross-reference):** if any friendly asks "should I invest in X / refinance through Y / claim deduction Z?" in the feedback call, the AFSL boundary applies — the answer is "speak to a licensed adviser" (playbook §11 + `REVIEW_SCOPE_AND_BOUNDARIES.md` §7). The friendlies are USING the app, not buying a Review, so it's a lower-risk surface than the paid Review service — but the discipline is the same.
- **Caveat:** "full access" pre-Basiq = everything *except* live bank feeds (Basiq not live — they use manual entry / CSV import, same as everyone now). Fine for feedback — the manual flow is what most users hit for months. CDR Part 13 still applies once Basiq is live, even to comped accounts — comped ≠ test environment, never seed prod with their data via a shortcut.
- **Sequence:** after GTM Step 1.1 (n8n) + 1.6 (Founder Daily Digest) — both DONE — friendlies can launch any time. Playbook in hand, infrastructure in place. The remaining gate is Reza picking the 5–10 names.
- **Why-this-matters (4-lens):** Behaviour psychologist — friendlies feel personally selected, not enrolled in a beta; 1:1 personal email is the right form. Designer — the system needs zero new UI; reuses the app's existing Feedback affordance + Airtable's existing schema. Architect — at 5–10 humans, automation is more cost than benefit; manual stack is correct (§0.4 restraint principle). Growth marketing — friendlies cohort is the lowest-cost, highest-signal-per-dollar growth investment pre-Phase-6, sequenced for feedback → testimonials → referrals.


### 0. Phase 14.6 — TRAIL-as-IA mobile + iPad navigation

- **Status:** 🟡 SHIPPING in this PR (`claude/improve-mobile-navigation-zKSTw`)
- **Started:** 2026-05-08
- **Owner:** Reza (direction) + Claude (code)
- **Last touched:** 2026-05-08
- **Why this matters:** The mobile UX before this PR forced 4 taps to reach a known sub-tab from the hamburger drawer (e.g. *My Accounts → Activity* required: tap hamburger → tap My Accounts → land on Balances → tap hamburger again → expand My Accounts → tap Activity). iPad portrait got the phone drawer instead of a tablet rail. Behavioural-psychology lens: financial stress already taxes 13 IQ points (Mani et al. 2013); the IA must give cognition back, not take more. Designer lens: hamburger is hidden navigation — the TRAIL journey is invisible at the moment users need orientation. This phase makes the framework the navigation: 5-tab bottom bar maps directly to Home + the four primary TRAIL stages.
- **Phases:**
  - [x] **Research + design synthesis** — confirmed pain point in code (`DashboardLayout.tsx:668-717` sidebar always links to section root, never directly to a sub-tab; sub-tabs use Radix Tabs state-only, no deep-linking; iPad <1024px gets phone drawer).
  - [x] **SSOT extracted** — `lib/navigation/trailNav.tsx` holds `trailNavItems`, `mobileTabBarItems`, `mobileMoreItems`, `TRAIL_STAGE_TONES`, `findActiveNavItem`, `findActiveMobileTab`, `isNavItemActive`. Both the desktop sidebar and the new mobile primitives read from this single file.
  - [x] **Mobile primitives** — `components/shell/MobileTabBar.tsx` (5-tab bottom nav, Apple-glass surface, TRAIL stage tones, ≥56px tap targets, `motion-safe:` honoured, iOS safe-area-inset). `components/shell/SectionTabsRow.tsx` (horizontal scrollable URL-routed pill row for sub-tabs on phones). `components/shell/MoreSheet.tsx` (bottom sheet for Safety Net / Household / Vault / Reports / Settings / Sign out, reuses §15.3 chrome).
  - [x] **DashboardLayout refactor** — sidebar visibility flipped from `lg:` (1024px) to `md:` (768px) → iPad portrait now gets the desktop rail. Hamburger drawer + `sidebarOpen` state deleted; mobile header now has avatar button → opens MoreSheet. MobileTabBar mounted as a fixed bottom element on phones; SectionTabsRow mounted at top of `<main>`.
  - [x] **Bottom-fixed components updated** — `CashQuickAddButton` raised to `bottom-24` on phones (`md:bottom-8` on desktop) so it clears the new tab bar; `BulkActionToolbar` raised to `bottom-[64px]` on phones so users can navigate away from bulk-edit mode without dismissing the toolbar first.
  - [x] **Doc-sync (CLAUDE.md §16):**
    - `docs/architecture/06_UI_UX_FOUNDATION.md` §12 rewritten as the canonical mobile + iPad navigation standard.
    - `docs/architecture/02_DESIGN_PRINCIPLES.md` §4.4 expanded into the three-tier responsiveness contract pointing at §12.
    - `docs/blueprint/TRAIL_FRAMEWORK.md` §5 — added "TRAIL on Mobile" subsection (the bottom bar IS the journey).
    - `docs/changelog/CHANGELOG_2026_05_08.md` — session entry appended.
  - [ ] **Section sub-tab URL migration** — many section pages still use Radix `<Tabs>` (state-based, not URL-routed). `<SectionTabsRow />` will only fully highlight active pills once these migrate to URL routes. Tracked here as a follow-up — touch-as-you-go rule: when working on a section page, prefer URL-routed sub-tabs.
- **Risk:** Low — purely additive on the routing side (no API change, no schema change, no CDR surface change). Visual surface migrated through existing tokens — theme unchanged. iPad-portrait sidebar is 256px on an 810px viewport (554px content area) — tight for very dense tables, mitigation queued (collapsed-rail variant) if testing surfaces issues.
- **Reversed-decision protection:** the hamburger-drawer pattern on phones is captured in `↩️ Reversed Decisions` so future sessions don't re-introduce it.
- **Not in scope (deferred):** native iOS/Android implementation of the same IA → Phase 15 (already blueprinted). Per-section URL-routing migration → touch-as-you-go.

### 0a. Phase 32-design-A2 — Org Portal full propagation + nav gating + admin polish

- **Status:** 🟡 In flight — PR (this one) follows A1 and propagates the shell layer to every Org Portal page that had a `PracticeHeader` or no hero, gates the broken sidebar nav links behind their feature flags, and sweeps Admin Portal blue focus rings → emerald.
- **Started:** 2026-05-06
- **Owner:** Reza + Claude
- **Last touched:** 2026-05-06
- **Phases (A1 — already shipped):**
  - [x] Extract shared shell layer at `components/shell/` (motion + GlassHero + MetricTile + practiceGlyphs)
  - [x] Org Portal sidebar palette → consumer-app gradient/sky-indigo active state
  - [x] Org Portal mobile top bar → glass + brand pill
  - [x] `/portal/dashboard` rebuilt against `GlassHero` + 3 `MetricTile`
  - [x] Admin Portal eyebrow tracking `0.08em → 0.18em`
- **Phases (A2 — this PR):**
  - [x] New `PortalPageHero` wrapper at `components/shell/` for non-dashboard portal surfaces
  - [x] `/portal/clients` — added hero (sky atmosphere, role badge in actions slot)
  - [x] `/portal/team` — added hero (emerald, role badge)
  - [x] `/portal/integrations` — added hero (violet)
  - [x] `/portal/conversations` — replaced PracticeHeader (violet)
  - [x] `/portal/requests` — replaced PracticeHeader (rose)
  - [x] `/portal/marketplace/listing` — replaced PracticeHeader (emerald, status badge in actions)
  - [x] `/portal/billing` — replaced PracticeHeader (amber)
  - [x] Sidebar nav gating — Tasks / API Keys / Reports gated on their existing feature flags; Settings link gated on `organizationManagement`. Defaults are `false` so all four links disappear in prod until those phases ship — kills 4 trust-leaking 404s.
  - [x] Auth pages polish — `/portal/signin`, `/portal/register`, `/portal/invite` blue focus rings → emerald
  - [x] Admin Portal blue → emerald focus ring sweep (`AdminForm.tsx`, `HelpTip.tsx`)
- **Phases (A3 — this PR, follows Reza directive 2026-05-06 "complete all deferred"):**
  - [x] Migrate 17 consumer wealth + dashboard + cfo + marketing files to consume `components/shell/motion` instead of local `appleEase` / `springy` definitions. Visual surface unchanged — pure dedupe. Files touched: `wealthGlyphs.tsx`, `AssetsHero.tsx`, `AssetTile.tsx`, `AdviceHero.tsx`, `AdviceChatThread.tsx`, `AIAdviceSection.tsx`, `AdviceRecommendationCard.tsx`, `PropertyTile.tsx`, `PropertiesHero.tsx`, `InvestmentsHero.tsx`, `HoldingTile.tsx`, `InvestmentAccountTile.tsx`, `AssetsHero.tsx`, `TrailStageIndicator.tsx`, `marketing/animations.tsx`, `marketing/TrailHero.tsx`, `documents/DocumentFolderView.tsx`, `app/dashboard/plan/page.tsx`. The bespoke hero/tile component contracts stay specialized — only the motion vocabulary deduplicated.
  - [x] `/portal/feedback` — added compact PortalPageHero (emerald) above the master-detail layout. Replaces the old custom header.
- **Phases (A4 — this PR, follows Reza directive 2026-05-06 "PR merged, continue"):**
  - [x] `/portal/clients/[id]/view` — replaced plain `<header>` with `<GlassHero atmosphere="sky">` + back-to-list breadcrumb above. Last unaligned portal surface — pitch-critical because it's the Step 2/3 drill-in shown to prospects.
  - [x] Tab toggle (Structure / Money Flow / Dashboard) repainted from `bg-slate-900` active fill to consumer-app sky-to-indigo gradient pill with `ring-sky-500/30` (matches `PortalSidebar` active state vocabulary). Emerald focus rings throughout.
  - [x] `AdviserOverlay` — Section helper updated (border slate-200/70, glass surface `bg-white/80 backdrop-blur-sm`, soft layered shadow, eyebrow tracking `0.08em → 0.18em`, tabular-nums on counts). Mobile expand button gets emerald focus ring.
- **Workstream complete:** Phase 32-design A1 → A4 has now aligned every visible Org Portal surface to the consumer Phase 39 vocabulary, gated 4 broken nav links, deduped `appleEase` to a single SSOT, and polished both Admin Portal and the drill-in adviser overlay.
- **Why this matters:** Drill-in is the highest-leverage Org Portal surface during a pitch — when an adviser shows a prospect "here's how I see Sarah's full picture," the page they land on must read as premium fintech. With this PR, every Org Portal page now opens with the same glass-hero vocabulary. The visual contract holds end-to-end.

### 0. Phase 32B — B2B2C Practice surface (adviser / broker / accountant lighthouse)

- **Status:** 🟡 In flight — PR1 of ~3 ships schema + design primitives + demo dataset; PR2 wires Practice dashboard + repaints sidebar; PR3 wires drill-in (canonical consumer dashboard with adviser overlay) + alert engine v1 against real snapshot deltas.
- **Started:** 2026-05-04
- **Owner:** Reza (vision + lighthouse adviser intro) + Claude (code)
- **Last touched:** 2026-05-04 (PR1)
- **Why this matters:** Per Reza brief 2026-05-04, the B2B2C model is the largest single revenue + value lever Monitrax has — Australian wealth flows through ~17k mortgage brokers, ~30k accounting firms, ~16k financial advisers, each with 80–250 clients, all currently bottlenecked on per-client overhead because no platform gives them a *cross-client* live financial view. Existing `/portal/*` is a consent+invitation harness only; the dashboard is "Coming Soon" stub, no alert stream, no role-flavoured columns, drill-in is a separate component instead of reusing the consumer dashboard. This workstream builds the actual moat: a Practice surface (book index + alert stream + role-flavoured columns) that turns the professional from reactive to proactive. Lighthouse adviser pitch demo gates PR1+2 completion.

**Hard constraints (non-negotiable):**
- **One canonical engine, two voices** — same `getMasterFinancialSnapshot()` and same Gemini AI advisor for D2C and org-attached users; tone + actions + disclaimer flip via a `voiceContext` parameter, NEVER fork the engine. (Documented in Open Questions Q-PRA-1 below pending Reza decision.)
- **AFSL / credit / TPB boundary enforced architecturally** — alert text NEVER recommends a specific product or action. The platform surfaces the *trigger*; the professional generates the *recommendation*. Reviewers must reject any future copy that crosses that line.
- **Profession forced at registration** — no MULTI option. Multi-discipline firms register two Orgs under one billing account.
- **Drill-in renders the canonical consumer dashboard** with an adviser overlay docked — not a separate `ClientDetail.tsx`. Every consumer dashboard improvement automatically benefits the professional view. Existing `components/portal/clients/ClientDetail.tsx` retired in PR3.
- **CDR consent and professional consent are NEVER collapsed** — three layers (CDR consent, professional consent, per-view access log). Scope filter applied at the service layer, not the UI. Every professional read writes a `ClientAccessLog` row.
- **Consumer experience is preserved verbatim** — no `mode: 'professional'` conditional rendering inside `app/dashboard/*`. The professional reaches the canonical dashboard via `/portal/clients/{id}/view` with a `viewerContext` prop tree.

**Phases (this PR ships 1–4 + 8):**
- [x] 1 — **Schema.** `Organization.profession` field added (Prisma + matching migration `20260504120000_add_organisation_profession/migration.sql`; reuses existing `OrganizationType` enum; backfills from `OrganizationPortalSettings.organizationType` where present; default `FINANCIAL_ADVISOR`). Additive — no `DROP`, §12.11 N/A. SSOT note: `OrganizationPortalSettings.organizationType` becomes a shadow; dedup queued under 🗑️ Dead Code #13.
- [x] 2 — **Practice design primitives.** `PracticeGlassCard` (Apple-glass tile — 22px radius, ring border, hover lift, optional severity accent strip), `TrailStageChip` (stage-coloured chip; matches consumer TRAIL banner palette so adviser + client see the same visual cue), `PracticeKpiStrip` (4-up hero KPI cards with delta), `PracticeAlertStream` (severity-sorted call-sheet — critical → opportunity → milestone), `PracticeClientBookTable` (cross-client table with profession-flavoured columns), `PracticeHeader` (greeting + practice-mode pill + demo-only profession switcher).
- [x] 3 — **Profession config registry.** `lib/portal/practice/professionConfig.ts` — adviser/broker/accountant variants for alert library + book columns + compliance footnote. Tax agent + bookkeeper + OTHER fall back to accountant/adviser at v1.
- [x] 4 — **Lighthouse demo dataset.** `lib/portal/practice/lighthouseDataset.ts` — 5 fictitious AU property-investor clients (Sarah K / David M / Emma L / Noah P / Olivia N) spanning all 5 TRAIL stages + 7 alerts spanning all v1 trigger kinds (TRAIL_ADVANCED, HEALTH_DROP, CASHFLOW_NEGATIVE, EMERGENCY_FUND_LOW, LVR_REFINANCE_WINDOW, BAS_DUE, TAX_POSITION_CHANGED).
- [ ] 5 — **DEFERRED to PR2.** Replace `/portal/dashboard` "Coming Soon" stub with Practice view (assemble Header + KpiStrip + AlertStream + ClientBookTable). Demo-mode profession switcher in chrome (production hides it; production reads `org.profession`).
- [ ] 6 — **DEFERRED to PR2.** Repaint portal shell sidebar (`PortalSidebar.tsx` + `PortalLayoutClient.tsx`) from slate-900 admin look to brand tokens (`brand-primary` deep navy + `brand-secondary` emerald + warm-ivory background). Match consumer design language.
- [ ] 7 — **DEFERRED to PR3.** Drill-in: render canonical consumer dashboard at `/portal/clients/{id}/view` with adviser overlay docked right (desktop) / bottom-sheet (mobile). Wire `viewerContext` through `masterFinancialService.ts` — service rejects calls without `seatId` + `clientUserId` + `accessScopes`. Scope filter at service layer (not UI). Retire `components/portal/clients/ClientDetail.tsx`.
- [x] 8 — **Dead-code removal.** `lib/portal/auth.ts` deleted (zero callers anywhere; flagged 2026-04-30 audit). `lib/portal/index.ts` comment block updated to record the removal + the rationale. Closes tech-debt #8.
- [ ] 9 — **DEFERRED to PR3.** Alert engine v1 — replace `LIGHTHOUSE_ALERTS` mock with real triggers reading from snapshot deltas. Five triggers: TRAIL stage advanced, Health score dropped >10 in 30d, Cashflow turned negative, LVR crossed below 80%, Emergency fund dropped below 1mo. Daily Cloud Scheduler job; in-app delivery only at v1, email digest at v2.
- [x] 10 — **SHIPPED (2026-05-09).** Profession-aware scope presets. SSOT at `lib/portal/scopePresets.ts` (`LENDING`: LOANS+PROPERTIES+TRANSACTIONS · `TAX`: TAX+TRANSACTIONS+DOCUMENTS · `ADVISORY`: FULL minus DOCUMENTS, enumerated). Quick-pick chips wired into `components/portal/team/InviteModal.tsx` (adviser-side scope-picking surface — the spec originally named `ConsentRequest.tsx`, but that's the consumer-side *approve* UI; presets belong on the adviser's *request* UI). Active chip de-highlights automatically when the selection deviates from a preset (`matchScopePreset` on each render). Adviser fine-tunes via the existing checkboxes afterward. ~½ day delivered.

**Risk:** Low for PR1. The new Practice components are isolated under `components/portal/practice/` and `lib/portal/practice/` and not yet imported by any production route — the existing `/portal/dashboard` stub still renders until PR2. The schema migration is purely additive (`ADD COLUMN ... DEFAULT` + a backfill `UPDATE` from a column that already exists). Dead-code removal is a true zero-caller delete. Build green; `tsc --noEmit` clean for new files.

**Blocking:** None. ~~Q-PRA-1~~ resolved 2026-05-04 (single-voice + marketplace + Ask-a-Professional). PR2 is unblocked and is next session's starting point.

**Closes tech-debt:** #8 (`lib/portal/auth.ts` deletion).

**Opens tech-debt:** #13 (OrganizationType duplication — see 🗑️ Dead Code below).

---

### 1. Phase 40 — My Guide AI Financial Advice (CFO page redesign)

- **Status:** 🟡 In flight — PR 1 (this PR) ships scenario engine + AI advisor service + API endpoints + page redesign + Tax fold-in
- **Started:** 2026-05-02
- **Owner:** Reza (vision/sign-off) + Claude (code)
- **Last touched:** 2026-05-02
- **Why this matters:** The current `/dashboard/cfo` "Prioritised Actions" tabs are deterministic rule-engine output — a long list of disconnected nudges with no narrative, no scenario projection, no follow-up affordance. Reza's brief: replace it with a real **AI-generated financial advice section** that diagnoses the user's situation, prioritises a way forward, and lets the user explore "what-if" (e.g. *"this property is way negative — what happens to my numbers if I sell it?"*) with concrete deterministic projections. Highest-leverage TRAIL Live-stage surface — closes the gap between Monitrax-the-tracker and Monitrax-the-CFO.

**Hard constraints (non-negotiable):**
- **Zero hallucinated numbers.** AI may *narrate* numbers, never *invent* them. Every figure shown to the user comes from `getMasterFinancialSnapshot()` (canonical) or the deterministic scenario engine. Gemini calls scenarios via function/tool calling — server runs the math, AI narrates the deltas.
- **CDR sanitisation before AI call.** No account numbers, BSBs, transaction-level descriptions, or payee names sent to Gemini. Aggregates only. Per CLAUDE.md §13.3.
- **Existing rule engine retained as deterministic safety net.** `lib/cfo/actionEngine.ts` keeps running — its output is fed into the AI as ground truth and used as fallback if Gemini fails.
- **No calc engine duplication.** Scenarios compose existing primitives where possible; new pure-function scenarios live under `lib/cfo/scenarios/` with single responsibility per file.
- **24h cache** on advice generation (Pro model is expensive — ~AU$0.05–0.15 per advice doc — and snapshots don't change minute-to-minute).
- **Design language** matches Phase 39 (Wealth) + Phase 37 (Budget) — glassmorphic 28px cards, sky/indigo Stage I atmosphere, framer-motion v12 appleEase, full `prefers-reduced-motion`.

**Phases (this PR ships 1–6 + 8):**
- [x] 1 — **Scenario engine.** `lib/cfo/scenarios/` — pure functions: `sellProperty`, `payDownLoan`, `refinanceLoan`, `redirectToOffset`, `cutSpendCategory`, `addInvestment`. Each takes `{ snapshot, params }` and returns `ScenarioResult` (cashflow delta, net worth delta, tax delta, health score delta, narrative-friendly explanation). Composes existing `lib/utils/calculations.ts` primitives.
- [x] 2 — **AI advisor service.** `lib/cfo/aiAdvisor.ts` — orchestrator: fetch snapshot, run rule engine, sanitise into AI-safe context document, call Gemini Pro with strict system prompt + tool definitions, validate every quoted number against snapshot, return structured `AIAdviceDocument`. **Stability rule** added 2026-05-02 per Reza: while a non-expired advice doc exists for the user, return it as-is regardless of fingerprint drift — avoids the surface flickering when transactions get imported. The advice changes only on (a) 24h TTL expiry or (b) explicit user "Refresh" click.
- [x] 3 — **API endpoints.** `POST /api/cfo/advice` (generate or return cached, 24h TTL), `POST /api/cfo/advice/chat` (follow-up Q&A scoped to the advice doc, persisted), `POST /api/cfo/scenarios/run` (run a deterministic scenario, return result with delta projections).
- [x] 4 — **Persistence (Prisma).** New models `AIAdviceDocument` + `AIAdviceChatMessage` with matching migration `prisma/migrations/20260502120000_add_ai_advice_phase_40/migration.sql` (additive — CREATE TABLE only, §12.11 N/A).
- [x] 5 — **UI components.** `components/cfo/AdviceHero.tsx` (glassmorphic stage-aware atmosphere — palette shifts with TRAIL stage, sky/indigo for Invest, sunrise amber for Live, etc.), `AdviceRecommendationCard.tsx` (per-recommendation card with Run-Scenario button + Ask-Follow-Up affordance + inline scenario projection), `AdviceChatThread.tsx` (slide-in side drawer for Q&A), `AIAdviceSection.tsx` (orchestrator owning the data lifecycle).
- [x] 6 — **Page redesign.** `/dashboard/cfo` re-wired: `<AIAdviceSection />` inserted at the TOP as the highlight surface; existing Health Hero + Quick Stats + Insight Tiles + Risk Radar all preserved BELOW as supporting context (per Reza's "keep existing data, this section is amended actions" direction). Old "Prioritised Actions" tabs removed — their findings now feed into the AI advisor as ground truth and surface as recommendations.
- [ ] 7 — **DEFERRED.** Tax fold-in (closes Up Next #9). Defer to a follow-up PR per Phase 37 precedent ("Standalone routes preserved" — `/dashboard/income`, `/dashboard/expenses`, `/dashboard/budget-analysis` are unchanged, full CRUD intact). The cfo page already surfaces the headline tax data via the existing `TaxInsights` tile; the AI advisor naturally references tax data and recommends tax actions. The deeper `/dashboard/tax` route stays alive as a deep-link destination. Re-queued under Up Next #9 with a sharper trigger: "after Phase 40 stabilises, evaluate whether the AI advice surface has made the standalone tax route redundant."
- [x] 8 — **Audit logging.** New action codes `AI_ADVICE_GENERATED`, `AI_ADVICE_CHAT`, `CFO_SCENARIO_RUN`. CDR-safe metadata only via `sanitizeCdrMetadata()` — no balances, no transaction text, no payee names. **⚠ Correction 2026-06-11:** the route code shipped but the enum values were NEVER added to `schema.prisma`, so every one of these audit writes silently failed at the DB layer for ~6 weeks (auditLog typed action as `string` + `as any` — no compile check). Fixed in the 2026-06-11 enum-backfill PR (migration `20260614000000_…`, + the auditLog service now uses the Prisma enum type so this class of drift is a compile error). See Recently Completed 2026-06-11.

**Risk:** Medium. New surface, new persistence (Prisma migration). Mitigations: (a) feature is additive — old action engine keeps running and feeds AI as ground truth; (b) advice generation falls back to deterministic action list if Gemini fails; (c) 24h cache caps Gemini cost; (d) scenario engine is pure functions, fully testable; (e) all CDR sanitisation centralised through one helper before the Gemini call.

**Blocking:** None.

**Supersedes:** Up Next #9 (My Guide simplification + Tax → Actions consolidation). The Tax fold-in is delivered as Phase 40 Phase 7 instead of as a separate session — bundling avoids re-touching the same surface twice. Closes the workstream queued there.

**Closes tech-debt:** #11 (`lib/cfo/trailStage.ts`) — Phase 17 placeholder is now wired up and used by the AI advisor for stage-aware prompt context.

---

### 1. Step 1a — DB authentication via Workload Identity Federation (WIF)

- **Status:** 🟢 **Phase 9 COMPLETE 2026-05-01** — Production now serving 100% of traffic via WIF + Cloud SQL Connector + IAM DB auth. Phase 10 (remove `0.0.0.0/0`) queued for +24h after stability is observed; Phase 11 (remove fallback path) queued for +30d.
- **Started:** 2026-04-30
- **Owner:** Reza (GCP/Vercel ops) + Claude (code)
- **Last touched:** 2026-05-01 (late night) — Cold-start hardening: `getOrInitConnectorClient()` now `.catch`-clears `globalForPrisma.prismaInitPromise` on rejection so a transient init failure (SQL Admin API jitter / STS throttle on cold start) doesn't wedge every subsequent query on the warm function instance for 5-15 min. Surfaced after Phase 9 cutover as "first navigation to a page shows empty data; navigating away and back works" because Vercel routed the retry to a different (healthy) instance. Documented in runbook §3.K. Earlier today: Phase 9 cutover landed via PRs #563 + #564; full doc sync (CLAUDE.md §13.6, CDR matrix §3.2, CDR WIF evidence §7, infrastructure §5.5, MASTER_BLUEPRINT identity stack, Cloud SQL ops, migration appendix, IMPLEMENTATION_PLAN, CHANGELOG_2026_05_01).
- **Why this matters:** Closed CDR `§3.2` DB-tier compliance gap (no long-lived password in env vars; full GCP audit trail under the SA principal; instance auth surface restricted to authenticated IAM identities). Implements CLAUDE.md `§13.6` (production DB accessible only via GCP IAM). Eliminates the long-lived password-in-URL fragility that broke prod on 2026-04-30.

**Phases:**
- [x] 1 — Service account `vercel-monitrax-db@monitrax-479700.iam.gserviceaccount.com` with `Cloud SQL Client` + `Cloud SQL Instance User` roles
- [x] 2 — Cloud SQL IAM user added to `monitrax-db-prod`
- [x] 3 — Postgres grants on `public` schema (CONNECT, USAGE, ALL on tables/sequences, default privileges)
- [x] 4 — Workload Identity Pool `vercel-pool` created in GCP
- [x] 5 — OIDC provider `vercel-oidc` configured with attribute condition `project_id == 'prj_UYQF...'`
- [x] 6 — WIF principal bound to service account (`roles/iam.workloadIdentityUser`)
- [x] 7 — Vercel env vars added (`GCP_WORKLOAD_IDENTITY_PROVIDER`, `GCP_SERVICE_ACCOUNT_EMAIL`, `CLOUD_SQL_CONNECTION_NAME`, `USE_CLOUD_SQL_CONNECTOR=false`); Vercel OIDC enabled at project level
- [x] 8 — **Code PR shipped** — `lib/db.ts` refactor with feature-flag branch + `prisma/schema.prisma` `previewFeatures = ["driverAdapters"]` + new packages (`@google-cloud/cloud-sql-connector`, `@prisma/adapter-pg@^5.22.0`, `pg`) + 6 doc updates + 3 new docs (see CHANGELOG_2026_04_30.md). Build green; default flag `false` so merge is zero-risk.
- [x] 9 — **COMPLETE 2026-05-01.** Production cutover to `USE_CLOUD_SQL_CONNECTOR=true` with full IAM DB auth chain working end-to-end. The cutover surfaced four issues, all resolved within the day:
  1. **OIDC token retrieval (PR #563)** — Vercel delivers the OIDC token as the per-request `x-vercel-oidc-token` HTTP header, NOT as `process.env.VERCEL_OIDC_TOKEN` (that env var only exists at build time / in `vercel env pull` output). Switched to `getVercelOidcToken()` from `@vercel/oidc` and added a Proxy-based lazy init so the auth chain runs inside a request context instead of at module load.
  2. **mTLS handshake / TLS alert 42 (PR #564 + GCP-side fix)** — Server rejected the ephemeral client cert at handshake. Root cause: the SA was never registered as a Cloud IAM **database** user on the instance (Phase 1 had been ✅-ticked but the per-instance `gcloud sql users create ... --type=CLOUD_IAM_SERVICE_ACCOUNT` step was missed). Fixed by creating the user + running `public`-schema GRANTs from Cloud SQL Studio as `monitrax_user`. Documented in `04_WIF_TROUBLESHOOTING.md` §3.G.
  3. **SCRAM no-password / SASL error (PR #564)** — The Cloud SQL Connector wraps the TLS socket but does NOT inject a Postgres-level password. In IAM-auth mode the password pg must send is the SA's OAuth access token; the application has to supply it. Added `password: async () => authClient.getAccessToken()` callback to `pg.Pool` in `buildConnectorPrisma()` so pg fetches a fresh token per connection. Documented in §3.H.
  4. **28P01 / trailing whitespace on `CLOUD_SQL_DB_USER` (PR #564)** — Vercel env var had a trailing space from a copy-paste. Postgres treats `...iam` and `...iam ` as different identifiers. Vercel env var corrected; defensive `.trim()` added on all WIF env-var reads in `lib/db.ts`. Documented in §3.J.
  Pro plan + region pinning to `syd1` confirmed. Separate observation: `/api/health` still routes via Edge → `iad1` despite `vercel.json` `regions: ["syd1"]` (non-blocking; revisit during Phase 10).
- [x] 10 — **CLOSED 2026-05-01 via "Option D" — IAM compensating control documented; `0.0.0.0/0` retained intentionally.** Re-evaluation discovered that removing `0.0.0.0/0` is not as simple as the original plan implied: Cloud SQL Connector with public IP still requires the source IP to be in authorized networks (the connector provides cert-based mTLS *over* the TCP layer the ACL gate-keeps; it does not bypass the ACL). Vercel does not publish a stable egress IP range to whitelist (per Vercel docs); the only stable path is the paid Vercel Static IP add-on (~AU$30-50/mo). Decision: at zero users, the marginal security improvement vs the operational/financial cost is not justified. Without a static credential, the network ACL is no longer protecting anything an attacker could exploit — IAM auth is the controlling boundary. Documented in `CDR_WIF_AUTHENTICATION_EVIDENCE.md` §8 with the explicit migration path to Vercel Static IP + restricted networks when triggered (first paying user / pre-Basiq submission / anomalous connection attempts). Optional one-time annotation: rename the authorized-network entry label to `public-iam-protected-cdr-3.2-doc` so the GCP console makes the intent visible to future operators / auditors.
- [ ] 11 — **Queued for +30d (target ≥ 2026-05-31).** Drop the legacy `buildStandardPrisma()` branch from `lib/db.ts`; remove `DATABASE_URL` from the Vercel **runtime** env scope (keep in **build** env scope so `prisma migrate deploy` keeps working); disable / drop the `monitrax_user` Postgres user.

**Risk:** Low. Feature flag default is `false`, so merging the PR is zero-risk — production keeps using `DATABASE_URL` until the flag is flipped. `DATABASE_URL` stays as fallback through Phase 9.

**Blocking:** None.

**Docs to update on completion (Phase 8):**
- `docs/compliance/CDR_BASIQ_COMPLIANCE_MATRIX.md` §3.2 PARTIAL → DONE; mark P1 Auth Proxy item SUPERSEDED
- `docs/architecture/09_INFRASTRUCTURE_AND_DEPLOYMENT.md` §5 — describe WIF + Connector flow
- `docs/blueprint/MASTER_BLUEPRINT.md` line 116 — correct stale "Render" reference; add WIF to identity stack
- `docs/migration/MIGRATION_RENDER_TO_GCP_STEPS.md` — mark legacy; add WIF appendix
- `docs/operational/database/01_CLOUD_SQL_OPERATIONS.md` — deprecate password examples
- `CLAUDE.md` §13.6 — mark IAM-based DB access as implemented
- `prisma/schema.prisma` — add doc comment on `datasource` block

**New docs to create (Phase 8):**
- `docs/operational/security/04_WIF_TROUBLESHOOTING.md` — runbook for token failures, OIDC issuer changes, binding rotation
- `docs/compliance/CDR_WIF_AUTHENTICATION_EVIDENCE.md` — evidence pack for Basiq accreditation (architecture diagram, token flow)
- `docs/changelog/CHANGELOG_2026_04_30.md` — full session entry

---

### 2. Phase 36 — Accounts/Loans page retirement

- **Status:** 🟡 Phase 1c shipped, Phase 2 queued for after WIF lands
- **Started:** 2026-04-29
- **Owner:** Reza (testing) + Claude (code)
- **Last touched:** 2026-04-30 — PR #558 (transient 401 retry on Balances) merged

**Sub-phases:**
- [x] 1 — Inline `AccountDetailDialog` on Balances (PR #552)
- [x] 1b — Inline create/edit form dialogs for accounts and loans (PRs #553/554)
- [x] 1c — `Connect Bank` toolbar button + 2-tile source picker (Import / Manual) for `+ Account` and `+ Loan` (PR #555)
- [x] 1d — Account delete in detail dialog + retry on intermittent 401/5xx (PRs #556, #558)
- [x] 2.0 — **Repoint non-Basiq references** (Home TRAIL banner, `LinkedDataPanel`, `ModuleHealthBlock`, CFO `router.push`, `learnMoreUrl` in cashflow intelligence) from `/dashboard/accounts` → `/dashboard/balances`. Basiq `?action=connect-basiq` / `?action=add` hrefs deliberately untouched (still depend on legacy page; tracked under 2b). Bundled with the home TRAIL banner redesign.
- [x] 2a — Inline `LoanDetailDialog` on Balances (replaces PR #550's `?focus=` redirect). Shipped PR #601 (2026-05-02).
- [x] 2b — Connect Bank toolbar action on Balances + `?action=` href flips (`BasiqHeroCard`, `DashboardEmptyStateGrid`, `SetupNextActionPanel` all repointed to `/dashboard/balances?action=...`). Shipped PR #742.
- [x] 2c — `Import` toolbar button on `/dashboard/balances` + `?action=import` deep link + `AccountDetailDialog` import buttons live (`onImportClick` → close detail + open `TransactionImportDialog`). Shipped 2026-05-09 (Phase 36 Phase 2c PR). No standalone `TransactionReviewPanel` migration was needed — it already lives inside `ImportWizard` inside `TransactionImportDialog`.
- [x] 2d — `/dashboard/accounts` + `/dashboard/loans` bare list pages redirect to `/dashboard/balances`. Shipped PR #742.
- [x] 2e — Sidebar cleanup — audit found no legacy entries; `matchRoutes` aliases retained for old-URL traffic. PR #742.

**Phase 36 is COMPLETE** — all sub-phases shipped (2026-05-09).

**Risk:** Medium. Phase 2 touches Basiq sync UI which has more state and side effects than the create/edit dialogs.

**Blocking:** Awaiting user testing of Phase 1c/1d in production. **Unblocked 2026-05-01** — WIF Phase 9 cutover is complete and Production DB auth is stable, so Phase 36 Phase 2 can resume next.

**Why this was paused:** WIF (workstream 1) was the higher-priority hardening. With Phase 9 now landed, we can ship the remaining UX migration into a stable, IAM-authenticated DB.

---

### 3. Phase 37 — My Budget IA simplification + premium redesign

- **Status:** 🟢 **PRs 1-5 SHIPPED 2026-05-01** — final 3-tab IA active; new `/dashboard/plan` hub live; Cashflow + Debt Freedom heroes uplifted to TRAIL banner v3 design language. PR 6 (Tax under My Guide) shipped as part of PR 1's sidebar move; PR 7 (telemetry + soft-retire of legacy routes) deferred — see Up Next.
- **Started:** 2026-05-01
- **Owner:** Reza (vision/sign-off) + Claude (code)
- **Last touched:** 2026-05-01 — Mega-PR shipped on `claude/phase-37-full-uplift` (single PR per Reza's request) bundling PRs 1-5 + 6. **Hard constraint honoured: zero calc engines touched, zero APIs touched, zero data sources duplicated.** Every number on every screen sourced from its existing canonical engine.
- **Why this matters:** Eliminated 6-tab decision paralysis in REDUCE stage; default landing now answers "am I OK this month?" (`/cashflow`) instead of opening a "Generate Budget Analysis" config CTA; tax-optimisation strategically aligned to LIVE stage under My Guide; warm-sentence behavioural framing applied to Cashflow + Debt Freedom heroes; new `/dashboard/plan` hub gives users a single, glanceable financial intent surface.

**Phases:**
- [x] 1 — **Sidebar IA.** `components/DashboardLayout.tsx`. My Budget = Cashflow / My Plan / Debt Freedom (3 tabs final). Default landing → `/cashflow`. Tax relocated to My Guide alongside Actions + Health. Existing routes preserved (deep-link compatibility).
- [x] 2 — **Cashflow uplift.** Replaced plain title block with `CashflowHero` glassmorphic banner (rounded-[28px], backdrop-blur-xl, animated mesh gradient that shifts colour with surplus/shortfall sign, framer-motion fade-up sequenced with appleEase 1.4s, full prefers-reduced-motion). Warm-sentence headline ("You're $312 ahead this month — keep going" / "You're $140 short this month — let's find it together"). Same `/api/cashflow/intelligence` endpoint — zero calc changes. Refresh button restyled as glass pill.
- [x] 3 — **New `/dashboard/plan` hub.** Apple-style segmented control (Money In / Money Out / Your Budget) with sliding selection pill (`layoutId` shared between segments) and AnimatePresence cross-fades (blur-in/blur-out, 0.45s). Hero glass card with morphing "$X in · $Y out · $Z surplus/shortfall" sentence. Each section shows a CONDENSED summary (top 5 income sources, top 6 spending categories with animated horizontal bars, 3 budget cards) and a "Manage all →" deep-link to the existing detail page. All data from existing APIs (`/api/cashflow/intelligence`, `/api/income`, `/api/expenses`, `/api/budget-analysis/latest`) called in parallel via `Promise.allSettled`.
- [x] 4 — **Standalone routes preserved.** `/dashboard/income`, `/dashboard/expenses`, `/dashboard/budget-analysis` are unchanged — full CRUD intact. They are the destinations of the "Manage all →" links from `/dashboard/plan`. Zero broken bookmarks. (NB: the original PR 4 plan called for extracting Income/Spending/Budget panels into shared components and embedding them inline. The pragmatic delivery uses condensed-summary + deep-link instead, which preserves all 5,000+ LOC of existing CRUD logic untouched. The IA outcome — single My Plan tab, tab-collapse from 6 → 3 — is identical.)
- [x] 5 — **Debt Freedom uplift.** Replaced PageHeader with `DebtFreedomHero` glassmorphic banner. Aspirational headline: "Debt-free by Oct 2031" with subtle 6s shimmer sweep over the date (collapses to static under reduced motion); fallback warm sentence when no plan computed yet. Two stat pills: interest saved (emerald) + months saved (violet). Numbers sourced from existing `aiAnalysis.projections` + `planResult` state — zero new calculations.
- [x] 6 — **Tax tab under My Guide.** Sidebar-level move shipped as part of PR 1; the existing `/dashboard/tax` route is reachable from My Guide → Tab `Tax`. Page itself unchanged. Future My Guide simplification session (Up Next #9) will fold non-duplicated tax data into the Actions surface and retire the standalone Tax route.
- [ ] 7 — **Telemetry + soft-retire window** for legacy routes (`/dashboard/income`, `/dashboard/expenses`, `/dashboard/budget-analysis`). DEFERRED — see Up Next #10. Routes stay alive as deep-link destinations from `/dashboard/plan`'s "Manage all →" links; soft-retire only makes sense if/when those links also disappear.

**Risk:** Low. Routes preserved — zero broken bookmarks, deep links, marketing URLs, browser history. No `prisma/schema.prisma` change. No destructive Prisma writes. No calc engine modifications. TypeScript clean (`npx tsc --noEmit` passes; only pre-existing tsconfig deprecation warning unrelated to this PR).

**Blocking:** None.

**Follow-up captured for the queued My Guide simplification session (NOT in scope of Phase 37):**
- **Tax → Actions de-duplication** — Reza's observation 2026-05-01 (with screenshots): the My Guide → Actions (`/dashboard/cfo`) page already surfaces a "Tax Position" card with *Estimated Position · Days Until EOFY · Effective Tax Rate · Total Deductions · Property Allocation · Neg-Gearing Benefit · Potential Missed Deductions tags*. The full `/dashboard/tax` page adds *tax-rate breakdown bars · full tax-calculation table (Tax on Income · Medicare Levy · Gross Tax · Net Tax Payable) · per-source income detail · Deductions sub-tab · Super sub-tab · full Recommendations list*. **Decision:** in the upcoming My Guide review session, compare both surfaces, fold non-duplicated tax data into Actions, then retire `/dashboard/tax` entirely. **Hard rule:** no calc duplication — Tax engine stays canonical; Actions just renders its outputs (CLAUDE.md §12.2).

---

### 4. Phase 38 — My Vault (document management uplift)

- **Status:** 🟢 **Phase 38 COMPLETE** — PRs 1 + 2 + 3 + 2.5 + 4 all shipped. My Vault IA · Smart Inbox interactive · Send-to-accountant Share Pass · folder view + toolbar redesign · universal upload migration · tax-status lens · FolderTree visual redesign.
- **Started:** 2026-05-01 (evening)
- **Owner:** Reza (vision/sign-off) + Claude (code)
- **Last touched:** 2026-05-01 — Audit confirmed extensive existing infrastructure (Phase 25 Document Management Engine, Phase 26 Document Intelligence Engine, `/api/documents/upload`, `/api/documents/analyze`, `/api/documents/export` ZIP endpoint, `FolderTree`, `DocumentFolderView`, `AnalysisPreviewCard`, `ExtractionReviewForm`). Phase 38 is mostly UX polish on top — the engines exist, the canonical upload cascade exists, the accountant ZIP endpoint exists. **Hard constraint: zero new calc engines, zero data duplication.**
- **Why this matters:** Documents are evidentiary trail for tax filings, refinancing, insurance claims, depreciation schedules. Reza's brief: *"these documents can be shared with the accountant for tax return activities and they should be very well structured and organised."* Tax-time stress (ATO research: 4–7 hrs hunting docs at EOFY) becomes retrieval relief when the vault is well-organised. Behavioural psychology: each upload = mini completion loop / dopamine hit; FY counter visibility drives engagement spike at exactly the right moment.

**Phases:**
- [x] 1 — **Sidebar IA + visual uplift + Smart Inbox surface (count-only).** Shipped as PR #575 (2026-05-01 evening). Split "Reports" into two top-level items: "My Vault" (`Archive` icon, between My Guide and Reports) + "Reports". `/dashboard/vault` alias redirects to canonical `/dashboard/documents`. Apple-typography hero + glassmorphic Smart Inbox card showing untagged-doc count.
- [ ] 2 — **Smart Inbox interactive + universal upload audit.** **THIS PR.** Smart Inbox card now expandable (Apple-style chevron toggle, AnimatePresence height + opacity reveal). When expanded, shows one row per doc awaiting review with: confidence dot (emerald/amber/rose), document-type badge, AI-extracted summary line ("Officeworks · $42.50 · 2025-03-12"), original filename, and two actions: **Open** (existing signed-URL `/api/documents/[id]` endpoint) + one-tap confirm (existing `/api/documents/analyze/confirm` — picks the highest-confidence suggested action, e.g. "Create expense"). Confirm button shows spinner during the call, doc disappears from inbox on success (refresh fires). All existing endpoints, all existing engines — zero new backend code. **Upload-audit findings**: see [Upload-path audit](#upload-path-audit-2026-05-01) below. Two of three upload codepaths route through Phase 25 DME (canonical); ONE legacy bypass identified (`hooks/useDocumentUpload.ts` → `POST /api/documents` calls `lib/documents/documentService.ts:uploadDocument` directly, skipping the Phase 25 RuleEngine). Affected callers: `components/ExpenseDialog.tsx`, `app/dashboard/expenses/page.tsx`. **Decision in this PR**: do NOT refactor ExpenseDialog or the 2,031-LOC expenses page in PR 2 (too risky for a single PR — would mix in unrelated form changes). Instead, captured as PR 2.5 below + tech-debt row #12 below.
- [ ] 2.5 — **Universal upload migration (focused PR).** Refactor `components/ExpenseDialog.tsx` and `app/dashboard/expenses/page.tsx` to use `useDocumentEngine` (canonical Phase 25 hook) instead of `useDocumentUpload` (legacy Phase 19 hook). Alternative path: refactor the legacy `lib/documents/documentService.ts:uploadDocument` to internally invoke `getDocumentManagementEngine().processUpload()`, so every existing caller benefits without touching the form code. Latter is lower-risk and fix-once-fix-everywhere — preferred. After PR 2.5, every document upload anywhere in the app is guaranteed to flow through the RuleEngine (storage routing, category inference, auto-linking, path generation, AI analysis trigger).
- [x] 3 — **Accountant bundle modal + secure share-link (Option C).** Reza decision 2026-05-01: ship Option C (secure shareable link); Option E (Xero/MYOB push) parked as future expansion. Shipped this PR. New `SharePackage` Prisma model with polymorphic `contentRefs` (Json) + `contentType` enum + `deliveryMethod` enum (extends without migration when Xero/MYOB push lands). Migration `prisma/migrations/20260501112526_add_share_package/migration.sql` (CLAUDE.md §12.12). API routes: `POST /api/share` (create), `GET /api/share` (list owner's shares), `DELETE /api/share/[id]` (revoke — soft, sets `revokedAt`), `GET /api/share/[token]` (PUBLIC read; token IS the credential), `GET /api/share/[token]/download` (PUBLIC ZIP — reuses JSZip layout from `/api/documents/export`), `GET /api/share/[token]/file/[docId]` (PUBLIC single-file streaming). Owner UI: `SendToAccountantDialog` on `/dashboard/documents` (purpose / recipient / expiry / structure → Download ZIP via existing `/api/documents/export`, OR Generate secure link via new `/api/share`). Recipient UI: minimal-aesthetic `/share/[token]` page (no app shell — logo + hero number + Download all + per-document list). Owner management: `/dashboard/settings/shares` (copy-link, view-counter, revoke). Default 30-day expiry; picker for 7 / 30 / 90. CDR audit-logged via `createAuditLog()` with `entityType='SharePackage'` on every CREATE / READ / EXPORT / DELETE. Watermark auto-generated server-side ("Generated from Monitrax · {FY} · {owner name}"). Token = 32 random bytes → 43-char base64url (≈256 bits entropy).
- [x] 2.5 — **Universal upload migration.** Shipped this PR. `hooks/useDocumentUpload.ts` migrated from legacy `POST /api/documents` (which bypassed the Phase 25 RuleEngine) to canonical `POST /api/documents/upload` (DME). New `LINK_FIELD_BY_ENTITY` map + `buildDmeFormData()` helper translate the hook's `links: { entityType, entityId }[]` shape into DME's individual entity form fields (`propertyId`, `expenseId`, `loanId`, etc.). Result: every document uploaded anywhere in the app — including from `ExpenseDialog` and the 2,031-LOC expenses page — now flows through the same canonical engine. No changes to the 2,031-LOC form needed (hook-level fix only). Tech-debt row #12 closed.
- [x] 4 — **Tax-status lens.** Shipped this PR. New "By Tax Status" root in `FolderTree` with three children: **Deductible** / **Non-deductible** / **Untagged**. Counts derived client-side from a new `expenseTaxMap: Map<expenseId, isTaxDeductible>` populated on page mount via `/api/expenses` (existing endpoint — no new API). Helper `getDocumentTaxStatus(doc)` buckets each document: at least one EXPENSE link with `isTaxDeductible === true` → DEDUCTIBLE; falls back to NON_DEDUCTIBLE if any false; UNTAGGED otherwise. Filter wires in via the existing `pathParts[0] === 'tax-status'` check in `filteredDocuments`. **Plus** — FolderTree visual redesign (Apple typography, glass active-row, tabular-nums count badges, top-level lenses rendered as uppercase tracked-out section headers). This was the visual refinement Reza flagged after the PR 3 merge: previously only the wrapping `<aside>` got the glass treatment; the tree internals were untouched. Now every row matches the rest of the page.

#### Upload-path audit (2026-05-01)

Three codepaths exist for file upload to a `Document` row. Two are canonical, one is legacy.

| Codepath | Entry | Routes through Phase 25 DME? | Notes |
|---|---|---|---|
| `POST /api/documents/upload` | `hooks/useDocumentEngine.ts`, direct fetch from `/dashboard/documents`, `DocumentUploadDropzone` | ✅ Yes — `getDocumentManagementEngine().processUpload()` | Canonical. RuleEngine resolves storage, category, links, path. |
| `POST /api/documents/analyze-for-form` | `components/documents/FormDocumentUpload` (used in income / loan / property forms) | ✅ Yes — also creates a Document via DME, then triggers analysis for form auto-fill | Canonical. |
| `POST /api/documents` | `hooks/useDocumentUpload.ts` → `lib/documents/documentService.ts:uploadDocument` | ❌ **No** — calls `uploadDocument()` legacy service directly, bypassing the Phase 25 RuleEngine | Legacy Phase 19 path. Affects: `components/ExpenseDialog.tsx`, `app/dashboard/expenses/page.tsx`. Doc rows still get created (so they appear in the Vault) but without RuleEngine-driven storage selection / auto-categorisation / auto-linking. |

Bank-import codepaths (`components/bank/ImportWizard`, `components/bank/TransactionImportDialog`) hit `POST /api/accounts/[id]/import`, not document endpoints — they're for transaction CSVs, not Document rows. Out of scope for the upload audit.

**Design language constraint:** Same as Phase 37 — extends Home TRAIL banner v3 (glassmorphic 28px cards, `appleEase` 1.4s, framer-motion v12 springs 320/28, AnimatePresence cross-fades, full `prefers-reduced-motion`). Zero new dependencies. Zero new design tokens.

**Risk:** Low. PR 1 = pure presentation layer (hero + Smart Inbox). PR 2 = UI composition over existing components + an audit-and-refactor pass. PR 3 introduces ONE new API route (share-link). No `prisma/schema.prisma` change in any PR. No calc engine changes. No data duplication.

**Blocking:** None.

**What we're explicitly NOT building (already exists per audit 2026-05-01):**
- Upload + auto-categorise + entity-link cascade — `DocumentManagementEngine.processUpload()` (Phase 25, `lib/documents/engine/`)
- OCR + Gemini metadata extraction — `DocumentIntelligenceEngine.analyzeDocument()` (Phase 26)
- ZIP bundle export — `POST /api/documents/export` already supports `financial-year-first` / `entity-first` / `category-first` structures
- AI suggestion accept/edit UI primitives — `AnalysisPreviewCard`, `ExtractionReviewForm` in `components/documents/intelligence/`
- Form auto-fill from receipt — `POST /api/documents/analyze-for-form` + `FormDocumentUpload` component
- Polymorphic entity linking — `DocumentLink` model + `LinkedEntityType` enum (9 entity types)

---

### 5. Phase 41f — Personal Bookkeeping Integration (Xero v1)

- **Status:** 🟢 **CORE CLOSED** — 41f.0 ✅ **PR #690**; 41f.1 ✅ **PR #691**; 41f.2 ✅ **PR #692**; 41f.3 ✅ **PR #693**; 41f.4 ✅ **PR #697** (closed Phase 41f core); 41f.5 Monitrax Express **superseded by Phase 42** (Q-41F-1 closed 2026-05-07).
- **Started:** 2026-05-07
- **Owner:** Reza (vision/sign-off + 5 D-41F decisions ✅) + Claude (code)
- **Last touched:** 2026-05-07
- **Why this matters:** Closes the loop between the entity-aware tax engine (Phase 41e) and real-world entity bookkeeping. Today the user enters distributable-surplus / trust net income / SMSF balances by hand, with no audit trail back to source. After 41f, those numbers come from a stamped `EntityAccountingSnapshot` row with `sourceProvider + sourceTenantId + pulledAt`. **Strategic positioning: Monitrax CONSUMES Xero data, never replaces it.** Per §1.1 scope boundary in the spec doc, Phase 41f is **storage + understanding only** — no legal-document generation, no Xero write-back, no regulator-filing.

**Hard constraints (non-negotiable):**
- **Reuse Phase 32 OAuth + sync infrastructure** — no parallel auth surface; one canonical `AccountingIntegration` model with a scope discriminator.
- **Trust deed = 4-step confirm-before-apply** — never auto-apply legal-instrument interpretation. The friction is the feature.
- **HR-1/HR-2 preserved** — every imported number stamps `sourceProvider + sourceTenantId + pulledAt`; downstream tax engines consume from the typed snapshot row, never from raw Xero JSON.
- **Xero-only at v1** — MYOB + QuickBooks deferred to v2.
- **CDR posture: Xero data is NOT CDR-regulated** — handled as standard Privacy Act 1988 PII (encrypted-at-rest tokens; sanitized audit logs).

**Sub-PRs:**
- [x] **41f.0** — Design doc + Reza sign-off block — `docs/blueprint/PHASE_41F_BOOKKEEPING_INTEGRATION.md`. ✅ **APPROVED 2026-05-07** (D-41F-1 through D-41F-5 + §1.1 scope-boundary callout). **PR #690 MERGED.**
- [x] **41f.1** — Schema migration ✅ **PR #691 MERGED 2026-05-07**. Extends `AccountingIntegration` with scope discriminator + XOR CHECK constraint + two partial unique indexes per scope. New `entity_accounting_snapshots` + `trust_deed_extracted_rules` tables. New enums + back-relations. `prisma validate` ✅ + `prisma generate` ✅ + `tsc` ✅. CLAUDE.md §12.11 + §12.12 both satisfied.
- [x] **41f.2** — Xero OAuth + connect surface ✅ **PR #692 MERGED 2026-05-07**. New `lib/integrations/xero/` (zero new deps; `fetch`-based) with config + state (HMAC-signed stateless) + oauth (authorize URL + token exchange + refresh + revoke + tenant list; read-only scopes per §1.1) + index. Four new API routes (status / connect / disconnect / callback). New `/dashboard/entities/[id]/connect-bookkeeping` page with three states.
- [x] **41f.3** — Snapshot import pipeline + Div 7A surplus wiring ✅ **PR #693 MERGED 2026-05-07**. Three new `lib/integrations/xero/` modules (`tokenRefresh` / `reportParser` / `snapshotPuller`) + 2 API routes (`POST /refresh` / `GET /snapshots`) + `<SnapshotCard />` UI + Phase 41e.6 Div 7A classifier opt-in `Div7AClassifyOptions.distributableSurplus` extension. 29 new tests; 449 existing tax-engine tests still pass.
- [x] **41f.4** — Trust-deed parser (PR #697, 2026-05-07) — **CLOSES PHASE 41f CORE**. New `lib/integrations/trust-deed/` module (types + Zod + `pdfTextExtractor` via `unpdf` zero-new-deps + `geminiExtractor` strict structured output + `extractionService` lifecycle) + new `lib/services/trustDeedRulesService.ts` consumer-side reader + 3 API routes (GET/POST/PATCH/POST) + 4-step UI page + 36 new tests (88 integration total green).
- [x] **41f.4-extension** — Trust-deed CONFIRMED rules wired end-to-end (this PR, 2026-05-07). New `lib/tax-engine/divisions/trustDeedValidation.ts` overlay + `MasterTaxPositionInput.confirmedDeedRulesByEntity` orchestrator input + new `getTrustDeedRules` AI advisor FACT_LOOKUP tool (registry size 10 → 11). Trust deeds are no longer decorative — extracted rules flow into both the tax engine + the AI advisor.
- [ ] **41i.6** — Surface-level audit (the trustworthiness commitment per Reza brief 2026-05-07; spec doc `PHASE_41I_6_SURFACE_AUDIT.md` shipping in this PR for sign-off; 3 sub-PRs ~6 days; ships AFTER 41f.4 to cover 41f's new surfaces in its first audit pass).
- [x] ~~**41f.5** — Monitrax Express~~ — **SUPERSEDED by Phase 42** (Q-41F-1 decided 2026-05-07). Consumer-bookkeeping completion now ships under Phase 42 (PR #695 design doc + PR #696 + PR #698 PR1/PR2 already merged). 41f.5 spec doc `PHASE_41F_5_MONITRAX_EXPRESS.md` archived. No standalone 41f.5 PR.

**5 strategic decisions ✅ APPROVED 2026-05-07 (PR #690):**
- **D-41F-1** schema scope — Option A (extend existing model with discriminator) ✅
- **D-41F-2** SDK scope at v1 — Xero only; MYOB + QuickBooks → v2 ✅
- **D-41F-3** trust-deed flow — 4-step confirm-before-apply ✅
- **D-41F-4** P&L surface placement at v1 — entity detail only; Money Flow Sankey integration → v2 ✅
- **D-41F-5** distributable surplus → Div 7A — auto-feed with audit log + per-loan override ✅

**Risk:** Medium-low. Schema migration is additive + zero existing rows in `accounting_integrations` to migrate (verified pre-flight). Trust-deed parsing has the highest user-facing risk; 4-step flow + status lifecycle (`EXTRACTED → CONFIRMED | REJECTED`) is the structural defence. Xero rate-limiting mitigated via 24-hour `EntityAccountingSnapshot` cache + debounced refresh button.

**Blocking:** None — Phase 41f core CLOSED (PR #697). 41f.5 Monitrax Express formally superseded by Phase 42 (Q-41F-1 closed 2026-05-07).

**Closes Up Next:** #30.

---

### 6. Phase 41i.6 — Surface-Level Numerical Audit (the trustworthiness commitment)

- **Status:** 🟢 **CLOSED pending merge** — design doc ✅ **PR #694**; 41f.4 ✅ **PR #697** (Phase 41f core closed); 41i.6a ✅ **PR #699**; 41i.6b ✅ **PR #701**; 41i.6c runtime harness shipping (this PR — **CLOSES PHASE 41i.6**).
- **Started:** 2026-05-07
- **Owner:** Reza (vision/sign-off + 5 D-41i.6 decisions) + Claude (code)
- **Last touched:** 2026-05-07
- **Why this matters:** Reza brief 2026-05-07: *"I need the calc Audit agent to be able to review all relevant calculations of monitrax and the results... continuously make sure all calculations and produced numbers in monitrax is accurate and trustworthy and not some made up incorrect numbers."* This is the **trustworthiness commitment**. Phase 41i.0–5 catches calc-engine drift; 41i.3b (queued) catches per-user engine drift; 41i.6 catches the missing layer — surface-rendering drift (rendered tile ≠ canonical engine output). Together they form the comprehensive HR-3 safety net.

**Hard constraints (non-negotiable):**
- **Extend, don't fork** — reuse the existing `CalcAuditFinding` lifecycle. New enum value `L4_SURFACE_AUDIT` on `CalcAuditFindingSource`. Same admin queue, same alerting path.
- **HR-3 extended** — `PHASE_41_REGULATORY_ARCHITECTURE.md` invariant 11 now covers BOTH calc-engine drift AND surface-rendering drift. Reviewers reject any new financial surface without a registered `SurfaceDescriptor`.
- **Static + runtime, not just one** — 41i.6b CI lint catches the bug class at PR time (fast feedback); 41i.6c runtime harness catches data-dependent drift.
- **Top 10 surfaces at v1** — coverage scope is bounded; expand the descriptor list as workstream matures. Top 10: cashflow / expense / income / net-worth / health / tax / debt-freedom / emergency-fund / property-equity / investment-total.
- **Single admin queue** — don't split L1/L2/L3/L4 into separate pages. Filter chips per source.

**Sub-PRs:**
- [x] **41i.6 design doc** — `docs/blueprint/PHASE_41I_6_SURFACE_AUDIT.md` (this PR, awaiting Reza sign-off on 9 ticks in §12)
- [x] **41i.6a** — Surface descriptor registry + first 10 descriptors + `L4_SURFACE_AUDIT` enum migration (this PR, 2026-05-07). New `lib/calc-audit/surfaces/` module: `types.ts` (`SurfaceDescriptor` + `DiffTolerance` + `SurfaceValueKind` + `DEFAULT_TOLERANCE_BY_KIND` + `exceedsTolerance`) + `registry.ts` (singleton + `assertDescriptor` invariants) + 10 descriptors split across `descriptors/masterSnapshotDescriptors.ts` (6 master-direct) + `descriptors/cashflowDescriptors.ts` (4 via `quickMetrics`) + `index.ts` (auto-bootstrap). Schema migration `prisma/migrations/20260511100000_add_l4_surface_audit_source/`. 30 new tests. 191 total green. tsc clean. **All 10 v1 descriptors point at `lib/services/masterFinancialService.ts:getMasterFinancialSnapshot()`** — current components that bypass master will surface as L4 findings on first 41i.6c run.
- [x] **41i.6b** — CI static-analysis pass (this PR, 2026-05-07). New `scripts/lint-financial-surfaces.ts` detects 3 patterns: inline frequency conversion (`* 12 / * 52 / * 26 / * 4 / * 365` + reciprocals on financial-named identifiers); inline arithmetic between two financial fields; hardcoded financial constants adjacent to financial fields. Annotation: `// @financial-math-allowed: <reason>` (sidecar JSON tracks). **Baseline mechanism** — pre-existing 28 violations grandfathered so build doesn't break; only NEW violations fail. Stale baseline entries warn. Wired into `vercel-build` pre-build. 23 new tests. tsc clean.
- [x] **41i.6c** — Runtime audit harness + `[Full scan]` button (this PR, 2026-05-07). New `lib/calc-audit/surfaceAudit.ts` (`runSurfaceAuditForUser` + `runSurfaceAuditFullScan` async-generator + idempotent `recordSurfaceFinding` dedup). New `POST /api/admin/calc-audit/full-scan` with dual auth (admin session OR Cloud Scheduler shared secret) + NDJSON streaming. Admin UI extended with `[Full scan (L4)]` button + live progress card + source-filter chips. 12 new tests (7 outcome paths + 3 dedup + 2 multi-descriptor). 65 total surface-audit tests. tsc clean. **CLOSES PHASE 41i.6.**

**5 strategic decisions gated on Reza sign-off:**
- **D-41i.6-1** coverage scope at v1 — top 10 surfaces (cashflow / expense / income / net-worth / health / tax / debt-freedom / emergency-fund / property-equity / investment-total)
- **D-41i.6-2** diff tolerance — AUD 0 cents; ratios 0.01 (1bp); scores 0.5pt
- **D-41i.6-3** ship both static (41i.6b) AND runtime (41i.6c) — complementary
- **D-41i.6-4** manual `[Full scan]` button now; scheduled Cloud Scheduler full scan deferred to PROD-ready
- **D-41i.6-5** full canonical chain on every L4 finding (surfaceId / route / canonical source / canonical value / rendered value / diff / userId)

**Risk:** Medium. Static-analysis lint rules can produce false positives — exception annotations + sidecar JSON tracking mitigate. Runtime harness performance (full scan iterates every USER × every SURFACE) — initial limit to ~100 users for v1; optimise / index / parallelise at scale. UI streaming-progress complexity — NDJSON streaming is straightforward; add cancel affordance to prevent runaway scans.

**Blocking:** Reza sign-off on `PHASE_41I_6_SURFACE_AUDIT.md` §12 (9 ticks).

**Dependencies:** Phase 41i.0–5 ✅ (calc-audit foundation + persistent findings + alerting). Phase 41h ✅ (provides the structural-validator pattern that 41i.6 mirrors at the surface layer). 41f.4 should close before 41i.6a starts so the audit's first pass covers all of 41f's surfaces.

---

### 7. Phase 41E reform 2026-27 — eight AU tax-law changes from the 12 May 2026 Budget

- **Status:** 🟢 **STAGE 1 COMPLETE 2026-05-16.** All 6 sub-PRs shipped (#763 → #769). Tax-reform infrastructure fully in place; zero behavioural change for any user (every `commencementVerified` flag remains `false`). Stage 2 per-measure rule mechanics + Stage 3 Royal-Assent flips queued — no calendar pressure until Treasury publishes exposure drafts.

- **Stage 1 sub-PR progress (COMPLETE):**
  - [x] **41E design + governance** (PR #763 merged 2026-05-16) — 14-section Phase doc + AI-provider strategy + CLAUDE.md §12.14 (NON-NEGOTIABLE Phase 41E reform-awareness rule for future sessions) + Q-AI-PROVIDER closed (keep Gemini default).
  - [x] **41E.0 — Foundation** (PR #764 merged 2026-05-16): `reformConstants.ts`, schema migration, `TaxYearConfig` 8 flags, 17 tests, §10.10 doc carry-forward.
  - [x] **41E.1 — Engine module skeletons** (PR #765 merged 2026-05-16): 6 new files + extended 2 existing modules + 47 tests. Zero behavioural change.
  - [x] **41E.2 — AI advisor** (PR #766 merged 2026-05-16): Knowledge pack + 2 new tools (registry 11 → 13) + 30 tests.
  - [x] **41E.3 — UI surfaces** (PR #767 merged 2026-05-16): `<TaxTreatmentBadge>` + `<TaxReformBanner>` calm card + dismissal API + UserPreference migration + entity API extension + 24 tests.
  - [x] **41E.4 — Form UI + tile wiring** (PR #768 merged 2026-05-16): PropertyTile renders badge + entity edit form gets trust-subtype selector + foreign-resident toggle.
  - [x] **41E.5 — Wizard + docs consolidation** (PR open 2026-05-16): `acquisitionContractDate` + `isNewBuild` capture in `PropertiesStep` (conditional on post-cut-over `purchaseDate`) + `trustType` + `isForeignResident` selectors in `EntitiesStep` + bulk-create API plumbing for the new columns + round-trip activation tests (every flag false → true → false reversibility + flag independence) + `docs/architecture/03_DATA_MODEL.md` §O consolidated reference + new `docs/operational/UNCOMPUTED_REGISTER.md` (operator playbook for the 10 UC-* codes) + Phase doc status flipped 🟡 → 🟢 COMPLETE.

- **What's next (no urgency):**
  - **Stage 2** — per-measure rule mechanics, ~10-20 days spread across months as Treasury publishes drafts. M4 (foreign-resident CGT) + M5 (loss carry-back) are code-ready first; M1+M2 (neg gearing + CGT) wait on Treasury draft Q4 2026 / Q1 2027; M3 (trust 30%) ~Q1 2027.
  - **Stage 3** — Royal Assent flips: per-measure `commencementVerified` flag flip + knowledge-pack status bump from `bill` to `assented` + strip "announced — final law TBC" UI caveats. Each ~1 day.
  - [ ] **41E.5 — Tests + docs + UNCOMPUTED register**: round-trip activation tests + boundary fixtures + `docs/architecture/03_DATA_MODEL.md` consolidation + Phase doc status flips + IMPLEMENTATION_PLAN updates.
- **Started:** 2026-05-13 (Reza research brief)
- **Owner:** Reza (regulatory sign-off + staging order) + Claude (architecture + module skeletons)
- **Last touched:** 2026-05-16
- **Why this matters:** The 12 May 2026 Federal Budget introduced eight tax-law changes — three structural (negative gearing restricted to new builds; 50% CGT discount → cost-base indexation + 30% min tax rate; 30% min tax on discretionary-trust *taxable income*) plus foreign-resident CGT, loss refundability, foreign-purchase ban extension, VC caps, EV FBT phased transition, dynamic PAYG instalments. Monitrax's whole value proposition is *"your numbers, accurately, under the actual rules"* — botched reform implementation produces numbers users plan with that are wrong by tens of percent. Compounds with the AFSL-boundary architecture: the AI advisor's structuring-advice scenarios (`runStructuringScenario`, `runReformedCgtScenario`) become qualitatively more useful with the reform tools.

**Hard constraints (non-negotiable, all already locked in Phase 41 architecture):**
- **D-1** — full regulatory scope ships in the demo cut; no demo/PROD split for reform measures
- **D-2** — AFSL/TPB/NCCP boundary is structural via the closed `FACT_LOOKUP | SCENARIO_RUN` tool-kind discriminant in `lib/ai/tax-advisor/registry.ts`; reform tools inherit this — no `RECOMMENDATION` kind exists
- **HR-1** — every numeric claim in an AI response must trace to a tool result (validator-enforced in `lib/ai/tax-advisor/policy/validators.ts`); reform-aware scenarios produce numbers from the deterministic engine, never from the LLM
- **HR-2** — every authority citation must come from the knowledge pack, never AI memory; reform measures sit in `lib/ai/tax-advisor/knowledge/reform-2026-27.ts` (Stage 1) with `status: 'announced' | 'exposure-draft' | 'bill' | 'assented'` per entry
- **HR-3** — user-visible calc errors unacceptable; UNCOMPUTED gate per measure until Royal Assent (silent admin-side via Phase 41i.6)
- **§9 versioning protocol** — modules ship with skeletons in Stage 1 returning `uncomputedReasons` flags until exposure draft + Royal Assent confirmed; `commencementVerified` flag per measure in `taxYearConfig.ts` (exact pattern from 41e.3 Div 296)

**Stage 1 sub-PR shape (queued, ~5-7 days, one PR — pending Reza approval):**
- [ ] Schema migration — additive nullable columns on `Property` (`acquisitionContractDate`, `acquisitionSettlementDate`, `isNewBuild`, `newBuildEvidence`), `Investment` (`acquisitionDate`), `LegalEntity` (`trustType TrustType?` enum). All additive — §12.11 N/A. One-time backfill flags pre-cut-over properties as `PRE_REFORM_GRANDFATHERED` where contract date is known.
- [ ] Module skeletons — `divisions/cgtIndexation.ts` + `divisions/cgtMinimumRate.ts` + `divisions/trustMinimumTax.ts` + `divisions/foreignResidentCgt.ts` + `divisions/lossRefundability.ts` — each returning `UC-*-PENDING-EXPOSURE-DRAFT` or `UC-*-PENDING-ROYAL-ASSENT` per §9 protocol.
- [ ] `taxYearConfig.ts` — `TAX_YEAR_2027_28` skeleton with reform-keyed fields gated by `commencementVerified` flag per measure.
- [ ] AI advisor knowledge pack — `lib/ai/tax-advisor/knowledge/reform-2026-27.ts` with status-versioned entries + ATO/Treasury citations.
- [ ] First reform-aware AI tool — `getReformedTaxRegimeStatus` (FACT_LOOKUP) returning `{regime, commencementDate, citation}` per property/investment.
- [ ] **"Reform impact for me" AI surface (Reza directive 2026-05-16)** — new `getReformImpactSummaryForUser` SCENARIO_RUN tool composes summary of law changes + personalised per-asset impact + realistic suggestions (within AFSL boundary — scenario CTAs + Ask-a-Pro routing, never recommendations) into a single user-asked answer. Prefilled prompt on `/dashboard/cfo/ask` ("How do the 2026-27 tax reforms affect me?"); CFO Guide card CTA wires to it. UNCOMPUTED narration for M1/M2/M3 dollar projections until Stage 2 mechanics land. See `PHASE_41E_REFORM_2026_27.md` §10.10.
- [ ] UI — per-asset "Tax treatment" badge on property/investment detail dialogs; one-time "Tax rules are changing" calm-tone explainer card on `/dashboard/cfo` (warm-words per CLAUDE.md §14, lead with the good news — existing investors grandfathered).
- [ ] UNCOMPUTED register entries per Phase 41 §10 convention.

**Stage 2 sequencing (per exposure draft drop, follow-up PRs):**
- **Measure 4 (foreign-resident CGT)** — exposure draft already published 10 Apr 2026; can ship rule mechanic in Stage 1 if scope allows, otherwise immediate follow-up.
- **Measure 5 (loss refundability — carry-back)** — current FY 2026–27 measure; second-most urgent.
- **Measures 1+2 (negative gearing + CGT)** — biggest measure; ~Q4 2026 / Q1 2027 exposure draft. Intersects with `acquisitionContractDate` migration; ~10-15 days.
- **Measure 3 (discretionary-trust 30% min)** — ~Q1 2027 exposure draft. **Intersects with Phase 41f.4** (CONFIRMED trust-deed extracted rules feed the beneficiary-credit computation).
- **Measure 8 (EV FBT phased)** — purely config; ~2-3 days when the implementing Bill is final.
- **Measures 6, 7, 9** — Tier 2/3 knowledge-pack/config updates; ~1 day each.

**Decision points Reza needs to make before Stage 1 ships (5-point summary from chat 2026-05-16):**
- [ ] Scope = the eight Budget measures in §2 of the Phase doc
- [ ] Grandfathering test = contract date with cut-over `2026-05-12T09:30:00Z` UTC
- [ ] One PR for Stage 1 (all scaffolding, zero behavioural change) vs piece-by-piece
- [ ] Calm copy on the "Tax rules are changing" card, not urgency
- [ ] Backfill: auto-write `acquisitionContractDate := purchaseDate` for pre-cut-over rows; leave post-cut-over rows NULL + nudge user
- [x] **Q-AI-PROVIDER DECIDED 2026-05-16** — keep Gemini default. `AIProvider` interface stays provider-agnostic for future flexibility.

**Risk:** Stage 1 is low-risk (additive schema, module skeletons that return UNCOMPUTED, knowledge pack with `status` field, one new FACT_LOOKUP tool, calm explainer UI). Stage 2 risk is per-measure and gated on exposure draft text — never guess in the calc, always pin against ATO worked examples per Phase 41 §1 principle 4.

**Blocking:** Reza approval of staging order + AI provider strategy decision (Q-AI-PROVIDER) — though Stage 1 doesn't *strictly* depend on the latter (the new tools are provider-agnostic).

**Dependencies:** Phase 41e COMPLETE (41e.0–41e.17). Phase 41h COMPLETE (41h.0–41h.7) — provider-agnostic gateway + 11-tool registry. Phase 41f in flight — Measure 3 needs `TrustDeedExtractedRules` from 41f.4.

**Anchors:** `docs/blueprint/PHASE_41E_REFORM_2026_27.md` (this PR's design doc — 9 sections); `docs/architecture/AI_PROVIDER_STRATEGY.md` (this PR's sibling — Gemini-vs-Claude analysis); `docs/blueprint/PHASE_41_REGULATORY_ARCHITECTURE.md` §0 (D-1, D-2), §1 (HR-1/HR-2/HR-3), §5 (Tier 1/Tier 2), §9 (versioning protocol), §10 (UNCOMPUTED register), §11.1 (Phase 41h tool sequence).

---

