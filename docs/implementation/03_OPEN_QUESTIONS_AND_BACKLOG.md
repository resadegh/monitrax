# Implementation Plan — Open Questions, Blocked & Backlog

> **Spoke of the Implementation Plan SSOT.** Hub + navigation: [`docs/IMPLEMENTATION_PLAN.md`](../IMPLEMENTATION_PLAN.md).
> Strategic decisions not yet made, items blocked on a decision/dependency, dead-code/tech-debt, and reversed decisions (so we don't re-do them).
> Update rules (incl. the per-spoke size budget) live in the hub under "How to update this document".

---

## 🚧 Blocked / Awaiting Decision

> Items that need user input or external dependency resolution before they can move.

| # | Item | Blocked by |
|---|---|---|
| (none right now) | — | — |

---

## ❓ Open Questions (strategic, not yet decided)

| # | Question | Why it matters | Decision |
|---|---|---|---|
| Q-SCOPE-1 | Should Monitrax ship **one module (Property) to market first**, with every other module hidden in PROD behind an admin toggle and staged back in later — and if so, exactly what is kept in v1? | Raised by an external accountant's review (relayed by Reza, 2026-08-03): the app is too wide, too manual, and its numbers cannot be validated. The repo agrees in writing — `MON-131`'s census records ~336 producers across 23 quantities, 22 of 23 MULTIPLE (`01_ACTIVE_WORKSTREAMS.md:17`); the registry holds 63 OPEN/FIXING issues (6 critical) at HEAD `1e2317b6`; Basiq is dark by GTM decision so all intake is manual (`GTM_EXECUTION_PLAN.md:34-38`). Scoping to property is uniquely viable: property surfaces do not read the household/tax/master-snapshot layers, `computePropertyCashflow` is already the SSOT, and the document module is genuinely property-aware. The decision matters most because it sets MON-131's denominator. Full analysis: `docs/strategy/PRODUCT_SCOPE_V1_RECOMMENDATION.md`. | ✅ **DECIDED 2026-08-04** — all eight rulings + three standing directives recorded in `docs/strategy/PROD_SIMPLIFICATION_PLAN.md` §0 (hide household cashflow · tax · CFO · Home/Housekeeping · Investments; safe entity default; Preview copy w/ §13.6 amendment + CDR sunset; WIP=1 freeze). Keep list: plan §2.1. *(Row landed 2026-08-04 by the P0.1 repair — the #1577 session could not carry this file through the connector.)* |
| Q-SCAN-FREQ | Should a receipt confirmed via the document-intelligence `CREATE_EXPENSE` flow create a **one-off** spend rather than defaulting to `MONTHLY`? | The `Frequency` enum has no one-off value, so `/api/documents/analyze/confirm` defaults a scanned receipt to `MONTHLY` — a $42.80 grocery receipt becomes $42.80/month in the budget (financial-adviser lens: silently wrong). Surfaced 2026-06-16 building the global "Scan a receipt" (Phase 26.7); the scan reuses the confirm route as-is, so it inherits this. Options: (a) add a `ONE_OFF` frequency + treat receipts as transactions not recurring expenses; (b) route receipts to `UnifiedTransaction` instead of `Expense`; (c) prompt the user for frequency in the confirm UI. | **OPEN — raised 2026-06-16.** Pre-existing behaviour, left unchanged by Phase 26.7 (scope was capture + recognition, not re-architecting the confirm route). Needs a product call before fixing. |
| Q-ENTITY-VALUE-SHARE | Should per-entity **value** (the "Entity Value" surfaces) become **share-weighted** — applying the `attributeAsset` fractional / beneficial split to VALUE the way the tax path already does — instead of the current binary legal-title (`ownerEntityId`) basis? (Phase 3 audit L2-1.) | Today "Entity Value" assigns a co-owned asset 100% to the legal-title entity, while its **tax** position is attributed by share/beneficial owner — so the two per-entity numbers don't reconcile for joint / tenants-in-common / beneficial-override assets (total net worth is unaffected). Option B (legal-title value + a "see Tax view" label) removes the *silent* disagreement cheaply and stays factual. Option A (share-weighted value) would make value and tax reconcile but is a product + AFSL decision — surfacing a beneficial-ownership split as the headline *value* implies a conclusion about who really owns what, which is closer to advice. | **L2-1 DECIDED Option B 2026-06-15 (Reza)** — Entity Value stays legal-title; `entityValueBreakdown.ts` math unchanged; "by legal title → ownership-share tax effects are in the Tax view" labels shipped (fix PR2). **Option A (per-share VALUE) is PARKED** as a future product decision — revisit only if user feedback shows the legal-title value is misleading despite the label; if revived it needs an explicit AFSL-boundary review (value must not imply a beneficial-ownership conclusion). |
| Q-IMPORT-1 | Should the QIF/CSV import create `UnifiedTransaction` rows for **ALL** parsed transactions regardless of AI confidence (low-confidence ones categorised `Other` + flagged for in-place review), retiring the `TransactionReviewQueue` diversion entirely? | The 2026-06-10 incident showed the queue is a black hole: items below the 0.90 auto-accept threshold never become transactions and there is NO live review UI (🗑️ row 31). Behaviour-psychology lens: “your 300 transactions are in, 12 need a category” beats a silent success with nothing imported. Architect lens: deletes an entire dead persistence path + the orphaned panel. The 2026-06-10 PR made the failure loud + recoverable but did NOT change the diversion semantics. | **OPEN — raised 2026-06-10.** Proposed by Claude during the silent-zero-import diagnosis; Reza has seen the recommendation but scoped the session to the Gemini fix. |
| ~~Q-EOF-1…5~~ | ~~Phase 47 Entity Ownership Fabric — five design gates~~ | | ✅ **DECIDED 2026-06-10** — Reza: *"go with your recommended"* — all five resolved per recommendation (correction-only re-attribution; retail super → personal member entity; derived ownership for holdings/txns/recurring; Household-flat dashboard default with opt-in entity lens; Stage D strictly after Stage C). **Plus one scope addition:** personal & joint ownership capture must be first-class for users with NO company/trust ("very complete" against Australian tax/property law) — codified as the binding §4A personal-tier completeness matrix (P1–P9: sole, joint tenants w/ TR 93/32 50/50 split + survivorship, tenants in common w/ fractional shares, co-ownership-≠-partnership guard, spousal attribution, minor/Div 6AA, deceased estate, nominee, exotic-flagged) in `PHASE_47_ENTITY_OWNERSHIP_FABRIC.md` v2. Stage A unblocked. |
| ~~Q-AUTH-1~~ | ~~Should password reset move to a custom prefetch-safe Monitrax-hosted action handler?~~ | | ✅ **DECIDED 2026-06-12 — BUILT.** Reza chose "build the full handler" while resolving the 2026-06-12 API-key incident (Firebase's hosted `firebaseapp.com/__/auth/action` handler signs links with the project's default web key = the **Maps/Places** key, whose HTTP-referrer allow-list blocked `firebaseapp.com` → `API_KEY_HTTP_REFERRER_BLOCKED` 403 on every verify link; plus `auth/network-request-failed` on sign-in until the **Monitrax Auth (Web)** key's referrer list was corrected). Fix: new prefetch-safe **`/auth/action`** page (`app/auth/action/page.tsx`) handles `verifyEmail` + `resetPassword` + `recoverEmail`, validating read-only on load (`checkActionCode` / `verifyPasswordResetCode`) and consuming the code only on explicit button-tap / form-submit. Operator flips Firebase → Authentication → Templates → **Customise action URL** → `https://www.monitrax.com.au/auth/action`. Result: every auth email is applied by OUR SDK = the app's **Monitrax Auth (Web)** key on `www.monitrax.com.au` — the Maps key + the firebaseapp.com hosted handler leave the auth path entirely, and the reset-link prefetch bug is fixed too. App key was already Monitrax Auth (Web) (Vercel `NEXT_PUBLIC_FIREBASE_API_KEY`), so no env change needed. **⚠️ CORRECTION 2026-07-01:** the "Customise action URL" step above is **not possible on this project** — setting `notification.sendEmail.callbackUri` returns `400 EMAIL_TEMPLATE_UPDATE_NOT_ALLOWED` (platform lock; confirmed via console AND REST). `config.client.apiKey` (the email-link signer) is also read-only = the Maps key. So `/auth/action` is built + deployed but NOT in the live email path; links stay on `firebaseapp.com` signed with the Maps key. **What actually fixed verification (verified working 2026-07-01):** added `identitytoolkit` + `securetoken` API targets (+ `firebaseapp.com` referrer) to the Maps key via `gcloud services api-keys update` (UID `03e1218e…`). Full write-up + the exact command in `docs/operational/security/01_AUTHENTICATION.md` § Troubleshooting. Standing hygiene follow-up: register a Firebase Web app to reclaim a clean `client.apiKey` and drop auth scopes off the Maps key. |
| ~~8~~ | ~~How is an SMSF's super modelled — `LegalEntity` or `InvestmentAccount(type=SUPERS)`?~~ | | ✅ **DECIDED 2026-06-02 (Phase 39.5)** — **SMSF = `LegalEntity(type=SMSF, role=SUPERANNUATION)`** that OWNS its assets (investment accounts / property / cash via `ownerEntityId`), NOT a `SuperannuationAccount` and NOT an `InvestmentAccount(type=SUPERS)`. Retail/industry super = `SuperannuationAccount` (external fund, userId-scoped). Research confirmed `InvestmentAccount(type=SUPERS)` is dead (UI-label-only, never read for tax/net-worth) and SMSF already exists as a first-class `LegalEntityType`. Implemented: `SuperannuationAccount.fundType` (INDUSTRY/RETAIL/SMSF) + nullable `ownerEntityId`→SMSF entity; net-worth excludes SMSF member balances (counted via entity assets); onboarding `SUPERS` removed. SMSF fund-level tax (`calculateSmsfIncomeTax`, already built) remains staged — not yet surfaced. |
| ~~1~~ | ~~Cloud SQL dedicated tier upgrade~~ | | ✅ **DECIDED 2026-05-02** — Cloud SQL upgraded to **Enterprise Plus**. Question closed. (Note: actual tier flag and gcloud command should be reflected in `docs/operational/database/01_CLOUD_SQL_OPERATIONS.md` if not already.) |
| 2 | Should we adopt **Prisma Accelerate** for managed connection pooling + edge cache? | Eliminates need for our own pool tuning. ~AU$30/mo on free tier limits. | **Not adopted** (2026-05-02). Cloud SQL Connector + IAM auth + Enterprise Plus tier covers our pooling needs at current scale. Re-evaluate only if Vercel function instance count grows beyond what WIF token caching handles cleanly. |
| ~~3~~ | ~~Vercel Hobby → Pro upgrade~~ | | ✅ **DECIDED 2026-05-01** — Vercel **upgraded to Pro** (already in place, see Phase 9 cutover record). Question closed. Pro plan enables `vercel.json` `regions: ["syd1"]` pinning + better build minutes; VPC peering option still on the table for the future Cloud SQL Private IP migration (Up Next #2 trigger path). |
| ~~4~~ | ~~Delete legacy `/api/portfolio/snapshot` and `/api/financial-snapshot`~~ | | ✅ **DECIDED 2026-05-02** — Reza delegates execution. Plan: (a) migrate the 3 callers of `/api/portfolio/snapshot` (`app/dashboard/page.tsx:348`, `hooks/useUISyncEngine.ts:282`, `app/api/linkage/health/route.ts:19`) to `/api/master-snapshot`; (b) migrate the 1 caller of `lib/services/financialSnapshot.ts` (`app/api/dashboard/insights/route.ts:101`) to `getMasterFinancialSnapshot()`; (c) delete both routes and the duplicate service in a single PR. Closes Tech Debt #1a + #1b. Trigger: next housekeeping pass. |
| ~~5~~ | ~~Delete legacy `/api/auth/login` and `/api/auth/register`~~ | | ✅ **DECIDED 2026-05-02** — soft-deleted 2026-05-01 (PR #591). Hard-delete confirmed: **after 2026-05-15**, if Vercel production logs show ZERO `[deprecated-route]` warnings during the soft-delete window, hard-delete both files. Closes Tech Debt #2. |
| Q-PRA-1 | ~~D2C vs org-attached AI voice context~~ | | ✅ **DECIDED 2026-05-04 — REJECTED in favour of single-voice + marketplace + Ask-a-Professional + in-app conversations.** Reza counter-proposal (2026-05-04): rather than two AI voices, give EVERY user the SAME AI Guide (general information / education only — never recommends a specific product or action), and on every recommendation card surface an "Ask a Professional" button. (a) D2C user with no professional linked → button opens an invite-only marketplace picker (Monitrax-curated 10–20 launch professionals; verified ASIC AFSL / ACL / TPB before listing). (b) Org-attached user → button pre-routes to the org's roster (grouped by discipline — Sarah for wealth, Tom for tax, Jess for refinance — all inside the org's branded experience). (c) Org-attached users see ONLY their org's professionals (Reza decision: org pays for Monitrax to be their CRM + comms channel; can't be a leaky funnel to competitors). The "next-level" advice is always human; the AI is structurally incapable of crossing the AFSL line. Two new revenue streams open up: (1) D2C marketplace lead fee tiered by user profile (AU$80 sub-AU$500k / AU$150 AU$500k–$2M / AU$250 AU$2M+), (2) Org subscription bundles all professional seats AND all client Wealth-tier features (no per-client charge), so the org's incentive to push the platform to their entire client book aligns naturally. **Closes Q-PRA-1.** Reversal logged below. New workstream **Phase 32C** queued. |

| ~~Q-FB-1~~ | ~~Phase 33g design-doc questions~~ | | ✅ **DECIDED 2026-05-05 — by Claude judgement on Reza directive "use your judgement and continue".** Naming: "Send feedback" for action affordances (verbs match other buttons), "Feedback" for page titles + sidebar entries. Email: in-app only for v1; `notifyAdviserOfReply()` is a documented swap-point in `lib/services/feedbackService.ts` for Phase 32C PR4d's SendGrid integration to fill in. Severity: default `MEDIUM` (frictionless — Reza's `/admin/feedback` is the canonical triage anyway). Internal-notes audit: every edit (CLAUDE.md §13.3 "audit everything" — `FEEDBACK_INTERNAL_NOTE_UPDATED` audit row on every onBlur where `internalNotes` changed; body of note is NOT logged, only previousLength + nextLength). SLA: 48 hours stated in the form copy (under-promise + over-deliver); cron-based flagging of `OPEN` threads > 48h deferred to a follow-up PR (cleaner ship). **Closes Q-FB-1.** Phase 33g shipped same session — Up Next #35 collapsed to ~~SHIPPED~~. |

| ~~Q-HELP-1~~ | ~~Phase 33h/i/j design-doc questions~~ | | ✅ **DECIDED 2026-05-09 — by Claude judgement on Reza directive "start the build, complete all steps".** (1) Sequencing: ship before lighthouse pitch in parallel — quality gain > delay cost. (2) AI scaffold provider: SKIPPED entirely — articles authored directly in this session, no runtime API dep needed for one-off authoring; future drift handled by `lastReviewed` timestamps. (3) Coverage map sign-off: trusted judgement; map shipped alongside articles in one PR. (4) Tooltip rollout: shipped component + dictionary + 7 wire-ups together. (5) Stage-matched help: inline framing within articles (no separate stage-picker UI). **Mid-build clarification from Reza ("instructions not necessarily help or advise")** triggered a complete rewrite of all 12 already-drafted articles from financial-education tone to navigation-instruction tone — every article now opens with "How to get here" + walks the page surface-by-surface + lists numbered "Common tasks". **Closes Q-HELP-1.** |

| ~~Q-41F-1~~ | ~~41f.5 Monitrax Express vs Phase 42 reconciliation~~ | | ✅ **DECIDED 2026-05-07 — by Claude judgement under Reza directive "continue with phase 41 remaining tasks".** Phase 42 (PR #695 design doc + PR #696 + PR #698 PR1/PR2 already merged into main) **supersedes / subsumes** the 41f.5 Monitrax Express scope. Rationale: (a) Phase 42 is broader (full consumer-bookkeeping module, ~6 weeks) where 41f.5 was a 5-day lite tier; (b) 41f.5 + Phase 42 would have created two parallel codebases for the same domain — direct CLAUDE.md §12.2 violation; (c) Phase 42 is already in flight, so no work is lost. **Action:** 41f.5 spec doc `PHASE_41F_5_MONITRAX_EXPRESS.md` archived; 41f.5 row removed from Phase 41f workstream sub-PR list; Phase 41f core CLOSED at 41f.4. **Closes Q-41F-1.** |

| ~~Q-GTM-1~~ | ~~Review price for first 5 friendlies — $197 / $247 / $297?~~ | | ✅ **DECIDED 2026-05-11 — Reza: $197 for the first 5 friendlies.** (Claude flagged: $197 anchors the public price too — recommend a ladder $197 founding → $297 standard → $397 with-adviser-session, public price to be re-decided once Reviews #3–5 are delivered and conversion is observed. Not blocking.) **Closes Q-GTM-1.** Reflected in `GTM_EXECUTION_PLAN.md` Step 3.2. |
| ~~Q-GTM-2~~ | ~~Outbound sending domain name?~~ | | ✅ **DECIDED 2026-05-11 — Reza: separate sending domain, `try-monitrax.com` (fallbacks `monitrax-pro.com` → `getmonitrax.com` → `hellomonitrax.com`).** Reza initially questioned whether a separate domain was needed (proposed `admin@monitrax.com.au`); Claude made the deliverability case — cold outreach generates spam-complaint signals tracked per sending domain; a burn would route the primary domain's product email + CDR consent confirmations to spam, with weeks-to-months recovery, right at launch — and Reza agreed ("fair call, let's go"). Tool: **Smartlead** ($39/mo, DFY managed inbox, n8n-friendly API; Instantly noted as the friendlier-UI alternative). One mailbox (`reza@try-monitrax.com`) sufficient at 30–50/day. **Closes Q-GTM-2.** Step-by-step setup guide delivered to Reza this session; reflected in `GTM_EXECUTION_PLAN.md` Step 1.3. |
| ~~Q-GTM-3~~ | ~~First aggregator to focus on (Connective / AFG / Loan Market / Mortgage Choice / Finsure)?~~ | | ✅ **DECIDED 2026-06-10 — Finsure first, Connective second** (recorded at [docs/marketing/gtm/BROKER_ICP.md](../marketing/gtm/BROKER_ICP.md) header; this row still said OPEN — drift reconciled 2026-07-15 by the Cowork truth-restore session. Reza: flag if the ICP record is not the ruling.) |
| ~~Q-DEC~~ | ~~Codebase-wide `Float` → `Decimal` migration for financial precision.~~ | | ✅ **DECIDED 2026-05-24 — Reza: "this can happen anytime, add to the plan and we will get on it."** Moved to Up Next row 69 with detailed scope + approach (4-PR cutover: additive Decimal columns → engine adapter layer → engine-by-engine route migration → Float column drop). Parallel-run shadow comparison reuses Phase 41I calc-audit harness. Strong recommendation in row 69: sequence BEFORE Phase 45 What If? public release (10-yr horizons compound Float error visibly) AND BEFORE /wealth-check public traffic (wrong-cents on result page = credibility kill). **Closes Q-DEC.** |
| Q-BASIQ-1 | **Do we onboard Basiq at all, given the initial + ongoing cost?** | Basiq is the Tier 1 bank-feed data source (live CDR account/transaction feeds). It is NOT the only data source — the app has a 3-tier model (Tier 1 Basiq / Tier 2 file import / Tier 3 manual entry), so the product is fully PROD-usable without it. But Basiq accreditation carries real cost: pen test (~AU$15-25k one-off), cyber insurance (ongoing), Basiq platform fees (ongoing per-call / per-connection), and the engineering overhead of the CDR compliance surface. **Raised 2026-05-20 — Reza: "I am still unsure if I would onboard BASIQ due to the initial and ongoing cost."** | **OPEN — not yet decided.** Consequences while open: Phase 0 console rows #17 (pen test) + #18 (cyber insurance) are ⏸ PARKED; the entire D-Day Bundle is on hold; the `BASIQ_INTEGRATION` feature flag stays OFF in prod (already the case). **No work is lost by waiting** — the CDR compliance code (consent lifecycle, `cdr_consents`/`cdr_complaints`/`cdr_disclosures` tables, lifecycle cron) is already built and dormant; it simply doesn't activate until Basiq is wired. **Decision framing for Reza:** the question is purely commercial — "is the live-bank-feed UX worth the accreditation cost vs. launching on file-import + manual entry and adding Basiq later once revenue justifies it?" Recommend: **launch PROD on Tier 2 + Tier 3 data sourcing, defer Basiq until paying-user revenue covers the accreditation cost.** This de-risks launch and makes Basiq a funded upgrade rather than a pre-revenue gamble. |
| Q-GTM-4 | **VA hire timing — parallel with Phase 2, or wait until first Review sells?** | Hiring early = trained + ready when volume hits; hiring late = no payroll burn while validating. Tradeoff is ~$300–500/mo for 6–8 weeks vs being the bottleneck the moment Reviews start landing. | Needs Reza decision before Step 3.7. Default recommendation: hire early. |
| Q-GTM-5 | **AFSL boundary — DIY scope doc + lawyer review, or engage an AFSL holder for the Review service from day one?** | DIY + lawyer review = ~$1–3k one-time + stay strictly factual / general-information. Engaging an AFSL holder = ~15–25% revenue share but unlocks personal-recommendation language. The Review product is genuinely better with personal recommendations; the regulatory risk on DIY is real. | Needs Reza decision before Step 3.1. Default recommendation: DIY + lawyer review for v1 (factual-only Review), revisit if customer feedback says "I needed someone to tell me what to actually do". |
| ~~Q-GTM-6~~ | ~~Advertising as a consumer revenue lever?~~ | | ✅ **DECIDED 2026-05-12 — NO display advertising.** Display ads in a personal-finance app are a trust-killer + advice-conflict generator (contradicts CLAUDE.md §0 + §14). Acceptable form = **tasteful, disclosed affiliate only** — and only for products that pass the financial-adviser test (a high-interest savings account when the user is building an emergency fund = aligned; a credit card to someone in debt = never), never inside the Guide's recommendation flow, always disclosed. A minor supplementary stream, never a pillar. Revenue priority order locked: (1) adviser/broker B2B subs, (2) D2C premium subs (Phase 6, post-Basiq), (3) marketplace lead fees, (4) tasteful affiliate. See workstream 0e. **Closes Q-GTM-6.** |
| Q-GTM-7 | **Friendlies private-beta cohort — who, exactly?** | Need 5–10 people spread across TRAIL stages (someone in debt, someone with property, someone just starting) for the private beta (workstream 0f). Quality of feedback > headcount. 6-month comp time-box. | Needs Reza to pick the names. Not blocking the build — blocking the send of the invitation email. |
| ~~Q-AI-PROVIDER~~ | ~~AI provider for the main tax-advisor surface~~ | | ✅ **DECIDED 2026-05-16 — Reza: keep Gemini for now.** Default Gemini 2.0 Flash for the main tax-advisor surface stands. `ClaudeProvider` may still be added as a sibling implementation when bandwidth allows + the capacity-Q&A pilot may run later, but neither is in the critical path. Document Intelligence (`lib/ai/gemini.ts`) stays on Gemini. **Closes Q-AI-PROVIDER.** Architecture stays provider-agnostic (`lib/ai/tax-advisor/providers/types.ts`) so a future flip remains a one-file change if the answer changes. See `docs/architecture/AI_PROVIDER_STRATEGY.md` for the analysis. |
| Q-CONV-1 | **STT provider for v2 of the conversational onboarding agent (Phase 12 Track E)?** | v1 ships on the Web Speech API (browser-native, no audio leaves the device, no new vendor / no new credential / no new CDR egress). For v2 we may want cloud STT (Whisper / Google STT) for better accuracy on Australian financial vocabulary + varied accents. The tradeoff is a new CDR-relevant vendor + new credential surface + new audit obligation. | **Claude recommendation:** stay on Web Speech API until v1 user feedback shows quality is the blocker. Defer the cloud-STT decision to post-v1. **Not blocking** — Track E v1 can ship without resolving this. Re-open the question only when real-user signal says quality is the gate. |
| ~~Q-ICP-1~~ | ~~Singular product ICP — wealth-builder vs mass-market beginner?~~ | | ✅ **DECIDED 2026-05-24 — wealth-builder (mass-affluent integration-debt persona).** Reza directive: "I am aiming for the wealth builder module and market." Aligns the product's deep engineering (Phase 41E entity tax, Phase 44 entity graph, Phase 14 scenarios) with a payable, underserved AU segment the B2B broker channel naturally delivers. The mass-market-beginner positioning is retired (race against free, bank-feed-enabled incumbents — Up/Frollo/WeMoney — that Monitrax cannot win). TRAIL warmth + behavioural grounding kept; applied to a different anxiety (complexity, not poverty). Sample-of-one design risk acknowledged — friendlies cohort job is to surface variance. New Up Next row 66 (positioning rewrite) + row 67 (What If? wedge feature) + row 68 (/wealth-check hook) all derive from this. **Closes Q-ICP-1.** |
| Q-HOOK-AFSL | **`/wealth-check` result-page copy — engage a real AU fintech lawyer for final sign-off, and when?** | **Claude AFSL pre-review completed 2026-05-24** (Reza directive: "be my AU fintech lawyer and perform the review yourself"). Verdict: the spec as originally drafted (`"$217/month closes ~$98k of your gap"`) is **probably on the personal-advice side of s766B** — user inputs personal info + output names a dollar-specific lever derived from it + reasonable-person test (s766B(3)(b)) triggers. **Structural fix required and now mandated by row 68 spec:** reframe from "you-and-your-lever" to "people-like-you-and-a-pattern" — factual information about a demographic cohort (RG 244 + RG 36.43–.49 safe-haven), with maths shown but no recommendation. **Mandatory inclusions** also now mandated on row 68 spec: (1) s949A General Advice Warning footer; (2) AFSL boundary disclosure + MoneySmart find-an-adviser link; (3) concessional contribution cap caveat ($30k FY24-25); (4) collapsed Calculation Assumptions panel; (5) no product names ever; (6) Privacy Act APP 5 microcopy at email-capture. **Structural code rules:** cap-headroom check before any super lever fires; NCCP-aware framing for any refinance/credit lever in Phase 45 What If? (modelling=safe; broker-recommendation=advice). | **Claude recommendation:** real-lawyer engagement IS still warranted — but reduced scope and de-risked timing. Engage AU fintech lawyer (~AU$1–3k, ~2 wk lead) to validate the *reframed* copy + stamp the General Advice Warning + sign off on the Phase 45 What If? lever boundary at the same time (economies of scale on legal spend). NOT blocking build. **Gate sequence:** build → friendlies retention signal positive → my reframed copy implemented → lawyer pass on both surfaces → public traffic-on. Reza decision needed: green-light the bundled lawyer engagement (both surfaces) when friendlies signal lands. |
| Q-HOOK-BENCHMARK | **ASFA / ABS / APRA benchmark refresh — yearly review trigger owner?** | The `/wealth-check` result-page numbers (ASFA Comfortable, ABS net-worth percentiles, APRA quarterly super) drift on a quarterly-to-yearly cadence. Stale benchmarks degrade credibility (the worst failure mode for an authority-anchored hook) and could push the page into incorrect territory if the gap calculation diverges from current ATO contribution caps or ASFA budgets. | **Claude recommendation:** schedule a yearly cron-driven check (Q1 each year) that compares hardcoded constants against the live ASFA/ABS/APRA pages and flags drift to Reza via the existing GCP alert channel. Bundle the constants into `lib/wealth-check/benchmarks.ts` with a `LAST_VERIFIED` timestamp and a CI lint rule that fails if `LAST_VERIFIED` is > 12 months stale. Reza decision needed: confirm cron approach + alert channel (rec: same Slack/email channel as A1/A7 alerts in workstream 0). |
| Q-CONV-2 | **Onboarding mode-selector — returning-user (mid-draft) experience?** | PR #818 (Phase 12 Track E.2c) added `OnboardingModeSelector` — a deliberate two-card landing screen that fixes the "tiny invisible toggle" problem for FIRST-TIME users (flag ON + no `?mode=` param + no draft progress). But it is **deliberately bypassed for users who already have an onboarding draft** — they land directly where they left off, still seeing the small hard-to-see pill toggle as their only mode-switch affordance. Reza observed this 2026-05-20 ("still the same as before with a small toggle"). The selector works; the gap is the returning-user case. | **Three options put to Reza 2026-05-20:** (A) leave it — selector handles the important first decision, mid-flow switching is rare; (B) enlarge / clarify the pill toggle for mid-draft users (~20 min PR); (C) show the selector to returning users too, reframed — "You're partway through — continue with chat or form?" with a prominent "Continue where you left off" + the two mode cards (most consistent with Reza's original intent "user makes the decision visibly"). **Claude recommendation: C.** Needs Reza decision before the follow-up PR. **Not blocking** — first-time users already get the fixed experience. |

**Open as of 2026-05-24:** Q-HOOK-AFSL (Claude AFSL pre-review complete + structural copy fix mandated on row 68 spec; real-lawyer engagement still warranted at reduced scope — bundle Phase 45 + Phase 46 in one pass, rec when friendlies signal lands) + Q-HOOK-BENCHMARK (rec yearly cron + LAST_VERIFIED lint) — both gate `/wealth-check` traffic-on but not build. Q-DEC + Q-ICP-1 DECIDED 2026-05-24 (Float→Decimal queued as Up Next row 69; wealth-builder is the singular ICP). Q-CONV-2 (mode-selector returning-user experience — rec option C). Q-GTM-3 (first aggregator — rec Finsure), Q-GTM-4 (VA hire timing — rec mid-Phase 2 small scope), Q-GTM-5 (AFSL boundary — rec DIY + lawyer for v1) — needed before Phase 2/3 of `GTM_EXECUTION_PLAN.md`. Q-GTM-7 (friendlies cohort) — needed before the private-beta invite email goes out (workstream 0f). Q-CONV-1 (STT for v2 of Track E conversational onboarding — rec stay on Web Speech API until v1 feedback shows quality is the blocker; **not blocking** v1). Q-AI-PROVIDER DECIDED 2026-05-16 (keep Gemini). Q-GTM-1 ($197) + Q-GTM-2 (`try-monitrax.com` via Smartlead) DECIDED 2026-05-11/12; Q-GTM-6 (no display ads, affiliate-only) DECIDED 2026-05-12. Q-PRA-1 closed by single-voice decision; Q-FB-1 + Q-HELP-1 + Q-41F-1 closed by Claude judgement under Reza directives.

---

## 🗑️ Dead Code / Tech Debt Backlog

> Found during work, NOT yet cleaned up. Each item: where it lives + why it's dead + when to remove.

| # | Item | Location | Why it's dead | Remove when |
|---|---|---|---|---|
| ~~1a~~ | ✅ **RECLASSIFIED 2026-05-02 — NOT a duplicate.** Pre-flight audit during PR #5 cleanup found that `/api/portfolio/snapshot` returns `SnapshotV2` (GRDCS-aware "Snapshot 2.0") while `/api/master-snapshot` returns `MasterFinancialSnapshot`. Different shapes, different scopes: master is the financial-calcs SSOT (Phase 28); portfolio/snapshot is the GRDCS / relational / linkage-health SSOT. Its callers (`/api/linkage/health`, `useUISyncEngine`, dashboard home) consume the GRDCS layer that master does not expose (`linkageHealth`, `moduleCompleteness`, `relationalInsights`, per-entity `_links` / `_meta`). Classification corrected in CLAUDE.md §12.2 (two canonical snapshot SSOTs, not one). Future SSOT consolidation (promoting GRDCS into master, deprecating portfolio/snapshot) is a design call, not a cleanup. | | |
| ~~1b~~ | ✅ **CLOSED 2026-05-02 (PR #598).** `/api/dashboard/insights` migrated from `getFinancialSnapshot()` → `getMasterFinancialSnapshot()` with field-path remapping (`loans.monthlyRepayments` → `quickMetrics.monthlyLoanRepayments`; `accounts.liquidCash` → `quickMetrics.liquidCash`; `healthScore.savingsRate` → `quickMetrics.savingsRate`; `healthScore.debtToIncome` → `healthScore.components.debtToIncome.value`). Deleted: `app/api/financial-snapshot/route.ts`, `lib/services/financialSnapshot.ts`, the legacy re-exports in `lib/services/index.ts`. | | |
| ~~2~~ | ✅ **CLOSED 2026-05-17 (this PR)** — Server-side auth routes (`app/api/auth/login/route.ts`, `app/api/auth/register/route.ts`) hard-deleted. Soft-deleted 2026-05-01 (PR #591); 16-day soft-delete window confirmed no `[deprecated-route]` warnings in prod logs. Files removed. Firebase Auth SDK is the single canonical client-side path (CLAUDE.md §12.4). | | | |
| 3 | `lib/onboarding/onboardingEstimateService.ts` | Marked disabled, throws `OnboardingDisabledError`. Per `app/onboarding/page.tsx` it's defence-in-depth for the R12 incident. | Once the bulk-create path has been stable in prod for 90 days (started ~2026-04-29) | ~2026-07-29 |
| ~~4~~ | ✅ **CLOSED 2026-05-19** — April 2026 changelog consolidation. 14 daily files (`CHANGELOG_2026_04_*.md`, ~2,800 LOC total) consolidated into a single monthly summary `docs/changelog/CHANGELOG_2026_04.md` (one paragraph per session, ~190 lines, scannable). Originals preserved under `docs/changelog/archive/` with a README explaining the consolidation rule for future months ("archive past months, keep current month live"). Rule established: when a month accumulates >5 daily changelog files AND the month is in the past, roll up + archive. Tech Debt #4 row marker rolled to closed. | | |
| ~~5~~ | ✅ **CLOSED 2026-05-19 (audit)** — premise was stale. Re-audited: 7 routes wrap entities with `wrapWithGRDCS` (`/api/loans`, `/api/expenses`, `/api/properties`, `/api/income`, `/api/accounts`, `/api/investments/holdings`, `/api/investments/accounts`); 8 consumer surfaces read `_links` / `_meta` (`/dashboard/expenses` + `/income` + `/investments/*` + `/properties`, `<AccountDetailDialog>`, `<LoanDetailDialog>`). **Every wrapped entity has at least one consumer** — no orphan `_links` fields. The tech debt row was filed 2026-04-10 when several detail dialogs hadn't been built; Phase 37 + Phase 38 closed the gap. | | |
| ~~6~~ | ✅ **CLOSED 2026-05-19 (audit)** — `DIRECT_URL` is genuinely unused. Codebase audit: `grep -rn 'DIRECT_URL'` across all source + config + scripts returns ZERO references (the only hit is this tech debt row). `prisma/schema.prisma` datasource block uses only `url = env("DATABASE_URL")` (no `directUrl` line). `vercel-build` runs `prisma migrate deploy` using `DATABASE_URL` only. **Reza-side action:** if `DIRECT_URL` is set as an env var in Vercel (Project Settings → Environment Variables), it's safe to delete in all 3 scopes (Production / Preview / Development) — no code reads it. Recorded in Phase 0 §Reza-side checklist below as a new console row #22. | | |
| ~~7~~ | ✅ **CLOSED 2026-05-18 (PR C, this PR)** — Barrel-audit sweep across all 56 `lib/*/index.ts` barrels. 6 risky re-exports found across 3 barrels: `lib/admin/index.ts` (`./auth`), `lib/auth/index.ts` (`./context` + `./guards`), `lib/cfo/decisionSupport/index.ts` (`./taxIntegration` + `./loanDecisionSupport` + `./investmentDecisionSupport`). All 6 removed; each barrel now carries a comment header documenting the rationale + the direct-import paths consumers must use. Zero bare-barrel consumers found in pre-removal grep — these were landmines waiting to bite a future PR. Audit method scripted; future PRs adding `export * from './<server-only>'` will be caught by the same script. | | | |
| ~~8~~ | ✅ **CLOSED 2026-05-04** — Phase 32B PR1 deleted `lib/portal/auth.ts`. Re-verified zero callers (only reference was a comment block in `lib/portal/index.ts`, also removed). Server-side portal auth is handled by route-level guards under `app/api/portal/**` using the existing `withPermission()` middleware from `lib/middleware.ts`. Future portal sessions should NOT re-introduce a parallel auth surface — extend `withPermission` instead. | | | |
| 9 | Remaining `/dashboard/accounts?action=…` hrefs | `components/dashboard/BasiqHeroCard.tsx:181,190`, `components/dashboard/DashboardEmptyStateGrid.tsx:135,136`, `components/setup/SetupNextActionPanel.tsx:90` | Each carries `?action=connect-basiq` or `?action=add` and the action handler currently lives only on `/dashboard/accounts`. Repointing today would break the Basiq Connect flow. Phase 36 Phase 2.0 (this PR) repointed everything else. | Phase 36 Phase 2b — once Connect Bank sync/disconnect UI is ported to `/dashboard/balances`, repoint these hrefs in the same PR. |
| ~~10~~ | ✅ **CLOSED 2026-05-17 (this PR)** — Legacy linear-wizard directory (`components/onboarding/linear/` — `LinearWizardContainer.tsx` + `primitives/` + `hooks/` + `design/` + `steps/`, full directory) hard-deleted. Soft-deleted 2026-05-01; replaced by Phase 12 v2.0 `components/onboarding/wizard/` (grid-based `WizardContainer`) + 2026-05-17 Phase 12 Track E conversational variant. Dead-code audit re-verified ZERO importers immediately prior to deletion. | | | |
| ~~11~~ | ✅ **CLOSED 2026-05-02** — Phase 40 wires `lib/cfo/trailStage.ts` into the AI advisor for stage-aware prompt context. Re-exported from `lib/cfo/index.ts`. | | | |
| ~~12~~ | ✅ **CLOSED 2026-05-01** — Phase 38 PR 2.5 shipped. `hooks/useDocumentUpload.ts` migrated from legacy `POST /api/documents` to canonical `POST /api/documents/upload` (Phase 25 DME). Solved at the hook layer with a `LINK_FIELD_BY_ENTITY` translation map + `buildDmeFormData()` helper — no edits to the 2,031-LOC `ExpenseDialog` or expenses page. Every upload anywhere in the app now flows through the canonical engine. | | |
| 13 | `OrganizationType` duplication — same enum stored on two rows | `Organization.profession` (canonical, added 2026-05-04) AND `OrganizationPortalSettings.organizationType` (shadow) | Phase 32B PR1 promoted `profession` to the parent `Organization` record because it's a fundamental property of the firm, not a portal-settings detail. The legacy field on `OrganizationPortalSettings` is now a shadow of the canonical value (backfill migration copies it across). Per CLAUDE.md §12.2 SSOT, exactly one canonical source per piece of data. | After Phase 32B PR2/PR3 ship and no production code reads `OrganizationPortalSettings.organizationType` directly: (1) audit grep for the field, (2) migrate any reader to `org.profession`, (3) drop the column in a follow-up Prisma migration, (4) delete the type-mirror in `lib/portal/types.ts`. Bundle as one PR — single touch. |
| 14 | `OrganizationPlan` enum naming drift — schema says `STARTER / PROFESSIONAL / BUSINESS / ENTERPRISE`, pricing matrix + UI say `STUDIO / PRACTICE / ENTERPRISE` | `prisma/schema.prisma` (`enum OrganizationPlan`) + `lib/portal/constants.ts` (`PLAN_LIMITS`) — mapped through `lib/portal/planTier.ts` (`mapPlanToTier`) added in PR3 | Phase 32B PR3 added `lib/portal/planTier.ts` as a clean mapping layer (`STARTER → STUDIO`, `PROFESSIONAL → PRACTICE`, `BUSINESS → PRACTICE` — uplifted, `ENTERPRISE → ENTERPRISE`) so `withPortalFeatureGate` can gate features without touching the on-disk enum. Mapping is the SSOT today. The canonical fix is renaming the enum + dropping `BUSINESS`. | Bundle the rename into Phase 32C PR6 (Stripe Billing) — Stripe will reference these as price IDs anyway, so the migration touches the same surface. Migration: `RENAME TYPE` + per-row `UPDATE` for any `BUSINESS` rows (treat as `PROFESSIONAL`/`PRACTICE`); drop `mapPlanToTier`; update `PLAN_LIMITS` keys; consumers auto-switch via TypeScript enum diff. |
| 15 | `components/portal/clients/ClientDetail.tsx` — DELETED 2026-05-04 | (was) `components/portal/clients/ClientDetail.tsx` | Phase 32B PR3 retired the component. Drill-in now renders the canonical consumer dashboard via `app/portal/clients/[id]/view/page.tsx` + `ClientCanonicalDashboard.tsx` per the Phase 32B hard constraint ("not a separate `ClientDetail.tsx`"). Re-export removed from `components/portal/clients/index.ts`. Zero callers re-verified at delete time. ✅ CLOSED in same PR — listed here for traceability. | Closed in PR3. |
| 16 | SendGrid integration code that needs migrating to Resend | `lib/services/feedbackService.ts` (notify stub), `lib/services/conversationService.ts`, `lib/email/conversationEmail.ts`, `lib/calc-audit/alertingService.ts` | Decision 2026-05-09: **Resend chosen** over SendGrid for transactional email (simpler SDK, generous 3k-emails/mo free tier, modern API). SendGrid integration scaffolded but no live billing — Reza hasn't registered. Code consolidation deferred until first email-out feature actually fires in prod. SSOT decision logged in `docs/operational/cost-control/00_VENDOR_INVENTORY.md` Tier 3. | Migrate to Resend SDK in the same PR that ships the first live email path. Probably alongside Phase 33g.2 (live AI feedback chat — `notifyAdviserOfReply()` swap-point) OR Phase 32C PR4d (in-app conversations email-through-app). Whichever lands first does the migration. |
| 17 | OpenAI integration — DELETED 2026-05-09 | (was) `lib/ai/openai.ts`, `lib/ai/contextBuilder.ts`, OpenAI stub in `lib/tie/categorisation.ts`, `openai` SDK dep in `package.json` | Phase 11 scaffolded an OpenAI-backed AI integration that was never wired up — `categoriseByAI()` always returned null; `lib/ai/openai.ts` + `lib/ai/contextBuilder.ts` had zero callers. Future AI work uses Gemini (already in deps). 595 LOC + 1 dep removed. ✅ CLOSED in cost-control-foundation PR — listed here for traceability. | Closed. |
| ~~19~~ | ✅ **CLOSED 2026-05-17 (this PR)** — Admin `+Create Flag` modal built. New `components/admin/feature-flags/CreateFlagModal.tsx` mounted in `app/admin/feature-flags/page.tsx`. POSTs to existing `POST /api/admin/feature-flags` (already shipped). Validates `^[A-Z][A-Z0-9_]*$` key format client-side; server re-validates + checks uniqueness. ESC-to-close + click-outside-to-close + reset on re-open. New flag defaults to `enabled: false`; operator flips after creation. Canonical path for new flags remains `prisma/seed-feature-flags.ts` (auto-runs on every `vercel-build`); this modal is the escape hatch for ad-hoc one-offs. | | | |
| ~~18~~ | ✅ **CLOSED 2026-05-19 (Day 2 session 5)** — Reza ran the `/api/admin/schema-drift` audit (after PR #806 fixed the endpoint's deserialization bug). Result: **3 missing tables + 4 missing enums in prod**, all CDR-critical: `cdr_consents` (model `CDRConsent`, Phase L Fix G18), `cdr_complaints` (model `CDRComplaint`, Fix G43), `cdr_disclosures` (model `CDRDisclosure`, Fix G43); enums `CDRConsentStatus`, `CDRComplaintCategory`, `CDRComplaintStatus`, `CDRDisclosureType`. Root cause: same as the 2026-05-12 `basiq_connections` drift — the Phase L CDR remediation (2026-04-11) was applied via historical `prisma db push` workflow with no migration file, so dev got the objects but prod didn't. Corrective migration `20260519100000_fix_pre_migration_cdr_tables_drift` shipped: idempotent CREATE TYPE × 4 (wrapped in `DO $$ ... EXCEPTION WHEN duplicate_object`) + CREATE TABLE IF NOT EXISTS × 3 + FK ALTER TABLE ADD CONSTRAINT in DO blocks + CREATE INDEX IF NOT EXISTS × 7. Pre-migration drift class now eliminated for the known CDR tables. Existing consumers (`app/api/admin/cdr/{compliance,complaints,consent}/route.ts`) already wrap their queries in `.catch(() => 0)` defensively so they degraded gracefully today; once this deploys they'll return real counts. **Pre-Basiq blocker resolved.** No further audit needed unless a future PR adds tables via `db push`. | | |
| ~~20~~ | ✅ **CLOSED 2026-05-18 (PR #798 hotfix + PR #800 sweep)** — codebase-wide audit closed; 17 fixes total (7 in PR #798 covering today's PRs + pre-existing TaxReformBanner; 10 in PR #800 covering pre-existing portal pages). Spurious-logout class eliminated codebase-wide. Reviewer-reject rule documented in both PR commit messages — any new client-side fetch to a protected `/api/...` endpoint MUST pass `Authorization: Bearer ${token}` from `useAuth()`. Admin pages exempt — auto-injected by `app/admin/AdminLayoutClient.tsx` interceptor. | | |
| 21 | **UI promises emails the backend doesn't send.** `/dashboard/settings/notifications` ships 4 user-controlled email toggles (Weekly Digest / Monthly Report / Important Alerts / Product Updates) backed by `UserPreference.email*` columns that persist correctly via the settings API — but **zero code paths actually read those columns to send emails**. Audited 2026-05-18: 5 live email paths (verification / MFA-stub / conversation / calc-audit / Firebase password-reset) are all transactional + bypass user preferences (correct for those paths). Risk: user sets a preference, expects emails, doesn't receive them → silent disappointment. | `app/dashboard/settings/notifications/page.tsx` (UI) + `UserPreference.email*` (DB) + missing emit-side code paths | **Two options:** (a) Wire the 4 toggles per `runbooks/11_EMAIL_NOTIFICATIONS_AUDIT.md` §5 (5-PR sequence, ~5-8 days total); OR (b) demote the 4 toggles to "Coming Soon" in the UI to match the Push Notifications pattern (1 PR, 30 min). **Recommendation:** option (a) — the work is bounded, the user-trust ROI is high. Up Next row #65 tracks the wiring follow-up. |
| 22 | **Legacy `/dashboard/setup` component island — orphaned 2026-05-20.** Reza decision (Open Question Q-CONV-2): the `/dashboard/setup` page (its own "Set up Monitrax — X of 7 done" Setup Tray checklist + one-recommended-action panel + empty-state tile grid + Basiq hero) is a DUPLICATE of the canonical `/onboarding` wizard (form + AI-chat). §12.2 SSOT violation — two setup surfaces that drift. `/dashboard/setup/page.tsx` now redirects to `/onboarding`; the component island is unreachable but not deleted ("mark and hide"). | `components/setup/SetupTray.tsx`, `components/setup/SetupNextActionPanel.tsx`, `components/dashboard/DashboardEmptyStateGrid.tsx`, `components/dashboard/BasiqHeroCard.tsx`, `lib/setup/tasks.ts` + `useSetupState` hook, `app/api/setup/state/route.ts` — all now orphaned (only ever used by the retired page; grep-verified 2026-05-20). | Delete the whole island in a single follow-up PR once Reza confirms the redirect behaves correctly in prod for a few days. Verify no remaining importers at delete time. Bundle with Phase 12 Track F (the wizard fully replaces this surface). |
| 24 | **n8n Friendlies Tracker — 3 outstanding gaps (cosmetic + security-hardening).** All discovered during the 2026-05-25 end-to-end verification session. (a) **Update Friendly Stage node has `"Email": "="` in its field mapping** — an empty expression that wipes the Email field on every Airtable update. Caused Test 9 to land in Signed up with no email visible. **Reza-side fix (~30 sec UI):** open the node, delete the Email row from field mapping; only `id` (match column) + `Friendly stage` should remain. (b) **Code node has `'Feedback given'` (lowercase g)** — Airtable enum is `Feedback Given` with capital G; will silently fail when a friendly submits their first feedback thread. **Reza-side fix (~30 sec UI):** one character change in `Verify HMAC + Enrich` Code node. (c) **HMAC verification disabled** — n8n Code-node sandbox blocks both `require('crypto')` AND the Web Crypto `crypto.subtle` global; HMAC verify removed from the Code node. Risk acceptably low (URL is obscure, HTTPS-only, worst-case impact is incorrect Airtable updates). **Reza-side fix (~5 min, needs SSH access to Hetzner):** set `NODE_FUNCTION_ALLOW_BUILTIN=crypto` env var in the n8n docker-compose stack, restart, then re-add the HMAC verify block to the Code node (canonical code stored in `lib/webhooks/n8n.ts` comment header). | n8n workflow `caJUA8f61GHsKEUj` — Update Friendly Stage node + Verify HMAC + Enrich Code node | (a) + (b) — cosmetic / silent-failure bugs; UI fixes blocked on Reza's hand. Cannot be safely automated via n8n SDK because the resourceMapper field config would be wiped (this is the exact regression we hit twice this session). (c) — security hardening; deferred until SSH access acquired. | (a) + (b) on Reza's next n8n session (~1 min total). (c) when Reza adds SSH key to the Hetzner box. |
| 23 | **Legacy "Welcome to Your TRAIL" getting-started card — removed from render 2026-05-20.** Reza spotted it as another redundant onboarding surface: the `isEmpty`-branch card on the dashboard home (`/dashboard`) — a numbered 5-step list + "Set up Monitrax" button + Add Property/Loan/Investment shortcuts. Redundant with the `/onboarding` wizard + the layout-level `OnboardingResumeBanner` + `OnboardingWelcomeModal`; its "Set up Monitrax" CTA pointed at the retired `/dashboard/setup`. Card JSX deleted; the `isEmpty` branch now renders `null`. | `app/dashboard/page.tsx` — the `isEmpty` ternary branch now renders `null`. The `isEmpty` derivation itself is now near-dead (only gates a `null`). | Future pass: drop the `isEmpty` derivation + the `isEmpty ? null :` branch entirely — let empty users fall through to the real dashboard's per-section empty states (verify the non-empty branch renders cleanly with zero data first). Bundle with Phase 12 Track F. |
| 26 | **`SuperannuationAccount` cap-default drift (Phase 39.4, 2026-06-01).** Column defaults `concessionalCap @default(27500)` / `nonConcessionalCap @default(110000)` lag the live `taxYearConfig` (30000 / 120000). **Cosmetic only** — runtime caps come from `TaxEngine.getCurrentConfig()` / `taxYearConfig`, never the column default; the stored column is reference data written at create time. Deferred from the Super page PR because flipping a default needs a real `prisma migrate dev` run against `monitrax-db-dev` (§12.12) which the build container couldn't safely do. | `prisma/schema.prisma` (`SuperannuationAccount.concessionalCap` / `nonConcessionalCap` defaults) | Refresh defaults to `30000` / `120000` via a proper `prisma migrate dev` migration; additive `ALTER COLUMN ... SET DEFAULT`, no data backfill, §12.11 N/A. Bundle with the next schema PR that runs migrations against dev. |
| ~~27~~ | ✅ **CLOSED 2026-06-02 (Phase 39.5).** The `SUPERS` option was removed from the onboarding `InvestmentsStep` picker once Open Question #8 settled the SMSF model: SMSF = `LegalEntity` (owns ordinary investment accounts), retail super = `SuperannuationAccount`. Research confirmed `InvestmentAccountType.SUPERS` was dead (UI-label-only, never read for tax/net-worth), so removal was safe; the graceful `?? INVESTMENT_TYPES[0]` fallback protects any legacy rows, and the Prisma enum value is retained for back-compat. SuperStep gained an SMSF hint pointing to "Your structure". | | |
| 25 | **Orphaned Phase2Enhancements exports after R-Charts consumer swap (2026-05-29).** `EntityComparisonChart` (the cashflow-by-entity horizontal bars, ~60 LOC) and its helper `buildEntityComparisonData` (~60 LOC) in `components/dashboard/Phase2Enhancements.tsx` had only one consumer — `app/dashboard/page.tsx` — and that consumer was swapped to `EditorialEntityBars` (net-value-by-entity) in the R-Charts consumer swap. Grep-verified zero remaining importers; other exports from the same file are still in use so the file stays. | `components/dashboard/Phase2Enhancements.tsx` — the `EntityComparisonChart` + `buildEntityComparisonData` exports (plus their TS interfaces `EntityComparisonItem`, `EntityComparisonChartProps`). | Delete in a focused housekeeping PR once R-Charts consumer-swap has been live for a few days and no regression reports surface. Verify zero importers at delete time. |
| ~~29~~ | ✅ **CLOSED 2026-06-07 (MA.4-001 — this PR).** `app/api/calculate/tax/route.ts` + `lib/tax/auTax.ts` both deleted. The `_note` field in `/api/portfolio/snapshot/route.ts:1028` now points at the canonical `/api/tax/position` endpoint. CLAUDE.md §12.2 + §12.3 SSOT restored. | | |
| ~~30~~ | ✅ **CLOSED 2026-06-07 (MA.4-002 fix — `claude/ma-finish-items-LIlK9`).** `portfolioEngine.calculateNetWorth` refactored to delegate to canonical `lib/calculations/netWorthCalculator.ts` SSOT. `PortfolioInput` extended with optional `superannuation` + `personalAssets` fields. `generatePortfolioSnapshot` now queries `SuperannuationAccount` + `Asset` tables. 6 new regression tests in `tests/intelligence/portfolioEngine.netWorth.test.ts` lock the contract. AI advisor + strategy engine now return the same net-worth number as the user's dashboard. **Note:** `calculateCashflow` + `calculateGearing` left untouched (their interest-only loan modeling is wired into stress-test math; refactoring requires a separate workstream — documented in the file header). | | |
| 28 | **`npm audit` CI gate — blocking criticals fixed 2026-06-02; high/moderate tracked.** The `security-audit` blocking gate was failing on **2 critical** advisories, both `vitest`/`@vitest/coverage-v8` (`^1.6.1`) — the dev-only "Vitest UI server arbitrary file read" (via `vite`/`vite-node`); fix is a SemVer-major 1→4 jump, and `@vitest/ui` isn't even installed (not exploitable here). **Resolved** by scoping the must-pass gate to production runtime: `npm audit --omit=dev --audit-level=critical` (the informational `--audit-level=high` step still surfaces everything). Policy documented in `docs/policy/APPROVED_DEPENDENCIES.md` §7.1. **Remaining (non-blocking, tracked):** (a) ✅ **DONE 2026-06-02** — **Next.js** bumped 15.2.6 → **15.5.19** (15.x security-backport line, non-major), clearing the high-severity advisories (SSRF/XSS/cache-poisoning/DoS); also migrated `npm run lint` off the now-deprecated `next lint` to the ESLint CLI (`.eslintrc.json`, prior rule set preserved). (b) ✅ **DONE 2026-06-03** — **xlsx/SheetJS** repointed to the SheetJS CDN build 0.20.3 (above both GHSA ranges → npm audit no longer flags it; prod 0 high / 0 critical). Zero code change; write-only usage meant the parse-triggered vulns were never reachable. (c) **vitest 1→4** major upgrade to clear the dev criticals at source — the only remaining tracked audit item. | `.github/workflows/security-audit.yml` (gate — DONE); `package.json` — `next`, `xlsx`, `vitest`. | (a) bump `next` to latest patched 15.x in a build-validated PR; (b) replace/CDN-migrate `xlsx`; (c) vitest 1→4 with 110-test-suite validation. Do NOT `npm audit fix --force` blindly. |
| 31 | **`TransactionReviewQueue` black hole — review UI unreachable.** `components/bank/TransactionReviewPanel.tsx` is imported by ZERO files (the Phase 36 Phase-2c claim that it “already lives inside `ImportWizard` inside `TransactionImportDialog`” is false — verified 2026-06-10, neither component renders it). The review API (`/api/accounts/[id]/import/[batchId]/review`) works but no page consumes it; the import route’s `reviewUrl` (`/accounts/…/review`) has no matching page route. Result: transactions classified `needsReview`/`requiresManual` are persisted but invisible to the user forever. Surfaced by the 2026-06-10 silent-zero-import incident. The affected user’s held rows (2+ import attempts, duplicated — dedupe doesn’t see queue rows) also need a one-off cleanup when this is resolved. | `components/bank/TransactionReviewPanel.tsx`; `app/api/accounts/[id]/import/[batchId]/review/route.ts`; `app/api/accounts/[id]/import/route.ts` (`reviewUrl`) | Resolve via Open Question **Q-IMPORT-1** (import-all structural fix would delete the queue path entirely) — do NOT build a review page before that decision. |
| 32 | **Duplicate Gemini clients (SSOT violation).** `lib/ai/gemini.ts` and `lib/ai/google/geminiClient.ts` both define `getGeminiClient()`/`isGeminiConfigured()`/completion wrappers with separate model lists + fallback chains. Only `lib/ai/google/geminiClient.ts` received the 2026-06-10 429-retry/backoff hardening; `lib/ai/gemini.ts` (Document Intelligence path) still throws immediately on 429. | `lib/ai/gemini.ts` vs `lib/ai/google/geminiClient.ts` | Consolidate to one client module (port the retry policy), next time either file is touched for a feature. |
| ~~34~~ | ✅ **RESOLVED 2026-07-28 (MON-106, PR-B `fix/mon-106-fy2026-27-config` — Reza's number-changing merge click is the sign-off gate the deferral required).** `TAX_YEAR_2026_27` landed with the legislated 15% lowest band (More Cost of Living in Every Pocket Act 2025; base amounts re-derived $4,020/$31,020/$51,370) — ≈$268/yr less tax for every income ≥ $45k, verified by the Ring-0 walk in `tests/tax/mon106Fy2026_27Config.test.ts`. ATO-indexed items (AWOTE super caps, Medicare/MLS thresholds, co-contribution, TBC) **carried forward from FY25-26 pending confirmed ATO figures** — each carried value commented in the config; `reviewSchedule.nextReviewBy` set to **2027-06-15** (FY27-28 config + the same Act's 15%→14% step + confirm indexed items). Durable prevention: a clock-derived CI guard now goes RED every 1 July the current FY lacks a config, and the engine surfaces `configStale` (the tax page announces "FYX rates are not yet configured — figures shown use FYY") instead of silently substituting. Supersedes the 2026-06-15 "defer to Basiq prep" interim (the honest-stale fallback stays as the read-path normaliser). ~~Original entry:~~ **FY2026-27 tax config not yet built — review DEFERRED to Basiq prep (Reza decision 2026-06-15).** The per-FY review checkpoint (`taxYearConfig.reviewSchedule.nextReviewBy`) fired on 2026-06-15 as designed: FY26-27 commences 1 Jul 2026, has at least one legislated change (lowest resident bracket **16% → 15%** from 1 Jul 2026, 2025 Budget) plus indexed items (super caps via AWOTE, Medicare/Div293 thresholds) that need confirmed ATO figures + a **registered-tax-agent** pass. Reza chose "extend the checkpoint, defer the config" over drafting unverified FY26-27 numbers. Interim mitigation shipped 2026-06-15: `nextReviewBy` bumped to **2026-09-30**, and `getTaxYearConfig` fallback changed from hard-coded FY24-25 → **latest available config (FY25-26)** so post-1-Jul-2026 "current FY" resolves to the most recent known year (honest-stale, not two-years-stale). **Known consequence until the config lands:** from 1 Jul 2026 the engine applies FY25-26 brackets (16% lowest, not the legislated 15%) to FY26-27 calcs. | `lib/tax-engine/config/taxYearConfig.ts` (no `TAX_YEAR_2026_27`; `getCurrentTaxYearConfig` falls back to FY25-26) | Build + verify the FY2026-27 `TaxYearConfig` (15% lowest bracket + confirmed ATO-indexed super/Medicare/Div293 thresholds + TBC) at Basiq prep, with the registered-tax-agent sign-off; then re-bump `nextReviewBy` to "before FY27-28 commences". |
| 33 | **Plan spokes over the §15.5 size budget (post-split carry).** Phase 2 PR-C (F-8) split the plan into a hub + `docs/implementation/` spokes, but did the *structural* move only — no within-section content surgery. `01_ACTIVE_WORKSTREAMS.md` (~289 KB) and `04_RECENTLY_COMPLETED.md` (~296 KB) still exceed the ~150 KB / ~600-line per-spoke budget. | `docs/implementation/01_ACTIVE_WORKSTREAMS.md`; `docs/implementation/04_RECENTLY_COMPLETED.md` | Next hygiene pass: retire the **completed** §0x workstreams (0·WI v1, 0·DG, 0·StD, 0·WX) from `01` → `04_RECENTLY_COMPLETED.md`, and roll `04` entries older than 30 days → `docs/changelog/IMPLEMENTATION_CHANGELOG.md`. |
| 35 | **Phase 3 static audit findings (2026-06-15) — calc-path + ownership-attribution divergences.** Net worth/value re-derived in non-canonical read-models; per-entity value uses binary `ownerEntityId` while tax uses fractional/beneficial attribution; ownership tables reference assets polymorphically (no FK → orphan-on-delete). 7 findings (4×P2/3×P3), RECORD-don't-fix. | `lib/services/wealthGraphService.ts:783`, `lib/reports/contextBuilder.ts:207-220`, `lib/calculations/entityValueBreakdown.ts:64-116`, `prisma/schema.prisma:889-905,928-955` | NOT dead code — cross-path consistency drift; canonical SSOT is correct | per the "Recommended fix PRs" in docs/audits/PHASE3_ENGINE_CORRECTNESS_2026-06-15.md |

> **Row #35 fix progress (2026-06-15):** **L1-1 / L1-2 / L1-3 resolved** by fix PR1 (#1113 — canonical `assetValuation` helper). **L2-2 resolved** by fix PR2 (this PR — in-transaction `cleanupAssetOwnership` on all 5 asset-delete handlers). **L2-1 DECIDED Option B** (Entity Value stays legal-title; "by legal title → Tax view" labels shipped this PR) — Option A (share-weighted *value*) parked as **Q-ENTITY-VALUE-SHARE** in Open Questions above. Remaining from #35: L1-4 (money-flow aggregator alignment, optional) + L1-5 / L2-3 (record-only, close with the Float-path retirement).

---

## ↩️ Reversed Decisions

> Things we tried and rolled back. **Do not re-attempt without explicit user confirmation.** Each entry: what was tried, why reverted, lesson.

| Date | What was tried | Why reverted | Lesson |
|---|---|---|---|
| 2026-04-30 | Append `?connection_limit=1&pool_timeout=20` to `DATABASE_URL` env var on Vercel | Caused 100% of API requests to 401. Prisma's URL parser broke on the combination of unencoded `@`/`%` in the password + new query params. | Don't append to a fragile URL. Either rotate password to URL-safe chars first, OR apply `connection_limit` via Prisma datasource override (Option α). Tracked in Up Next #7. |
| 2026-04-29 | Add `calculateOffsetInterestSavings(offsetBalance, loanAnnualRate)` to `lib/utils/calculations.ts` | User direction: "the calculations and relationships on all the legacy and current pages are correct, so don't create new logics without confirmation … use the existing logics and engines for the new changes." | No new calc engines without explicit go-ahead. Compose existing primitives (`calculateEffectivePrincipal`, `calculateInterestForPeriod`) only when behaviour preservation is verified. |
| 2026-04-29 | Compose offset interest savings as `interestForPeriod(principal, rate, 1) − interestForPeriod(effective, rate, 1)` (would correctly cap savings when `offset >= principal`) | Even though "more correct" in the edge case, behaviour deviates from the legacy `offsetBalance × loanAnnualRate`. User direction: existing math is correct. | Mathematical "correctness" is not the bar. **Preserved behaviour** is. New calc behaviour requires explicit user sign-off. |
| 2026-04-15 | `prisma db push` in Vercel build script | R12 incident — silently dropped legacy tables on every deploy. Lost data risk. | Build scripts NEVER run `db push`. Only `prisma migrate deploy`. Schema changes need a migration file (CLAUDE.md §12.12). |
| 2026-05-04 | Two-voice AI model for D2C vs org-attached users (`voiceContext: 'self' \| 'professional-attached'` parameter on AI advisor) | Reza counter-proposal: single-voice AI for everyone + "Ask a Professional" affordance is a cleaner architecture, simpler AFSL boundary, AND opens a second revenue surface (marketplace lead fees). Voice-switching adapter would have added test surface and drift risk for no compensating benefit. | (a) Architecturally simpler is almost always better — `voiceContext` was a clever solution to a problem we didn't need to have. (b) AFSL boundaries are best enforced *structurally* (the AI is incapable of crossing them) rather than *editorially* (a per-context output adapter). (c) Always look for the answer that opens new revenue rather than the answer that closes a legal gap; the marketplace + ask-a-pro model does both. |
| 2026-05-08 | **Hamburger-drawer primary navigation on phones** (sidebar slide-in triggered from a `Menu` icon in the mobile header; sub-tabs only reachable by re-opening the drawer). | Two-tap-to-section + two-tap-to-sub-tab = four-tap path to a known destination. Hidden navigation makes the TRAIL journey invisible at the moment users most need orientation (financial-stress UX research, Mani et al. 2013). Apple/Robinhood/Cash App/Up Bank all use a persistent bottom tab bar on phones; hamburger is desktop-secondary nav, not mobile-primary nav. iPad portrait <1024px also got the phone drawer, missing the tablet rail tier entirely. **Replaced 2026-05-08 by Phase 14.6** — 5-tab bottom nav mapped to TRAIL stages + horizontal sub-tab pill row + dedicated tablet sidebar at the `md:` (768px) breakpoint. | (a) On phones, primary nav must be visible at all times — the IA *is* the behavioural intervention. (b) Tablet is a first-class tier, not a "narrow desktop"; it gets the persistent rail. (c) When IA matches a published framework (TRAIL), make the framework the visible IA — don't hide it behind a hamburger. (d) **Never re-introduce hamburger primary nav on phones** without explicit user sign-off; if a future surface needs more than 5 destinations, add to MoreSheet, not to the bottom bar. |
| 2026-05-11 | **Consumer-first GTM** (SEO content engine + paid ads + creator partnerships + freemium acquisition funnel as the *primary* go-to-market motion for the next 90 days) | Considered and rejected during the 2026-05-11 architect-mode GTM synthesis. Three structural reasons: (a) **Basiq economics** — $10k upfront + $2k/mo + ~1mo onboarding means we need ≥AU$3–5k MRR funding it *before* paying it. Consumer subscriptions take 12–18 months to reach that MRR from a cold start; B2B can reach it in 90 days from one signed broker contract. (b) **Pre-Basiq product gap** — the killer feature ("connect your banks, see everything") is unavailable until Basiq is live. Selling consumers a manual-entry PFM when Frollo/WeMoney/PocketSmith/Up offer auto bank feeds for free is a losing pitch. (c) **CAC payback** — AU consumer fintech CAC payback is brutal (12+ months); we have no runway for that even if the unit economics worked. **Replaced 2026-05-11 by B2B-led GTM** — `docs/marketing/GTM_EXECUTION_PLAN.md` Phases 1–4 (broker pilots + Review service) front-load 90-day revenue + warm subscription leads; consumer scale becomes Phase 6, post-Basiq. | (a) Don't fight Basiq economics — sequence the GTM to fund the gate, don't pay the gate to enable an unvalidated motion. (b) Pre-accreditation, the product's perceived value to a consumer is a fraction of what it'll be post-Basiq — sell to buyers who don't need the auto-feed (brokers using their own loan data) first. (c) **Do not re-attempt consumer-first GTM as the primary 90-day motion** without explicit user sign-off. Consumer waitlist + a single high-leverage creator partnership can run in parallel as low-cost preparation, but the burn-rate engine (SEO content production, paid ads, influencer fees) does not turn on until Phase 6 trigger fires. |
| 2026-05-07 | **Pending-actions popup-on-login** (modal-on-arrival overlay on `/dashboard` that bundled up to 3 actions; PR6.5b initial implementation) | Behavioural-friction risk: years of cookie banners + newsletter modals + permission prompts have wired users to X-out *anything* that blocks the page on arrival, regardless of content. Even with snooze + opt-out, the first impression of every day became "things you owe Monitrax" rather than "look how your wealth is going" — direct opposite of the warm-words / no-shame framing in CLAUDE.md §14. Fogg Behavior Model: anxiety reduces ability — a daily anxiety pulse makes the categorisation task *harder*, not easier. Apple/Linear/Stripe almost never interrupt with "things to do" modals on landing; modals-on-login are a SaaS re-engagement pattern, not a premium-product pattern. Reza decision: *"go with your recommendations"* → pivoted to **non-modal collapsible strip** (Option A) within the same PR. Same SSOT aggregator + API + once-per-day gate; presentation-layer swap only. | (a) Pending-actions FRAMING is sound (the *what*); modal DELIVERY was wrong (the *how*). (b) Non-blocking surfaces > blocking surfaces for routine engagement — the user finds them when they're ready instead of being interrupted. (c) When a pattern is structurally indistinguishable from a nag (cookie banner / newsletter modal / permission prompt), users dismiss reflexively before reading — the message never lands. (d) **Never re-introduce a modal-on-login pattern for routine engagement on `/dashboard` without explicit user sign-off**, even if the data behind it is genuinely useful. Strips, badges, in-context affordances — those are the right surfaces. |
| 2026-05-27 | **Dropping desktop sign-out from the sidebar account row** — the Phase R2b chrome swap (commit `810d849`, "swap visual chrome inside DashboardLayout.tsx, preserve everything else") replaced the old navy sidebar with the editorial sidebar but unintentionally dropped the inline `<LogOut>` icon button from the user account row. Reported as missing by Reza 2026-06-01. Restored 2026-06-01 (`EditorialSidebar.tsx` gains an `onSignOut` prop; `DashboardLayout` wires `logout`). | A missing sign-out is a security-trust regression — users can't tell whether they're protected. Restoration was reflexive, no design call required. | (a) Chrome-swap PRs that claim "preserve everything else" need an inventory diff per affordance, not just "preserve everything that compiles". (b) Sign-out is one of ~3 affordances that must be present on every authenticated surface (others: who-am-I and how-do-I-get-help). When swapping shells, walk the inventory explicitly. (c) **Do not drop sign-out from any future shell variant without an explicit user-facing alternative** (mobile MoreSheet is acceptable on phones; desktop needs an always-visible affordance). |
| 2026-06-01 | **Three fixed `top-right` floating bubbles (AI / Help / Feedback) coexisting with the editorial topbar's right cluster** — the bubble layout (Reza directive 2026-05-07: "utility affordances belong in the header bar") predated the Phase R2b editorial topbar's 240px search pill + bell + avatar in the same fixed-top-right area. The bubbles overlapped the search pill (screenshot-confirmed 2026-06-01). Refactored 2026-06-01 to a `placement: 'fixed' \| 'inline'` prop on each button + a `chromeButtons` slot on `EditorialTopBar` — the buttons now render inline inside the topbar's right cluster on desktop (resolving the collision structurally), while mobile keeps the floating pattern via a `md:hidden` wrapper in `DashboardLayout`. | Two `fixed` clusters racing for the same top-right pixels is structurally fragile — every future chrome change risks the same collision. Putting both clusters in the same layout component (the topbar's right cluster) makes the relationship explicit + reviewable. Sidebar-tab consolidation (Reza's first proposal) was considered and overridden through architect-mode + behaviour-psychologist + designer lenses: AI Chat is a primary feature and burying it 2-taps deep behind a "Help" tab demotes it. | (a) When two affordances live in the same physical area, they must live in the same layout component — not as two independent `fixed`-positioned siblings. The Phase R2b chrome swap created the conflict by treating them as independent. (b) Mobile-vs-desktop placement difference is acceptable when the constraints differ — mobile has no topbar right cluster to host the buttons, so floating still wins there. (c) **Do not re-introduce a desktop `fixed top-right` chrome cluster outside `EditorialTopBar` without explicit sign-off** — same collision will recur. |

---


---

### 🔎 Finding (2026-06-26) — 3 proven tax engines are UNWIRED in production (a MISS, not dead code)

**Surfaced by:** Reza's Neomatrix-explorer review ("are they really connected in the app or only the graph?") → audited in source.

| Engine | File | Status |
|---|---|---|
| PSI classifier | `lib/tax-engine/divisions/psiClassifier.ts` | **MISS** — 0 production callers |
| Div 152 small-business CGT | `lib/tax-engine/divisions/div152SmallBusinessConcessions.ts` | **MISS** — 0 production callers |
| FTE/IEE distributions | `lib/tax-engine/divisions/fteIeeClassifier.ts` | **MISS** — 0 production callers |

**Verdict (verified):** NOT dead code. `lib/tax-engine/orchestrator/masterTaxPosition.ts:24-39` designs them as **step-3 "per-entity advanced overlays"** — the *same list* as trust-loss + company-loss rules, which **were** wired (`masterTaxPosition.ts:230-246`). The overlay loop was completed for 2 of 5 engines; these 3 never got their `input.*ByEntity` wiring. The trust path even captures `hasFamilyTrustElection` (`entityTaxRouter.ts:387`) but never calls the FTE/IEE engine that consumes it.

**Scope expanded to FIVE engines (2026-06-26):** the same audit found **trust loss rules** + **company loss rules** are *also* dormant — they ARE wired into the orchestrator loop (`masterTaxPosition.ts:231-245`) but their inputs (`input.trustLossByEntity` / `companyLossByEntity`) are **never populated**, AND `buildMasterTaxPosition` itself has **zero production callers** (`grep app/ → ∅`; the live per-entity path is `entityTaxRouter`). So all 5 step-3 overlays are dormant.

**Fix (a real feature — DEFERRED + PLANNED):** wire the engines as step-3 overlays mirroring the loss-rule pattern. **v1 surfaces the rule outcome + citations + UNCOMPUTED flags only — does NOT change the result number** (per the documented v1/v2 boundary). The assembler (`entityTaxFactsAssembler.ts`) populates the inputs from entity data + new capture.

**Reza decision (2026-06-26):** *"for uncomputed shells go with your recommendation, however document and plan it for later implementation."* → **Do NOT ship empty router shells now** (they'd return UNCOMPUTED-for-everyone, which implies a capability we don't have — §19). The full per-engine **data-capture + wiring + sequencing plan** is documented at **`docs/blueprint/TAX_OVERLAY_WIRING_PLAN.md`** (recommended order: **FTE/IEE end-to-end first** as the proving slice — its trigger flag `hasFamilyTrustElection` is already captured, smallest new-capture gap). Implementation deferred until explicitly scheduled.

**Surfaced in the graph:** the 3 unwired Neomatrix nodes are annotated with this verdict + flagged as allowed islands by the A6 connectivity gate (reviewed allowlist, §22.2) — they show honestly as disconnected (because they ARE unwired), not faked-connected. When a slice ships, the engine moves off the A6 allowlist + its lineage edges to the assembler/route are added in the **same PR** (§21.2.1 zero-drift) — the graph becoming connected is the proof the wiring is real.

---

### 🔎 Finding (2026-07-03) — Property detail cashflow/rent don't reconcile (7 issues, tracker opened)

**Surfaced by:** Reza's review of a property (Thornland / Bankwest) whose numbers don't add up. Investigated per §19 (verified in source, no guessing) + §21.5 (Neomatrix-first) + §12.2.1 (SSOT).

**Tracker (SSOT for this cluster):** `docs/audits/PROPERTY_CASHFLOW_ISSUES_2026-07-03.md` — 7 issues (P-1…P-7) with verified `file:line` root causes, severity, number-changing flags, proposed fixes, and a recommended sequence.

**Headline root cause:** `app/dashboard/properties/[id]/page.tsx` computes **every per-property KPI inline from DECLARED records** (`Income/Expense/Loan` × frequency) — no canonical engine, and it **ignores the reconciled-transaction actuals the API already computes** (§12.2.1 + §19.1). Highlights: **P-1** fortnightly rent stored/treated as MONTHLY (reconcile write paths never persist the detected cadence — `transactions/[id]/link/route.ts:318/156-166/656-663`) → rent ~54% off; **P-5** DEPRECIATION/YR always $0 (`page.tsx:170` reads `d.annualClaim`, a field absent from the `DepreciationSchedule` model); **P-3** loan repayment missing from the "Cashflow rhythm" list though cashflow subtracts it; **P-4** expense tile → global page, no per-property summary (Reza wants a summary card + drill-down; Stitch-first §18.2.1).

**Status:** investigation complete; fixes DEFERRED — P-1/P-2/P-5/P-6 change user-facing money numbers → Reza's go-ahead + scheduling required (§19.3). None of these inline compute helpers are modelled in the Neomatrix (the blind spot that let the drift slip); modelling them with a `semanticKey` is part of the P-2 fix so A3 convergence catches future drift.
