# Neo Inventory — NI-3 Backfill Worklist (the exact gap to 100%)

> Generated by `npm run neomatrix:coverage` (`scripts/neomatrix/reconcile-registry.mjs`). Read-only; regenerate any time.
> Plan: [`../blueprint/NEO_INVENTORY.md`](../blueprint/NEO_INVENTORY.md) · Baseline: [`NEO_INVENTORY_BASELINE.md`](NEO_INVENTORY_BASELINE.md) · Date: 2026-06-26.

## The number (exact, not claimed)

| | Count |
|---|---|
| **Proven** (calc-audit registries — differential `register()` + shadow Float/Decimal, deduped by base name, source-verified via `sourcePath`) | **84** engines |
| **Modelled** (a semantic `engine` node exists in the engine's `sourcePath`) | **47 (56%)** |
| **Backfill worklist** (proven, not yet modelled) | **37 registrations · 26 unique source files** |

This is the honest, finite gap between PROVEN and MODELLED — the denominator that closes to 100% as NI-3 backfills each file as a verified semantic node. (Some files carry two registrations — a differential `tax.X` + a shadow `tax.divisions.X` — so 37 registrations map to 26 files; modelling a file covers both.)

## The worklist, by domain sub-PR

Each file below becomes one or more **verified** semantic engine nodes (formula + authority + inputs + `file:line` **read in source**, §19.2 — never guessed; unverifiable → `status:unverified` with reason; a suspected bug → `suspected-issue` for Reza, never silent-fix). No financial logic changes.

### NI-3b — PROPERTY + CORE primitives (1 file) — overlaps closed #1255 salvage
- `lib/utils/calculations.ts` → `property.LVR`, `property.equity`, `property.rentalYield` (+ the interest/PI primitives from #1255)

### NI-3c — CFO + cashflow (12 files) — overlaps closed #1250–#1254 salvage
- `lib/cfo/scenarios/{sellProperty,addInvestment,payDownLoan,redirectToOffset,refinanceLoan}.ts` (the what-ifs)
- `lib/cfo/decisionSupport/{loanDecisionSupport,investmentDecisionSupport,propertyDecisionSupport,taxIntegration}.ts`
- `lib/cfo/intelligenceEngine.ts`, `lib/cfo/riskRadar.ts`, `lib/cashflow/incomeNormalizer.ts`

### NI-3d — TAX divisions + orchestrator (13 files)
- `lib/tax-engine/divisions/{capitalLossNetting,companyLossRules,div152SmallBusinessConcessions,div7aLoanClassifier,fteIeeClassifier,negativeGearing,psiClassifier,smsfTriumvirateClassifier,trustDistribution,trustLossRules}.ts`
- `lib/tax-engine/income/taxabilityRules.ts` (franking), `lib/tax-engine/core/paygCalculator.ts`, `lib/tax-engine/orchestrator/masterTaxPosition.ts`

## Re-homed from the closed Trust-Engine PRs (#1250–#1257)

The closed PRs' net-new *properties* (accounting identities, refuse-to-compute guards, breakdown additivity, Float⇄Decimal parity, interest/PI) are folded in as **calc-audit fixtures** on these same engines during NI-3c/NI-3b — not a parallel test silo.

## How to read this any time
`npm run neomatrix:coverage` — prints the live proven/modelled/worklist. As NI-3 lands each domain, the worklist shrinks; when it hits 0, NI-4 flips it to a hard build gate (proven ⊆ modelled).
