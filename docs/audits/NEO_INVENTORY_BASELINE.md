# Neo Inventory — Baseline (NI-1)

> Generated by NI-1 (`npm run neomatrix:graphify`). The **first time the true denominator is on screen.**
> Plan: [`docs/blueprint/NEO_INVENTORY.md`](../blueprint/NEO_INVENTORY.md) · Rules: CLAUDE.md Part 22.
> Built at commit: see `structural-graph.json._meta.builtAtCommit`. Date: 2026-06-26.

---

## The three layers, measured (not claimed)

| Layer | What it is | Measured | Source |
|---|---|---|---|
| **L0 — Structural (Graphify)** | every file + symbol + call/import edge, from code (tree-sitter AST) | **1,064 files** · **8,587 nodes** · **15,041 edges** | `structural/structural-graph.json` — generated, gated |
| **L1 — Semantic (Neomatrix)** | hand-curated *meaning* (formula / law / lineage) | **157 nodes** — 64 engine · 8 orchestrator · 11 number · 27 input-field · 28 law · 12 ui-surface · 7 verification | `financial-graph.json` |
| **Proof (calc-audit registry)** | engines with golden / shadow fixtures in CI | **93** engine/fixture names | `lib/calc-audit/engines/*` |

## What this says (honestly)

- **L0 structural completeness is now PROVABLE and gated:** 1,064 `.ts(x)` files on disk under `lib/`+`app/` → **1,062 represented + 3 allowlisted = 100%** (the gate fails the build on any uncovered file). The 3 allowlisted are verified graphify-0.8.45 limitations, none a financial calc — see `structural/coverage-allowlist.json`.
- **The meaning layer is a small, honest fraction of the whole.** The codebase has **8,587** structural symbols; the semantic Neomatrix models **64 engines**; calc-audit proves **93**. So *"is everything captured?"* now has two distinct, measured answers:
  - **Structurally: yes (100%, gated).** Every file and symbol is on the map.
  - **Semantically: partially**, and the gap is now **visible** — un-curated code is a delta against a complete L0, not silently absent.
- This is the denominator that was always missing. Coverage % from here on is `modelled ÷ this`, printed by the build — never a human claim (CLAUDE.md Part 22).

## How completeness is guaranteed (the mechanism, not a promise)

1. **L0 is extracted from code, not typed.** `graphify update lib app` parses 100% of files (tree-sitter); the run reports `540/540` + `528/528` (100%).
2. **An INDEPENDENT disk-vs-graph gate** (`scripts/neomatrix/check-layer0-coverage.mjs`, pure-node, in `neomatrix:check` → `vercel-build`) reconciles every on-disk `.ts(x)` against the committed graph. It **caught 3 real omissions** graphify's own "100%" missed (2 secret-name skips + 1 id-collision drop) — proving the gate works. A new uncovered file → red build.
3. **Regenerated + committed**, so the map can never silently lag the code (the failure mode that produced the recurring "gaps").

## The allowlist (must stay shrinking + reviewed)

| File | Reason (verified) |
|---|---|
| `lib/gcp/credentials.ts` | graphify secret-name skip (verified: neutral-named copy IS extracted). Auth infra, no calc. |
| `lib/share/tokens.ts` | same secret-name skip. Share-token helpers, no calc. |
| `app/api/entities/[id]/accounting/snapshots/route.ts` | full-corpus node-id collision (verified: extracts in isolation). Route handler, no calc. NI-2 collision-handling candidate. |

## NI-2 — binding + coverage readout (shipped)

The three layers are now reconciled at the file level and the coverage is a build output (`scripts/neomatrix/check-binding-coverage.mjs`, in `neomatrix:check`):

| Layer | Count |
|---|---|
| **L0 structural (Graphify)** | 1,062 files · 8,587 nodes — whole codebase, **gated** |
| **Proven (calc-audit registry)** | **84** engines (tax 50 · cfo 24 · core 5 · property 3 · cashflow 2) |
| **Modelled (semantic Neomatrix)** | **64** engines · 157 nodes |
| **Semantic→L0 binding** | **75/75** code-node (engine/orchestrator/number) file anchors resolve — **gated** |

**The binding gate** fails the build if any engine/orchestrator/number node's `file` is no longer in the Layer-0 map (code moved/renamed/deleted but the node didn't follow) — the structural drift sentinel. `law` nodes (cite a module) and `ui-surface` nodes (cite a page dir) use coarser anchors by design and are exempt.

**Honest coverage read:** structurally **100%** (gated); the proven money-producer surface is **84 engines**; the semantic layer models **64**. The exact proven↔modelled match + the backfill that closes the gap toward 100% is **NI-3** (it also re-homes the closed #1250–#1257 properties as calc-audit fixtures). No precise overlap % is claimed here — that requires the exact name/file reconciliation NI-3 does.

## Next (NI-3)

Exact `calc-audit registry ↔ semantic` reconciliation (match each of the 84 proven engines to its semantic node by file:line; the unmatched are the named backfill worklist); re-home the #1250–#1257 net-new properties as calc-audit fixtures; add the per-engine `astHash` body-drift sentinel (A2) now that nodes are bound to structural symbols.
